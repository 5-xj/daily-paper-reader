---
title: A Data-Free Symbolic Regression Approach for Solving Equations
title_zh: 一种无数据符号回归方法用于求解方程
authors: "Sergei Garmaev, Vinay Sharma, Olga Fink"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.07152v1"
tags: ["query:ad"]
score: 9.0
evidence: 无数据符号回归求解方程
tldr: 科学领域中的许多方程无法解析求解，通常依赖数值方法，但无法得到显式符号表达式。现有符号回归方法需要输入-输出数据，而本文提出的SES框架将方程求解建模为可微符号模型的优化问题，目标函数仅由方程和边界条件构成，无需数据。SES在代数方程、常微分方程、偏微分方程等任务上恢复出紧凑且匹配解析解的符号表达式。该方法推动了解析求解自动化，为科学发现提供新工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.07152v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1553, \"height\": 1287, \"label\": \"Figure\"}]"
motivation: 现有符号回归需要数据，但许多方程仅知其方程形式而无数据，因此需要无需数据的符号方程求解方法。
method: SES将方程求解转化为可微符号模型的优化问题，目标函数由方程和初边值条件构成，通过梯度下降学习显式符号表达式。
result: 在代数方程、超越方程、常微分方程及偏微分方程上，SES成功恢复出紧凑符号表达式，与解析解一致。
conclusion: SES首次实现无数据符号回归求解方程，生成可分析的显式解，拓展符号回归应用范围。
---

## 摘要
许多科学中出现的方程目前无法通过现有的解析技术求解，因此只能进行数值求解，而无法得到显式的符号表达式。现有的符号回归方法可以恢复符号表达式，但需要从底层过程中获取训练数据，而不仅仅是控制方程。我们提出了符号方程求解器（SES），这是一个将方程求解转化为可微符号模型上的优化问题的框架。SES根据方程以及初始或边界条件构建其目标函数，从而消除了对成对输入-输出数据的需求。学习到的模型以显式符号形式表达，便于进一步分析。我们在具有代表性的代数方程和微分方程上评估了SES，包括一个代数方程组、一个包含超越项的方程、一个常微分方程以及具有不同初始或边界条件的偏微分方程。在这些设置中，SES恢复了与相应解析解相匹配的紧凑符号表达式。

## Abstract
Many equations arising in science currently cannot be solved by available analytical techniques and are therefore solved numerically, without yielding explicit symbolic expressions. Existing symbolic regression approaches can recover symbolic expressions, but require training data obtained from the underlying process, rather than the governing equation alone. We propose the Symbolic Equation Solver (SES), a framework that formulates equation solving as an optimization problem over differentiable symbolic models. SES constructs its objective from the equation together with initial or boundary conditions, eliminating the need for paired input-output data. The learned model is expressed in explicit symbolic form, enabling further analysis. We evaluate SES on representative algebraic and differential equations, including a system of algebraic equations, an equation with transcendental terms, an ordinary differential equation, and partial differential equations with different initial or boundary conditions. Across these settings, SES recovers compact symbolic expressions that match the corresponding analytical solutions.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：科学和工程中大量方程（代数方程、超越方程、常/偏微分方程）无法解析求解，只能依赖数值方法获得近似解。数值解虽然准确，但难以揭示函数依赖关系、对称性等内在结构；而显式符号表达式则能提供可解释性和进一步分析的基础。现有符号回归方法（如遗传编程、强化学习、神经网络符号回归）虽然能恢复符号表达式，但**需要成对的输入-输出训练数据**，而许多方程求解问题仅给出方程本身和初/边界条件，没有观测数据。另一方面，残差基方法（如PINNs）能从方程直接训练，但输出是神经网络隐含的数值近似，而非显式符号形式。因此，同时满足“从方程和约束出发”、“无需监督数据”、“返回显式符号解”三个特性的方法尚属空白。

- **整体含义**：本文提出一种无需数据、直接利用方程残差优化可微符号模型的符号方程求解框架（SES），旨在填补上述空白，使方程求解兼具符号回归的显式性和连续约束驱动的数据无关性。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将方程求解转化为在**可微符号函数空间**中的优化问题。候选解由一个类似EQL（Equation Learner）的可微符号网络表示，其参数通过最小化由方程残差和初/边界条件残差构成的目标函数来学习，无需任何监督数据。

- **关键技术细节**：
  - **符号模型表示**：采用全连接网络，激活函数替换为预定义的符号算子库（包括恒等、常数、平方、指数、tanh、乘法）。网络层数为2（最大树深度为2），每层每个算子实例化两次。网络权重对应符号表达式中的系数。
  - **目标函数**：将方程所有项移至左侧定义残差 \(F_k\)，辅助条件定义残差 \(B_j\)。目标函数为：
    \[
    \mathcal{L}(\theta) = \frac{1}{N_F}\sum_{i=1}^{N_F}\sum_{k=1}^K F_k^2 + \frac{1}{N_B}\sum_{i=1}^{N_B}\sum_{j=1}^J B_j^2
    \]
    其中 \(N_F\) 为方程配置点数量，\(N_B\) 为边界/初始条件配置点数量。导数通过自动微分计算。
  - **训练流程**：三阶段优化：
    1. 纯残差优化（50,000 epoch，Adam优化器）。
    2. 添加 \(\ell_1\) 惩罚促进稀疏性（200,000 epoch），并周期性剪枝贡献最小的25%权重，直至剩余最少5个活跃权重（Poisson方程保留更多）。
    3. 移除 \(\ell_1\) 惩罚，微调剩余权重（20,000 epoch）。
  - **解提取**：训练后，对权重四舍五入至5位小数，遍历符号计算图提取显式符号表达式。

- **算法流程**（文字说明）：
  - 输入：方程、初/边界条件、算子库、配置点采样方案。
  - 初始化：构建EQL型符号网络作为候选解。
  - 循环采样：每epoch从域内采样256个配置点。
  - 计算目标函数：利用网络前向传播和自动微分计算残差平方和。
  - 三阶段优化：Adam更新权重，第二阶段结合稀疏正则化和剪枝。
  - 输出：从训练后的稀疏网络中提取符号表达式。

## 3. 实验设计：数据集 / 场景、基准、对比方法

- **实验场景（共6个场景）**：
  1. **代数方程组**：线性方程组 \(2x+3y=7, x-y=1\)，精确解 \(x=2, y=1\)。用两个SES模型，以辅助变量t作为输入。
  2. **超越方程**：\(e^x + x^3 = e + 1\)，精确解 \(x=1\)。单变量SES模型，t为辅助变量。
  3. **非线性常微分方程**：\(dy/dt = 1-y^2, y(0)=0\)，精确解 \(y=\tanh t\)。SES模型输入为t。
  4. **线性传输方程**：\(\partial u/\partial t + \partial u/\partial x = 0, u(x,0)=x^2\)，精确解 \(u=(x-t)^2\)。SES模型输入为(x,t)。
  5. **二维泊松方程（第一组边界）**：\(\partial^2 u/\partial x^2 + \partial^2 u/\partial y^2 = 4+2x\)，混合边界条件，精确解 \(u=x^2+y^2+xy^2\)。
  6. **二维泊松方程（第二组边界）**：相同算子，不同边界条件，精确解 \(u=2x^2+xy^2\)。

- **基准**：由于SES是全新的框架，论文**未进行与传统方法（如PINNs、EQL、遗传编程）的定量对比**。主要验证是：恢复的符号表达式与已知解析解的比较。

- **对比方法**：文中仅定性提及现有方法（PINNs、EQL、稀疏辨识、深度符号回归等）的局限性，未做直接实验对比。

## 4. 资源与算力

- **未明确提及**：论文没有说明使用的GPU型号、数量、训练时长或总计算量。仅提到每个epoch采样256个配置点，训练总epoch为270,000（50k+200k+20k），使用Adam优化器。具体硬件资源信息缺失。

## 5. 实验数量与充分性

- **实验数量**：共6组实验（2个代数/超越 + 4个微分方程），每组仅展示一个典型算例。**没有消融实验**（如不同算子库大小、不同网络深度、不同剪枝策略的效果对比），也未对同一方程改变采样点数、初始化方式等进行鲁棒性测试。
- **充分性评价**：
  - **优点**：覆盖了代数、超越、常微分、偏微分方程等多种类型，展示了框架的通用性。
  - **不足**：数量较少，尤其缺乏对更复杂非线性PDE（如Navier-Stokes、Burger方程）或高维问题的测试。未涉及系统间耦合更强的多变量问题。所有测试的解析解都在算子库表达范围内，未测试需要符号推广（如特殊函数）的场景。实验设计较初步，更多是概念验证。

## 6. 论文的主要结论与发现

- SES能够**在完全无监督数据的情况下**，仅利用方程和边界条件的残差，通过可微符号网络优化恢复出与解析解一致的显式符号表达式。
- 框架统一适用于代数方程和微分方程（常/偏）。
- 恢复的符号表达式虽然经过近似优化和剪枝，但能精确匹配解析结构（常数、多项式、tanh等）。
- 该方法填补了符号回归（需监督数据）和残差基神经网络（返回数值解）之间的空白，为从方程自动发现符号解提供了新途径。

## 7. 优点

1. **数据无关性**：完全不需要成对的输入-输出数据，仅依赖方程本身，这在实际科学计算中极具价值。
2. **符号可解释**：输出是显式符号表达式，便于进一步分析、推导和理论检验，相比PINNs等黑箱数值解有本质优势。
3. **统一框架**：无论是代数方程还是微分方程，都采用相同的残差最小化范式，易于扩展。
4. **可微分优化**：利用自动微分计算导数，避免了有限差分误差；梯度下降优化的效率较高。
5. **稀疏性诱导**：通过ℓ1正则化和剪枝，自动得到简洁表达式，符合科学直觉。

## 8. 不足与局限

1. **实验规模小且不充分**：仅6个算例，缺乏大规模、多样化测试；未进行与现有方法的定量性能对比（精度、计算时间、鲁棒性、泛化能力）。
2. **表达力受限**：算子库固定（id, const, 平方, exp, tanh, 乘），且网络深度仅2层，无法表示三角函数、对数、复合函数等更复杂的函数形式，限制了适用方程类型。
3. **配置点采样**：每epoch 256个点，数量较少，可能影响残差评估的精度；未讨论采样策略的影响。
4. **数值精度与符号等价性**：恢复的系数可能存在轻微偏差（如传输方程中0.95184和1.025），虽然结构正确，但并非精确解析解，需要后处理四舍五入。POE方程系数完全匹配，但其他方程有微小误差。
5. **优化策略的手动设计**：三阶段训练、剪枝阈值、活跃权重数量均为手工设置，缺乏自适应机制。
6. **可扩展性未知**：对于高维PDE、多变量耦合系统、复杂边界或非线性强的问题，SES的性能未得到验证。
7. **缺失算力信息**：未报告计算成本，阻碍了复现和效率评估。

（完）
