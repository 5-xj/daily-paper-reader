---
title: "LinTree: Improving LLM Reasoning with Explicitly Structured Search Histories"
title_zh: LinTree：通过显式结构化的搜索历史提升LLM推理
authors: "Liwei Kang, Yee Whye Teh, Wee Sun Lee"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.31492v1"
tags: ["query:ad"]
score: 8.0
evidence: LLM推理结合显式结构化搜索历史，与启发式搜索对比
tldr: LLM推理轨迹可视为线性化搜索树，但隐式表示限制了历史信息的利用。本文提出LinTree，通过添加父指针显式表示树结构，使模型在回溯时明确识别搜索状态。在三个推理任务中，LinTree相比隐式推理和LLM启发式搜索显著提升性能与效率。该工作表明显式树结构对发挥搜索历史优势至关重要，推动结构感知的LLM推理方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.31492v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 907, \"height\": 1703, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.31492v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1450, \"height\": 423, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.31492v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1456, \"height\": 332, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.31492v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1273, \"height\": 1177, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.31492v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 832, \"height\": 270, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.31492v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 370, \"height\": 229, \"label\": \"Table\"}]"
motivation: 发现LLM推理中搜索树仅隐式表示，回溯时未明确标识状态，导致仅凭历史信息难以超越启发式搜索。
method: 提出LinTree方法，在推理轨迹中添加父指针，显式构建线性化搜索树结构。
result: 在Blocks World、网格导航和Sokoban任务中，LinTree优于隐式推理模型和LLM启发式搜索。
conclusion: 显式树结构能有效利用搜索历史，提升LLM推理性能与效率，启发结构感知推理。
---

## 摘要
大型语言模型（LLM）通常通过生成中间轨迹来探索和修正部分解决方案，从而解决推理问题。从搜索的角度来看，这些轨迹可视为线性化的搜索树，模型在其中扩展部分解，当其失败时放弃，并回溯尝试其他替代方案。与传统启发式引导的搜索相比，这种策略具有潜在优势：它基于整个搜索轨迹进行条件判断，而不仅仅是当前局部状态。我们首先通过将轨迹条件化推理策略与配备仅观察当前局部状态的LLM启发式的最佳优先搜索进行比较，来测试LLM是否利用了这一优势。在三个受控推理环境（积木世界、网格导航和推箱子）中，我们发现仅原始访问搜索历史并不足以可靠地优于启发式搜索。随后我们研究了一个可能原因：在LLM推理轨迹中，底层搜索树仅被隐式表示，当模型回溯或切换分支时，轨迹并未明确标识正在重新访问哪个先前的搜索状态。我们证明，添加简单的父指针以显式表示线性化树（LinTree）结构，相比隐式推理模型和LLM启发式引导的搜索，能够提升任务性能和搜索效率。这些结果表明，当树结构被显式化时，搜索历史最为有用，这促使为LLM推理设计更具结构感知的表示。

## Abstract
Large language models (LLMs) often solve reasoning problems by generating intermediate traces that explore and revise partial solutions. From a search perspective, these traces can be viewed as linearized search trees, where the model extends a partial solution, abandons it when it fails, and backtracks to try alternatives. Compared with traditional heuristic-guided search, such a policy has a potential advantage: it conditions on the whole search trace rather than only on the current local state. We first test whether LLMs utilize this advantage by comparing trace-conditioned reasoning policies against best-first search equipped with an LLM heuristic that only observes the current local state. Across three controlled reasoning environments, Blocks World, grid Navigation, and Sokoban, we find that raw access to search history alone is not enough to reliably outperform heuristic search. We then study one possible reason: in LLM reasoning traces, the underlying search tree is only implicitly represented, and when the model backtracks or switches branches, the trace does not explicitly identify which earlier search state is being revisited. We show that adding simple parent pointers to explicitly represent the linearized tree (LinTree) structure improves both task performance and search efficiency relative to implicit reasoning models and LLM-heuristic-guided search. These results suggest that search history becomes most useful when its tree structure is made explicit, motivating more structure-aware representations for LLM reasoning.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：LLM 推理过程可视为线性化的搜索树，模型在生成推理轨迹时依赖于整个搜索历史。然而，传统的启发式搜索（如最佳优先搜索）仅基于当前局部状态。理论上，访问完整搜索历史应能带来优势，但实际中 LLM 是否充分使用了这一信息？  
- **研究背景**：当前 LLM 推理方法（如 Chain-of-Thought、Tree-of-Thought）本质上是隐式搜索，轨迹中的回溯和分支切换并未明确标识底层树结构。作者怀疑这种隐式表示可能限制了历史信息的利用。  
- **研究意义**：如果能显式表示搜索树结构（例如添加父指针），是否能让 LLM 更好地利用历史，从而超越仅依赖局部状态的启发式搜索？这项工作推动了更结构感知的 LLM 推理方法。

## 2. 论文提出的方法论

### 核心思想
- **LinTree**：在 LLM 推理轨迹中为每个搜索步骤添加显式的父指针（`sid` 标识符），将线性化轨迹转化为具有清晰拓扑的序列化搜索树。  
- 对比两种轨迹表示：  
  - **隐式**：仅记录动作和结果状态，不标记树结构（如 `EXPAND ACT ... -> state`）。  
  - **显式**：每条记录包含源状态 ID 和子状态 ID（如 `EXPAND sid=0 ACT ... -> sid=1 state`）。

### 关键技术细节
- **训练 pipeline**（两阶段）：  
  1. **SFT**：使用最佳优先搜索（BFS）生成的轨迹进行监督微调，模型学习模仿搜索行为。  
  2. **GRPO**：通过强化学习优化搜索效率，奖励函数结合正确性和带几何衰减的步数惩罚：  
     \[
     R(\tau) = \mathbb{1}[\text{valid} \land \text{correct}] \left(1 - \lambda \sum_{t=0}^{N_{\text{exp}}(\tau)-1} \gamma^t\right)
     \]  
     其中 \(\gamma=0.99, \lambda=0.005\)，保证每个正确轨迹仍获正奖励。

- **局部状态 LLM 启发式搜索**（对比基线）：  
  - 使用 LLM 作为启发式函数，仅输入当前状态和动作，输出启发值；  
  - 外部控制器维护搜索树，按最小启发值选择扩展节点（算法1）；  
  - 同样经过 SFT 和 RL 训练，但与隐式/显式推理模型形成公平对比。

## 3. 实验设计

### 数据集/场景
- **三个受控规划领域**：  
  - **Blocks World**：3 堆，4–10 块，生成 20k SFT + 20k RL 实例。  
  - **Grid Navigation**：10×10 网格，35% 障碍，20k + 20k 实例。  
  - **Sokoban**：基于 Boxoban 级别，随机去除两个盒子，20k + 20k 实例。

### Benchmark 与对比方法
- **方法列表**：  
  - SFT-implicit（隐式轨迹）  
  - SFT-explicit（显式父指针）  
  - GRPO-implicit  
  - GRPO-explicit  
  - BFS (pretrained) + LLM 局部启发式  
  - BFS (RL) + LLM 局部启发式  
- **评价指标**：  
  - 成功率（Solve rate ↑）  
  - 搜索扩展次数（Expansions ↓）  

## 4. 资源与算力

- 文中明确说明：“All training runs in this paper are performed on one NVIDIA H100 GPU.”  
- 训练具体参数：SFT 4 epochs，学习率 1e-5；GRPO 1 epoch，学习率 1e-5，每实例采样 N=8 条轨迹。  
- **未说明**：总训练时长、各域具体耗时、是否使用并行优化。但可推断算力需求相对适中（单卡 H100）。

## 5. 实验数量与充分性

- **实验数量**：共 3 个领域，每个领域对比 5–6 种方法，每种方法报告成功率与扩展次数（含标准误差）。  
- **消融与分析**：  
  - 计划提取失败率分析（表4）  
  - 状态空间多样性分析（表5，平均成对距离）  
  - 讨论了 GRPO 奖励设计的替代方案（附录 B）  
- **充分性与公平性**：  
  - 训练数据规模一致（SFT 20k，RL 20k），模型参数相同（Qwen3-0.6B）。  
  - 本地启发式方法同样经过 SFT+RL 训练，对比公平。  
  - 但在 Sokoban 中，显式模型加上生成约束后成功率从 89.6% 提升至 98.9%，说明输出约束引入额外优势。  
- **局限**：仅使用 0.6B 模型，未测试更大模型（如 7B 或 70B），可扩展性未知。

## 6. 论文的主要结论与发现

1. 仅访问完整搜索历史（隐式表示）不足以可靠超越局部状态 LLM 启发式搜索——隐式推理模型的成功率低于 BFS (RL)。  
2. 显式添加父指针（LinTree）显著提升推理性能：  
   - SFT 阶段：显式模型成功率平均提高 5 个百分点。  
   - GRPO 阶段：显式模型在 Blocks World 和 Navigation 达到 100% 成功率，Sokoban 达 98.9%（加约束后），且搜索扩展次数更少。  
3. 显式树结构有助于：  
   - 更可靠地从搜索轨迹中提取最终计划（提取失败率降低）。  
   - 更有效地探索状态空间（平均成对距离更高）。  
4. 结论：搜索历史的价值高度依赖于其表示形式，显式树结构能最好地发挥历史信息优势。

## 7. 优点

- **方法简洁高效**：仅改变轨迹格式（添加 `sid` 字段），不增加模型复杂度或额外模块，却带来显著性能提升。  
- **实验设计严密**：对比了隐式 vs 显式、轨迹条件 vs 局部启发式，控制变量清晰。  
- **多领域验证**：在三个不同难度的规划任务上获得一致结论，增强了泛化说服力。  
- **深入机理分析**：不仅报告整体指标，还剖析了计划提取、状态多样性等差异原因。

## 8. 不足与局限

- **应用范围有限**：仅测试了三个完全可观测的受控规划域，未在开放域推理（如数学、常识推理）中验证。  
- **模型规模单一**：仅使用 Qwen3-0.6B，无法判断收益是否随模型规模扩大而增加。  
- **计算资源限制**：未报告详细训练时间，仅单卡 H100 完成，可能无法直接推广到更大模型。  
- **生成约束依赖性**：显式模型在 Sokoban 上需要额外“无效动作过滤”约束才能匹配 BFS 成功率，显式格式本身不能完全避免环境错误。  
- **未讨论零样本/少样本泛化**：所有模型均在同一领域训练和测试，跨领域迁移能力未知。

（完）
