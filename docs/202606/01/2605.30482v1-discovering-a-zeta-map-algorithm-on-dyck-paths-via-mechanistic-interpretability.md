---
title: Discovering a Zeta Map Algorithm on Dyck Paths via Mechanistic Interpretability
title_zh: 通过机制可解释性发现Dyck路径上的Zeta映射算法
authors: "Xiaoyu Huang, Blake Jackson, Kyu-Hwan Lee"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.30482v1"
tags: ["query:ad"]
score: 9.0
evidence: 使用小型transformer发现Dyck路径上zeta映射算法
tldr: 数学发现中，机器学习旨在提供可验证的显式构造而非单纯预测。本文以Dyck路径上的zeta映射为案例，训练小型编码器-解码器Transformer，利用机制可解释性分析其计算过程。通过解码器交叉注意力、线性探针和因果干预，揭示模型基于路径层级进行结构化遍历。提取出“脚手架映射”算法，并证明其与zeta映射一致，仅标签方向反转。该方法展示了可解释性驱动数学发现的有效性，将模型黑盒转化为人类可理解的组合构造。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30482v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 604, \"height\": 541, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30482v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1687, \"height\": 935, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30482v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1765, \"height\": 621, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30482v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 439, \"height\": 438, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30482v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1627, \"height\": 280, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30482v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1770, \"height\": 1529, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30482v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1261, \"height\": 2134, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30482v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1767, \"height\": 1528, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30482v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1762, \"height\": 748, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30482v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1763, \"height\": 812, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30482v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 442, \"height\": 441, \"label\": \"Figure\"}]"
motivation: 机器学习辅助数学发现面临输出需可验证的挑战，本文以zeta映射为案例，探索从模型行为中提取显式算法的可能性。
method: 训练单层单头编码器-解码器Transformer学习zeta映射，使用交叉注意力分析、线性探针和因果干预揭示其层级遍历机制。
result: 发现基于输入路径层级的选择性遍历模式，转化得到“脚手架映射”算法，并证明其等价于zeta映射（标签反转）。
conclusion: 机制可解释性可以从小模型提取精确的组合算法，为AI辅助数学发现提供可控范例。
---

## 摘要
机器学习越来越多地应用于数学发现，但在数学中，期望的输出往往不是预测本身，而是可以独立验证的显式构造。我们通过Dyck路径上的zeta映射（q,t-卡塔兰数组合中的一个经典双射）来研究这一场景。我们在这个映射上训练了一个刻意设计的小型单层单头编码器-解码器Transformer，并使用机制可解释性工具（包括解码器交叉注意力分析、线性探测和因果干预）分析其学到的计算过程。分析揭示了一种基于水平的机制：编码器表示使得路径水平线性可访问，而解码器则以结构化方式选择并遍历输入位置。将这些信号转化为组合学，我们得到了scaffolding映射，这是一种针对Dyck路径的显式以峰为中心的遍历算法。我们证明该算法与zeta映射一致，仅在标记的逆转约定上有差异。这提供了一个受控的AI辅助数学发现示例，其中机制可解释性将模型行为转化为精确且人类可验证的组合算法。

## Abstract
Machine learning is increasingly used in mathematical discovery, but in mathematics the desired output is often not a prediction itself, but an explicit construction that can be checked independently. We study this setting through the zeta map on Dyck paths, a classical bijection in the combinatorics of the q,t-Catalan numbers. We train a deliberately small one-layer, one-head encoder-decoder transformer on this map and analyze its learned computation using mechanistic interpretability tools, including decoder cross-attention analysis, linear probing, and causal intervention. The analysis reveals a level-based mechanism: encoder representations make path levels linearly accessible, while the decoder selects and traverses input positions in a structured way. Translating these signals into combinatorics leads to the scaffolding map, an explicit peak-centered traversal algorithm for Dyck paths. We prove that this algorithm agrees with the zeta map, modulo a reversal convention in the labeling. This gives a controlled example of AI-assisted mathematical discovery in which mechanistic interpretability turns model behavior into a precise, human-verifiable combinatorial algorithm.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：机器学习辅助数学发现面临核心挑战——模型输出的应不是单纯预测，而是可独立验证的显式构造（如算法、猜想或组合双射）。传统AI数学应用以高精度预测或生成为主，缺乏可验证性。
- **研究背景**：以Dyck路径上的zeta映射为测试案例。zeta映射是q,t-卡塔兰数理论中的经典双射，用于连接两种不同的统计量描述（dinv,area）与（area,bounce），已有多种已知算法描述（如面积序列、sweep map）。本文旨在展示：通过训练小型Transformer并利用机制可解释性，可从模型行为中提取出一个人类可理解的、全新的组合算法。
- **整体含义**：提供一种受控的AI辅助数学发现工作流：模型不是最终数学对象，而是结构化证据的来源，启发人类提出新定理并独立验证。这为将ML用于组合学发现建立了可复现的范例。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：训练一个极小化的编码器-解码器Transformer（单层、单头）来学习zeta映射，然后通过机制可解释性工具（包括解码器交叉注意力分析、线性探针、因果干预）解读其内部计算，将观察到的模式（如基于“层级”的注意力选择、避免使用向上步位置、三角形顺序偏移等）转化为显式组合算法——scaffolding映射。
- **关键技术细节**：
  - **模型架构**：标准Transformer，嵌入维度128，前馈维度256，1个编码器块+1个解码器块，1个注意力头，无Dropout，使用学习位置编码。总参数约33.97万，称为Minimal Dyck Transformer。
  - **数据生成**：对每个半长度n，使用SageMath的`area_dinv_to_bounce_area_map()`函数生成输入（Dyck词）和输出（zeta映射像）；注意该函数输出等价于rev∘ζ，其中rev表示反向并翻转0/1。
  - **可解释性方法**：
    - **交叉注意力分析**：观察解码器每步生成时对输入位置的注意力权重，发现仅关注少数East步位置，且呈“三角形顺序偏移”模式。
    - **因果干预（消融）**：在解码时掩盖所有对输入North步的交叉注意力，仅导致精度轻微下降，证明North步位置不是必要信息载体。
    - **线性探针**：从编码器隐藏状态训练线性分类器预测对应位置的层级值ℓ_i，达到84%准确率，说明编码器隐层线性编码了层级信息。
  - **提取算法（scaffolding map）**：
    1. 计算输入Dyck词w（二进制）的层级序列ℓ_i。
    2. 选取所有East步位置（w_i=0），定义峰位置（w_i=1, w_{i+1}=0）。
    3. 按层级从高到低处理：初始化当前层为最大峰层级；每层构造队列（当前活跃agent+该层峰位置），按位置降序输出对应步符号；然后根据agent所在位置是否为East步，分别右移或左移；同时从峰位置派生新agent。
    4. 最终输出序列即为scaffolding映射结果。
  - **等价性证明**：在论文附录D中，通过对比scaffolding映射与rev∘ζ的按层面积序列构建，证明二者一致（定理6.1）。该算法定义对所有半长度通用，不依赖固定n。

## 3. 实验设计：数据集/场景、基准、对比方法

- **数据集**：
  - 后缀为n=11,12,13,14,15,16的Dyck路径集合，大小分别为58,786；208,012；742,900；2,674,440；9,694,845；35,357,670（卡塔兰数）。所有数据可精确生成，无噪声。
  - 训练目标：输入Dyck词，输出其zeta映射像（按SageMath约定即rev∘ζ）。
- **基准与对比方法**：
  - 论文未设置传统数值对比基线（如SVM、LSTM等），而是专注于**模型内部的机制理解**以及**算法提取后的独立证明**。实验主要对比了不同架构变体对学习准确率的影响：
    - 架构：编码器-解码器 vs 编码器-only vs 解码器-only。
    - 层数：1～4层。
    - 注意力头数：1～8。
  - 结果表明几乎所有设置都能达到近完美准确率，证明任务可学习性强。
  - 最终聚焦于最小模型（1层1头）以促进可解释性分析。

## 4. 资源与算力

- **论文未明确说明所使用的GPU型号、数量或训练时长**。仅提及实验训练了多个模型（如d_model=256的四层模型及d_model=128的单层模型），但未提供具体硬件配置或训练时间。对于大规模数据集（如n=16的3500万样本），训练可能使用了GPU（如NVIDIA A100或V100），但文本中没有细节。

## 5. 实验数量与充分性

- **实验组数**：
  - 在不同半长度（n=11～16）上训练多个模型，验证准确率>99%。
  - 探索多种架构变体（编码器-only、解码器-only、不同层数/头数），结果均近完美，说明任务本身对模型容量不敏感。
  - 主要可解释性分析聚焦于在n=13上训练的最小模型（0.34M参数），重复运行多次均获得近完美或完美准确率。
  - 消融实验：因果干预（掩盖North步注意力），仅测试了少量示例（图3c中展示了k步精度曲线），未系统统计在全数据集上的退化幅度，但声称“negligible degradation”。
  - 线性探针：仅在n=13的模型上训练探针，报告84%准确率，未在其他n上验证泛化性。
- **充分性评价**：
  - **优点**：实验覆盖了不同规模、不同架构，证明了学习的可重复性；可解释性手段相对完整（注意力+消融+探针）。提取的算法是全新且可证明的，具有较强说服力。
  - **不足**：因果干预的实验统计量不够系统（仅示例图）；线性探针准确率84%不算特别高，表明层级信息并非完全线性可分，模型可能使用了更复杂的编码；未对不同随机种子的模型进行系统比较（可能学到不同策略）；未在更大模型（多注意力头）上尝试提取算法，仅证明在小模型上可解释。

## 6. 论文的主要结论与发现

1. **Transformer能够高精度学习zeta映射**，即使使用极小模型（单层单头）也能达到完美或近完美准确率。
2. **模型内部计算基于层级进行组织**：解码器交叉注意力主要关注输入中East步（w_i=0）位置，且选择顺序呈“三角形偏移”模式；编码器隐藏状态线性编码了层级信息（线性探针84%准确率）。
3. **North步位置（w_i=1）在解码时并非必需**：因果干预掩盖这些位置后精度几乎没有下降。
4. **从模型行为提取出“scaffolding map”算法**：一种以峰为中心、按层级遍历Dyck路径的显式过程，与zeta映射只在标签反转上不同。
5. **提供了受控的AI辅助数学发现工作流**：模型不是最终产品，而是引出结构化证据的源头；人类据此形成可独立验证的数学定理（scaffolding map等价于rev∘ζ）。

## 7. 优点

- **方法论新颖**：将机制可解释性系统应用于组合学双射学习，首次从训练模型提取出全新的、可证明的组合算法。
- **模型极小化设计**：刻意使用极小容量模型，确保内部表示可被直接检查，避免“黑盒陷阱”。
- **可解释性工具多样**：结合交叉注意力可视化、因果干预消融和线性探针，多种证据相互印证，减弱单纯依赖注意力的误导风险（Jain & Wallace指出注意力≠解释）。
- **提取算法独立可验证**：算法用纯数学语言陈述，并在附录中给出完整证明，不依赖神经网络，真正实现了“可验证的显式构造”。
- **问题选择精巧**：zeta映射具有可精确生成的数据和已知算法，便于对照验证，是理想的测试平台。

## 8. 不足与局限

- **模型规模局限性**：解释仅针对极小模型（1层1头，33万参数），更大、更复杂的模型可能实现完全不同的算法，无法保证可解释性同样成功。
- **可解释性证据的模糊性**：线性探针准确率仅84%，说明层级信息并非完美线性可被表示，模型可能使用了更复杂的非线性组合；注意力模式本身不能作为机制证明，依赖因果消融但消融实验未在全数据集系统统计退化幅度。
- **随机种子影响未探讨**：论文仅提及“即使小模型在不同随机种子下可能有多重内部解”，但未系统对比不同种子下模型是否学到一致策略，可能削弱“模型行为代表唯一算法”的结论。
- **数据规模与实际部署差距**：实验中最大n=16（3500万样本），实际数学研究中会用到更大的n，模型能否泛化未知。
- **未使用自动化工具**：当前发现主要靠人工观察注意力图和手工提取算法，未使用自动电路发现或激活翻译方法，可扩展性有限。
- **仅适用于对称双射情况**：该方法扩展到其他组合对象（如平面二叉树、标准杨表）是否有效未验证。

（完）
