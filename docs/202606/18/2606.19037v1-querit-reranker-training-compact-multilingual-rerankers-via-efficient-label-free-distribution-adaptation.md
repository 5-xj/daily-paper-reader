---
title: "Querit-Reranker: Training Compact Multilingual Rerankers via Efficient Label-Free Distribution Adaptation"
title_zh: "Querit-Reranker: 通过高效无标签分布适应训练紧凑型多语言重排序器"
authors: "Yunfei Zhong, Jun Yang, Wei Huang, Yinqiong Cai, Haosheng Qian, Yixing Fan, Ruqing Zhang, Lixin Su, Daiting Shi, Jiafeng Guo"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.19037v1"
tags: ["query:rerank-train"]
score: 10.0
evidence: 通过无标签分布适应训练紧凑的多语言重排序器
tldr: "多语言重排序模型在不同语言、领域间泛化困难，且适应新分布需要大量人工标注。Querit-Reranker通过数据驱动流水线实现标签高效适配：先用大规模排序数据学习通用相关性，再通过合成查询挖掘和教师软标签进行分布适应，最后用球面线性插值合并检查点。在BEIR和MIRACL上分别提升nDCG@10达5.17和7.83个点，4B版本在MTEB多语言重排序上达到开源最优。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19037v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 792, \"height\": 882, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19037v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 748, \"height\": 335, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19037v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 790, \"height\": 451, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.19037v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1649, \"height\": 999, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19037v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1648, \"height\": 498, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19037v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1652, \"height\": 401, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19037v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 810, \"height\": 541, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19037v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1601, \"height\": 823, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19037v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1647, \"height\": 797, \"label\": \"Table\"}]"
motivation: 现有跨语言重排序器适应新分布需要大量标注数据，成本高昂。
method: 先在大规模排序数据上预训练通用模型，再用合成查询挖掘和教师软标签做分布适应，最后以球面线性插值合并检查点。
result: "Querit-Reranker-A0.4B在BEIR上nDCG@10提升5.17，在MIRACL上提升7.83；4B版本达到开源最优。"
conclusion: 提出了一种免标签、高效的多语言重排序训练方案，显著提升跨语言检索性能。
---

## 摘要
可部署的多语言重排序器必须能够跨语言、领域和目标排序任务进行泛化，同时保持足够的效率以用于第二阶段重排序。然而，将它们适应新的目标分布通常需要大量特定任务的相关性标注，这些标注获取成本高昂。我们提出 Querit-Reranker，这是一个多语言交叉编码器重排序器家族，采用数据为中心的流水线进行标签高效适应。我们将其实例化为 Querit-Reranker-A0.4B（从内部 MoE 骨干网络初始化，激活参数为 0.4B）和 Querit-Reranker-4B（从 Qwen3-Embedding-4B 初始化）。我们的流水线首先从大规模面向排序的数据中学习通用相关性建模，然后通过合成查询挖掘和教师分数作为连续软标签来适应目标分布。为了巩固互补的任务适应优势，我们进一步通过球面线性插值合并检查点，获得单个可部署模型，无需运行时集成开销。使用 Qwen3-Embedding-0.6B 作为共享的第一阶段检索器，Querit-Reranker-A0.4B 在 BEIR 上将平均 nDCG@10 从 54.11 提升至 59.28，在 MIRACL 上从 59.87 提升至 67.70。在 MTEB Multilingual v2 重排序任务中，它也显著优于基于嵌入的更大基线模型，而 Querit-Reranker-4B 在公开可用模型中进一步实现了最先进的性能。我们在 Hugging Face 上发布了这两个模型。

## Abstract
Deployable multilingual rerankers must generalize across languages, domains, and target ranking tasks while remaining efficient enough for second-stage reranking. However, adapting them to new target distributions typically requires extensive task-specific relevance annotations, which are costly to obtain. We present Querit-Reranker, a family of multilingual cross-encoder rerankers trained with a data-centric pipeline for label-efficient adaptation. We instantiate it as Querit-Reranker-A0.4B, initialized from an in-house MoE backbone with 0.4B activated parameters, and Querit-Reranker-4B, initialized from Qwen3-Embedding-4B. Our pipeline first learns general relevance modeling from large-scale ranking-oriented data, then adapts to target distributions through synthetic-query mining with teacher scores as continuous soft labels. To consolidate complementary task-adapted strengths, we further merge checkpoints via spherical linear interpolation, obtaining a single deployable model without runtime ensembling overhead. Using Qwen3-Embedding-0.6B as the shared first-stage retriever, Querit-Reranker-A0.4B improves average nDCG@10 from 54.11 to 59.28 on BEIR and from 59.87 to 67.70 on MIRACL. On MTEB Multilingual v2 Reranking, it also substantially outperforms larger embedding-based baselines, while Querit-Reranker-4B further achieves state-of-the-art performance among publicly available models. We release both models on Hugging Face.

---

## 论文详细总结（自动生成）

# Querit-Reranker: 论文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：多语言重排序器需要跨语言、领域和目标排序任务泛化，但适应新分布通常需要大量人工标注的相关性数据，成本高昂且不切实际，尤其在多语言场景下。
- **动机**：现代检索系统（如 AI 搜索、RAG）普遍采用“检索-重排序”管道，第二阶段的交叉编码器重排序器需要高效且准确。然而，仅靠强多语言骨干网络（如 Qwen3、Llama）不足以保证有效重排序，训练分布与目标分布之间的匹配至关重要。
- **整体含义**：论文旨在开发一种 **标签高效** 的多语言重排序训练方法，在有限或零人工标注下将多语言骨干网络转换为准确且高效的重排序器，同时避免运行时集成开销。

## 2. 方法论
### 核心思想
- 提出 **Querit-Reranker**，一种数据驱动的三阶段训练流水线，结合大规模排序预训练、合成查询挖掘与教师软标签、以及模型参数合并。

### 关键技术细节
1. **模型架构**：交叉编码器。输入格式为 `[指令; 查询; 文档标题; 文档内容; [CLS]]`，使用 [CLS] token 的表示经轻量分类头输出相关概率（低相关 / 高相关），最终得分由期望值公式给出：
   \[
   s(q,d) = -p_0 + p_1
   \]
   其中 \(p_0, p_1\) 为 softmax 输出。
2. **损失函数**：成对铰链损失（pairwise hinge loss）：
   \[
   L_{\text{rank}} = \frac{1}{|P|} \sum_{(i,j) \in P} \max(0, m(y_i - y_j) - (s_i - s_j))
   \]
   其中 \(m\) 为基于教师分数差距动态缩放边距的系数。
3. **三阶段训练**：
   - **Stage I（大规模预训练）**：在开源检索/重排序数据集（~3.52M）及私有业务数据（~2.05M）上训练，使用 Llama-Embed-Nemotron-8B 作为教师对 top-100 候选进行评分，并通过分数分层采样（16个等宽区间）保持多样性。
   - **Stage II（目标适应）**：针对 MMTEB 四个代表性多语言重排序任务（法语、俄语、中文、日语），使用 DeepSeek-R1 生成文档条件查询（任务特定提示），复用教师评分和分层采样管道生成伪排序监督，无需人工标注。
   - **Stage III（模型合并）**：通过球面线性插值（SLERP）合并多个检查点（来自不同数据混合和训练运行），得到单一部署模型，无运行时集成成本。
4. **实例化**：
   - Querit-Reranker-A0.4B：基于内部 MoE 骨干 Querit-A0.4B（激活参数 0.4B）。
   - Querit-Reranker-4B：基于 Qwen3-Embedding-4B。

## 3. 实验设计
### 使用数据集 / 场景
- **MMTEB**（8个重排序任务）：包括 6 个多语言任务（AlloprofReranking、RuBQReranking、T2Reranking、VoyageMMarcoReranking、WebLINXCandidatesReranking、WikipediaRerankingMultilingual）和 2 个英文任务（AskUbuntuDupQuestions、MindSmallReranking）。
- **BEIR**（13个英文检索任务）：评估通用英文重排序性能。
- **MIRACL**（18个语言）：评估多语言检索重排序性能。

### Benchmark / 对比方法
- 所有实验使用 Qwen3-Embedding-0.6B 作为共享第一阶段检索器，控制候选数（top-100）。
- **对比模型**（按参数规模分组）：
  - 小规模 (<4B)：F2LLM-v2-330M/0.6B/1.7B, jina-embeddings-v5, jina-reranker-v3, gte-Qwen2-1.5B-instruct 等。
  - 中规模 (4B–8B)：Qwen3-Embedding-4B/8B, F2LLM-v2-4B/8B, gte-Qwen2-7B-instruct, llama-embed-nemotron-8b, Octen-Embedding-8B 等。
  - 更大模型 (14B–27B)：F2LLM-v2-14B, harrier-oss-v1-27b 等，仅用于 MTEB 子集对比。
- **评价指标**：nDCG@10（BEIR、MIRACL），官方任务指标（MMTEB）。

### 实验公平性
- 使用统一的第一阶段检索器（Qwen3-Embedding-0.6B）对 BEIR 和 MIRACL 进行控制比较。
- 严格遵循官方 MTEB leaderboard 评估协议。
- 对比模型均选自公开且可复现的来源。

## 4. 资源与算力
- 论文 **未明确说明** 使用的 GPU 型号、数量及训练时长。
- 仅提供训练数据量统计：Stage I 约 5.57M 样本，Stage II 约 9.4M 样本（合成数据）。
- 未提及计算硬件细节或训练时间。

## 5. 实验数量与充分性
- **实验数量**：主要结果包含 3 个基准测试（MMTEB 8任务、BEIR 13任务、MIRACL 18语言），每个基准均与多个基线对比。共展示约 6 个表格和 1 个图表。
- **充分性**：
  - 覆盖了多种语言（英语、法语、俄语、中文、日语、阿拉伯语等）和任务类型（教育QA、事实检索、新闻推荐）。
  - 包含跨规模对比（激活参数 0.4B 到 27B）。
  - 但在 **消融实验** 方面存在明显不足：未系统评估每个阶段（Stage I vs II vs III）的独立贡献，未对比不同合并方法（如 SLERP vs 简单平均），也未验证合成查询数量或教师模型选择的影响。
  - 合成数据挖掘仅覆盖 4 种语言，未涵盖 MMTEB 所有任务，因此泛化性存疑。

## 6. 主要结论与发现
- **Querit-Reranker-A0.4B**：在 BEIR 上平均 nDCG@10 从 54.11 提升至 59.28（+5.17），在 MIRACL 上从 59.87 提升至 67.70（+7.83），超越其他同规模重排序器（如 Qwen3-Reranker-0.6B、bge-reranker-v2-m3），展示了出色的参数效率。
- **Querit-Reranker-4B**：在 MTEB-multilingual-v2 上达到 71.08 平均分，位列公开模型榜首；在 BEIR 和 MIRACL 上均取得最佳成绩，显著优于更大模型（如 Qwen3-Reranker-8B、F2LLM-v2-14B）。
- 三阶段训练配方（大规模预训练 + 合成查询适应 + SLERP 合并）可有效提升多语言重排序能力，且从紧凑骨干扩展到 4B 骨干时持续有效。

## 7. 优点
- **标签高效**：通过合成查询和教师评分实现零人工标注的目标分布适应，大幅降低数据构建成本。
- **跨语言强泛化**：在 18+ 语言上表现优异，尤其在低资源语言上仍具竞争力。
- **参数高效与可部署**：0.4B 激活参数的 MoE 优化版模型性能接近甚至超越 1.5B–7B 多种基线，适合资源受限场景；模型合并减少推理开销。
- **系统设计干净**：三阶段流水线逻辑清晰，SLERP 合并避免运行时集成。
- **开源友好**：模型已发布在 Hugging Face，便于复现和部署。

## 8. 不足与局限
- **缺少消融实验**：未定量验证每个阶段的独立贡献（如去掉 Stage II 或 III 的影响），也未分析合成数据规模、教师模型选择、SLERP 超参数等敏感性。
- **合成数据覆盖不完整**：仅对 MMTEB 中 4 个任务生成合成查询，忽略了其他语言（如阿拉伯语、印地语、泰语等）和任务类型，可能导致模型在这些任务上欠优化。
- **未显式处理跨语言重排序**：训练数据不包含查询与文档不同语言的配对，缺乏跨语言对齐目标，可能在跨语言检索场景下表现不均。
- **计算资源未披露**：无法评估方法的训练成本，不利于工程落地预算估计。
- **实验局限性**：
  - 第一阶段使用了私有业务数据（虽脱敏），但未详细说明其分布和规模，可能影响可复现性。
  - 未与专门针对低资源语言的模型（如 XLM-R 类）进行对比。
  - MIRACL 结果中某些语言（如 Yo、Te）Querit-Reranker-A0.4B 分数低于第一阶段检索器，说明适应存在负迁移风险。

（完）
