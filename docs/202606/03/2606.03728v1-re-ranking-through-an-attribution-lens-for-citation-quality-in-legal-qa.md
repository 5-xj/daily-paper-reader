---
title: Re-Ranking Through an Attribution Lens for Citation Quality in Legal QA
title_zh: 通过归因视角重排序以提升法律问答中的引文质量
authors: "Mohamed Hesham Elganayni, Selim Saleh"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03728v1"
tags: ["query:rerank-train"]
score: 8.0
evidence: 使用连续扰动归因得分训练轻量级交叉编码器用于重排序
tldr: 法律问答的检索增强生成系统通常基于语义相似度检索段落，但相似度与模型实际引用归因不相关。本文提出训练轻量级cross-encoder，利用连续扰动归因分数对候选段落重排序。在AQuAECHR基准上，该方法大幅提升引用忠实性和与专家答案的对齐程度，且独立训练于不同语言模型的两个重排器在原始归因一致性基础上进一步收敛，表明归因分数能减少模型特定噪声，提供跨模型共享的相关信号，但同模型重排效果更优。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03728v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1344, \"height\": 995, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.03728v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1221, \"height\": 391, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03728v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 617, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03728v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 695, \"height\": 195, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03728v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1214, \"height\": 390, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.03728v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1591, \"height\": 908, \"label\": \"Table\"}]"
motivation: 现有RAG基于语义相似度检索无法保证段落被模型有效引用，而扰动归因分数更能反映实际引用价值。
method: 训练轻量级cross-encoder，以连续扰动归因分数（如C-LIME）为监督信号，对检索候选段落进行重排序。
result: 重排序显著提升引用忠实性和与专家答案的一致性，独立训练的重排器在跨模型时也展现出部分迁移能力。
conclusion: 扰动归因可作为模型无关的训练信号，有效构建引用感知的检索系统，并减少模型特定噪声。
---

## 摘要
面向法律问答的检索增强生成系统通常基于语义相似度检索段落，并将其提供给语言模型，由后者生成带引文的答案。先前的研究假设排名靠前的段落最有可能被模型有效引用。基于扰动的归因方法（如C-LIME）此前仅用于事后解释。然而，在AQuAECHR基准测试中，语义相似度与段落归因并不相关。在检索器的候选池中，基于相似度的排序在呈现标准引文段落方面的表现甚至不如随机选择。为解决这一局限，我们训练了一个轻量级交叉编码器，利用连续扰动归因分数在生成前对段落进行重排序。该方法在AQuAECHR基准上使用两个语言模型和五折交叉验证进行评估。重排序器显著提升了引文忠实度以及与标准专家答案的对齐程度。值得注意的是，基于不同模型独立训练的两个重排序器，其一致性超越了原始归因一致性。这一发现表明，交叉编码器减少了模型特定的噪声，并产生了可跨模型部分迁移的共享相关性信号，尽管同模型重排序仍然更有效。这些结果证明，基于扰动的归因为引文感知检索提供了一种实用的、与模型无关的训练信号。

## Abstract
Retrieval-augmented generation systems for legal question answering typically retrieve passages based on semantic similarity and provide them to a language model, which then generates cited answers. Prior work assumes that highly ranked passages are most likely to be usefully cited by the model. Perturbation-based attribution methods, such as C-LIME, have been used exclusively for post-hoc explanation. However, on the AQuAECHR benchmark, semantic similarity does not correlate with passage attribution. Within a retriever's candidate pool, similarity-based ranking performs worse than random selection at surfacing gold citation paragraphs. To address this limitation, a lightweight cross-encoder is trained on continuous perturbation-based attribution scores to re-rank passages prior to generation. This approach is evaluated on the AQuAECHR benchmark, using two language models and five-fold cross-validation. The re-ranker substantially improves citation faithfulness and alignment with gold expert answers. Notably, two re-rankers trained independently on different models converge beyond their raw attribution agreement. This finding indicates that the cross-encoder reduces model-specific noise and produces a shared relevance signal that partially transfers across models, although same-model re-ranking remains more effective. These results demonstrate that perturbation-based attribution provides a practical, model-agnostic training signal for citation-aware retrieval.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、核心问题与整体含义（研究动机和背景）

- **研究动机**：在面向法律问答的检索增强生成（RAG）系统中，标准做法是基于语义相似度检索段落，然后由语言模型生成带引文的答案。但该假设存在根本性缺陷：语义相似度与语言模型实际引用段落的行为之间缺乏相关性。
- **核心问题**：在AQuAECHR基准（欧洲人权法院问答）上，基于相似度的检索器对标准引文段落的排序效果甚至不如随机选择。模型常常忽略检索器排在前面的段落，转而依赖低排名段落或参数记忆进行“事后合理化”（post-rationalized）引用，导致引文质量低下。
- **整体含义**：需要一种能够反映语言模型实际引用行为的训练信号来改进检索排序，而基于扰动的归因方法（如C-LIME）可以测量段落对生成结果的实际影响，但先前仅用于事后解释。本文首次将其作为重排序的训练信号，旨在弥合检索器相关性与生成器实用性之间的鸿沟。

## 二、方法论：核心思想、关键技术细节

### 核心思想
- 将基于扰动的归因分数（C-LIME）从后验解释工具转变为重排序器的训练标签，使重排序器学会优先选择语言模型实际依赖的段落。
- 训练一个轻量级交叉编码器（cross-encoder），以问题-段落的连续归因分数为回归目标，在生成前对候选段落进行重排序。

### 关键技术细节
1. **归因评分**：
   - 使用GTR-T5-XL密集双编码器检索每道题的top-10候选段落（定义候选池）。
   - 对每个问题，用语言模型（Mistral-7B或Llama-3-8B）贪婪解码生成答案。
   - 采用C-LIME方法计算每个段落的连续归因分数：通过移除子集段落观察生成对数概率变化，拟合局部线性模型（oversampling factor=10，最多替换2个单元）。
   - 得分连续，衡量段落对生成结果的因果影响（而非二进制引用指示）。

2. **交叉编码器训练**：
   - 架构：ms-marco-MiniLM-L-6-v2（22M参数），预训练于相关性判断。
   - 输入：[问题, 段落]对，截断至512 token。
   - 输出：标量分数，训练损失为均方误差（MSE），目标为连续C-LIME分数。
   - 优化：AdamW，学习率2×10⁻⁵，batch size=32，训练5个epoch。
   - 采用5折交叉验证（分层按问题划分），每个语言模型每个折叠训练一个交叉编码器（共10个）。

3. **重排序与生成**：
   - 推理时：先检索top-50候选段落（扩大池子5倍），再用训练好的交叉编码器打分，选取top-10输入语言模型。
   - 使用与AQuAECHR基线相同的提示和贪婪解码。
   - 跨模型迁移实验：将基于一个模型训练的编码器用于另一模型的重排序。

## 三、实验设计

- **数据集/场景**：AQuAECHR基准测试，包含1116个带标注引文的问答对，源自欧洲人权法院判例法。
- **基准方法**：
  - 基线：GTR-T5-XL检索top-10 + 语言模型生成（Mistral-7B-Instruct-v0.2或Llama-3-8B-Instruct）。
  - 对比1：使用预训练交叉编码器重排序（无C-LIME训练）。
  - 对比2：C-LIME训练的交叉编码器重排序（本文方法）。
- **评估指标**（6个）：
  - 答案正确性：ROUGE-L（词汇重叠）、BERTScore（语义相似度）、Claim Recall（通过TRUETeacher/NLI判断陈述蕴含）。
  - 引文质量：Citation Faithfulness（引文段落支持对应句子比例）、Exact Match F1（引用段落标识符精确匹配）、NLI-based Citation Similarity（生成引文与标准引文语义等价程度）。
- **跨模型迁移**：将基于Mistral训练的编码器用于Llama，反之亦然，并报告性能。

## 四、资源与算力

- 论文明确提到：
  - 交叉编码器22M参数，单张A40 GPU上评分50个候选段落约需80ms，显存占用低于440 MiB。
  - 交叉编码器与7B参数语言模型可同时放入同一GPU，且有剩余空间。
  - 重排序步骤占每问题总延迟不到1%（主导为LLM生成）。
- **未明确说明**：训练交叉编码器的具体GPU数量、总训练时长、基线检索和LLM推理的详细计算资源。

## 五、实验数量与充分性

- **实验组数**：覆盖两个语言模型（Mistral-7B、Llama-3-8B），每个模型进行五折交叉验证（5 folds），共10组主要实验。加上预训练编码器对比（2组）、基线（2组）、跨模型迁移（4组），总计约18组核心结果。
- **消融分析**：
  - 对比了“有无C-LIME训练”的影响，证明改进来自归因训练而非架构本身。
  - 报告了交叉编码器与C-LIME分数的斯皮尔曼相关性（训练前vs训练后）。
  - 对比了重排序前后标准引文段落平均排名变化。
  - 跨模型迁移实验验证信号共享性。
- **充分性**：实验设计完整，覆盖了同模型、跨模型、与基线的对比，并进行了量化相关性分析。但缺乏在法律其他领域或更大规模模型上的验证，也未进行人类专家评估。总体较为充分但可扩展。

## 六、主要结论与发现

1. **C-LIME训练大幅提升引文质量**：Citation Faithfulness绝对提升+5.02pp（Mistral）和+2.70pp（Llama），NLI相似度提升+6.67pp和+5.72pp，且所有6个指标同时改善。
2. **改进源于归因训练而非架构**：预训练交叉编码器提升仅约33-57%，相关性从斯皮尔曼0.187/0.139升至0.686/0.400。
3. **跨模型收敛现象**：两个语言模型原始C-LIME分数一致性仅0.480，但独立训练后的重排序器一致性升至0.858，表明交叉编码器减少了模型特定噪声，学到了共享相关性信号。
4. **跨模型迁移保留43-51%的增益**：表明共享信号有效但同模型重排序更优。
5. **重排序提升了与专家标准引文的对齐**（NLI增益>忠实度增益），尽管训练中未使用标准引文作为监督。

## 七、优点（方法或实验设计亮点）

- **创新性**：首次将基于扰动的归因信号用于重排序训练，将事后解释工具转化为训练信号，填补了检索-生成对齐的空白。
- **实用性**：轻量级交叉编码器（22M参数）计算开销极小（<1%推理延迟），可低成本集成到现有RAG流水线。
- **实验设计严谨**：采用五折交叉验证，对比“有/无归因训练”的消融，跨模型迁移验证，并报告相关性指标，证明机制有效。
- **定性分析补充**：附录给出两个代表性案例，展示重排序如何纠正基于关键词匹配的检索错误，提供了定量指标之外的直观证据。
- **连续目标优于二进制标签**：保留了排名信息，避免损失细粒度排序。

## 八、不足与局限

- **评估依赖自动指标**：所有引文质量评估基于NLI自动度量（TRUETeacher），缺乏法律专家人工验证。自动指标可能无法完全捕捉法律引文的精确性和权威性要求。
- **模型偏差风险**：C-LIME分数反映的是生成器行为而非独立法律正确性，重排序可能强化原有模型偏差。跨模型收敛（斯皮尔曼0.858）部分缓解但未完全消除。
- **域与模型泛化性有限**：仅在ECHR（欧洲人权法院）一个法律子域上测试，仅使用了两个7-8B参数模型。未知对更大模型（如GPT-4、Llama-70B）、其他司法管辖区（如美国、中国）、多语言场景的适用性。
- **未与当前最先进重排序方法对比**：如RankLLaMA、monoT5等，仅对比了基线检索和预训练交叉编码器。缺少与专用重排序模型的直接比较。
- **候选池大小固定**：推理时扩大至top-50，但未探索更大池子或动态调整策略。
- **计算资源细节不完整**：未报告训练交叉编码器所需的具体GPU数量和总训练时长，难以复现资源成本。

（完）
