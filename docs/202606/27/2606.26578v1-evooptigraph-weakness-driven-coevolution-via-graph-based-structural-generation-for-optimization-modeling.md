---
title: "EvoOptiGraph: Weakness-Driven Coevolution via Graph-Based Structural Generation for Optimization Modeling"
title_zh: EvoOptiGraph：基于图的结构生成实现弱点驱动的协同进化以优化建模
authors: "Qingcan Kang, Mingyang Liu, Xiaojin Fu, Shixiong Kai, Tao Zhong, Mingxuan Yuan"
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26578v1"
tags: ["query:ad"]
score: 9.0
evidence: 大模型与进化计算自动生成优化模型
tldr: LLM自动化优化建模面临训练语料结构单一和数据生成静态两大挑战。EvoOptiGraph将混合整数线性规划表示为属性二分图，通过保持有效性的进化算子生成结构多样实例，并利用模型弱点信号驱动数据与模型协同进化。两阶段训练包括监督微调和基于可验证奖励的强化学习，形成闭环更新训练分布。在六个公开数据集上，该方法在准确性、可执行性和泛化性上显著优于大型通用模型和专门基线，证明了数据-模型协同进化的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26578v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1424, \"height\": 763, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26578v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1442, \"height\": 668, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26578v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1452, \"height\": 670, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26578v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1452, \"height\": 671, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26578v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1450, \"height\": 663, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.26578v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1417, \"height\": 941, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26578v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1458, \"height\": 676, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26578v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1290, \"height\": 205, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26578v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1499, \"height\": 1510, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26578v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1496, \"height\": 2326, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26578v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1490, \"height\": 2342, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26578v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1497, \"height\": 605, \"label\": \"Table\"}]"
motivation: 现有训练语料缺乏结构多样性，且数据生成流水线静态，与模型学习脱节，限制了LLM在优化建模任务上的性能。
method: 将MILP表示为属性二分图，用进化算子生成结构多样实例，经编译和反向翻译得到代码与自然语言；采用SFT+RLVR两阶段训练，弱点信号指导新实例生成。
result: 在六个公开数据集上，EvoOptiGraph在准确性、可执行性和泛化性上大幅超越大型通用模型、代理方法和专门基线。
conclusion: 针对性的数据-模型协同进化是提升LLM优化建模能力的有效策略，为自动化建模提供了新的范式。
---

## 摘要
利用大语言模型从自然语言自动生成优化模型面临两个关键挑战。首先，训练语料库缺乏结构多样性。其次，数据生成流程仍然静态且与模型学习脱节。为应对这些挑战，我们提出EvoOptiGraph——一种新颖的框架，其中数据和模型由模型弱点驱动共同进化。EvoOptiGraph将每个混合整数线性规划表示为属性二分图，并应用保有效性进化算子生成结构多样的实例。进化后的图通过确定性编译和验证性反向翻译转换为求解器代码和自然语言。训练分两个阶段进行：在初始数据集上进行监督微调，随后进行具有可验证奖励的强化学习，其中图衍生的弱点信号指导针对模型失败的新实例生成。这形成了持续更新训练分布的闭环。在六个公开数据集上的实验结果表明，EvoOptiGraph在准确性、可执行性和泛化性上显著优于更大的通用模型、代理方法和专用基线。这些结果表明，针对性的数据-模型协同进化是提升大语言模型在优化建模任务上表现的有效策略。

## Abstract
Automating optimization modeling from natural language with large language models (LLMs) faces two key challenges. First, training corpora lack structural diversity. Second, data generation pipelines remain static and decoupled from model learning. To address these challenges, we propose EvoOptiGraph, a novel framework where data and model co-evolve, driven by model weaknesses. EvoOptiGraph represents each mixed-integer linear program (MILP) as an attributed bipartite graph and applies validity-preserving evolutionary operators to generate structurally diverse instances. The evolved graphs are converted into solver code and natural language via deterministic compilation and verified back-translation. Training proceeds in two stages: supervised fine-tuning (SFT) on an initial dataset, followed by reinforcement learning with verifiable rewards (RLVR), where graph-derived weakness signals guide the generation of new instances targeting the model's failures. This forms a closed loop that continuously updates the training distribution. Empirical results on six public datasets show that EvoOptiGraph significantly outperforms larger generalist models, agentic methods, and specialized baselines in accuracy, executability, and generalization. These results demonstrate that targeted data-model coevolution is an effective strategy for improving LLMs on optimization modeling tasks.

---

## 论文详细总结（自动生成）

# 论文核心总结：EvoOptiGraph——基于图的结构生成实现弱点驱动的协同进化以优化建模

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：大语言模型（LLM）从自然语言自动生成优化模型（如混合整数线性规划 MILP）面临两个关键瓶颈：
  - 训练语料缺乏**结构多样性**：现有数据集（如 NL4Opt、MAMO）仅覆盖有限的结构模式，大多数合成方法仅扰动系数或文本模板，未改变底层优化结构。
  - 数据生成流水线**静态**：数据集一次性构建后，模型训练与数据分布脱节，无法针对模型弱点动态生成新实例。
- **整体含义**：针对上述问题，论文提出一个**数据-模型协同进化**框架 EvoOptiGraph，通过图表示统一结构生成、失败诊断和模型更新，形成闭环训练分布自适应机制，从而提升 LLM 在优化建模任务中的准确性、可执行性和泛化性。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将 MILP 表示为**属性二分图**（节点对应变量和约束，边编码系数），在此基础上：
  - 使用**保有效性进化算子**（交叉、变异）直接在图上生成结构多样且有效的新实例；
  - 通过**确定性编译**将图转为求解器代码（如 Gurobi），再经**验证性反向翻译**得到自然语言描述；
  - 采用**两阶段训练**：先监督微调（SFT）初始化，再通过**具有可验证奖励的强化学习（RLVR）**优化，其中**弱点信号**（从图结构特征中提取）引导新实例生成，形成闭环迭代。

- **关键技术细节**：
  - **图表示**：G = (V, E, A)，V 包含约束节点（含目标节点）和变量节点；边存储系数；属性包括约束类型、边界、变量类型等。
  - **遗传操作**：
    - 交叉：交换兼容子图（同类别或跨类别），目标节点独立处理；
    - 变异：参数变异（系数、边界）、结构变异（增删变量/约束）、规模变异（如扩展供需节点）；
    - 每轮生成后通过有效性检查（图结构合法、可编译执行）。
  - **适应度函数**（用于选择新实例）：
    \[
    f(G) = \beta \tilde{f}_{\text{diff}} + \gamma \tilde{f}_{\text{div}} + \delta \tilde{f}_{\text{weak}}
    \]
    - \(\tilde{f}_{\text{diff}}\)：从求解器统计量导出的难度；
    - \(\tilde{f}_{\text{div}}\)：与当前种群的图编辑距离（GED）；
    - \(\tilde{f}_{\text{weak}}\)：候选图结构特征向量 \(\phi(G)\) 与当前模型弱点向量 \(w\) 的余弦相似度。
  - **弱点分析**：对验证集上做错的实例，提取 103 维图结构特征，训练 XGBoost 分类器预测正确性，计算 SHAP 值得到每个特征对错误的贡献，生成弱点向量 \(w\)。
  - **RL 优化**：采用 GRPO 算法，奖励函数为多阶段和：
    \[
    R(c) = R_{\text{process}} + R_{\text{solvable}} + R_{\text{proximity}} + R_{\text{correctness}}
    \]
    - 涵盖代码可运行、有可行解、相对误差<10%、最优匹配。
  - **算法流程**（Algorithm 1）：
    1. 初始种子生成 → 初始数据集 \(D_0\)；
    2. SFT 热身 → 得初始策略 \(M_{\text{warm}}\)；
    3. 循环 T 轮：
       - 评估模型，得错误集 \(E_t\)，计算弱点向量 \(w_t\)；
       - 用弱点对齐适应度进化新数据 \(D_{\text{RL}}\)；
       - 对每个 prompt 采样 N 个候选，执行代码获取奖励，通过 GRPO 更新模型。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：六个公开基准：
  - NL4Opt（214 清洗实例）
  - MAMO（Easy LP 545 实例，Complex LP 111 实例）
  - NLP4LP（178 清洗实例）
  - ComplexOR（18 实例）
  - IndustryOR（42 清洗实例）
- **基准**：采用 Pass@1 准确率（生成代码求解得到的最优目标值与参考值匹配）。
- **对比方法**：
  - 零样本通用 LLM：GPT-4o、DeepSeek-V3、Qwen3-32B、Qwen2.5-72B-Instruct；
  - 代理方法：OptiMUS-v0.3、Chain-of-Thought (CoT)、Chain-of-Experts (CoE)；
  - 微调专用 LLM：ORLM (8B)、LLMOPT (14B)、OptMATH (32B)（后两者因数据集版本差异，结果仅作参考）。
- **消融实验**：SFT-only 和 RL-only 两种变体，对比完整方法。
- **数据多样性比较**：使用结构相似性度量与 OptMATH 对比。
- **协同进化循环分析**：跟踪从基模型到 SFT 再到三轮 RL 的准确率轨迹。

## 4. 资源与算力

- 论文**未明确说明**使用的 GPU 型号、数量、训练时长等具体算力资源。仅指出基础模型为 Qwen3-8B，采用 LoRA 微调（rank=32, alpha=64），SFT 学习率 1e-4，RL 学习率 5e-6，使用 GRPO 训练 3 轮。附录中提到使用 Gurobi 求解器（时间限制 300 秒，MIP gap 0.01）进行验证。未见详细算力报告。

## 5. 实验数量与充分性

- **主实验**：在 6 个基准上对比 10+ 个基线方法（表 1），结果充分。
- **消融实验**（表 2）：对比完整方法 vs SFT-only vs RL-only，验证两阶段和协同进化必要性。
- **数据多样性量化**：结构相似性对比（66.93% vs 93.04%），证明图进化带来更高多样性。
- **协同进化轨迹分析**（Figure 3）：展示基模型、SFT、RL1、RL2、RL3 性能变化，分析非单调性等。
- **附录补充**：图 4/5 展示 SFT-only 和 RL-only 的轨迹，以及更详细的特征列表、提示模板、遗传算法参数等。
- **评价**：实验设计较全面，消融验证了各组件的贡献，对比了多种类型方法（零样本、代理、微调），且使用公共清洗版本以保证公平性。但缺少跨模型规模（如 7B vs 14B vs 32B）的对比（仅 8B），且 ComplexOR 和 IndustryOR 实例数较少（18 和 42），可能影响统计稳健性。未报告多次随机重复的标准差。

## 6. 论文的主要结论与发现

- EvoOptiGraph 在六个基准上**平均 Pass@1 达到 78.1%**，显著优于所有对比方法，包括更大规模模型（如 DeepSeek-V3 671B、Qwen2.5-72B）。
- **数据-模型协同进化闭环有效**：SFT 提供基础提升，后续 RL 轮次持续带来增益；弱点引导的生成使扩展具有针对性。
- **图进化生成的数据结构更多样**（结构相似性远低于单纯参数扰动的 OptMATH），有利于泛化。
- **两阶段缺一不可**：SFT-only 在后续轮次性能退化，RL-only 提升有限，两者结合最佳。
- 结果表明：针对性的结构数据进化比单纯扩大模型规模更有效。

## 7. 优点：方法或实验设计上的亮点

- **创新性**：提出将数据生成与模型训练**闭环耦合**，通过图表示统一结构生成、弱点诊断和模型更新，克服了静态数据分布的局限。
- **结构刻画**：利用属性二分图完整表达 MILP，既保持可编译性，又支持图编辑距离计算和遗传操作，比纯文本进化控制力更强。
- **弱点驱动自适应**：通过 XGBoost + SHAP 提取可解释的弱点特征，指导进化方向，使生成数据“对症下药”。
- **奖励设计细致**：多阶段奖励函数（可运行、有可行解、10% 内、精确匹配）逐步引导模型生成高质量代码。
- **实验充分**：在多个难度层级（EasyLP → ComplexLP → ComplexOR）上评估，消融和分析揭示各组件贡献，对比基线涵盖主流方法。

## 8. 不足与局限

- **适用范围有限**：当前框架仅针对 MILP，未覆盖非线性规划、凸优化等更广泛类型；种子生成器局限于 53 个经典问题族，对新兴问题模式可能缺乏覆盖。
- **验证成本高**：大规模实例的求解器验证（300 秒/个）在数据生成和 RL 训练阶段计算开销大，论文未提供具体成本分析。
- **弱点向量是实用信号而非因果解释**：SHAP 特征重要性反映了相关性，但可能无法揭示真正的因果失败机制，存在混淆因素。
- **可能过拟合特定结构**：弱点驱动生成可能使模型过度适应验证集错误模式，导致泛化偏差（论文在 MAMO-complex 上观察到非单调性）。
- **实验规模局限**：仅使用 8B 模型，未探索不同参数量级下的规律；ComplexOR 和 IndustryOR 样本量小（<50），结果统计显著性待确认。
- **缺乏随机种子重复**：未报告多次独立运行的平均值和方差，结果稳定性未知。
- **未开源**（假设）：论文未提及代码或模型开源，可复现性待验证。

（完）
