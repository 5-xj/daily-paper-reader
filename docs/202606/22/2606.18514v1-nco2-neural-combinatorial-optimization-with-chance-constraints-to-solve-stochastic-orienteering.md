---
title: "N(CO)$^2$: Neural Combinatorial Optimization with Chance Constraints to Solve Stochastic Orienteering"
title_zh: N(CO)$^2$：采用机会约束的神经组合优化求解随机指向问题
authors: "Anas Saeed, Marcos Abel Zuzuárregui, Stefano Carpin"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.18514v1"
tags: ["query:ad"]
score: 8.0
evidence: 神经组合优化学习启发式用于算法发现
tldr: 随机定向问题（SOP）需在不确定性下优化路径，传统方法依赖手工启发式。本文提出N(CO)^2，将神经组合优化与机会约束结合，通过强化学习自动学习启发式策略。实验表明，该方法在多种SOP实例上泛化良好，性能媲美混合整数线性规划（MILP）。该工作减少了手工启发式设计，实现了不确定性下的自适应决策。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18514v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 730, \"height\": 603, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18514v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1791, \"height\": 796, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18514v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 877, \"height\": 567, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18514v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1797, \"height\": 596, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18514v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 877, \"height\": 630, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.18514v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1729, \"height\": 490, \"label\": \"Table\"}]"
motivation: 现有神经组合优化多关注确定性，缺乏有效处理随机组合优化的方法。
method: 构建结合机会约束的强化学习框架，自动学习在不确定性下优化路径选择的启发式。
result: 在多种随机定向问题实例上，方法泛化性好，性能与先进MILP相当。
conclusion: N(CO)^2减少了手工启发式设计，实现了不确定性下的自适应高效决策。
---

## 摘要
神经组合优化（NCO）通过从数据中学习启发式方法，为求解复杂图优化问题提供了一种有前景的替代传统启发式方法。这类问题在自动化领域经常出现，因为它可以用来建模多种应用。虽然NCO在确定性组合优化问题方面已被广泛研究，但只有少数工作旨在解决随机组合优化问题。在这项工作中，我们提出了N(CO)$^2$：采用机会约束的神经组合优化来求解随机指向问题（SOP），而无需使用手工设计的启发式方法。通过集成强化学习（RL）框架，该模型在不确定性下优化路径选择，有效平衡了探索与利用。实验结果表明，我们的方法能够很好地泛化到各种SOP实例，在与该任务的最先进的混合整数线性规划（MILP）相比时取得了有竞争力的性能。所提出的方法减少了启发式设计的人工工作量，同时能够在不确定环境中实现自适应和高效的决策。

## Abstract
Neural combinatorial optimization (NCO) offers a promising alternative to traditional heuristic-based methods for solving complex graph optimization problems by proposing to learn heuristics through data. This class of problems frequently arises in automation, as it can be used to model a variety of applications. While NCO has been extensively studied for deterministic combinatorial optimization problems, there are only a few works that aim to solve stochastic combinatorial optimization problems. In this work, we present N(CO)$^2$: Neural Combinatorial Optimization with Chance cOnstraints to solve the Stochastic Orienteering Problem (SOP) without the use of hand-crafted heuristics. By integrating a reinforcement learning (RL) framework, the model optimizes path selection under uncertainty, effectively balancing exploration and exploitation. Empirical results demonstrate that our method generalizes well across diverse SOP instances, achieving competitive performance compared to the state-of-the-art mixed-integer linear program (MILP) for the task. The proposed approach reduces human effort in heuristic design while enabling adaptive and efficient decision-making in uncertain environments.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：随机定向问题（Stochastic Orienteering Problem, SOP）是一类在自动化领域（如精准农业、路径规划、物流）中常见的组合优化问题。其难点在于边成本服从随机分布，需要满足机会约束（概率性预算约束），但传统求解方法（如基于MCTS的启发式或MILP）要么依赖手工设计的启发式函数（泛化性差、适应性弱），要么求解时间过长（无法实时）。
- **整体含义**：本文旨在消除手工启发式设计，提出一种基于强化学习的神经组合优化框架，自动学习用于求解机会约束SOP的启发式策略，实现不确定性下的高效自适应决策，同时保持与最先进MILP解相当的性能。

## 2. 论文提出的方法论

- **核心思想**：将SOP的求解转化为**边热图预测+强化学习**问题。学习一个参数化启发式 \( H_\theta \)（热图矩阵），为每条边赋予加入最优路径的概率，然后使用基于机会约束的路径构建算法（Algorithm 1）生成可行解，并通过REINFORCE算法优化 \( \theta \)。
- **关键技术细节**：
  - **问题形式化**：SOPCC模型在经典定向问题基础上引入机会约束 \(\Pr[C(P) > B] \le P_f\)，使用样本平均近似（SAA）估计失败概率。
  - **启发式结构**：定为一个 \( n \times n \) 的热图矩阵 \( H \)，通过软最大化得到从当前节点选择下一节点的概率。对不符合约束的节点（已在路径中或违反机会约束）置零。
  - **神经网络架构**：采用**边增强图变换器（EGT）**作为编码器，输入为节点特征（归一化奖励、是否为起/终点）和边特征（归一化平均采样成本、失败概率阈值、起/终点指示）。输出为学习到的边嵌入；解码器为单层前馈网络，生成热图矩阵 \( H \)。
  - **强化学习训练**：
    - 使用REINFORCE算法，目标函数为 \(\nabla_\theta J(\theta) = \mathbb{E}_{P\sim H_\theta}[(f_s(P)-b)\nabla_\theta \log \Pr[P|H]]\)，其中奖励函数 \( f_s(P) = R(P) \cdot (1 - \max(0, \hat{F}_P - P_f)) \) 在估计失败概率超过阈值时惩罚。
    - 同时采用基于群体的REINFORCE（Poppy）变体，仅用最高奖励路径更新，提升性能。
  - **算法流程**：
    - **Algorithm 1**：给定热图启发式，通过SAA逐顶点扩展路径，检查加入候选顶点后失败概率是否超限。
    - **Algorithm 2**：离线预训练：从问题分布采样实例，用Algorithm 1采样K条路径，计算梯度更新 \( \theta \)。
    - **Algorithm 3**：在线活动搜索微调：针对单个实例，直接优化热图矩阵（冻结网络权重），快速适应。

## 3. 实验设计

- **数据集与场景**：
  - **训练集**：50节点随机图（节点坐标和奖励均匀采样于[0,1]），预算 \( B_i \in [1.5,3] \)，失败概率 \( P_f \in [0.01,0.15] \)。
  - **评估集**：4个合成数据集（各25或50个图）：
    - n=50, \( B_i=1.5, P_f=0.1 \)
    - n=50, \( B_i=2, P_f=0.05 \)
    - n=100, \( B_i=1.5, P_f=0.1 \)
    - n=100, \( B_i=2, P_f=0.05 \)
  - **真实世界图**：来自TSPLIB的6个实例（berlin52, st70, eil101, ch150, tsp225, a280），覆盖52至280节点，预算取TSP最优路径费用的一半。
  - **边成本分布**：指数分布 \( \kappa d_{ij} + \text{Exp}((1-\kappa)d_{ij}) \)，\( \kappa=0.5 \)。
- **基准方法（Benchmark）**：
  - **MILP**：使用GUROBI求解器，对每个实例设时间上限（n<100为600s，n<200为900s，n<300为1200s），失败概率约束调整为[0.05,0.01]。
  - **基线启发式**：来自前期工作的启发式 \( R(v_j) / \text{mean}(sCosts(v_i,v_j,S)) \)，贪心选边。
- **对比方法**：REINFORCE与群体REINFORCE两种训练目标；Active Search不同迭代轮次的影响。

## 4. 资源与算力

- **计算平台**：所有实验在MacBook Pro M2上运行（12核CPU，32GB RAM），未使用GPU（PyTorch在CPU上运行）。MILP使用GUROBI同样利用全部12核CPU。
- **模型规模**：EGT模型共108,458个可学习参数（节点嵌入64维，边嵌入16维，4个注意力头，3层）。
- **训练配置**：预训练100个epoch，每epoch 50步，每步32个图实例。每步采样K=100条路径。使用AdamW优化器，学习率0.001。
- **时间消耗**：在TSPLIB实例上，Activity Search包括模型推理时间（毫秒级）、100次活动搜索迭代（数秒至分钟）、路径采样（毫秒级），总计约3–60秒；MILP需数十秒至数百分。

## 5. 实验数量与充分性

- **实验组数**：
  - 训练过程中在4个评估集上每epoch记录平均奖励（见图2），体现学习曲线。
  - 验证SAA样本数（100-1000）对失败概率达标的影响（图3）。
  - 在100节点数据集上测试Active Search迭代次数（0-100）的效果（图5）。
  - 在6个TSPLIB实例上分别对2种预算/约束设置进行50次重复（N(CO)^2和基线），MILP运行5次，报告平均奖励、失败概率和时间（表I）。
  - 对比两种REINFORCE变体（baseline vs pop）。
- **充分性与公平性**：
  - 实验覆盖了合成图和真实图，不同规模和参数，足以验证泛化能力。
  - 与MILP比较时设置了合理时间上限，且给出了实际解的质量（奖励）和约束满足情况（失败概率）。
  - 控制实验（基线启发式）使用相同路径构建算法（Algorithm 1）但不同启发式，对比公平。
  - 消融实验（Active Search迭代次数、不同RL目标）体现了各组件贡献。
  - 但缺乏与更多随机组合优化基线（如其他NCO方法）的比较，主要是因为SOP较少被研究。

## 6. 论文的主要结论与发现

- **方法有效**：N(CO)^2学习到的启发式能够生成高质量可行解，在合成图和真实图上均接近或媲美MILP解，且计算时间远低于MILP（尤其在100+节点图上）。
- **群体REINFORCE（Poppy）优于基础REINFORCE**：训练过程中，基于群体的目标始终获得更高平均奖励。
- **Active Search提升显著**：在未见过的实例上微调热图可进一步提高解的质量，迭代次数越多效果越好。
- **SAA样本数影响约束满足**：使用100–250个样本即可使解的实际失败概率接近给定阈值\( P_f \)。
- **大图性能超越MILP**：在tsp225和a280实例上，N(CO)^2在更短时间内找到比MILP（在时间限制下）更高奖励的可行解。
- **消除了手工启发式设计**：完全通过数据驱动学习，减少了人工工作量。

## 7. 优点

- **创新性**：首次将NCO与机会约束结合用于求解随机定向问题，提出端到端学习框架。
- **自动化**：无需手工设计启发式，适应不同实例和参数。
- **高效性**：推理速度快（毫秒级预计算，数秒内微调），适合在线或近实时应用。
- **泛化能力**：在仅用50节点图训练后，可泛化至100节点及更大真实图（如a280）。
- **可扩展性**：使用边增强图Transformer可处理任意大小图；Batch实现支持GPU（虽然实验在CPU）。
- **鲁棒性**：通过SAA和机会约束确保解的概率可行性。

## 8. 不足与局限

- **实验资源限制**：仅在CPU上运行，未使用GPU加速，未探索更大模型或更大训练数据的影响。MILP也仅用GUROBI默认设置，可能未充分调优。
- **训练数据规模有限**：训练集仅为50节点随机图，未在更大图或不同分布上预训练，泛化到更复杂拓扑可能有限。
- **仅考察单一随机分布**：边成本仅使用指数分布（kappa=0.5），未验证对其他分布（如高斯、均匀）的适应性。
- **SAA采样开销**：路径构建时需多次采样（100次），虽远少于MILP但仍有计算成本。
- **对比方法较少**：只与MILP和手工基线对比，未与其他神经优化方法（如基于GNN的MCTS、强化学习方法）在SOP上直接比较。
- **机会约束满足性评估**：失败概率估计基于SAA，在有限样本下可能不精确，且未做严格的统计检验。
- **缺少可重复性细节**：未提供代码训练的全部超参数（如梯度裁剪范围0.5，但未解释各超参数搜索过程）；MILP的失败概率阈值调整（从0.1或0.05降低）可能影响结果公平性。

（完）
