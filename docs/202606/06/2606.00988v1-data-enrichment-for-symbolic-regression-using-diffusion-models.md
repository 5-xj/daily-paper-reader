---
title: Data Enrichment for Symbolic Regression Using Diffusion Models
title_zh: 使用扩散模型的符号回归数据增强
authors: "Simon De Reuver, Tamas Kristof Toth, Teddy Lazebnik"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.00988v1"
tags: ["query:sr"]
score: 8.0
evidence: 使用扩散模型的符号回归
tldr: 符号回归从观测中挖掘可解释方程，但稀疏、含噪及物理不完整数据严重制约其可靠性。本文提出物理引导的潜在扩散框架，结合变分自编码器、条件扩散模型和物理信息残差校正器，生成受控制方程约束的合成数据以补全观测。在热传导、不可压缩Navier-Stokes流动和牛顿引力势上验证，该方法显著提升稀疏数据下GPLearn等符号回归方法的方程恢复性能。无需额外领域知识即可有效增强方程发现，拓展了符号回归的实用范围。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00988v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1630, \"height\": 410}, {\"url\": \"assets/figures/arxiv/2606.00988v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1638, \"height\": 894}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.00988v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1563, \"height\": 760}, {\"url\": \"assets/tables/arxiv/2606.00988v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1528, \"height\": 1036}, {\"url\": \"assets/tables/arxiv/2606.00988v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1658, \"height\": 867}, {\"url\": \"assets/tables/arxiv/2606.00988v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1044, \"height\": 944}, {\"url\": \"assets/tables/arxiv/2606.00988v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1659, \"height\": 850}]"
motivation: 稀疏、含噪及物理不完整数据导致符号回归可靠性下降，现有数据增强缺乏物理约束易误导模型。
method: 提出物理引导的潜在扩散框架，结合VAE、条件扩散模型和物理残差校正器，生成受控制方程约束的合成数据。
result: 在热传导、流体和引力系统上，物理校正增强一致提升稀疏数据下GPLearn等方法的方程恢复能力。
conclusion: 生成式增强无需额外领域知识即可有效强化符号回归，有望促进科学发现自动化。
---

## 摘要
符号回归（SR）通过将观测转化为可解释的控制方程，为科学发现提供了一条途径。然而，尽管前景广阔，但在实际中常见的时空测量稀疏、噪声大或物理不完整的情况下，其可靠性会急剧下降。数据增强（DE）已被证明能够缓解这一局限性，但除非额外样本保留了目标系统的物理结构，否则它们可能会误导方程发现。这种DE的含义需要狭窄的领域专业知识以及技术流畅性，极大地限制了其实用性。在本研究中，我们引入了一个物理引导的潜在扩散框架，用于下游SR模型的数据增强。该框架结合了变分自编码器、条件潜在扩散模型和物理信息残差校正器，用受控制关系约束的合成场来补全稀疏观测。我们在热传导、不可压缩纳维-斯托克斯流以及移动单质量牛顿引力势上，使用GPLearn、DEAP和PySR作为下游SR后端评估了该方法。我们的结果表明，物理校正的增强在稀疏区域中始终改善了物理动力学和SR模型的恢复。这些结果表明，生成式增强可以在无需额外领域专业知识的情况下加强方程发现。

## Abstract
Symbolic regression (SR) offers a route to scientific discovery by converting observations into interpretable governing equations. However, despite its promise, its reliability degrades sharply when spatiotemporal measurements are sparse, noisy, or physically incomplete, as commonly occurring in practice. Data enrichment (DE) has been shown to be able to mitigate this limitation, yet additional samples can mislead equation discovery unless they preserve the physical structure of the target system. Such implication of DE requires narrow domain expertise as well as technical fluidity, highly limiting its practical usefulness. In this study, we introduce a physics-guided latent diffusion framework for DE for down the line SR models. The proposed framework combines a variational autoencoder, a conditional latent diffusion model, and a physics-informed residual corrector to complete sparse observations with synthetic fields constrained by governing relations. We evaluate the approach on heat conduction, incompressible Navier-Stokes flow, and a moving single-mass Newtonian gravitational potential, using GPLearn, DEAP, and PySR as downstream SR backends. Our results reveal that physics-corrected enrichment consistently improves recovery in sparse regimes across physical dynamics and SR models. These results show that generative enrichment can strengthen equation discovery without additional domain expertise.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将基于您提供的论文内容，遵循您的要求，用中文、以 Markdown 形式，对该论文进行结构化、深入、客观的总结。

---

### 论文详细中文总结：使用扩散模型的符号回归数据增强

#### 1. 核心问题与整体含义（研究动机和背景）

*   **核心问题**：符号回归（Symbolic Regression, SR）是一种能从观测数据中发现可解释的数学方程的技术，对于科学发现非常重要。然而，其性能严重依赖于数据的质量、密度和覆盖范围。在现实世界中，时空测量数据通常**稀疏、噪声大或物理上不完整**，这导致SR的可靠性急剧下降。
*   **现有局限**：数据增强（Data Enrichment, DE）技术（如使用高斯过程）虽然能缓解数据稀疏问题，但生成的额外样本若不能保持目标系统的物理结构，反而会误导方程发现。传统的DE方法依赖于狭窄的领域专业知识，严重限制了其实用性。
*   **研究含义**：本文旨在探索一种**通用、无需额外领域知识**的数据增强方法，通过生成**物理一致**的合成数据来提升SR在稀疏数据下的性能。其核心假设是：只有保留物理结构的合成数据才对SR有益。

#### 2. 论文提出的方法论（核心思想与关键技术）

该论文提出了一个名为 **“物理引导的潜在扩散框架”**，用于为下游的SR模型生成增强数据。它是一个三阶段的结构：

1.  **阶段一：变分自编码器（VAE）**：负责将高维的时空物理场压缩到低维的潜在空间。这能降低后续生成模型的复杂度，使其能更优雅地处理高维数据。
2.  **阶段二：条件潜在扩散模型（LDM）**：在VAE的潜在空间中运行，根据给定的物理参数（如热导率、雷诺数、质量、源位置等）作为条件，生成新的、统计上合理的潜在序列。它使用了3D U-Net结构来建模时空演化。
3.  **阶段三：物理信息残差校正器（Physics-Informed Corrector）**：对LDM解码回物理空间的生成结果进行“二次精修”。它不是从头生成，而是预测一个残差项，加到LDM的输出上。其训练目标包含一个核心的**物理损失函数**，该函数根据不同的物理系统定制（例如，对热传导惩罚傅里叶定律的残差，对纳维-斯托克斯方程惩罚不可压缩性残差等）。

**训练流程**：
1.  先训练VAE，将其编码器和解码器固定。
2.  基于编码后的潜在特征训练LDM。
3.  缓存VAE+LDM链路的预测结果，然后以此来训练校正器。这一阶段的分离设计使得计算上昂贵的物理损失可以在相对低成本下应用。

**关键技术细节**：
*   **条件扩散**：模型的生成过程可以受控于“物理参数”（如粘度、质量、源位置）和“空间线索”（如障碍物位置）。
*   **物理损失（L_phys）**：这是框架的核心，它惩罚违反物理定律的行为。例如对于热传导，L_phys包含了针对热方程残差、傅里叶定律误差、边界条件违反、时空平滑性等多项损失（L_P, L_F, L_BC, L_S等）。对于纳维-斯托克斯，则包括不可压缩性、动量残差、边界条件等损失（L_D, L_M, L_BC等）。对于势，包括拉普拉斯、源轨迹等损失。
*   **辅助损失（Auxiliary Losses）**：对于纳维-斯托克斯，该论文还加入了专门针对符号回归任务的辅助损失（如特征匹配损失、频谱损失、系数对齐损失等），目标是更好地保留SR所需的导数结构。

#### 3. 实验设计

*   **物理系统与数据**：在三个经典时空物理系统上验证：
    *   **热传导**：2D热方程，目标是恢复**傅里叶定律**（`qx = -k * dT/dx`）。
    *   **不可压缩纳维-斯托克斯流动**：2D圆柱绕流，目标是恢复**垂直动量方程**。
    *   **单质量牛顿引力势**：2D移动点源的引力势场，目标是恢复**逆缩放引力势形式**。
    所有数据均为合成生成，物理参数（如热导率、雷诺数、质量）被采样用于条件生成和控制。
*   **基准方法（Baselines）**：
    *   **Real only**：仅使用真实稀疏数据训练SR模型。
    *   **Real + LDM**：使用真实数据和未经物理校正的LDM生成数据训练SR模型。
    *   **Real + PhysLDM (提出的方法)**：使用真实数据和经物理校正器校正后的合成数据训练SR模型。
*   **下游SR模型（Backends）**：使用了三种流行的、开箱即用的SR模型：**GPLearn**、**DEAP** 和 **PySR**。这保证了评估的广泛性和客观性。
*   **评估指标**：
    *   **方程发现性能**：通过**均方误差（MSE）** 和**决定系数（R²）** 来评估SR模型恢复的数学表达式与真实目标值之间的一致性。这些指标分别针对每个物理系统的特定目标（如热通量 `q`、时间导数 `d_t_v`、势函数 `phi`）。
    *   **生成数据质量**：针对每个物理系统，评估了生成数据的**场保真度**（如通量MSE）和**物理有效性**（如散度损失、傅里叶残差、轨迹误差等）。
*   **稀疏度协议**：模拟时间稀疏性，保留不同数量的真实帧（N = 2, 4, 8, 16），并用合成数据补全至原序列长度。

#### 4. 资源与算力

论文**并未明确提及**所使用的GPU型号、数量、显存或总训练时长等算力信息。它只给出了各模块的详细训练超参数（如epoch数、学习率、batch size等）。

#### 5. 实验数量与充分性

*   **实验数量**：实验非常**充分**。
    *   在**稀疏数据实验**中，使用了3个物理系统 × 4个稀疏度（N=2,4,8,16） × 3个SR后端 × 3种数据变体 = **108种**组合。每个结果来自30个序列和5个随机种子的聚合，共**150次运行**。
    *   在**纯合成数据实验**中，同样覆盖了3个物理系统 × 3个SR后端 × 2种变体（LDM vs PhysLDM）。
    *   在**生成数据质量评估**中，对3个物理系统分别评估了多种物理一致性指标。
*   **实验充分性与公平性**：实验是**客观、公平且充分的**。它系统性地对比了有无物理校正的增强，并且使用了不同的SR后端来验证方法的泛化性。稀疏度的选择（2,4,8,16）也覆盖了从极稀疏到相对充足数据的场景。使用MSE和R²作为客观指标。

#### 6. 论文的主要结论与发现

1.  **物理校正是关键**：在所有物理系统和SR后端中，经过物理校正的合成数据（Real + PhysLDM）在**低观测数据（稀疏）** 情况下，始终比仅使用真实数据（Real only）或未校正的生成数据（Real + LDM）表现更好。
2.  **纯合成数据也可用**：在没有真实数据的情况下，纯物理校正的合成数据（PhysLDM）也能在很大程度上支持SR恢复目标方程，其性能远优于未校正的LDM生成数据。
3.  **具体表现差异**：
    *   **热传导**：Real+PhysLDM在所有稀疏度下均表现出压倒性优势，其MSE可达~10^-4，R²接近1.0。
    *   **纳维-斯托克斯**：Real+PhysLDM的优势主要集中在**高度稀疏**（N=2,4）的情况下。当真实数据较多（N=8,16）时，Real only的表现更好。这体现了导数发现任务对高精度数据的强烈依赖。
    *   **牛顿引力势**：Real+PhysLDM对GPLearn和PySR提升显著，但对DEAP后端提升有限。这表明物理增强的效果也依赖于下游SR模型的特性。
4.  **数据生成质量提升**：物理校正器（PhysLDM）在**所有场水平和物理一致性指标**上都显著优于纯LDM，例如将傅里叶残差RMSE降低了96.32%，将散度损失降低了65.90%。

#### 7. 优点（方法与实验设计的亮点）

*   **方法创新性显著**：
    *   将**生成式AI（扩散模型）** 与**物理信息**巧妙结合，用于解决SR中的数据稀缺难题，创新性很高。
    *   设计了一个**三阶段式、模块化**的框架，将表示学习、无条件/条件生成和物理约束精修清晰分离，便于理解和扩展。
    *   物理损失的定制化设计（包含对SR友好的辅助损失）是其核心创新点之一。
*   **实验设计系统且严谨**：
    *   覆盖了**多个物理系统**（传热、流体、势场），验证了方法的通用性。
    *   使用了**多个SR模型**作为下游后端，避免了方法对特定SR模型的依赖，结果更具说服力。
    *   设计了**控制变量**实验（Real only vs. Real+LDM vs. Real+PhysLDM），清晰地揭示了物理校正的根本作用。

#### 8. 不足与局限

*   **基于合成数据**：所有实验都是在合成数据上进行的，物理参数和边界条件完全已知。这未包括真实实验中存在的所有不确定性（如系统噪声、模型偏差、未知干扰）。向真实应用场景的迁移性仍需验证。
*   **条件匹配设置**：其核心实验是“条件匹配”的，即生成的合成数据与真实数据来自**同一个物理场景**（参数相同）。这限制了其在**外推（Extrapolation）** 到未见物理参数时的性能评估。
*   **时间一致性问题**：目前的数据补全方法是**拼接式**的（将生成的合成数据与真实的稀疏帧拼在一起），论文也承认这并未明确强制处理保留帧与生成帧之间界面处的时间一致性。这可能导致不连续。
*   **计算成本**：虽然论文未明确说明，但这个三阶段框架（VAE + LDM + Corrector）可能**计算成本较高**，尤其在训练和推理阶段。其能否在资源受限的场景下高效部署仍需考量。
*   **方法局限性**：对纳维-斯托克斯问题，其优势主要体现在高度稀疏场景，这揭示了物理信息注入虽有益，但当数据量足够时，其贡献相对有限，且高度依赖SR模型本身的灵敏度。此外，**物理信息质量**直接影响最终性能，若引入错误的物理先验可能更糟糕。

（完）
