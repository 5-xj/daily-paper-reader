# CADEvolve: Creating Realistic CAD via Program Evolution
# CADEvolve：通过程序演化创建逼真的 CAD

**Authors**: Maksim Elistratov, Marina Barannikov, Gregory Ivanov, Valentin Khrulkov, Anton Konushin, Andrey Kuznetsov, Dmitrii Zhemchuzhnikov \\
**Date**: 2026-02-18 \\
**PDF**: https://arxiv.org/pdf/2602.16317v1 \\
**Tags**: <span class="tag-label tag-blue">精读区</span> <span class="tag-label tag-pink">keyword:EOH</span> <span class="tag-label tag-pink">keyword:EAA</span> \\
**Score**: 8.0 \\
**Evidence**: program evolution pipeline for automated CAD generation and optimization \\

---

## Abstract
Computer-Aided Design (CAD) delivers rapid, editable modeling for engineering and manufacturing. Recent AI progress now makes full automation feasible for various CAD tasks. However, progress is bottlenecked by data: public corpora mostly contain sketch-extrude sequences, lack complex operations, multi-operation composition and design intent, and thus hinder effective fine-tuning. Attempts to bypass this with frozen VLMs often yield simple or invalid programs due to limited 3D grounding in current foundation models. We present CADEvolve, an evolution-based pipeline and dataset that starts from simple primitives and, via VLM-guided edits and validations, incrementally grows CAD programs toward industrial-grade complexity. The result is 8k complex parts expressed as executable CadQuery parametric generators. After multi-stage post-processing and augmentation, we obtain a unified dataset of 1.3m scripts paired with rendered geometry and exercising the full CadQuery operation set. A VLM fine-tuned on CADEvolve achieves state-of-the-art results on the Image2CAD task across the DeepCAD, Fusion 360, and MCB benchmarks.

## 摘要
计算机辅助设计（CAD）为工程和制造提供了快速、

---

## 论文详细总结（自动生成）

这篇论文介绍了 **CADEvolve**，这是一个通过演化算法生成高质量 CAD 程序数据集的框架，并基于此训练出了在 Image2CAD 任务上达到 SOTA（最先进）水平的模型。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：CAD 自动化生成的进展受限于**高质量数据**。现有的公开 CAD 数据集（如 DeepCAD, Fusion 360 Gallery）大多仅包含简单的“草图-拉伸”（sketch-extrude）序列，缺乏复杂的工业级操作（如旋转、放样、扫掠、圆角等）和设计意图。
*   **整体含义**：论文提出了一种“演化生成”的思路，利用视觉语言模型（VLM）作为“变异器”，通过自动化的执行和验证反馈，从简单的几何原语逐步演化出复杂的 CAD 程序，从而构建了一个包含 130 万个脚本的大规模、全算子覆盖的数据集。

### 2. 方法论：核心思想与技术细节
CADEvolve 包含一个三层的流水线（CADEvolve-3L）：
*   **演化合成 (CADEvolve-G)**：
    *   **种子库**：从 46 个手写的 CadQuery 基础原语开始。
    *   **提议-执行-过滤循环**：利用 VLM（如 GPT-5-mini）对父代程序进行编辑和扩展。
    *   **多阶段验证**：包括代码执行检查（是否生成单体固态）、几何合法性检查、以及视觉-文本一致性检查（VLM 验证渲染图是否符合描述）。
*   **程序采样与解析 (CADEvolve-P)**：
    *   对生成的参数化生成器进行采样，通过黑盒优化（CMA-ES）寻找能产生多样化几何形状的参数。
    *   通过代码重写（Augmentation）增加代码风格的多样性，避免模型过拟合特定模板。
*   **规范化与训练 (CADEvolve-C)**：
    *   **规范化**：统一代码格式、居中对齐、缩放至统一尺度，并将数值参数二值化（取整），使模型专注于构造逻辑而非浮点噪声。
    *   **混合增强**：引入 ABC 和 ShapeNet 数据集的预测结果，并结合 CAD-Recode 的草图多样性进行增强。
*   **策略训练 (CADEvolve-M)**：
    *   使用 Qwen2-VL-2B 作为骨干网络。
    *   采用 **SFT（监督微调）** + **RL（强化学习）** 的两阶段训练。RL 阶段使用基于 IoU 的奖励函数和 Dr. CPPO 算法。

### 3. 实验设计
*   **任务场景**：**Image2CAD**（根据多视图渲染图重建 CAD 程序）。
*   **数据集/Benchmark**：
    *   **DeepCAD**、**Fusion 360 Gallery**：传统的 CAD 序列基准。
    *   **MCB (Mechanical Components Benchmark)**：复杂的机械零件基准。
*   **对比方法**：主要对比了当前的 SOTA 模型 **cadrille**。
*   **评估指标**：倒角距离 (CD)、体素交并比 (IoU)、无效率 (IR)。

### 4. 资源与算力
*   **模型**：使用了 GPT-5-mini 用于离线数据生成，Qwen2-VL-2B 作为最终的推理模型。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到了在演化过程中有“计算预算”限制，且 RL 阶段进行了 20 个 epoch 的训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   生成了约 8k 个复杂参数化生成器，最终构建了约 130 万个训练脚本。
    *   针对数据集规模做了消融实验（Small, Middle, Big 三个等级），验证了引入外部数据预测和规范化的有效性。
*   **充分性与公平性**：实验覆盖了三个主流基准，并采用了与对比方法（cadrille）相同的 RL 训练协议（Dr. CPPO），确保了对比的公平性。通过对 MCB 数据集的重新划分，验证了模型在处理复杂工业零件时的泛化能力。

### 6. 主要结论与发现
*   **数据质量胜过单纯的数量**：代码的规范化（Canonicalization）和参数二值化对提升模型性能至关重要。
*   **全算子覆盖的必要性**：包含旋转（Revolve）、放样（Loft）等复杂操作的数据集能显著提升模型对真实工业零件的重建精度。
*   **SOTA 性能**：CADEvolve-M 在所有三个基准测试中均取得了领先的 CD 和 IoU 指标，尤其是在复杂的 MCB 数据集上提升巨大。

### 7. 优点与亮点
*   **演化式数据生成**：巧妙地解决了 CAD 领域缺乏带历史记录的复杂数据的问题，将 VLM 的生成能力与 CAD 内核的确定性验证相结合。
*   **全算子支持**：是首个公开的支持全套 CadQuery 操作符的大规模执行序列数据集。
*   **规范化流程**：提出的居中、缩放、取整等规范化步骤，为后续 CAD 生成研究提供了标准化的预处理参考。

### 8. 不足与局限
*   **合成数据分布偏差**：演化生成的数据虽然复杂，但其分布可能与