# Enhancing Genetic Algorithms with Graph Neural Networks: A Timetabling Case Study
# 利用图神经网络增强遗传算法：排班调度案例研究

**Authors**: Laura-Maria Cornei, Mihaela-Elena Breabăn \\
**Date**: 2026-02-09 \\
**PDF**: https://arxiv.org/pdf/2602.08619v1 \\
**Tags**: <span class="tag-label tag-blue">精读区</span> <span class="tag-label tag-pink">keyword:EOH</span> <span class="tag-label tag-pink">keyword:EAA</span> \\
**Score**: 8.0 \\
**Evidence**: hybridizes genetic algorithms with neural networks for optimization search \\

---

## Abstract
This paper investigates the impact of hybridizing a multi-modal Genetic Algorithm with a Graph Neural Network for timetabling optimization. The Graph Neural Network is designed to encapsulate general domain knowledge to improve schedule quality, while the Genetic Algorithm explores different regions of the search space and integrates the deep learning model as an enhancement operator to guide the solution search towards optimality. Initially, both components of the hybrid technique were designed, developed, and optimized independently to solve the tackled task. Multiple experiments were conducted on Staff Rostering, a well-known timetabling problem, to compare the proposed hybridization with the standalone optimized versions of the Genetic Algorithm and Graph Neural Network. The experimental results demonstrate that the proposed hybridization brings statistically significant improvements in both the time efficiency and solution quality metrics, compared to the standalone methods. To the best of our knowledge, this work proposes the first hybridization of a Genetic Algorithm with a Graph Neural Network for solving timetabling problems.

## 摘要
本文探讨了将多模态遗传算法与图神经网络相结合

---

## 论文详细总结（自动生成）

这篇论文探讨了如何通过结合**遗传算法（GA）**和**图神经网络（GNN）**来解决复杂的排班优化问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：解决**员工排班问题（Staff Rostering）**，这是一种典型的 NP-hard 组合优化问题，涉及在满足多种硬约束（如法律法规、工时限制）和软约束（如员工偏好）的前提下，优化排班质量。
*   **研究背景**：
    *   **遗传算法（GA）**：擅长全局搜索和保持解的多样性，但在面对新实例时需要从头开始，耗时较长。
    *   **图神经网络（GNN）**：具有强大的泛化能力，能从训练数据中提取领域知识并快速生成解，但容易陷入局部最优。
*   **研究动机**：通过混合两种方法，利用 GNN 作为 GA 的改进算子，既能利用 GNN 的快速推理能力引导搜索，又能利用 GA 的探索能力跳出局部最优。

### 2. 方法论
*   **核心思想**：将预训练好的 GNN 集成到多模态遗传算法中，作为一种**染色体改进算子（Improvement Operator）**。
*   **关键技术细节**：
    *   **图建模**：构建异质图，包含三种节点：员工、班次、日期。边连接包括“员工-班次”、“班次-日期”以及相邻日期的“班次-班次”（捕捉时间流信息）。
    *   **GNN 架构**：采用 `TransformerConv` 层处理异质图特征，后接多层感知机（MLP）预测每个（员工, 日期）对的班次概率分布。
    *   **训练策略**：使用监督学习，训练集包含（可行解-最优解）和（不可行解-可行解）的配对，通过交叉熵损失函数最小化预测与目标之间的差异。
    *   **GA 流程**：
        1.  **初始化**：随机生成种群。
        2.  **变异与交叉**：标准的遗传操作。
        3.  **GNN 改进**：对变异后的后代应用 GNN，直接预测并修正排班表以提高质量。
        4.  **多样性维护**：引入**概率拥挤（Probabilistic Crowding）**机制，通过计算染色体间的汉明距离（排班差异）来保持种群多样性。

### 3. 实验设计
*   **场景与数据集**：
    *   基于 100 名员工、7 天周期的排班场景。
    *   生成了 250 个实例用于训练 GNN，50 个独立实例用于测试。
*   **对比方法（Benchmark）**：
    *   **独立 GNN**：直接输出预测结果。
    *   **独立 GA（两个版本）**：GA_v1（找到最优解即停）、GA_v2（固定容忍度停机）。
    *   **混合方法（GA+GNN）**：本文提出的方法。
*   **评估指标**：适应度值（Fitness）、硬/软约束惩罚值、可行解比例、最优解数量、计算时间、种群多样性（拥挤距离）。

### 4. 资源与算力
*   **硬件配置**：Intel Core Ultra 9 285k CPU, **NVIDIA GeForce RTX 5080 (16GB)** GPU, 192GB DDR5 内存。
*   **软件环境**：Python, PyTorch Geometric, Numba (JIT 编译加速), Gurobi (用于生成训练集的最优解)。
*   **训练细节**：使用了早停法（Early Stopping），但未明确说明 GNN 的具体训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：对 50 个测试实例中的每一个都进行了 10 次独立运行，以确保统计意义。
*   **充分性**：
    *   进行了超参数搜索（40 组随机搜索试验）以优化 GNN。
    *   使用了 **Welch's t-test**（95% 置信水平）进行显著性检验。
    *   对比了不同停机条件下的表现，实验设计较为严谨、客观。

### 6. 主要结论与发现
*   **效率大幅提升**：在 GA_v1 设置下，混合方法（GA+GNN）比纯 GA 快了 **10 倍以上**（平均 2.26 分钟 vs 23.1 分钟