---
title: Memory-Guided Tree Search with Cross-Branch Knowledge Transfer for LLM Solver Synthesis
title_zh: 面向LLM求解器综合的跨分支知识迁移记忆引导树搜索
authors: "Fatemeh Haji, Javier Delarosa Quiros, Peyman Najafirad"
date: 2026-05-17
pdf: "https://arxiv.org/pdf/2605.17539v1"
tags: ["query:ad"]
score: 9.0
evidence: 利用大语言模型自动生成组合优化求解器，结合记忆引导树搜索和跨分支知识迁移
tldr: "组合优化问题中，现有LLM求解器合成方法因缺乏跨分支知识转移而重复相同约束违反。本文提出MEMOIR框架，通过分支本地内存保留细化细节，全局内存存储算法与失败模式摘要，在分支终止时反思蒸馏以实现跨分支迁移。在7个CO问题上，MEMOIR实现96.7%解有效性（超基线9.2点），归一化分数提升7.3点，且运行间方差低一个数量级，展示了记忆引导探索的稳健改进。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.17539v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1448, \"height\": 484, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.17539v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1269, \"height\": 586, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.17539v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1445, \"height\": 660, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.17539v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 712, \"height\": 299, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.17539v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 714, \"height\": 300, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.17539v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1412, \"height\": 1652, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.17539v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1445, \"height\": 783, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.17539v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1437, \"height\": 313, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.17539v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 674, \"height\": 334, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.17539v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1436, \"height\": 252, \"label\": \"Table\"}]"
motivation: 现有树搜索和进化智能体并行细化候选轨迹，缺乏显式知识转移，导致重复约束违反和算法收敛于相似家族。
method: 提出MEMOIR，包含分支本地和全局两级记忆，分支终止时通过反思步骤提取跨分支可转移的算法与失败模式摘要。
result: "在7个CO问题上，解有效性96.7%（超最强基线9.2点），归一化分数提升7.3点，运行间方差低于所有基线一个数量级。"
conclusion: 记忆引导的跨分支知识转移显著提升LLM求解器合成的有效性、质量和稳定性，且改进非源于采样方差。
---

## 摘要
组合优化是支撑从物流到芯片设计等决策制定的基础，其中不可行的解在操作上无法使用，而微小的质量提升可转化为巨大的经济价值。近期研究利用大型语言模型自动化求解器综合：从自然语言规范生成可执行的求解器程序。然而，现有的树搜索和进化智能体并行优化候选轨迹，缺乏显式知识迁移，导致重复引入相同的约束违反，并收敛于相似的算法族。我们提出MEMOIR，一种具有二级记忆层次结构的记忆引导树搜索框架：分支局部记忆在单次算法设计的迭代过程中保留基于执行的细化细节，而全局记忆则跨分支存储压缩的算法和失败模式摘要。分支终止时的反思步骤提炼这些摘要，实现跨分支知识迁移，同时避免用低级调试痕迹污染未来上下文。在涵盖调度、路由、装箱和几何设计的七个组合优化问题上，MEMOIR实现了96.7%的解有效性（比最强基线高出9.2个百分点），并在匹配每方法执行预算下将平均归一化分数提高了7.3个百分点。在四个问题上的三次独立运行中，MEMOIR的运行间有效性标准差比我们在此设置下评估的每个基线低一个数量级以上，表明记忆引导的探索带来了一致的改进，而非采样方差。

## Abstract
Combinatorial optimization (CO) underlies decision-making from logistics to chip design, where infeasible solutions are operationally unusable and small quality gains translate into substantial economic value. Recent work uses large language models (LLMs) to automate solver synthesis: generating executable solver programs from natural-language specifications. However, existing tree-search and evolutionary agents refine candidate trajectories in parallel without explicit knowledge transfer, reintroducing the same constraint violations and converging on similar algorithm families. We introduce MEMOIR, a memory-guided tree-search framework with a two-level memory hierarchy: branch-local memory preserves execution-grounded refinement details within a branch as it iterates on a single algorithmic design, while global memory stores compressed algorithmic and failure-mode summaries across branches. A reflection step at branch termination distills these summaries, enabling cross-branch transfer without polluting future contexts with low-level debugging traces. Across seven CO problems spanning scheduling, routing, packing, and geometric design, MEMOIR achieves 96.7% solution validity (a 9.2 point gap over the strongest baseline) and improves the average normalized score by 7.3 points at matched per-method execution budget. Over three independent runs on four problems, MEMOIR's run-to-run validity standard deviation is more than an order of magnitude below that of every baseline we evaluated in this setting, suggesting that memory-guided exploration yields consistent improvements rather than reflecting sampling variance.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题背景**：组合优化（CO）问题（如调度、路由、装箱、几何设计）是 NP 难的，传统依赖专家设计的启发式求解器，开发成本高、泛化性差。近期工作利用大型语言模型（LLM）自动合成求解器程序，但现有树搜索和进化方法并行探索多个候选轨迹时，缺乏跨分支的显式知识迁移，导致重复出现相同的约束违反，并收敛于相似的算法族。
- **核心挑战**：如何在不污染上下文的前提下，将一条分支中的执行经验和失败教训有效地传递给后续分支，从而避免重复犯错，提升搜索效率和求解质量。
- **本文目标**：提出 MEMOIR 框架，通过两级记忆层次结构（分支本地记忆 + 全局记忆）和分支终止时的反思步骤，实现跨分支的算法与失败模式迁移，在固定执行预算下显著提升解的有效性和得分。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将求解器合成视为树搜索，每个分支对应一个独立的算法设计。分支内部利用**分支本地记忆**保留基于执行的细化细节（修复和优化操作的记录），避免同一分支内重复失败；分支终止时通过**反思算子**将整条分支的经验压缩为算法设计、失败模式、避免指令三要素，存入**全局记忆**，指导后续分支探索全新的设计空间。这样既实现了跨分支的知识迁移，又防止低级调试细节污染未来上下文。
- **关键技术细节**：
  1. **五类 LLM 算子**：
     - **Propose**：基于问题描述和全局记忆，生成初始算法描述和求解器代码。
     - **Repair**：当前分支全部解无效时，选择最接近有效性的父节点，利用分支记忆修复代码。
     - **Improve**：已有有效解时，对最高得分有效解进行原子级改进，保持可行性。
     - **Critic**：对比父子节点执行结果，生成诊断（如是否修复了 bug、性能变化）。
     - **Reflect**：分支结束后，从整条轨迹中提炼出算法设计、失败/停滞原因、负面约束，写入全局记忆。
  2. **两级记忆结构**：
     - **分支本地记忆**：存储该分支所有节点的记录（包括算法描述、诊断、有效性标志、得分、执行输出），用于指导修复和优化。
     - **全局记忆**：存储压缩的分支总结（平均约 272 tokens），以 O(m) 规模增长，确保上下文不会膨胀。
  3. **搜索流程**：在总执行预算 B=16 下，循环打开新分支（约 m=4 个分支，每分支深度上限 n=5）。每分支初始化执行一次，随后在深度内进行 repair/improve 迭代，若无法再提升则提前终止。分支终止时执行反思，更新全局记忆。搜索结束后，从所有节点中选出 dev 集上得分最高的有效求解器，在 test 集上评估。
- **公式与算法**：论文给出了完整的求解器合成形式化定义（式 1 - 3），以及 MEMOIR 算法的伪代码（Algorithm 1），详细描述了分支初始化、内循环、反射及最终选择的步骤。

## 3. 实验设计

- **数据集/场景**：采用 CO-Bench 基准中的 7 个经典 CO 问题：Aircraft Landing、Periodic Vehicle Routing、Container Loading、Container Loading with Weight Restrictions、Resource Constrained Shortest Path、Crew Scheduling、Euclidean Steiner。每个问题有 dev 集（用于合成）和 test 集（用于最终报告）。
- **评价指标**：归一化平均得分（Avg, [0,1]，更高更好）和有效性（Valid, 可行解占实例比例）。
- **对比方法**：
  - **经典求解器**：各问题公开的专家设计求解器分数。
  - **直接 LLM 合成**：Llama-3.3-70B-Instruct、Qwen2.5-Coder-32B、GPT-5 Chat 及推理模型 o3-mini-high、DeepSeek-R1-Distill-Llama-70B、QwQ-32B、GPT-5-mini，单次生成。
  - **LLM 智能体方法**：GreedyRefine、AIDE、FunSearch、ReEvo、MCTS-AHD，所有方法共享 16 次执行预算。
- **实验充分性**：主要实验报告了 7 个领域上的 Avg 和 Valid 表格（Table 1），并提供 per-instance 得分分布图（Figure 2）、收敛曲线（Figure 3）以及难度分层分析（Appendix D）。消融实验（Table 2）验证了全局记忆、分支本地记忆、失败节点记录的必要性。3 次独立运行稳定性分析（Appendix F）展示了 MEMOIR 方差极低。

## 4. 资源与算力

- 论文未明确指定使用的 GPU 型号或数量。**所有方法均通过 LLM API（如 GPT-5-mini、GPT-5）调用完成**，求解器执行在 CPU 沙箱（Python 3.10.18）中进行，每实例超时 T=10s。
- Appendix E 报告了每运行的平均 token 消耗和美元成本：MEMOIR（GPT-5-mini w/ GPT-5 critic）约 $0.52/run，单骨干变体约 $0.06/run，合成时间约 20-40 分钟。整体算力需求较低，未涉及大规模分布式训练。

## 5. 实验数量与充分性

- **实验数量**：涵盖 7 个不同特性的 CO 问题，每个问题报告 test 集结果。消融实验 3 种变体（w/o Global Memory、w/o Branch-Local Memory、w/o Failed Nodes）。稳定性实验在 4 个问题上重复 3 次。额外难度分层分析在 2 个关键问题（Aircraft Landing、Periodic Vehicle Routing）上给出 per-bin 分布。
- **充分性与公平性**：
  - 所有 LLM 智能体方法均使用相同执行预算（B=16）和骨干模型（GPT-5-mini），MEMOIR 的 Critic/Reflect 升级版（w/ GPT-5 critic）也明确说明，并提供了单骨干版本对比。
  - 消融实验隔离了每个记忆组件的贡献，结果清晰。
  - 运行间方差对比显示 MEMOIR 高度稳定（Avg stdev 0.0098, Valid stdev 0.0005），远低于所有基线。
- **局限**：仅测试了低预算（16 次执行），未在进化规模下评估；预算分配规则为启发式，未用学习策略优化。

## 6. 论文的主要结论与发现

- MEMOIR 在 7 个 CO 问题上平均达到 **96.7% 有效性**（比最强基线 FunSearch 高 9.2 个百分点），**归一化得分 0.7301**（比 FunSearch 高 7.3 个百分点）。
- 即使使用同一骨干（GPT-5-mini）的 MEMOIR 也全面超越所有基线，说明记忆设计本身是关键。
- 消融实验表明：
  - 去掉全局记忆 → Avg 下降 6.81 点，Valid 下降 5.32 点。
  - 去掉分支本地记忆 → 最严重下降（Avg -12.09, Valid -16.55）。
  - 去掉失败节点记录 → Avg 下降 8.37 点，Valid 下降 6.80 点。
- 全局记忆与分支本地记忆的分离，以及反思步骤的压缩，是避免上下文污染并实现有效迁移的核心。
- 运行间方差极低，表明记忆引导的改进具有一致性，而非抽样方差。

## 7. 优点

- **方法设计清晰**：两级记忆层次直观且有效，回答了“什么该保留在本地、什么该跨分支共享”的核心问题。
- **跨分支知识迁移**：通过反思将调试细节过滤，仅传递高层算法教训，避免 LLM 上下文被噪声淹没。
- **实验全面**：覆盖多种 CO 类型、多类基线、消融实验、稳定性测试、难度分层。
- **结果显著且稳定**：在低预算下即大幅领先，且运行方差极低，实用性较强。
- **开源承诺**：提供匿名仓库和详细提示模板，利于复现。

## 8. 不足与局限

- **预算限制**：仅测试了 B=16 的低预算场景，未探索进化规模（如数百次执行）下的行为。
- **启发式预算分配**：分支间预算分配基于固定规则（深度上限 n=5），未采用自适应或学习策略。
- **领域覆盖有限**：7 个问题虽具代表性，但未涵盖更多结构（如大规模图优化、约束规划等）。
- **反思依赖 LLM**：全局记忆条目由 LLM 总结，可能错误表征失败模式，进而排除有用启发式家族。
- **无跨问题迁移**：当前针对每个问题独立运行，未尝试将全局记忆在同类问题间复用。
- **算力报告不详细**：未披露 LLM 推理的硬件配置，仅提及 API 调用成本，影响可复现性的细节。

（完）
