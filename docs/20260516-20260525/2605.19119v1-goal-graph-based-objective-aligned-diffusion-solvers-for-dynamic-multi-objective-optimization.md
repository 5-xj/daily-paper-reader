---
title: "GOAL: Graph-based Objective-Aligned Diffusion Solvers for Dynamic Multi-Objective Optimization"
title_zh: "GOAL: 基于图的目标对齐扩散求解器用于动态多目标优化"
authors: Xingyu Li
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.19119v1"
tags: ["query:rl-comb-opt"]
score: 9.0
evidence: 基于图扩散的神经组合优化方法用于多目标调度
tldr: "现有神经组合优化求解器局限于单目标最小化和静态约束，通过模仿最优解进行搜索。本文提出GOAL，一种基于关系图表示的条件扩散求解器，通过异构图编码区分不同约束类型的边，实现按人类指定目标的可控决策生成。在Flow Shop、Job Shop和Flexible Job Shop三个调度基准上，GOAL在多达20个作业60个操作的规模下达到100%可行解，多目标MAPE低于0.20%，且求解速度比NSGA-II和MOEA/D快达25倍，无需修改架构即可泛化到不同约束和问题类型。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.19119v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1431, \"height\": 313, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.19119v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 673, \"height\": 510, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.19119v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 508, \"height\": 477, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.19119v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1454, \"height\": 543, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.19119v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 764, \"height\": 476, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.19119v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 935, \"height\": 424, \"label\": \"Table\"}]"
motivation: 现有方法仅适用于单目标与静态约束，缺乏可控的多目标优化能力，且难以泛化到不同约束结构。
method: 提出GOAL，利用条件扩散模型对关系图进行可控生成，通过异构图编码按约束类别定制消息传递路径。
result: "在三个调度基准上，GOAL均实现100%可行解，多目标MAPE低于0.20%，推理速度比NSGA-II等快25倍。"
conclusion: GOAL在动态多目标优化中兼具高质量、可控性和强泛化性，为神经组合优化提供了新范式。
---

## 摘要
现有的神经组合优化求解器将解搜索视为最优决策的模仿，本质上将其效用限制在单目标最小化和静态约束上。我们提出GOAL，一种基于关系图表示的条件扩散求解器，通过以人类指定的目标为条件，实现可控的决策生成。我们引入了一种异构图编码，其中不同的边类型对应不同类别的约束，定义了图神经网络的消息传递结构，使得信息能够根据每个约束的本体论进行选择性传播。GOAL在三个具有不同约束复杂性的经典调度基准上进行了实例化和评估：流水车间问题（FSP）、作业车间调度问题（JSP）和柔性作业车间调度问题（FJSP）。在不修改架构的情况下，展现了跨结构不同约束体制和问题类型的泛化能力。在所有三个基准上，对于最多20个作业和60个操作的问题规模，GOAL在多个目标上实现了100%的解可行性和接近零的MAPE（低于0.20%），在解质量和推理速度上均优于NSGA-II和MOEA/D，最高提升25倍。

## Abstract
Existing neural combinatorial optimization solvers frame solution search as imitation of optimal decisions, inherently limiting their utility to single-objective minimization and static constraints. We propose GOAL, a conditioned diffusion solver over relational graph representations that enables controllable decision generations by conditioning on human-specified objectives. We introduce a heterogeneous graph encoding in which distinct edge types, corresponding to different classes of constraints, define the message passing structure of the graph neural network, which allows information to propagate selectively according to the ontology of each constraint. GOAL is instantiated and evaluated on three canonical scheduling benchmarks of various constraint complexity: the Flow Shop Problem (FSP), the Job Shop Scheduling Problem (JSP), and the Flexible Job Shop Scheduling Problem (FJSP). Generalization is demonstrated across structurally distinct constraint regimes and problem types without architectural modification. On all three benchmarks, GOAL achieves 100% solution feasibility and near-zero MAPE (below 0.20%) on multiple objectives for problem sizes up to 20 jobs and 60 operations, outperforming NSGA-II and MOEA/D in both solution quality and inference speed by up to 25x.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）
- 现有神经组合优化求解器局限于**单目标最小化和静态约束**，通过模仿最优解进行搜索，无法处理**动态多目标优化问题（DMOPs）**。
- 在自主资产（如机器人）集成到生产系统的背景下，约束拓扑（如机器分配、作业顺序）和人类偏好（如最大完工时间、弹性）随时间变化，需要求解器能够**根据动态约束和指定目标直接生成可行决策**，而非先构建Pareto前沿再选择。
- 论文提出GOAL，旨在实现**可控的、满足给定目标条件**的可行解生成，无需显式枚举Pareto前沿，并能**泛化到不同约束结构的问题类型**（FSP、JSP、FJSP）。

## 2. 论文提出的方法论
### 核心思想
- 将决策变量表示为**二进制向量**（编码操作间优先关系），使用**条件扩散模型**生成可行解，并通过**异构图神经网络（RGNN）** 对动态约束进行结构化建模。
### 关键技术细节
- **问题形式化**：多目标优化 min [o₁(x),...,o_I(x)]，受约束 cⱼ(x)≤0。决策变量 x∈{0,1}^N（N=K²，K为操作数）。
- **异构图编码**：构建关系图 Gs=(V, E₁,...,E_J)，每种边类型对应一类约束（如作业优先约束、机器冲突约束），边类型特定的消息传递允许信息按约束本体选择性传播。
- **离散扩散模型**：前向过程独立将每个边向伯努利(0.5)噪声腐蚀；反向过程学习 pθ(x₀|s,u,Gs)。训练目标为二进制交叉熵（BCE）预测原始决策 x₀。
- **条件注入**：三个独立编码器分别处理扩散时间步 t、问题实例 s、目标向量 u，通过仿射特征调制（FiLM）注入每个GNN层。
- **RGNN层**：使用门控机制（sigmoid门控）控制每条边消息强度，按边类型分别聚合邻居消息，更新节点和边嵌入。最终边读出层输出每个边的二值化 logit。
- **分类器自由引导（CFG）**：训练时以概率 p_drop=0.1 将 u 置零，使模型同时具备条件与无条件生成能力；推理时引导预测 ˆx_guided = ˆx_uncond + γ(ˆx_cond - ˆx_uncond)，γ=2.0。
- **加速推理**：采用离散DDIM，使用余弦子序列将扩散步数从T=1000降到少量步数，聚焦低噪声区域。
- **解码策略**：将预测的优先关系矩阵按拓扑顺序得到最早可行开始时间，生成完整调度。

## 3. 实验设计
- **数据集**：合成生成JSP、FSP、FJSP实例，操作数 no=3，作业数 nj∈{5,7,...,20}，机器数 nm∈{3,...,10}，处理时间 U[1,5]。每个实例用OR-Tools CP-SAT求解器枚举200个可行调度作为训练标签。训练集每类型5000实例，测试集100实例。
- **基准任务**：三个调度问题：Flow Shop（FSP，固定机器顺序）、Job Shop（JSP，每机器灵活排序）、Flexible Job Shop（FJSP，操作可分配多机器）。
- **对比方法**：NSGA-II、MOEA/D，对其适应度函数改为目标平方相对误差并加入约束惩罚，以统一“时间到达ε可行解”指标。
- **评估指标**：MAPE（最大完工时间Cmax和弹性R）、可行性率、解重复率、time-to-ε（5%和10%阈值）。

## 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量、训练时长等硬件信息。只提到训练25个epoch，batch size 64，学习率1e-4，梯度裁剪到ℓ2范数1.0。读者无法直接复现训练成本。

## 5. 实验数量与充分性
- **实验组数**：包含三大类实验：
  - **规模可扩展性**（表1）：9种问题尺寸（3×3到20×3），对比NSGA-II、MOEA/D，报告MAPE、可行性、重复率。
  - **跨问题类型泛化**（表2）：JSP、FSP、FJSP各3种尺寸（5×3、7×3、10×3），报告MAPE、重复率、可行性。
  - **约束泛化**（表3）：在JSP上训练部分机器数（5×3：nm=3,4,5；7×3：nm=4,5,6,7；10×3：nm=4,5,6,7,8,9,10），测试未见机器数（灰色标记），报告MAPE和可行性。
  - **推理时间分析**（图3、图4）：time-to-ε随问题类型和机器数的变化。
- **缺失实验**：未做消融实验（如不同扩散步数、引导强度、网络深度、是否使用CFG等），也未与最新扩散组合优化方法（如DIFUSCO、LMDM）直接比较。仅两个目标（makespan和resilience），未测试更多目标。动态约束变化仅考察了机器数变化，未包括更复杂约束拓扑（如任务插入、资源故障）。
- **充分性评价**：实验设计较全面，覆盖了规模、多问题类型、新约束泛化，对比了经典多目标进化算法，结果客观。但消融和最新基线缺失降低了结论的鲁棒性验证。

## 6. 论文的主要结论与发现
- GOAL在所有测试问题上实现**100%可行性**，对JSP和FSP的MAPE低于0.20%（多数为0%），对FJSP稍高（Cmax MAPE ≤6.97%，R MAPE ≤12.53%）。
- 推理速度**比NSGA-II快25倍**（20×3规模下148.8ms vs 3816.4ms），比MOEA/D快8-11倍。
- 泛化能力：**无需修改架构**即可在FSP、JSP、FJSP上直接推理，且对训练中未见机器数也能保持高质量（MAPE<0.5%）。
- 重复率随问题规模增大迅速降至0%，表明生成多样性良好。

## 7. 优点
- **创新性**：首次将条件扩散模型与异构图神经网络结合，解决动态多目标优化中**约束拓扑变化**和**目标条件生成**两大挑战。
- **可控性**：通过CFG直接生成符合人类指定目标值的解，无需显式构建Pareto前沿，减轻认知负担。
- **强泛化**：单一模型处理三种不同约束结构的调度问题，且对新机器数零样本泛化。
- **高效性**：DDIM加速使推理时间在毫秒级，远优于传统进化方法。
- **结构建模**：异构图RGNN显式区分不同约束类型，为每个约束提供可分解的结构信号，优于传统二元可行性惩罚。

## 8. 不足与局限
- **FJSP性能下降**：在柔性分配场景下MAPE显著升高（R达到12.53%），说明对更大决策空间的适应性不足。
- **目标数量限制**：仅验证两个目标（Cmax和R），未扩展到更多目标（≥3），而实际问题常需更多维度。
- **缺乏与最新扩散方法对比**：未与DIFUSCO（单目标静态图扩散）或LMDM（大规模离散扩散）等基线比较，削弱了方法先进性的论证。
- **训练数据依赖CP-SAT**：需要外部求解器枚举可行解，对大规模实例可能昂贵，限制可扩展性。
- **动态约束变化覆盖有限**：仅测试了机器数变化，未包括作业插入、机器故障、任务重新分配等更复杂的动态。
- **缺失消融实验**：各组件（RGNN vs GNN、CFG、DDIM等）的贡献未通过消融量化，模型设计选择缺乏验证。
- **硬件信息缺失**：未报告训练所需GPU型号与时间，影响可复现性。

（完）
