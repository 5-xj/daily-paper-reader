---
title: "UniRank: Unified List-wise Reranking via Confidence-Ordered Denoising"
title_zh: UniRank：基于置信度有序去噪的统一列表式重排序
authors: "Pengyue Jia, Hailan Yang, Shuchang Liu, Xiaobei Wang, Wanyu Wang, Xiang Li, Yongqi Liu, Kaiqiao Zhan, Kun Gai, Xiangyu Zhao"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10527v1"
tags: ["query:rerank-train"]
score: 10.0
evidence: 统一列表式重排序训练方法
tldr: "列表式重排序中，自回归方法能建模项目间依赖但易传播错误，非自回归方法避免错误传播却弱化了交互建模。UniRank统一框架通过迭代去噪和置信度有序预测融合两者优势，引入任务接地扩散接口实现项目级别去噪。在三个数据集上超越现有方法，在线A/B测试中用户平均使用时长提升0.159%，分享率提升1.016%。该工作首次统一两种范式，提出高效重排序新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10527v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1437, \"height\": 254, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.10527v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1425, \"height\": 516, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.10527v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1433, \"height\": 284, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.10527v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 720, \"height\": 297, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.10527v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1441, \"height\": 529, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.10527v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1442, \"height\": 525, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.10527v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1454, \"height\": 487, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.10527v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1174, \"height\": 437, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.10527v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 645, \"height\": 515, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.10527v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1130, \"height\": 651, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.10527v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1449, \"height\": 443, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.10527v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 946, \"height\": 215, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.10527v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1094, \"height\": 315, \"label\": \"Table\"}]"
motivation: 现有自回归与非自回归重排序方法各有缺陷，缺乏统一架构兼得两者之长。
method: 提出UniRank，通过迭代去噪过程双向建模列表，每步填充最置信位置；并引入TGD接口进行项目级去噪。
result: "在Amazon Books、MovieLens-1M和工业数据集上超越SOTA，在线A/B测试中用户平均使用时长提升0.159%，分享率提升1.016%。"
conclusion: UniRank统一了自回归和非自回归重排序，验证了其在实际系统中的有效性。
---

## 摘要
列表式重排序将特定请求的候选项目池排列成一个有序的列表，以最大化用户满意度。现有的生成式重排序器分为两种范式：自回归（AR）重排序器从左到右构建列表，并捕捉曝光列表中的项目间依赖关系，但由于早期错误会影响后续位置，它们存在错误传播问题。非自回归（NAR）重排序器并行预测所有位置，避免了错误传播，但在位置独立性假设下削弱了项目间交互建模。这引发了一个核心问题：是否存在一种统一架构，能够结合两种范式的优势并提供更强的重排序性能？我们通过UniRank来回答这个问题，这是一个统一的列表式重排序框架，其推理时变体将AR和NAR重排序器作为特例恢复。UniRank将双向列表建模融入迭代去噪过程，并在每一步填充最置信的位置。为了将这一框架实例化为重排序，我们引入了任务接地扩散接口（TGD），它在项目级别进行去噪，并将预测限制在特定请求的候选项目池中。TGD将每个项目的语义令牌聚合成单个项目嵌入，并直接针对候选池对每个位置进行评分。在Amazon Books、MovieLens-1M和一个工业短视频数据集上的实验表明，UniRank持续优于最先进的基线方法。在真实工业平台上的在线A/B测试进一步验证了其有效性，用户平均使用时长提升了+0.159%，分享率提升了+1.016%。

## Abstract
List-wise reranking arranges a request-specific pool of candidate items into an ordered slate that maximizes user satisfaction. Existing generative rerankers fall into two paradigms: Autoregressive (AR) rerankers construct the slate left to right and capture inter-item dependencies in the exposure list, but they suffer from error propagation because early mistakes affect subsequent slots. Non-autoregressive (NAR) rerankers predict all slots in parallel and avoid error propagation, but they weaken inter-item interaction modeling under a slot independence assumption. This raises a central question: is there a unified architecture that combines the strengths of both paradigms and delivers stronger reranking performance? We answer this question with UniRank, a unified list-wise reranking framework whose inference time variants recover AR and NAR rerankers as special cases. UniRank integrates bidirectional slate modeling into an iterative denoising process and fills the most confident slot at each step. To instantiate this framework for reranking, we introduce the Task Grounded Diffusion Interface (TGD), which performs denoising at the item level and restricts prediction to the request-specific candidate pool. TGD aggregates each item's semantic tokens into a single item embedding and scores each slot directly against the candidate pool. Experiments on Amazon Books, MovieLens-1M, and an industrial short video dataset show that UniRank consistently outperforms state-of-the-art baselines. Online A/B tests on a real-world industrial platform further validate its effectiveness, yielding significant improvements of +0.159% in user average app-time and +1.016% in share-rate.

---

## 论文详细总结（自动生成）

# 论文详细总结：UniRank: Unified List-wise Reranking via Confidence-Ordered Denoising

## 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：推荐系统中的重排序阶段需要将候选项目排列成有序列表（slate），同时建模项目间的交互（如替代性、互补性、多样性）。现有生成式重排序器分为两大范式：**自回归（AR）** 方法从左到右逐步生成列表，能捕获已曝光项目的依赖关系，但早期错误会向后传播，导致生成稳定性差；**非自回归（NAR）** 方法并行预测所有位置，避免错误传播，但通常假设位置间独立，削弱了交互建模能力。两者之间存在“依赖建模”与“生成稳定性”的权衡。
- **核心问题**：能否设计一个统一架构，同时结合AR和NAR的优势——具备双向列表建模能力，又能避免固定顺序导致的错误传播？
- **整体含义**：该工作提出UniRank，通过置信度有序的迭代去噪过程统一两种范式，为列表式重排序提供了一种更有效、更灵活的新框架。

## 2. 方法论

### 2.1 核心思想
- 将重排序建模为**掩码离散扩散**过程：从全掩码的初始列表开始，每步利用一个共享的双向Transformer（去噪器）预测所有掩码位置的“项目置信度”，选择置信度最高的位置-项目对进行提交（填充），重复直到列表完整。
- 推理时通过改变提交策略（commit policy），同一框架可恢复AR和NAR：固定左到右顺序提交→AR；一步并行提交全部→NAR。默认策略是每步提交最置信的slot-item对，即**置信度有序去噪**。

### 2.2 关键技术细节
- **任务接地扩散接口（TGD）**：包含两个模块：
  - **语义融合层（SFL）**：将每个项目的M个语义ID令牌嵌入通过拼接和投影聚合成单一项目嵌入，使每个去噪位置对应一个项目（而非令牌序列）。
  - **潜在池选择（LPS）**：用点积分数直接计算列表位置隐藏状态与候选项目隐藏状态之间的匹配得分，分布通过softmax得到，支持严格限制在请求特定的候选池中。
- **训练**：对每个训练样本随机掩码n个位置（n~Uniform{1,...,L}），优化基于掩码位置的标签重加权交叉熵损失 \( \mathcal{L} = \frac{1}{L} \sum_{j \in M} \frac{r_j}{\alpha} \text{CE}(\text{softmax}(Z_{j,:}), \ell_j) \)，其中\(r_j\)为标签重权重（正反馈1，否则0），\(\alpha=n/L\)确保梯度贡献不依赖于掩码率。
- **推理**：从全掩码开始，每步选择最高置信度的位置-项目对提交，经L步完成；可选精炼阶段（refinement），重新掩码最低置信项目并用同一去噪器更新。

### 2.3 公式与算法流程（文字说明）
- 输入序列：[用户历史H | 分隔符 | 候选池C | 分隔符 | 当前部分掩码列表x_α]
- 双向Transformer输出列表隐藏状态HS和候选隐藏状态HC
- 计算得分矩阵 Z = (HS W_q) H_C^T / √d
- 对掩码位置j取softmax得到候选概率，选择最大概率的候选作为填充项

## 3. 实验设计

### 3.1 数据集与场景
- **Amazon Books**：35,732用户，38,121项目，约196万交互，31万列表
- **MovieLens-1M**：6,020用户，3,043项目，约99.5万交互，16万列表
- **工业数据集（Industry）**：来自快手短视频平台，200,775用户，17,014项目，约517万交互，58.9万列表
- 每个样本：历史长度|H|=50，候选池大小|C|=50，生成列表长度L=6

### 3.2 Benchmark与对比方法
- **Generator-only 基线**：DNN、DLCM、PRM、GoalRank、GloRank
- **Generator-Evaluator 基线**：PIER、NAR4Rec、G-n（n=3,20,100）、DCDR
- 评价指标：Precision@L、NDCG@L、MAP@L、F1@L（L=6）

### 3.3 在线实验
- 在快手短视频平台进行A/B测试，控制组为G-n风格（基于GoalRank生成器），处理组将生成器替换为UniRank。10%流量分配，运行7天。

## 4. 资源与算力

- **硬件**：2块 NVIDIA RTX PRO 6000 Blackwell GPU（未提及具体型号和训练时长）
- **模型大小**：默认配置为 M（5.08M参数），训练使用bf16混合精度、AdamW优化器、峰值学习率3e-3、batch size=64
- 论文未报告总训练时间或计算成本。

## 5. 实验数量与充分性

- **主实验**：一个总表（表1），覆盖3个数据集、4个指标、9个基线，充分展示整体性能优势。
- **消融实验**：表2，分别考察输入组件（SID替换、历史填充移除）和模型架构（SFL移除、迭代解码移除、LPS移除），共5个变体，验证各组件贡献。
- **去噪参数分析**：图3，考察步数（1~6）和两步策略下的分配方式（如1,5; 5,1等），并在附录F给出完整四指标结果。
- **缩放分析**：图4，模型大小从0.29M到27.59M共5种配置（XS、S、M、L、XL），揭示最佳容量点。
- **在线实验**：表3，统计平台级和交互级共10个指标，6个达显著水平（p<0.05）。
- **模拟器评估**：附录E，在MovieLens-1M上用模拟器计算平均奖励，与其他方法对比。
- **精炼分析**：附录A，探索精炼步骤的影响。

**总体评价**：实验设计较全面，覆盖离线、在线、模拟器多种评估；消融合理；参数分析细致。但未进行跨数据集泛化测试（如从Amazon Books到MovieLens-1M的迁移），也未深入讨论鲁棒性（如候选池噪声）。总体充分且客观。

## 6. 主要结论与发现

1. **UniRank显著优于最强基线**：在Amazon Books上相对提升Precision 5.43%、NDCG 4.35%、MAP 6.44%、F1 5.44%；在MovieLens-1M和Industry上也有1-2%提升。
2. **统一框架有效**：通过设置不同commit policy可恢复AR和NAR，且置信度有序去噪策略最佳。
3. **TGD接口关键**：移除SFL导致最大性能下降（如Amazon Books Precision从88.30降到81.13），LPS和迭代解码也提供显著增益。
4. **更多去噪步骤提升性能**：步数从1到6逐步改善，体现test-time scaling收益。
5. **模型大小存在最优范围**：5.08M的M配置最佳，更大模型因数据不足而退化。
6. **在线验证**：用户平均使用时长+0.159%，分享率+1.016%，有效提升平台核心指标。

## 7. 优点

- **方法创新性**：首次将置信度有序迭代去噪引入重排序，统一AR和NAR两种范式，提供灵活的推理策略。
- **技术细节合理**：SFL和LPS针对任务定制，解决项目级去噪和候选池约束，设计简单有效。
- **实验严谨**：多数据集覆盖公共和工业场景，消融、参数分析、缩放分析、在线实验形成完整验证链。
- **可解释性与实用性**：分析不同策略（如步数、分配）的影响，给出针对低延迟场景的建议（去掉迭代解码作为NAR特例）。
- **在线结果扎实**：在真实大规模平台证明有效，提升关键业务指标。

## 8. 不足与局限

- **上游依赖**：UniRank仅从给定候选池中选项目，最终列表质量受限于上游检索和排序阶段的候选集质量，无法主动扩展候选空间。
- **推理延迟**：默认6步迭代去噪增加推理计算量，虽然可通过减少步数或使用NAR特例缓解，但未提供详细的延迟-效果权衡分析。
- **数据规模限制**：缩放分析显示模型超过5.08M时性能下降，说明在较小数据集（百万级交互）上大模型容易过拟合；更丰富的大规模场景未验证。
- **精炼阶段收益有限**：附录A显示精炼对多数指标增益很小（约0.03%），且过多步骤可能损害性能，论文未深入解释原因。
- **缺乏跨域泛化研究**：仅在三个数据集上评估，未测试模型在不同领域（如新闻、视频推荐）的适应性。
- **超参数敏感性**：未讨论掩码率分布（均匀uniform）的选择对训练的影响，以及标签重权重r_j的设置（仅二元0/1）是否最优。
- **代码与可复现性**：论文未提供开源代码或详细超参数配置，增加了复现难度。

（完）
