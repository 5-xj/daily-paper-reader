---
title: "Tree of Thoughts as a Classical Heuristic Search Problem: Formal Foundations and Design Patterns"
title_zh: 思维树作为经典启发式搜索问题：形式基础与设计模式
authors: Guni Sharon
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28566v1"
tags: ["query:ad"]
score: 8.0
evidence: 将思维树建模为经典启发式搜索，形式化LLM推理
tldr: 大语言模型的自回归生成存在短视和级联错误的问题，Tree-of-Thoughts框架通过搜索中间步骤来缓解，但现有研究分散且术语不一致。本文首次将ToT形式化为经典启发式搜索问题，将推理过程映射为状态表示、后继生成和启发式评估三个组件。分析发现，浅层确定性任务宜采用最佳优先搜索，而深层多步推理则需深度优先搜索或MCTS等前瞻策略。该工作建立了LLM推理的启发式搜索基础，呼吁社区关注并解决跨领域算法挑战。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.28566v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1836, \"height\": 505, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.28566v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1580, \"height\": 539, \"label\": \"Table\"}]"
motivation: 现有Tree-of-Thoughts研究在NLP和自动规划领域碎片化，缺乏统一的形式化基础，导致术语不一致和实现杂乱。
method: 通过经典启发式搜索术语统一分类，将LLM推理映射为状态表示、后继生成和启发式评估三个核心组件。
result: 识别出两类设计模式：系统搜索（最佳优先）用于浅层确定性任务，前瞻策略（DFS、MCTS）用于深层多步推理。
conclusion: 为LLM推理提供了启发式搜索的形式化基础，并指出开放挑战，促进跨领域协作。
---

## 摘要
大型语言模型（LLMs）展现了卓越的推理能力，但其标准生成过程——自回归令牌预测——本质上是短视的，容易产生级联错误。为了解决这一问题，思维树（Tree-of-Thoughts, ToT）框架在中间推理步骤上创建了一个搜索空间，使搜索模型能够探索、预判和回溯。然而，当前的ToT研究在自然语言处理和自动规划社区之间仍然碎片化，常常使用不一致的术语和临时实现。因此，我们通过基于经典启发式搜索术语的统一分类法综合了ToT领域。我们将基于LLM的推理映射到经典搜索组件：状态表示（思维的粒度）、后继生成（提示操作符）和启发式评估（自我评估进展）。我们在分类法的背景下分析了现有工作，并识别出新兴的设计模式：针对浅层确定性任务的系统搜索（最佳优先搜索）和针对深度多步推理的强预判策略（DFS、MCTS）。最后，我们指出了启发式搜索与LLM推理交叉领域中的开放算法挑战，并呼吁启发式搜索社区参与这一新兴领域。

## Abstract
Large Language Models (LLMs) have demonstrated remarkable reasoning capabilities, yet their standard generation process -- auto-regressive token prediction -- is inherently myopic and prone to cascading errors. To address this, the Tree-of-Thoughts (ToT) framework creates a search space over intermediate reasoning steps, allowing search models to explore, look ahead, and backtrack. However, current ToT research remains fragmented across Natural Language Processing and Automated Planning communities, often using inconsistent terminology and ad-hoc implementations. Consequently, we synthesize the ToT landscape through a unified taxonomy based on classical heuristic search terminology. We map LLM-based reasoning to classical search components: state representation (granularity of thoughts), successor generation (prompting operators), and heuristic evaluation (self-assessment of progress). We analyze existing work within the context of our taxonomy and identify emerging design patterns: systematic search (Best-First Search) for shallow, deterministic tasks and lookahead-heavy strategies (DFS, MCTS) for deep multi-step reasoning. We conclude by identifying open algorithmic challenges at the intersection of heuristic search and LLM reasoning, and call on the heuristic search community to engage with this emerging domain.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLMs）的标准自回归生成过程（逐令牌预测）本质上是**短视的**，容易产生**级联错误**。Chain-of-Thought（CoT）虽然引入中间推理步骤，但仍然是线性的、无回溯的，一旦出错就无法纠正。
- **已有解决方案的不足**：Tree-of-Thoughts（ToT）框架通过维护多条推理路径、允许回溯和预判来改进，但现有研究在自然语言处理（NLP）和自动规划（Automated Planning）社区之间**碎片化**，使用**不一致的术语和临时实现**，缺乏统一的形式基础，导致难以比较方法、理解设计权衡以及转移经典搜索技术。
- **本文目标**：首次将ToT形式化为**经典启发式搜索问题**，建立统一的分类法（taxonomy），将LLM推理映射为搜索组件（状态表示、后继生成、启发式评估），分析现有工作并识别设计模式，同时指出开放算法挑战，**呼吁启发式搜索社区参与这一新兴领域**。

## 2. 论文提出的方法论

### 核心思想
将ToT视为一个由**状态空间S**、**后继函数O**、**代价函数（g值）**、**启发式函数（h值）** 和**目标测试G**定义的启发式搜索问题。通过精确映射经典搜索组件，使传统搜索算法（如A*、MCTS）可以系统应用于LLM推理。

### 关键技术细节

- **状态表示**：状态s定义为思维序列 `s = [z0, z1, ..., zt]`，其中z0是问题提示，每个zi（i>0）是一个“思维”（一段连续令牌序列）。初始状态s0 = [z0]。
- **后继生成（提案机制）**：从当前状态生成候选下一思维。在两个正交维度上分类：
  - **采样策略**：
    - S1. 独立采样（top-k、nucleus等解码策略重复b次）
    - S2. 显式多样性采样（惩罚n-gram重复、嵌入相似度阈值）
    - S3. LLM枚举提案（一次提示生成多个选项，但可能模式坍缩）
  - **结构约束**（可组合）：
    - C1. 领域特定约束（限制为合法动作）
    - C2. 语法约束（如SQL语法）
    - C3. 长度约束
    - C4. 语义约束（基于世界状态过滤无效后继，由外部验证器执行）
- **节点级剪枝策略**（后生成）：
  - P1. 束剪枝（每层保留top-k节点）
  - P2. 局部分支剪枝（每节点保留top-b后继）
  - P3. 局部阈值剪枝（剪掉f值超过阈值的节点）
- **代价函数（g值）**：
  - G1. 均匀代价（深度）
  - G2. 无代价（g=0）
  - G3. 负对数似然代价（NLL，基于令牌概率）
- **启发式函数（h值）**：通常为成功似然（h<sub>success</sub>），需通过变换（如 `h_cost = -log(h_success)`）与g兼容。三种类型：
  - H1. 标量值估计（LLM评分或分类映射）
  - H2. 思维概率（序列联合概率）
  - H3. 外部函数（模拟器、代码解释器）
- **目标测试G**：
  - T1. 确定性验证（如表达式计算）
  - T2. LLM基评估（主观判断）
  - T3. 外部环境验证（在模拟器中执行）
- **搜索算法**：BFS、DFS、LTS（Levin Tree Search）、MCTS等，由搜索代理（search agent）管理，LLM作为**无状态组件**被调用。

### 公式/算法流程（文字说明）
- 搜索树节点n包含：`n.s`（状态）、`n.parent`、`n.g`、`n.h`、`n.f = n.g + n.h`（传统A*风格）。但在ToT中，f仅作为**节点排序函数**，不一定保证最优性。
- 搜索代理维护开放列表，按f值选择节点，调用LLM生成后继，可选剪枝，直到找到满足G的状态。

## 3. 实验设计

**本文为一篇形式化/综述论文，本身未提出新实验**，但基于对**12项现有ToT实现**（详见表1）的系统分析，覆盖多个基准域：

- **基准域**（详见附录C.1）：
  - 约束满足与谜题：Game of 24、Crosswords、逻辑谜题
  - 数学与符号推理：数学应用题、Text-to-SQL
  - 经典规划：Blocksworld、ALFWorld、BabyAI-Text
  - 交互式Web：WebShop
  - 编程：代码生成（如HumanEval、MBPP）
  - 开放式创意生成：创意写作

- **对比方法**：论文未进行直接对比，但表1列出了每项工作的具体设计选择（提案、剪枝、搜索策略、g/h、目标测试），隐含不同方法之间的差异。

## 4. 资源与算力

- **论文未明确说明自身使用的算力资源**（因其为形式化综述）。
- 仅在效率评估部分（附录C.2）提及计算成本的度量方式：节点扩展数、令牌总数、KV-cache优化（前缀缓存等），并指出“LLM推理成本由推理计算主导”。
- 未提及GPU型号、数量、训练时长等具体信息。

## 5. 实验数量与充分性

- 作为形式化论文，**没有直接进行实验**，因此无法评估“实验数量与充分性”。
- 但它**分析并分类了12项已有工作**，这些工作在各自域内进行了实验（如Game of 24准确率、Blocksworld成功率、编程Pass@1等），间接展现了广泛覆盖。
- **局限性**：缺乏针对自身分类法的消融实验或量化比较（例如不同剪枝策略在同一任务上的效果对比）；分类的有效性未经过实证验证。

## 6. 论文的主要结论与发现

- **两大设计模式**：
  1. **浅层确定性任务**（如Game of 24、数学推理）：宜采用**系统搜索**（Best-First Search/Levin Tree Search）探索多样组合，配合**统一或似然代价**和**激进的剪枝**（束剪枝、结构约束）。
  2. **深层多步推理**（如编程、Blocksworld、Crosswords）：宜采用**前瞻策略**（DFS、MCTS）以进行预判，依赖**外部反馈**（T3）和**语义约束**（C4）早期消除无效分支。
  3. **开放式创意生成**（如创意写作）：依赖**LLM基启发式**（H1）引导，而非硬剪枝。

- **开放挑战**（Section 5）：
  - **OC1. 经典算法适应**：将anytime搜索、对称性破缺、部分扩展等技术移植到ToT，但面临g值无意义、状态空间高维等困难。
  - **OC2. 启发式设计与学习**：LLM基启发式缺乏可采纳性保证；可通过搜索轨迹微调改善。
  - **OC3. 新搜索挑战**：随机部分扩展（重复展开同一状态得不同后继）、语义状态等价（不同思维序列可能对应相同世界状态）、令牌预算搜索（建立基于令牌消耗的性能边界）。

- **核心呼吁**：启发式搜索社区应系统参与ToT，解决这些新挑战，并将经典技术转移至LLM推理。

## 7. 优点

- **统一形式化**：首次将ToT严格映射为经典启发式搜索问题，提供了清晰、一致的术语体系，弥合NLP与规划社区间的概念鸿沟。
- **全面分类法**：对提案机制、剪枝策略、成本函数、启发式类型、目标测试等维度的分类系统且可组合，有助于后续研究定位和比较。
- **识别设计模式**：基于任务结构（深度、分支因子、验证方式）给出设计建议，具有实践指导意义。
- **指出开放问题**：明确列出三个方向的具体挑战，为跨领域合作提供了路线图。
- **附录充实**：提供完整的接口定义、迭代修复对比、基准域表格和案例研究，增强了可理解性。

## 8. 不足与局限

- **缺乏实证验证**：自身提出的分类法和设计模式未经过新的实验验证（如在不同域的系统比较），其有效性和可推广性有待检验。
- **理论假设未充分解决**：如启发式可采纳性、一致性、随机后继下的收敛性等经典性质仅被提及，未提供深入理论分析或恢复方案。
- **可能忽略的LLM特性**：过度依赖经典搜索框架可能忽视LLM特有的挑战（如非平稳输出分布、上下文窗口限制、对齐偏差对多样性影响），部分假设（如p(success|s)≈p(s)）未经验证。
- **覆盖范围有限**：表1仅包含12项研究，可能遗漏其他重要工作；对某些域（如WebShop、BabyAI-Text）只是提及，未详细分析。
- **实用建议不足**：虽然给出了高层的模式建议，但未提供具体的算法参数指导（如束宽、温度选择）或计算成本比较曲线。

（完）
