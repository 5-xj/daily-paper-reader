---
title: "RankEvolve: Automating the Discovery of Retrieval Algorithms via LLM-Driven Evolution"
title_zh: RankEvolve：通过大语言模型驱动的演化实现检索算法发现的自动化
authors: "Jinming Nian, Fangchen Li, Dae Hoon Park, Yi Fang"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16932v1"
tags: ["query:ahd"]
score: 10.0
evidence: 通过LLM驱动的进化自动发现检索算法
tldr: 本研究针对传统检索算法（如BM25）过度依赖人工直觉的问题，提出了RankEvolve框架。该框架基于AlphaEvolve，利用大语言模型驱动程序进化，将检索算法表示为可执行代码，通过在12个数据集上迭代变异、重组和筛选，自动探索更优的词法检索算法。实验表明，RankEvolve发现的新算法不仅在BEIR和BRIGHT基准上表现出色，还具有极强的跨任务迁移能力，为自动化算法发现提供了新范式。
selection_source: fresh_fetch
motivation: 传统检索算法主要依赖人工直觉和参数微调，缺乏自动化的创新发现机制。
method: 提出RankEvolve框架，利用大语言模型对代码形式的检索算法进行迭代变异、重组和基于性能的进化筛选。
result: 进化出的新算法在BEIR、BRIGHT及TREC DL等多个基准测试中展现出优于传统算法的性能和良好的迁移性。
conclusion: 实验证明，由评估器引导的LLM程序进化是自动发现新型高效排序算法的一条切实可行的路径。
---

## 摘要
BM25 和带有狄利克雷平滑（Dirichlet smoothing）的查询似然（query likelihood）等检索算法仍然是强大且高效的第一阶段排序器，但其改进主要依赖于参数调优和人类直觉。我们研究了在评估器和演化搜索的指导下，大语言模型是否能够自动发现改进的词法检索算法。我们介绍了 RankEvolve，这是一种基于 AlphaEvolve 的程序演化设置，其中候选排序算法被表示为可执行代码，并根据在 BEIR 和 BRIGHT 的 12 个信息检索（IR）数据集上的检索性能进行迭代变异、重组和筛选。RankEvolve 从两个种子程序开始：BM25 和带有狄利克雷平滑的查询似然。演化出的算法具有新颖性、有效性，并在完整的 BEIR 和 BRIGHT 基准测试以及 TREC DL 19 和 20 上表现出良好的迁移能力。我们的结果表明，评估器指导的大语言模型程序演化是实现自动发现新颖排序算法的一条切实可行的路径。

## Abstract
Retrieval algorithms like BM25 and query likelihood with Dirichlet smoothing remain strong and efficient first-stage rankers, yet improvements have mostly relied on parameter tuning and human intuition. We investigate whether a large language model, guided by an evaluator and evolutionary search, can automatically discover improved lexical retrieval algorithms. We introduce RankEvolve, a program evolution setup based on AlphaEvolve, in which candidate ranking algorithms are represented as executable code and iteratively mutated, recombined, and selected based on retrieval performance across 12 IR datasets from BEIR and BRIGHT. RankEvolve starts from two seed programs: BM25 and query likelihood with Dirichlet smoothing. The evolved algorithms are novel, effective, and show promising transfer to the full BEIR and BRIGHT benchmarks as well as TREC DL 19 and 20. Our results suggest that evaluator-guided LLM program evolution is a practical path towards automatic discovery of novel ranking algorithms.

---

## 论文详细总结（自动生成）

以下是对论文《RankEvolve: Automating the Discovery of Retrieval Algorithms via LLM-Driven Evolution》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
传统的词法检索算法（如 BM25 和带有狄利克雷平滑的查询似然法 QL-Dir）在工业界仍被广泛用作第一阶段排序器。然而，这些算法的改进长期以来主要依赖于研究人员的直觉、手动公式推导或简单的参数微调（如 Grid Search）。
**核心问题：** 能否利用大语言模型（LLM）作为优化器，在无需人工干预的情况下，自动发现并进化出性能更优、具有新颖数学结构的检索算法？
**研究背景：** 借鉴了 AlphaEvolve 等程序进化系统的思路，将检索算法表示为可执行的 Python 代码，利用 LLM 的推理能力进行有目的的变异和重组。

### 2. 方法论：核心思想与关键技术
RankEvolve 框架将检索算法的发现建模为**由 LLM 引导的程序合成与进化搜索过程**：
*   **核心思想：** 将算法表示为约 300 行的 Python 代码，通过“变异-评估-筛选”的循环不断优化。
*   **搜索空间定义：** 提供两个种子程序（BM25 和 QL-Dir），定义可进化的接口（如文档表示、查询表示、评分函数等）。
*   **种群管理：** 
    *   **岛屿模型（Island-based Evolution）：** 将种群划分为多个独立进化的“岛屿”，定期进行个体迁移以保持多样性。
    *   **MAP-Elites 算法：** 根据代码复杂度和多样性（编辑距离）将程序映射到网格中，确保不仅保留高分个体，也保留具有潜力的差异化个体。
*   **变异机制：** LLM（如 GPT-5.2）根据当前最优程序、评估指标反馈以及先前的失败尝试，以 `SEARCH/REPLACE` 的 diff 格式提出代码修改建议。
*   **适应度函数（Fitness）：** 优化目标定义为 `0.8 × 平均 Recall@100 + 0.2 × 平均 nDCG@10`，侧重于提升第一阶段检索的召回率。

### 3. 实验设计
*   **数据集：** 共涉及 28 个 IR 数据集。
    *   **演化阶段（Seen）：** 使用来自 BEIR 和 BRIGHT 的 12 个数据集进行适应度评估。
    *   **测试阶段（Unseen）：** 在剩余 16 个完全未见的数据集（包括 BEIR 剩余子集、BRIGHT 剩余子集以及 TREC DL 2019/2020）上验证泛化性。
*   **基准方法（Benchmarks）：** 
    *   词法基准：BM25, BM25+, BM25-adpt。
    *   语言模型基准：QL-Dir, QL-JM。
*   **对比方式：** 比较进化出的算法（BM25★ 和 QL-Dir★）与原始种子算法及经典变体在召回率和排序质量上的差异。

### 4. 资源与算力
*   **LLM 模型：** 使用了 **GPT-5.2**（通过 API 调用），设置温度系数为 0.85。
*   **进化规模：** BM25 种子运行了 300 个演化步，QL-Dir 种子运行了 200 个演化步。
*   **算力说明：** 文中未明确提及运行评估器（Indexing 和 Retrieval）的具体硬件（如 GPU/CPU 型号）及总耗时。但提到每个演化步涉及在 12 个数据集上进行完整的索引和检索测试，计算开销主要集中在 LLM API 调用和评估器的执行上。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 28 个数据集，跨越了通用检索（BEIR）、推理密集型检索（BRIGHT）和 Web 检索（TREC DL），实验覆盖面非常广泛。
*   **消融实验：** 专门研究了“种子程序的结构自由度”对进化结果的影响，对比了受限型（仅调参）、组合型（模块化重写）和自由型（全代码演化）三种模式。
*   **客观性：** 采用了“留出法”验证泛化性，确保算法不是简单地过拟合 12 个训练数据集。使用了配对 t 检验来验证显著性，实验设计较为严谨。

### 6. 主要结论与发现
*   **性能超越：** 进化出的算法在多个基准测试中显著优于原始种子和人类设计的变体（如 BM25+）。
*   **自动“再发现”：** 进化过程自动产生了一些经典的 IR 概念，如**软停用词过滤、点互信息（PMI）特异性权重、自适应词频饱和以及对数文档长度归一化**。
*   **结构创新：** 进化出的 BM25★ 采用了多通道（前缀、双字母组、微型 token）评分机制；QL-Dir★ 则引入了漏斗式激活函数（Leaky Rectifier）和复杂的背景语言模型平滑技术。
*   **泛化能力：** 在演化过程中未见过的 TREC DL 等数据集上，进化算法依然保持了领先地位。

### 7. 优点
*   **范式创新：** 首次将基于 LLM 的程序进化应用于信息检索算法的底层公式发现，跳出了简单的参数搜索。
*   **可解释性：** 与黑盒神经网络不同，RankEvolve 输出的是 Python 代码，研究人员可以直接阅读并理解其数学逻辑。
*   **灵活性：** 框架不限于词法检索，理论上可扩展至稠密检索、重排序等任何可代码化的 IR 任务。

### 8. 不足与局限
*   **计算延迟：** 进化出的算法结构极其复杂（如多通道分词和多层嵌套函数），导致查询延迟显著增加（BM25★ 比原始 BM25 慢约 11 倍）。
*   **效率缺失：** 优化目标中未包含效率指标，导致 LLM 倾向于通过增加计算复杂度来换取微小的精度提升。
*   **依赖性：** 搜索效果高度依赖于底层 LLM 的代码生成能力和初始种子程序的接口设计。
*   **范围限制：** 目前仅探索了词法检索，尚未涉及深度学习检索模型的架构进化。

（完）
