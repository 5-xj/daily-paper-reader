---
title: "K-Search: LLM Kernel Generation via Co-Evolving Intrinsic World Model"
title_zh: K-Search：通过协同演化内在世界模型实现 LLM 算子生成
authors: "Shiyi Cao, Ziming Mao, Joseph E. Gonzalez, Ion Stoica"
date: 2026-02-22
pdf: "https://arxiv.org/pdf/2602.19128v2"
tags: ["query:ahd"]
score: 8.0
evidence: 通过协同演化世界模型和搜索自动生成GPU算子
tldr: 针对GPU算子优化中LLM缺乏规划能力且易受中间实现错误干扰的问题，本文提出K-Search框架。该框架引入协同演化的内在世界模型，将高层算法规划与底层程序实例化解耦，利用LLM的领域知识引导搜索。在FlashInfer和GPUMode任务中，K-Search显著超越了现有进化搜索方法，在复杂MoE算子上实现高达14.3倍的性能提升，并刷新了H100上的TriMul性能记录。
selection_source: fresh_fetch
motivation: 现有的自动化GPU算子优化方法缺乏显式规划能力，难以处理需要多步协同结构变换的复杂算子。
method: 提出协同演化的世界模型，通过解耦高层规划与底层实现，利用LLM的先验知识在非单调优化路径中进行主动探索。
result: 在FlashInfer多样化算子上平均提升2.10倍性能，并在GPUMode TriMul任务中超越了人类设计和现有进化方案。
conclusion: K-Search证明了结合世界模型的LLM搜索框架能有效解决复杂硬件算子优化难题，具有极高的实用价值。
---

## 摘要
优化 GPU 算子对于高效的现代机器学习系统至关重要，但由于设计因素之间复杂的相互作用以及硬件的快速演进，这仍然是一项具有挑战性的任务。现有的自动化方法通常仅将大语言模型（LLM）视为启发式引导演化循环中的随机代码生成器。这些方法在处理需要协调的多步结构变换的复杂算子时往往表现不佳，因为它们缺乏显式的规划能力，并且经常由于中间实现效率低下或错误而放弃有前景的策略。为了解决这一问题，我们提出了“通过协同演化世界模型进行搜索”的方法，并据此构建了 K-Search。通过将静态搜索启发式替换为协同演化的世界模型，我们的框架利用 LLM 的先验领域知识来引导搜索，主动探索优化空间。该方法显式地将高层算法规划与底层程序实例化解耦，使系统能够导航非单调的优化路径，同时对临时的实现缺陷保持鲁棒性。我们在来自 FlashInfer 的多种复杂算子上评估了 K-Search，包括 GQA、MLA 和 MoE 算子。结果表明，K-Search 显著优于最先进的演化搜索方法，在复杂 MoE 算子上实现了平均 2.10 倍的性能提升，最高增益达 14.3 倍。在 GPUMode TriMul 任务中，K-Search 在 H100 上达到了最先进的性能，耗时仅 1030us，超越了此前的演化算法和人工设计的方案。

## Abstract
Optimizing GPU kernels is critical for efficient modern machine learning systems yet remains challenging due to the complex interplay of design factors and rapid hardware evolution. Existing automated approaches typically treat Large Language Models (LLMs) merely as stochastic code generators within heuristic-guided evolutionary loops. These methods often struggle with complex kernels requiring coordinated, multi-step structural transformations, as they lack explicit planning capabilities and frequently discard promising strategies due to inefficient or incorrect intermediate implementations. To address this, we propose Search via Co-Evolving World Model and build K-Search based on this method. By replacing static search heuristics with a co-evolving world model, our framework leverages LLMs' prior domain knowledge to guide the search, actively exploring the optimization space. This approach explicitly decouples high-level algorithmic planning from low-level program instantiation, enabling the system to navigate non-monotonic optimization paths while remaining resilient to temporary implementation defects. We evaluate K-Search on diverse, complex kernels from FlashInfer, including GQA, MLA, and MoE kernels. Our results show that K-Search significantly outperforms state-of-the-art evolutionary search methods, achieving an average 2.10x improvement and up to a 14.3x gain on complex MoE kernels. On the GPUMode TriMul task, K-Search achieves state-of-the-art performance on H100, reaching 1030us and surpassing both prior evolution and human-designed solutions.

---

## 论文详细总结（自动生成）

以下是对论文《K-Search: LLM Kernel Generation via Co-Evolving Intrinsic World Model》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
高性能 GPU 算子（Kernel）是现代机器学习系统的基石，但其优化面临三大挑战：
*   **设计空间巨大**：涉及分块（Tiling）、内存布局、同步和硬件原语的复杂组合。
*   **硬件演进极快**：不同架构（如 Hopper 到 Blackwell）的最优策略完全不同。
*   **现有自动化方法局限**：现有的 LLM 演化方法（如 OpenEvolve）仅将 LLM 视为“随机代码生成器”，缺乏显式规划。当优化需要多步结构变换且中间步骤可能导致性能下降或编译错误时，这些方法往往会过早放弃正确路径。

**核心动机**：通过引入“世界模型”概念，将高层算法规划与底层代码实现解耦，使 LLM 能够像人类专家一样进行长程规划和容错搜索。

### 2. 论文提出的方法论：K-Search
K-Search 的核心思想是**通过协同演化的内在世界模型进行搜索**。
*   **搜索状态（Search State）**：维护一个树状结构，包含“已关闭节点”（已实现的程序）和“前沿节点”（待探索的优化假设）。
*   **核心流程（三个阶段迭代）**：
    1.  **动作选择（Action Selection）**：世界模型根据内在先验知识为前沿节点打分（Priority Score $V$），选择得分最高的优化意图（如“应用 Split-K 优化”）。
    2.  **程序实例化（Local Refinement）**：将高层意图转化为具体代码。采用局部精炼策略，允许在达到“停滞限制”（Stagnation Limit $K$）前进行多次尝试，从而对临时的语法错误或小 Bug 保持鲁棒。
    3.  **世界模型协同演化（World Model Update）**：根据执行反馈（性能、编译日志），LLM 重新审视搜索树，执行**插入（Insert）**新假设、**更新（Update）**现有节点评分或**剪枝（Prune）**无效分支的操作。

### 3. 实验设计
*   **实验场景**：针对 FlashInfer 库中的高性能算子，包括 GQA Paged Decode、MLA Paged Prefill/Decode 和 FP8 MoE。
*   **Benchmark**：以人类专家高度优化的 FlashInfer 算子作为性能基准（1.0 分）。
*   **对比方法**：
    *   **OpenEvolve**：基于质量-多样性搜索（MAP-Elites）的演化方法。
    *   **ShinkaEvolve**：基于种群的演化框架。
*   **额外挑战**：参加了 GPUMode 的 TriMul（三角形乘法更新）公开竞赛任务。

### 4. 资源与算力
*   **硬件环境**：实验在 NVIDIA **H100** 和 **B200** GPU 上进行。
*   **软件环境**：CUDA 12.8, FlashInfer 0.5.3, PyTorch 2.8.0。
*   **搜索预算**：每个算子固定为 **120 次迭代**（即 120 次编译/评估尝试）。
*   **模型使用**：主要使用 `gemini-3-pro-preview`；在 TriMul 任务中使用了 `GPT-5.2` 和 `Gemini-3-Pro` 的组合。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 4 类代表性算子，共计 **152 个工作负载追踪（Workload Traces）**。
*   **重复性**：每种方法重复运行 **3 次**，并报告了均值及最小-最大范围带。
*   **公平性**：所有方法使用相同的初始程序（Seed）、相同的编译工具链、验证套件和基准测试环境。
*   **充分性**：实验不仅对比了最终性能，还通过搜索轨迹可视化（Trace Visualization）深入分析了模型如何进行逻辑推理和剪枝，实验设计较为客观、充分。

### 6. 论文的主要结论与发现
*   **性能大幅领先**：K-Search 平均性能是 OpenEvolve 的 **2.10 倍**，是 ShinkaEvolve 的 **2.21 倍**。
*   **复杂任务突破**：在极具挑战性的 MoE 算子上，K-Search 实现了 **14.3 倍** 的性能增益。
*   **刷新纪录**：在 GPUMode TriMul 任务中，K-Search 跑出了 **1030 μs** 的成绩，超越了所有已知的人工设计和自动化方案，成为新的 SOTA。
*   **发现非单调路径**：实验证明 K-Search 能够识别出某些优化（如 Split-K）在单独使用时无效、但与其他优化组合时极具威力的特性。

### 7. 优点
*   **解耦规划与实现**：有效解决了 LLM 在编写复杂代码时“顾此失彼”的问题，避免了因临时编译错误而丢失正确优化方向。
*   **高效搜索**：通过世界模型的剪枝和更新机制，显著缩小了搜索空间，在极低的预算（120 次尝试）下即可达到极高性能。
*   **结构化洞察**：模型不仅生成代码，还生成了可解释的优化树，展示了其对硬件特性的深刻理解（如寄存器驻留、异步流水线等）。

### 8. 不足与局限
*   **特定场景退化**：在 GQA 任务的小 Batch Size 场景下，K-Search 生成的 Split-K 策略因同步开销反而不如简单的单块设计。
*   **对 LLM 推理能力依赖强**：该框架高度依赖底层 LLM 的逻辑推理和领域知识，若模型本身对 CUDA 编程理解不足，搜索效率可能下降。
*   **计算成本**：虽然搜索迭代次数少，但每次迭代调用高阶 LLM（如 Gemini Pro 或 GPT-5）进行推理和状态更新仍存在一定的 API 成本和延迟。

（完）
