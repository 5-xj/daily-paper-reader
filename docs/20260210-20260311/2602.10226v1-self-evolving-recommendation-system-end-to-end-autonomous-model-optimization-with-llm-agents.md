---
title: "Self-Evolving Recommendation System: End-To-End Autonomous Model Optimization With LLM Agents"
title_zh: 自演化推荐系统：基于 LLM Agent 的端到端自主模型优化
authors: "Haochen Wang, Yi Wu, Daryl Chang, Li Wei, Lukasz Heldt"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.10226v1"
tags: ["query:ahd"]
score: 9.0
evidence: 自主生成、训练和部署高性能模型变更及优化器
tldr: 本研究提出了一种基于大语言模型（LLM）智能体的自演化推荐系统，旨在解决大规模推荐模型优化中手动迭代效率低的问题。该系统利用Google Gemini模型构建了离线和在线双环智能体，分别负责高通量假设生成与生产环境验证。该系统能自主优化算法、架构及奖励函数，并在YouTube生产环境中成功部署，显著提升了开发效率和模型性能。
selection_source: fresh_fetch
motivation: 传统的推荐系统优化依赖大量手动迭代来探索复杂的超参数空间和模型架构，难以应对全球化视频平台对用户行为捕捉的极高要求。
method: 提出由离线智能体（负责基于代理指标生成假设）和在线智能体（负责基于核心业务指标验证候选方案）组成的端到端自动化工作流。
result: 该系统在YouTube的多个生产场景中成功上线，证明了LLM智能体在发现新优化算法、架构改进和创新奖励函数方面的卓越能力。
conclusion: 自主进化的LLM驱动工作流在开发速度和模型性能上均能超越传统的工程模式，为大规模机器学习系统的自动化优化开辟了新路径。
---

## 摘要
优化大规模机器学习系统（例如全球视频平台的推荐模型）需要探索庞大的超参数搜索空间，更关键的是，需要设计复杂的优化器、架构和奖励函数，以捕捉细微的用户行为。在这些领域取得实质性改进并非易事，传统上依赖于大量的迭代手动测试新假设。我们提出了一种自演化系统，利用大语言模型（LLM），特别是 Google 的 Gemini 系列，在端到端自动化工作流中自主生成、训练和部署高性能且复杂的模型变更。该自演化系统由一个离线 Agent（内环）和一个在线 Agent（外环）组成。离线 Agent 使用代理指标进行高吞吐量的假设生成，而在线 Agent 则在生产环境中根据滞后的北极星业务指标验证候选方案。我们的 Agent 充当专业的机器学习工程师（MLE）：它们展现出深度的推理能力，能够发现优化算法和模型架构中的新颖改进，并制定针对长期用户参与度的创新奖励函数。该方法的有效性已通过在 YouTube 的多次成功生产发布得到证实，证明了自主的 LLM 驱动演化在开发速度和模型性能方面均能超越传统的工程工作流。

## Abstract
Optimizing large-scale machine learning systems, such as recommendation models for global video platforms, requires navigating a massive hyperparameter search space and, more critically, designing sophisticated optimizers, architectures, and reward functions to capture nuanced user behaviors. Achieving substantial improvements in these areas is a non-trivial task, traditionally relying on extensive manual iterations to test new hypotheses. We propose a self-evolving system that leverages Large Language Models (LLMs), specifically those from Google's Gemini family, to autonomously generate, train, and deploy high-performing, complex model changes within an end-to-end automated workflow. The self-evolving system is comprised of an Offline Agent (Inner Loop) that performs high-throughput hypothesis generation using proxy metrics, and an Online Agent (Outer Loop) that validates candidates against delayed north star business metrics in live production. Our agents act as specialized Machine Learning Engineers (MLEs): they exhibit deep reasoning capabilities, discovering novel improvements in optimization algorithms and model architecture, and formulating innovative reward functions that target long-term user engagement. The effectiveness of this approach is demonstrated through several successful production launches at YouTube, confirming that autonomous, LLM-driven evolution can surpass traditional engineering workflows in both development velocity and model performance.

---

## 论文详细总结（自动生成）

这篇论文由 Google 的 YouTube 团队发表，介绍了一个利用大语言模型（LLM）智能体实现推荐系统端到端自主优化的“自演化”框架。以下是对该论文的深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：现代大规模推荐系统（如 YouTube）的优化已进入瓶颈期。传统的 AutoML 擅长数值型超参数微调，但无法处理**结构化创新**（如发明新的神经网络层）和**语义化逻辑设计**（如编写复杂的强化学习奖励函数）。
*   **核心问题**：如何打破“人类工程师是实验迭代唯一瓶颈”的现状，利用 LLM 的推理能力自主完成从假设生成、代码编写到生产环境部署的全流程优化。

### 2. 方法论
该系统采用**双环架构（Dual-Loop System）**，模拟人类工程师的工作流：
*   **离线智能体（内环 - 快速迭代）**：
    *   **核心思想**：高频生成候选方案，利用廉价的代理指标（如离线 Loss）进行初步筛选。
    *   **关键技术**：
        *   **多角色设定（Personas）**：分为优化器专家（Optimizer）、架构专家（Architecture）和奖励函数专家（Reward）。
        *   **工具调用**：智能体可调用 `compute_loss`（训练模型）或 `run_sql_query`（分析日志相关性）。
        *   **闭环迭代**：遵循“思考-编码-验证”循环，生成的代码（Diff 格式）会经过 Linter 检查语法。
*   **在线智能体（外环 - 生产验证）**：
    *   **核心思想**：低频验证，管理候选模型在真实流量中的 A/B 测试。
    *   **流程管理**：通过一个五阶段的有向无环图（DAG）管理模型生命周期：提案 -> 验证 -> 训练 -> 灰度发布 -> 指标汇总。
    *   **安全护栏**：自动监控在线指标，若出现异常（如某核心指标下跌超过 1%）则自动熔断。
*   **实验日志（Experiment Journal）**：作为系统的“长期记忆”，记录所有历史尝试及其结果，供 LLM 学习以避免重复失败。

### 3. 实验设计
*   **应用场景**：YouTube 视频推荐页的排序模型（基于深度神经网络的强化学习模型）。
*   **Benchmark（基准）**：由资深人类工程师手动调优的生产环境最强模型（SOTA）。
*   **对比任务**：
    1.  **优化器发现**：自主将 Adagrad 替换为优化后的 RMSprop。
    2.  **架构创新**：自主设计了类似 GLU 的“门控路径（Gated Path）”结构。
    3.  **奖励工程**：自主合成了一个包含“主动参与度”、“用户-频道关系”和“视频质量”的多目标奖励函数。

### 4. 资源与算力
*   **算力消耗**：文中提到单个模型的训练通常需要 **$\Theta(hours)$**（数小时）。
*   **具体细节**：论文**未明确说明**具体的 GPU 型号和数量。但提到系统通过优化训练效率，将训练延迟降低了 8 倍，从而提升了内环的吞吐量。
*   **LLM 使用**：主要使用 Gemini 2.5 Pro 作为推理核心。

### 5. 实验数量与充分性
*   **实验规模**：内环每轮会异步训练数百个候选模型；消融实验中，每组配置运行 6 次独立实验，每组探索 70 个想法。
*   **充分性**：实验涵盖了从底层优化器到高层业务逻辑（奖励函数）的多个维度，并在 YouTube 真实生产流量中获得了显著的“北极星指标”提升。
*   **客观性**：通过消融实验对比了不同模型规模（Pro vs. Flash）、提示词策略（有无角色扮演）和上下文管理（有无排序日志）的影响，论证了系统性能的来源。

### 6. 主要结论与发现
*   **超越人类**：LLM 智能体能够发现人类工程师未曾尝试的结构化改进（如特定的门控机制），并在 YouTube 核心指标上取得统计学显著的增长。
*   **效率红利**：实验吞吐量从人类时代的每周 1-10 个提升到每周 100 个以上，且工程师成本降为 0。
*   **推理能力至关重要**：Gemini Pro 的表现显著优于轻量级的 Flash 模型，证明了深度推理在算法发现中的必要性。
*   **上下文的重要性**：提供排序后的历史实验记录（Experiment Journal）能显著提升智能体找到更优解的速度。

### 7. 优点
*   **端到端自主性**：不仅是辅助工具，而是实现了从代码生成到 A/B 测试的完全闭环。
*   **语义化创新**：突破了传统 AutoML 只能调参的限制，能够进行逻辑层面的“奖励函数工程”。
*   **工业级落地**：在超大规模生产系统（YouTube）上验证了可行性，具有极高的实用价值。

### 8. 不足与局限
*   **冷启动依赖**：当实验日志为空时，智能体倾向于提出教科书式的通用建议，需要一定的“预热期”。
*   **幻觉风险**：尽管有 Linter，但 LLM 仍可能生成逻辑错误或导致训练不稳定的代码，需依赖严格的离线过滤。
*   **算力成本**：虽然节省了人力，但大规模并行训练数百个模型并频繁调用高性能 LLM 带来了巨大的计算开销。
*   **安全边界**：目前的奖励函数优化仍需在预设的指标池内进行，尚未实现完全脱离人类定义的“价值观发现”。

（完）
