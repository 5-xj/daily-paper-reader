---
title: "DIRECTOR: Dynamic Index-based Recommendation with Transport-Optimized Retrieval"
title_zh: "DIRECTOR: 基于动态索引与传输优化检索的推荐方法"
authors: "Yuanhao Pu, Chenghao Zhang, Chao Feng, Xiang Li, Defu Lian"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.26418v1"
tags: ["query:rerank-train"]
score: 7.0
evidence: 动态索引与传输优化检索的生成式重排序模型
tldr: 重排序需从候选中选择并排序高效用列表，现有自回归模型顺序生成导致延迟，非自回归模型又缺乏跨位置协调。DIRECTOR将候选映射到连续隐空间，基于最优传输并行生成动态检索索引，并以全局硬匹配避免重复。训练中采用熵正则化OT提供冲突感知监督，结合前缀锚定信用分配对齐全局奖励。在工业推荐数据集上离线与在线实验均取得显著提升，兼顾并行效率与全局优化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26418v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1843, \"height\": 750, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.26418v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 879, \"height\": 218, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26418v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1279, \"height\": 221, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26418v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 878, \"height\": 275, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26418v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1834, \"height\": 657, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26418v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 877, \"height\": 277, \"label\": \"Table\"}]"
motivation: 现有生成式重排序在并行效率与跨位置协调之间难以两全，自回归延迟高，非自回归易产生重复或冲突选择。
method: 提出DIRECTOR，将候选映射到连续空间，并行生成动态索引，训练用熵正则化最优传输监督，推理用全局硬匹配去重。
result: 离线与在线实验显示DIRECTOR显著优于自回归、非自回归及传统重排序基线，在工业场景提升明显。
conclusion: DIRECTOR以最优传输为桥梁，实现并行重排序的全局结构协调，为大规模推荐提供高效可靠方案。
---

## 摘要
重排序是一个组合决策问题，旨在从特定请求的候选集中选择并排序一个高效用的项目列表。一大类生成式重排序器采用自回归（AR）模型，通过逐位置构建列表来捕捉位置间的依赖关系。然而，在实际的贪心或有限宽度解码下，基于前缀的搜索可能会过早剪枝掉全局有希望的排列，并带来固有的顺序延迟，从而在固定服务预算下限制了有效搜索空间。非自回归（NAR）替代方法通过位置并行预测缓解了这一效率瓶颈，但朴素的逐位置分解将不同位置处理得过于独立，导致跨位置协调不足，并可能产生重复或冲突的项目选择。为了在保持并行效率的同时引入全局结构协调，我们提出了基于动态索引与传输优化检索的推荐方法（DIRECTOR），一种传输引导的并行重排序框架。DIRECTOR 将候选项目映射到连续潜空间，并并行地为所有目标位置生成请求条件化的动态检索索引。在训练期间，它使用熵正则化的最优传输（OT）提供冲突感知的监督；在推理时，它直接在相似度矩阵上执行全局硬匹配，无需迭代传输即可生成无重复的列表。为了进一步使生成器与仅返回标量效用的不透明列表级评估器对齐，我们引入了一种前缀锚定的信用分配机制，将全局奖励转换为位置特定的训练信号。大量的离线和在线实验表明，DIRECTOR 持续优于强重排序基线，在大规模工业推荐场景中取得了显著改进。

## Abstract
Reranking is a combinatorial decision problem that aims to select and order a high-utility slate from a request-specific candidate set. A major line of generative rerankers adopts autoregressive (AR) models, which construct the slate one position at a time to capture inter-position dependencies. However, under practical greedy or bounded-width decoding, prefix-based search may prematurely prune globally promising permutations and incurs inherently sequential latency, restricting the effective search space under a fixed serving budget. Non-autoregressive (NAR) alternatives alleviate this efficiency bottleneck through position-parallel prediction, but naive position-wise factorization treats different positions too independently, leading to insufficient cross-position coordination and potentially duplicate or conflicting item selections. To retain parallel efficiency while introducing global structural coordination, we propose Dynamic Index-based RECommendation with Transport-Optimized Retrieval (DIRECTOR), a transport-guided parallel reranking framework. DIRECTOR maps candidate items into a continuous latent space and generates request-conditioned dynamic retrieval indices for all target positions in parallel. During training, it uses entropy-regularized OT to provide conflict-aware supervision; at inference, it directly performs global hard matching on similarity matrix, producing duplicate-free slates without iterative transport. To further align the generator with an opaque list-wise evaluator that returns only a scalar utility, we introduce a prefix-anchored credit assignment mechanism that converts the global reward into position-specific training signals. Extensive offline and online experiments demonstrate that DIRECTOR consistently outperforms strong reranking baselines, achieving significant improvement in large-scale industrial recommendation scenarios.