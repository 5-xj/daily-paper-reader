Title: Approximating two-terminal network reliability

URL Source: https://arxiv.org/pdf/2608.02523v1

Published Time: Tue, 04 Aug 2026 02:45:08 GMT

Number of Pages: 31

Markdown Content:
# Approximating two-terminal network reliability 

Weiming Feng ∗ Yucheng Fu ∗ Heng Guo †

Abstract 

We present a fully polynomial-time randomised approximation scheme (FPRAS) for the two-terminal reliability problem on general graphs, both directed and undirected. We also show that the complementary unreliability question is # BIS -hard. The key idea of the algorithm was discovered by GPT-5.6 Sol Ultra. 

# 1 Introduction 

In 1979, Valiant [Val79] introduced the computational complexity class # P, a counting analogue of NP . One of his main motivations was to capture the intrinsic hardness of computing network reliability measures. Here, the setting is that we are given a stochastic network, which can be a directed or undirected graph. Each arc in a directed graph, or edge in an undirected graph, opens or closes independently with some probability, and reliability is measured by the probability of being connected in the open subgraph. One of the most prominent variant is the two-terminal reliability, which asks for the probability that if a given terminal s can reach another given terminal 

t in the stochastic graph. Indeed, this is listed as one of the first thirteen # P-complete problems by Valiant [Val79]. Reliability measures have been extensively studied, and it turns out that most variants are #P-complete [Bal80, Jer81, BP83, PB83, Bal86, Col87], which means that efficient exact computation is unlikely. For two terminal reliability, it remains # P-complete when every failure probability is 1/2 [Val79], or on directed acyclic graphs [PB83], or on planar graphs with maximum degree three [Pro86]. Due to the apparent intractability, it is then natural to ask for approximation. However, progress about the approximation complexity of reliability measures is much slower. For these problems, the appropriate notion of approximation is fully polynomial-time randomised approximation schemes (FPRAS), which asks for a relative approximation that runs in polynomial time in the input size and inverse relative error. The first FPRAS in this context is due to Karger [Kar01] for all-terminal undirected unreliability, whereas the complementary all-terminal reliability approximation is solved much later by Guo and Jerrum [GJ19]. 1 For two-terminal reliability, much less is known. The special case restricted to directed acyclic graphs (DAGs) admits an FPRAS. This is because the special case puts the problem into a certain complexity class called # SpanL , and Arenas,     

> ∗School of Computing and Data Science, The University of Hong Kong, HK, China
> †School of Informatics, University of Edinburgh, Informatics Forum, Edinburgh, EH8 9AB, UK E-mail address: wfeng@hku.hk ,fyc0130@connect.hku.hk , and hguo@inf.ed.ac.uk .
> 1While reliability and unreliability sum to 1, either quantity can be exponentially small, which prevents a relative approximation of one to translate to that of the other.

1arXiv:2608.02523v1 [cs.DS] 3 Aug 2026 Croquevielle, Jayaram, and Riveros [ACJR21] showed that all problems in # SpanL admit FPRASes. See also [AvBGM25, FG24]. The approximation complexity of two-terminal reliability in general, despite its old history [KL85, ZL11], remains unresolved. It was explicitly asked in the survey by Kannan [Kan94] if an FPRAS exists. In this paper we answer this question positively, by giving algorithms for both the directed and the undirected case. In fact, we first give an algorithm for the directed version, and then reduce the undirected version to it. The reduction is essentially due to Jerrum [Jer81]. Thus, we formally introduce the directed version next. Let G = ( V, E ) be a directed graph with two distinguished vertices s, t . Each arc e ∈ E is associated with an open probability pe ∈ (0 , 1] .The two-terminal network reliability of G asks the following question: suppose that each arc is independently open with probability pe, what is the probability that there is a path from s to t?Equivalently, the probability can be written as 

Rel (s, t ) = Pr Gp [s → t] = ∑      

> F⊆E:
> scan reach tin (V,F )
> ∏
> e∈F

pe

> ∏
> e∈E\F

(1 − pe),

where Gp denotes the random subgraph of open arcs. In the special case when all pe = 1 /2, the problem amounts to counting the spanning subgraphs of G in which s can reach t.By FPRAS (fully polynomial randomised approximation scheme), we mean a randomised algo-rithm that, given a directed graph G = ( V, E ), two terminals s, t ∈ V , open probabilities pe ∈ (0 , 1] 

for each arc e ∈ E, and an error parameter ε > 0, outputs a random variable ˆR such that 

Pr 

> [

(1 − ε)Rel (s, t ) ≤ ˆR ≤ (1 + ε)Rel (s, t )

> ]

≥ 34 ,

and whose running time is polynomial in the input length and in ε−1.

Theorem 1. There is an FPRAS for two-terminal reliability on directed graphs. On an input with 

n = |V | vertices, m = |E| arcs, minimum open probability pmin = min e∈E pe, its running time is 

> ˜

O

> (

n3m2 ( 1 

> ε2

+ n log  

> (2
> pmin
> )))

, where ˜O(·) hides factors polylogarithmic in n, ε−1, and log  

> (2
> pmin
> )

.

The algorithm in Theorem 1 is a combination of simulated annealing and Markov chain Monte Carlo. It is a significant departure from previously known reliability algorithms, such as [Kar01, GJ19, CGZZ24, ACJR21]. In spirit, the closest algorithm is perhaps the celebrated approximation algorithm for non-negative permanents by Jerrum, Sinclair, and Vigoda [JSV04]. We give a high-level overview of the algorithm in Section 1.1. Another variant of the problem is the undirected version, where G is undirected and we ask the probability that s and t are connected in the random subgraph. For this variant, efficient algo-rithms are known only for graphs of low tree-width [SW85, GM20], and no efficient approximation algorithm is known. Jerrum [Jer81] gave an exact constant-size gadget reduction to the directed version that preserves pmin : replace each undirected edge with the directed gadget below. 

u vpuv u ae be v

1 puv 111

Figure 1: Directed gadget for undirected edge {u, v } with open probability puv .2For a connected undirected graph with n vertices and m edges with m ≥ n − 1, the reduction produces a directed graph with n + 2 m vertices and 5m arcs, while preserving the minimum open probability. Combined with Theorem 1, this gives the following result. 

Corollary 2. There is an FPRAS for two-terminal reliability on undirected graphs. On an input with n vertices, m edges, minimum open probability pmin , and error parameter ε > 0, its running time is ˜O

> (

m5 ( 1 

> ε2

+ m log  

> (2
> pmin
> )))

.

In light of Karger’s celebrated algorithm [Kar01], it is also interesting to ask the approximation complexity for unreliability UnRel (s, t ) = 1 − Rel (s, t ). Feng and Guo [FG24] showed that even in DAGs, approximating s − t unreliability is # BIS -hard. Here, # BIS is the problem of counting the number of independent sets in bipartite graphs. While # BIS is not known to be NP -hard to approximate, it is also conjectured to have no FPRAS [DGGJ04]. On the other hand, the same problem in undirected graphs is less clear. The only prior result is due to Karger [Kar01], where an FPRAS is presented if the minimum s − t cut has the same order as the global minimum cut. We show that in undirected graphs, the problem is # BIS -hard as well, and thus is unlikely to admit an FPRAS. 

Theorem 3. For any p ∈ (0 , 1) , if undirected s − t unreliability at fixed open probability p for all edges admits an FPRAS, then # BIS admits an FPRAS. 

Indeed, Provan and Ball [PB83] already gave a parsimonious reduction from # BIS to the number of s − t minimum cuts. The “ground states” for s − t unreliability are exactly these min-cuts, and thus we just need a boosting argument so that these states contribute a significant portion of the unreliability. A full proof is given in Section 5 for completeness. Note that if we consider s − t

minimal cuts, the problem becomes NP -hard [GGL16]. The # BIS -hardness of Theorem 3 and in [FG24] leaves open the possibility of s − t unreliability being either NP -hard or # BIS -easy. We find both possibilities to be plausible and would not make a conjecture here. 

1.1 Proof overview 

Given a graph G = ( V, E ), it is natural to define a so-called Gibbs distribution, whose states are 

F ⊆ E such that s can reach t in (V, F ), and the probability of F is proportional to the probability of F being open in the product distribution (bond percolation). Approximating Rel (s, t ) can reduce to sampling from this Gibbs distribution, and the standard way to do so is Glauber dynamics. Since this recipe works well for all-terminal reliability (see, e.g. [CGZZ24]), it is tempting to try it for two-terminal reliability as well. However, it fails quite badly for a simple example where s and t are connected by two long paths. If either path is open, s can reach t, but it is exponentially unlikely to switch from one path to another. It implies straightforward Glauber dynamics mixes slowly on graphs like this. To avoid this rigidity, one idea is to enlarge the state space to include both a subset F ⊆ E of arcs and a marked vertex u, so that s can always reach u in (V, F ). If we assign a uniform weight on all such states, then the resulting distribution biases towards the vertices u whose Rel (s, u ) is large. To ensure that we can sample (t, F ) with an at least inverse polynomial probability, we will need to add a correction term of roughly, say, 1/Rel (s, u ) for each possible marked vertex u. However, our original goal is to estimate Rel (s, t ), so this sounds like we are going in circles. 3The key idea to salvage the above is to use simulated annealing. Instead of open probability pe

for each e, we first consider pe = 1 for all e. In this case it is trivial to evaluate Rel (s, u ) for all u.Then, we gradually reduce each pe by a small multiplicative factor, which is essentially the step of adjusting temperatures in simulated annealing. This ensures that Rel (s, u )’s between two adjacent temperatures change only by a constant factor. Then, we can use the estimates of Rel (s, u ) of the previous temperature to define the distribution of the next temperature, which has only a constant multiplicative error to what we desire. With the target distribution in hand, the remaining question is how to sample from it. We consider a Markov chain with two types of transitions, either to update the arc set or to update the marked vertex together with an incident arc. We use the multicommodity flow method [Sin92] to bound the congestion of this chain. The key is to route the probability mass between any pair of states while maintaining a low congestion. In fact, the pioneering work of canonical paths by Jerrum and Sinclair [JS89] has already shown us how to distribute the probability mass evenly when the global changes are flipping paths and cycles. In our case, for any two states (u, F ) and 

(u′, F ′), there is a canonical way of choosing a (backwards) path from u to s and then from s to 

u′. We use the idea of [JS89] to distribute the flow to flip arcs along this path. However, one complication is that when some arc is removed, the marked vertex needs to move too. This is allowed in our enlarged state space, and indeed appears to be the main benefit of considering it. Our (random) path from (u, F ) to (u′, F ′) always goes through an intermediate state (s, U ), where 

U is a random subgraph from the product distribution. This random U distributes the weights somewhat evenly in the state space. Intuitively, there is no constraint on the arcs once the marked vertex is s, so that arcs can be freely changed. Some extra care is needed here and our constructed flow actually interleaves between the movement of the marked vertex and other arc flips. This is the most involved step of the algorithm, and the details can be found in Section 3.4. Overall, the high level idea of using simulated annealing together with Markov chain Monte Carlo is very similar to the non-negative permanent approximation algorithm by Jerrum, Sinclair, and Vigoda [JSV04]. However, the details are quite different and the construction of the flow paths appears to be new. We hope this framework to find more applications in the future. 

# 2 Proof of Theorem 1 

In this section, we first give a high-level outline of the algorithm in Theorem 1. The main algorithm and the annealing is explained in Section 2.1 and the Markov chain step in Section 2.2. The analysis of the Markov chain is in Section 3. Let G = ( V, E ) be a directed graph with two distinguished vertices s, t . If s = t, then Rel (s, t ) = 1 

and there is nothing to estimate, so assume that s̸ = t. If G contains self-loops, they can be safely removed. If G contains parallel arcs, say with open probability p1 and p2, then they can be merged into a single arc with an effective open probability p1 + p2 − p1p2. Thus we may assume that G is simple. This reduction never decreases the value of pmin . It does not affect the running time upper bound in Theorem 1. Moreover, we assume throughout that s can reach every vertex of G. This is without loss of generality: no path starting at s can use an arc incident to a vertex that s cannot reach, so every such vertex and arc can be deleted without changing Rel (s, t ), and if t is deleted then Rel (s, t ) = 0 . Let pe ∈ (0 , 1] be the open probability of each arc e ∈ E. Let p = ( pe)e∈E denote the vector of open probabilities. Let Dp be the product measure with Bernoulli distribution with 4parameter p (namely bond percolation): 

∀F ⊆ E, Dp(F ) := ∏

> e∈F

pe

> ∏
> e∈E\F

(1 − pe).

For any vertex v ∈ V , define the collection of arc sets 

Ωv := {F ⊆ E : s can reach v via arcs in F }.

Clearly, the probability that s can reach v, denoted by qv(p), is given by 

qv(p) := ∑ 

> F∈Ωv

Dp(F ).

Note that qv is a function of p, and we write qv if p is clear from the context. In particular, 

qt = Rel (s, t ) is the value of interest and qs = 1 . As s can reach every vertex, qv > 0 for every 

v ∈ V .It is convenient to consider the more general problem of approximating qv for all vertices v ∈ V

simultaneously. Define the following set of pairs 

Ω = {(v, F ) | v ∈ V, F ∈ Ωv}.

We regard every pair X = ( v, F ) ∈ Ω as a state, and call v the marked vertex of X and F its arc set. Suppose one can draw a random state X = ( v, F ) ∈ Ω with probability proportional to Dp(F ). The states whose marked vertex equals v have total weight ∑  

> F∈Ωv

Dp(F ) = qv, so the marked vertex of X equals v with probability exactly qv/∑ 

> u∈V

qu. Given N independent samples of X, let Nv be the number whose marked vertex is v. Then E [Nv/N ] = qv/∑ 

> u∈V

qu for every v ∈ V , and since 

qs = 1 , the ratio Nv/N s estimates qv with the normalising constant ∑ 

> u∈V

qu cancelled. However, this estimator is useless as it stands. If qv is exponentially small, then so is the probability that a single sample has marked vertex v, and one needs exponentially many samples before Nv is nonzero. The solution is to reweight the vertices so that none of them is rare. We introduce the following distribution. 

Definition 4. Given a vector p of open probabilities and a vector of positive vertex weights 

c = ( cv)v∈V , define a distribution πc,p over Ω by 

∀(v, F ) ∈ Ω, πc,p(v, F ) = cvDp(F ) 

> ∑
> u∈V

cuqu(p) ∝ cvDp(F ).

Suppose we can draw X = ( v, F ) ∼ πc,p and let Nv be the number of samples whose marked vertex is v among N independent samples of X. Then E [Nv/N ] = cvqv/∑ 

> u∈V

cuqu, and since 

qs = 1 , the normalising constant once again cancels in the ratio: one can estimate qv by cs 

> cv

· Nv 

> Ns

.The point of the weights is that they can flatten the distribution of the marked vertex. Ideally, one would set cv = 1 /q v for all v ∈ V , in which case the marked vertex is uniform over V , as it equals v

with probability cvqv/∑ 

> u∈V

cuqu = 1 /n for every v, where n = |V |. No value of the marked vertex is then rare, and N can be taken polynomially large. The ideal weights are of course the very quantities to be approximated, and cannot be set in advance. It turns out that knowing 1/q v up to a constant factor is enough. The following lemma shows that such weights already allow all the qv to be estimated within relative error ε, and that sampling from πc,p can be done efficiently by a Markov chain. 5Lemma 5. Suppose we are given open probabilities p = ( pe)e∈E and positive vertex weights c =(cv)v∈V such that 14qv (p) ≤ cv ≤ 4  

> qv(p)

for all v ∈ V .1. There is a Markov chain on Ω with unique stationary distribution πc,p. Independently draw 

F0 ∼ D p and start the chain from the random state X0 = ( s, F 0). For any η ∈ (0 , 1) , after running 

T = O

> (

nm 2 log  

> (

nη

> ))

steps, it outputs a random X ∈ Ω satisfying dTV (X, π c,p) ≤ η, where dTV is the total variation distance. Moreover, for every ζ ∈ (0 , 1) , the simulation of T -step Markov chain takes time 

O

> (

n2m2 log  

> (

nη

> )

+ m log  

> (

1

ζ

> ))

with probability at least 1 − ζ.2. For any ε, δ ∈ (0 , 1) , using N = O  

> (nε2

log ( nδ

> ))

samples of the above Markov chain, each with 

η = δ 

> 4N

, one can produce random (ˆ qv)v∈V such that 

Pr [ ∀v ∈ V, (1 − ε)qv(p) ≤ ˆqv ≤ (1 + ε)qv(p)] ≥ 1 − δ

in worst-case time 

O 

> (

n3m2

ε2 log  

> (

nεδ 

> )

log  

> (

nδ

> ))

.

The Markov chain is described in Section 2.2. Once Item (1) is established, Item (2) follows from the estimator discussed earlier. The proof of Item (1) is deferred to Section 3, and the proof of Item (2) is deferred to Section 4. In Section 2.1 next, we show how to use Lemma 5 to approximate the two-terminal reliability. 

2.1 The main algorithm 

Recall that p = ( pe)e∈E is the vector of open probabilities. To apply Lemma 5, we need to construct a vector c = ( cv)v∈V satisfying 14qv (p) ≤ cv ≤ 4  

> qv(p)

for all v ∈ V . We call such a vector c admissible. This can be done by an annealing procedure on open probabilities. The same idea was used by Jerrum, Sinclair, and Vigoda [JSV04] to approximate the permanent of a non-negative matrix. We first record that the comparison between two nearby probability vectors depends only on the size of a reachability certificate, rather than on the total number of open arcs. 

Lemma 6. Let p′ = ( p′

> e

)e∈E and p = ( pe)e∈E be two vectors of open probabilities. If, for some 

β ∈ (0 , 1] , it holds that βp e ≤ p′ 

> e

≤ pe for all e ∈ E, then, for every v ∈ V , qv(p′) ≥ βn−1qv(p).

Proof. Sample F ∼ D p. Conditional on F , independently retain every arc e ∈ F with probability 

p′

> e

/p e, and let F ′ be the resulting arc set. The indicators of membership in F ′ are independent, and each arc e belongs to F ′ with probability pe(p′

> e

/p e) = p′

> e

. Hence F ′ ∼ D p′ .6For every F ∈ Ωv, fix a simple directed path P (F, v ) from s to v contained in F . Such a path contains at most n − 1 arcs. Given F ∈ Ωv, the probability that all arcs of P (F, v ) survive the thinning is at least 

∏ 

> e∈P(F,v )

p′

> e

pe

≥ β|P (F,v )| ≥ βn−1.

Whenever this path survives, F ′ ∈ Ωv. Therefore qv(p′) = Pr[ F ′ ∈ Ωv] ≥ qv(p)βn−1.

In our algorithm, we set the parameter 

β := 1 − 14n .

Definition 7. Define a sequence of open probabilities p(0) , p(1) , . . . , p(L), where each p(i) = ( p(i) 

> e

)e∈E

is a vector of open probabilities for the arcs e ∈ E. The sequence satisfies 

• p(0) = 1 is the all-one vector and p(L) = p is the input open probabilities. 

• for any 0 < i ≤ L and any e ∈ E, βp (i−1)  

> e

≤ p(i) 

> e

≤ p(i−1)  

> e

.The following lemma shows that one can construct a sequence of open probabilities that satisfies the above definition with a length polynomial in the input size. The proof is given in Section 4. 

Lemma 8. Let pmin = min e∈E pe. One can construct a sequence of open probabilities that satisfies Definition 7 with a length L = O

(

n log 

( 2

> pmin

)) 

in time O(mL ).

Fix the underlying graph G = ( V, E ), two terminals s, t ∈ V , and the sequence of open proba-bilities as in Definition 7, and think of the open probabilities as gradually changing from p(0) = 1

to p(L) = p. At the i-th stage, we consider the two-terminal reliability problem with the open probabilities p(i), and abbreviate by q(i) 

> v

= qv(p(i)) the probability that s can reach v at that stage. The point of Definition 7 is that consecutive stages have comparable reachability probabilities, so that the reciprocals of the estimates of q(i) 

> v

obtained at the i-th stage are admissible vertex weights 

cv for the (i + 1) -th stage. Formally, we have the following lemma. 

Lemma 9. For every vertex v ∈ V and every 1 ≤ i ≤ L,

34 ≤ q(i)

> v

q(i−1) 

> v

≤ 1.

Proof. As p(i) 

> e

≤ p(i−1)  

> e

for every e ∈ E, it is easy to verify q(i) 

> v

≤ q(i−1)  

> v

. For the reverse inequality, the definition of the schedule and Lemma 6 give 

q(i) 

> v

≥ βn−1q(i−1)  

> v

≥

(

1 − n − 14n

)

q(i−1)  

> v

> 34 q(i−1)  

> v

,

where the second inequality is Bernoulli’s inequality. This completes the proof. 7Proof of the main result Now we are ready to describe the algorithm of Theorem 1. It computes estimates ˆq(i) 

> v

of q(i) 

> v

for every v ∈ V , one stage at a time for i = 0 , 1, . . . , L , and outputs ˆq(L) 

> t

. At the 0-th stage, p(0)  

> e

= 1 for every e ∈ E, so q(0)  

> v

= 1 for every v ∈ V and the algorithm takes ˆq(0)  

> v

= 1 .Consider the i-th stage for 1 ≤ i ≤ L, and assume inductively that the estimates of the previous stage satisfy 

910 q(i−1)  

> v

≤ ˆq(i−1)  

> v

≤ 11 10 q(i−1)  

> v

(v ∈ V ), (1) which holds trivially when i = 1 . Set the vertex weights 

c(i) 

> v

:= 1 /ˆq(i−1)  

> v

(v ∈ V ),

and call the algorithm in Item (2) of Lemma 5 on the open probabilities p(i) with the weights c(i),using fresh randomness. These weights are admissible: combining (1) with Lemma 9, 

c(i) 

> v

q(i) 

> v

= q(i)

> v

ˆq(i−1) 

> v

≥ 10 11 · q(i)

> v

q(i−1) 

> v

≥ 10 11 · 34 > 14 and c(i) 

> v

q(i) 

> v

≤ 10 9 · q(i)

> v

q(i−1) 

> v

≤ 10 9 < 4,

so that 14q(i)

> v

≤ c(i) 

> v

≤ 4

> q(i)
> v

for every v ∈ V , as Lemma 5 requires. For i < L , the algorithm is called with ε = 110 and δ = 110 L , and its output satisfies (1) at the next stage with probability at least 

1 − 110 L . For i = L, it is called with the target relative error ε and with δ = 110 . Since p(L) = p, the returned value ˆq(L) 

> t

estimates q(L) 

> t

= Rel (s, t ).Let Ai be the event that the i-th stage meets its requirement, that is, (1) holds with i in place of i − 1 if i < L , and (1 − ε)q(L) 

> t

≤ ˆq(L) 

> t

≤ (1 + ε)q(L) 

> t

if i = L. As every stage uses fresh randomness, the two bounds above apply conditionally on the previous stages, and 

Pr 

[ L⋂

> i=1

Ai

]

=

> L

∏

> i=1

Pr 

Ai

∣∣∣∣∣∣⋂

> j<i

Aj

 ≥

(

1 − 110 L

)L−1 (

1 − 110 

)

≥ 34 .

The output is therefore within a factor 1 ± ε of Rel (s, t ) with probability at least 34 .It remains to bound the running time. The bound of Lemma 5 applies uniformly at every stage. Each of the first L − 1 stages costs 

O

(

n3m2 log 2(nL )

)

= ˜O

(

n3m2)

,

and the last stage costs ˜O

( n3m2

> ε2

)

. Since L = O

(

n log 

( 2

> pmin

)) 

by Lemma 8, the total running time is 

˜O

(

n4m2 log 

( 2

pmin 

)

+ n3m2

ε2

)

,

which is at most the bound stated in Theorem 1. 82.2 The Markov chain 

We now describe the Markov chain used in Lemma 5. To simplify the notation, we fix open probabilities p = ( pe)e∈E and vertex weights c = ( cv)v∈V such that 14qv ≤ cv ≤ 4 

> qv

for all v ∈ V ,where qv = qv(p). We use π to denote the distribution πc,p over Ω. From now on we also restrict 

Ω to the support of π, discarding every pair (v, F ) with Dp(F ) = 0 , so that π(X) > 0 for every 

X ∈ Ω. This restriction is vacuous unless pe = 1 for some arc e, and it is harmless: the discarded states carry no probability mass. Moreover, if F0 ∼ D p, then Dp(F0) > 0 almost surely, so the random state (s, F 0) used below belongs to the restricted state space almost surely. The Markov chain for sampling from πc,p is described as follows. It starts from a random initial state X0 with some distribution μ on Ω. For the sampling algorithm, we independently draw 

F0 ∼ D p and take X0 = ( s, F 0). The chain uses two transition kernels K1 and K2. At the k-th step, with half probability, stay at the current state Xk+1 = Xk; with probability 14 , it uses K1

to update Xk to Xk+1 , and with probability 14 , it uses K2. In other words, the overall transition kernel K is given by 

K = 12 I + 14 K1 + 14 K2, (2) where I is the identity. Let Xk = ( v, F ) be the current state. The kernel K1 updates the marked vertex v, but it may also change the arc set F . The other kernel K2 updates the arc set F . The detailed definitions are as follows. 

Metropolis update K1: pick an arc e = ( a, b ) ∈ E uniformly at random, and propose a candidate state X′ 

> k+1

as follows. 

• If v = a, then with half probability propose X′ 

> k+1

= ( b, F ∪ { e}) and with half probability propose X′ 

> k+1

= Xk.

• If v = b and e ∈ F , then with half probability propose X′ 

> k+1

= ( a, F ∪ { e}) and with half probability propose X′ 

> k+1

= ( a, F \ { e}).

• In all other cases ( v / ∈ { a, b } or (v = b and e / ∈ F )), propose X′ 

> k+1

= Xk.With probability min 

> {

1, π(X′ 

> k+1 )
> π(Xk)
> }

, accept the proposal and set Xk+1 = X′

> k+1

; otherwise, reject the proposal and set Xk+1 = Xk. We remark that the proposal X′ 

> k+1

can be infeasible ( X′ 

> k+1

/∈ Ω), in which case π(X′

> k+1

) = 0 and the proposal is always rejected. 

Heat-bath update K2: pick an arc e ∈ E uniformly at random, and let F1 = F ∪ { e} and 

F0 = F \ { e}. If s can reach v via arcs in F0, then let Xk+1 = ( v, F 1) with probability pe and 

Xk+1 = ( v, F 0) with probability 1 − pe. Otherwise, let Xk+1 = ( v, F 1).For any Markov chain P over some finite state space Ω, it is called irreducible if for any two states X, Y ∈ Ω, there exists a positive integer t such that Kt(X, Y ) > 0. Moreover, it is aperiodic 

if for any state X ∈ Ω, the greatest common divisor of the set of all positive integers t such that 

Kt(X, X ) > 0 is 1, and it is reversible with respect to π if for any two states X, Y ∈ Ω, the detailed balance condition π(X)K(X, Y ) = π(Y )K(Y, X ) holds. If a chain is irreducible, aperiodic, and reversible, then it converges to a unique stationary distribution. See [LP17]. 9Proposition 10. The Markov chain K defined in (2) is irreducible, aperiodic, and reversible with respect to π.

Proposition 10 is verified in Section 3.2. Thus the chain K has π as its unique stationary distribution, and the distribution of Xk converges to π as k → ∞ . Given a random initial state X0

with distribution μ on Ω and an accuracy η ∈ (0 , 1) , define the mixing time of the Markov chain 

K as the smallest number of steps after which the chain is within total variation distance η of π,that is, 

Tmix (μ, η ) = min 

> {

k ≥ 0 : dTV (μK k, π ) ≤ η

> }

,

where μK k is the distribution of Xk. We call μ an M -warm start with respect to π if 

∀Y ∈ Ω, μ(Y ) ≤ M π (Y ).

The analysis in Section 3 shows the following bound on the mixing time. 

Theorem 11. Let G = ( V, E ) be a directed graph with n = |V | vertices and m = |E| arcs such that the source s can reach every vertex v ∈ V . Let p = ( pe)e∈E be open probabilities with pe ∈ (0 , 1] for all e ∈ E, and let c = ( cv)v∈V be vertex weights such that 14qv ≤ cv ≤ 4 

> qv

for all v ∈ V . For every initial distribution μ that is M -warm with respect to π and every η ∈ (0 , 1) , the mixing time of the Markov chain K satisfies 

Tmix (μ, η ) = O

> (

nm 2

> (

log M + log  

> (

1

η

> )))

.

Theorem 11 is proved in Section 3 by analyzing the spectral gap of the transition matrix. Now, assume the mixing time is known. We are ready to prove the first item in Lemma 5. 

Proof of Item (1) of Lemma 5. The Markov chain is K as defined in (2). Independently draw 

F0 ∼ D p and take X0 = ( s, F 0). This is a legal state almost surely because s can always reach itself and Dp(F0) > 0 almost surely. Let μ denote the distribution of X0, and write Z = ∑ 

> u∈V

cuqu. For every (v, F ) ∈ Ω, we have 

μ(v, F ) = 

> {

Dp(F ), v = s, 

0, v̸ = s. 

Recall that qs = 1 , so cs ≥ 14 , while cuqu ≤ 4 for every u ∈ V gives Z ≤ 4n. Consequently, 

∀(s, F ) ∈ Ω, μ(s, F )

π(s, F ) = Zcs

≤ 16 n, 

and the same pointwise domination is trivial for states whose marked vertex is not s. Thus μ is a 

16 n-warm start with respect to π. By Theorem 11, running the chain for 

Tmix (μ, η ) = O

> (

nm 2

> (

log(16 n) + log  

> (

1

η

> )))

= O

> (

nm 2 log  

> (

nη

> ))

steps outputs a random X ∈ Ω with dTV (X, π ) ≤ η.10 Implementation and running time. A trivial implementation of each transition takes O(m)

time: as every vertex is reachable from s, we have m ≥ n − 1, and one breadth-first search tests the required reachability condition in O(m) time. We next give an implementation with a sharper bound for a sequence of transitions. Store the static outgoing adjacency lists of G. Let (v, F ) denote the current state. Represent 

F by a bit vector in {0, 1}E , and maintain a simple directed path P from s to the current marked vertex v using arcs of F , together with arrays recording the vertices and arcs on P . Initially, when 

X0 = ( s, F 0), take P to be the length-zero path at s. Consider first a K2 update of an arc e. If 

e / ∈ P , then P remains a witness that s reaches v in F \ { e}, so no search is needed. If e ∈ P ,run breadth-first search in F \ { e}. If it finds another path from s to v, use that path as the new witness; otherwise K2 must retain e, and the old path remains valid. For a K1 update, suppose first that the selected arc is e = ( v, w ). The candidate state with marked vertex w is automatically feasible: if the proposal is accepted, append e to P when w is not on P , and use the prefix ending at w when w is on P . Now suppose that the selected arc is 

e = ( u, v ) ∈ F . Run a breadth-first search in the arc set of the proposed state to test whether s

can reach u. If the proposal is feasible and accepted, use the path returned by the search as the new witness; otherwise, retain P . Thus, a K1 update only requires a search in this last case. Apart from a breadth-first search, the path can be updated in O(n) time. Since F and F ′ differ on at most one arc, the Metropolis acceptance probability can be computed in O(1) time. In the above implementation, only some transitions require a breadth-first search. Recall that 

T is the total number of transitions we simulate. For k ∈ [T ], let Ik ∈ { 0, 1} indicate that the 

k-th transition performs a breadth-first search, and let Hk−1 contain the full history before that transition. Conditional on this history, the current witness path has at most n − 1 arcs. A K2 step performs a search only if its uniformly selected arc lies on this path. A K1 step performs a search only if its selected arc is an incoming arc of the current marked vertex. Since G is simple, the latter also has at most n − 1 possibilities. Recalling the coefficients of K1 and K2 in (2), we obtain 

E[Ik | H k−1] ≤ n − 14m + n − 14m ≤ n − 12m . (3) The random variables I1, . . . , I T need not be independent, but (3) gives 

E [exp( Ik) | H k−1] ≤ 1 + (e − 1) n − 12m ≤ exp 

> (

(e − 1) n − 12m

> )

.

Iterating this conditional bound yields 

E

> [

exp  

> (T∑
> k=1

Ik

> )]

≤ exp 

> (

(e − 1) (n − 1) T

2m

> )

.

Consequently, Markov’s inequality implies that, for every ζ ∈ (0 , 1) ,

Pr  

> [T∑
> k=1

Ik > e( n − 1) T

2m + log  

> (

1

ζ

> )]

≤ ζ. (4) Let Time (T ) denote the total running time of one simulation consisting of initialisation followed by 

T transitions. Drawing F0 and initialising the data structure take O(m) time. Since a transition 11 costs O(n) time apart from a possible O(m) breadth-first search, (4) shows that, for a sufficiently large universal constant C,

Pr 

> [

Time (T ) > C 

> (

m + nT + m log  

> (

1

ζ

> ))]

≤ ζ. (5) Finally, substituting T = O (nm 2 log ( n/η )) into (5) proves the running-time bound in Item (1). 

# 3 Analysis of the Markov chain 

In this section, we prove Proposition 10 and Theorem 11. This section is organized as follows. In Section 3.1, we review some preliminaries of the Markov chain analysis. In Section 3.2, we verify Proposition 10, namely the irreducibility, aperiodicity, and reversibility of the chain K. In Section 3.3, we prove a lower bound on the spectral gap of K, which implies the mixing time bound in Theorem 11. 

Intuition of rapid mixing Before turning to the analysis, we give some intuition why K mixes rapidly. Consider the most natural chain for the problem. The quantity to be estimated is qt =

Dp(Ω t), and the direct approach is to sample an arc set F from Dp conditioned on the event F ∈ Ωt,namely conditioned on s reaching t via the open arcs. The standard chain is the heat-bath Glauber dynamics on the state space Ωt: from a current arc set F ∈ Ωt, pick an arc e ∈ E uniformly at random and resample its membership according to the conditional marginal given the other arcs, keeping F \ { e} fixed. This is precisely the kernel K2 with the marked vertex fixed at t.This natural chain can mix exponentially slowly. Suppose that G consists of two vertex-disjoint 

s–t paths of length ℓ, and that every arc is open with probability 12 . Reaching t requires at least one of the two paths to be entirely open, so Ωt = A ∪ C, where A and C are the sets of arc sets in which the upper, respectively the lower, path is entirely open; see Figure 2. As the dynamics changes one arc at a time, it cannot leave A before the lower path has become entirely open, so every trajectory from A to C has to pass through B = A ∩ C. Such a bottleneck forces the mixing time to be 2Ω( ℓ), exponential in the size of the graph.                     

> ststst
> (a) F∈A: upper path open (b) F∈B=A∩C: both paths open (c) F∈C: lower path open

Figure 2: The Glauber dynamics can pass from A to C only through the rare set B. Solid arcs are open, and dotted arcs are subject to no constraint. Our chain circumvents this issue by letting the marked vertex move. Recall that a state is a pair (v, F ) in which the marked vertex v is reachable from s via the arc set F , and that the kernel 

K1 updates the marked vertex. Consider again the state (t, F ) in which the upper path is open, and let w be the predecessor of t on that path. Then K1 can move the marked vertex from t back to w, updating the arc e between them along the way, as illustrated in Figure 3. The reachability constraint travels with the marked vertex: in the new state only w has to be reachable from s.12 swt    

> eswt
> e
> tw
> K1
> (t, F )(w, F \ { e})

Figure 3: One step of K1, retreating the marked vertex from t to w.Consider now the ideal choice of vertex weights cv = 1 /q v. Then the marked vertex is uniform under π: for every v ∈ V , the states whose marked vertex is v have total stationary mass 1/n .The K1 moves allow the reachability requirement to move locally along the underlying undirected graph, while K2 refreshes arcs that are not forced by the current requirement. In the two-path example, the marked vertex can retreat along the upper path from t to s, so that the old witness path is progressively no longer required. When the marked vertex is s, the reachability constraint is vacuous, the conditional distribution of the arc set is Dp, and K2 is the ordinary product heat-bath dynamics. The chain can therefore forget the upper witness path and subsequently grow towards 

t along the lower path, without requiring both paths to be open simultaneously. Thus the relevant gateway is the entire collection of states whose marked vertex is s, which has stationary mass 

1/n , rather than the exponentially rare set B. The same layer also provides a warm start for the sampling algorithm: draw F ∼ D p and mark s. This is exactly π conditioned on the marked vertex being s, and under admissible vertex weights it is a 16 n-warm start with respect to π. In the following analysis, the multicommodity-flow argument in Section 3.3 and Section 3.4 makes this intuition rigorous. We also note that in the two-path example above, if we start from s, it is roughly an unbiased simple random walk to reach t, which would take time Ω( ℓ2). The update rate is roughly 1/ℓ ,making the overall mixing time Ω( ℓ3) to drop the total variation distance error down to O(1 /n ).Thus there is a Ω( n3) lower bound on the mixing time of K, which is a polynomial factor away from the upper bound in Theorem 11. It is easy to see that this mixing time can be improved to Θ( n2)

if we change K1 to update only an adjacent arc instead of a uniformly at random arc. However, even for that version there is still an Ω( n3) mixing example, and there is no improvement on our mixing time upper bound as the bottleneck from K2 is unchanged. For some further discussion on the lower bound side of the mixing time, see Section 3.5. 

3.1 Preliminaries of Markov chain analysis 

Let π be a distribution over a finite set Ω with π(x) > 0 for every x ∈ Ω, and let K be a transition kernel on Ω that is irreducible and reversible with respect to π. Assume in addition that K is lazy ,namely that in every step it stays at the current state with probability at least 12 ; in particular 

K is aperiodic. Reversibility makes K self-adjoint with respect to the inner product ⟨f, g ⟩π = 

> ∑
> x∈Ω

π(x)f (x)g(x) on RΩ, so all eigenvalues of K are real; laziness makes them nonnegative. The 

|Ω| eigenvalues of K can thus be arranged as 1 = λ1 > λ 2 ≥ · · · ≥ λ|Ω| ≥ 0 and the spectral gap of 

K is defined as gap( K) = 1 − λ2 ∈ (0 , 1] .

Lemma 12 (Spectral-gap mixing bound; cf. [LP17, Theorem 12.4]) . Let K be as above. For every 

13 probability distribution μ on Ω and every integer t ≥ 0,

dTV (μK t, π ) ≤ 12 (1 − gap( K)) t

 ∑

> x∈Ω

μ(x)2

π(x) − 1



> 1/2

. (6) 

Consequently, if μ is an M -warm start with respect to π, then 

dTV (μK t, π ) ≤ 12

√M − 1 e −t gap( K). (7) 

Proof. Let h = μ/π − 1, viewed as a function in RΩ. Then ⟨h, 1⟩π = 0 . Reversibility gives 

(μK t)( y)

π(y) − 1 = ( Kth)( y) (y ∈ Ω) .

Since K is lazy, all its eigenvalues are nonnegative, and its operator norm on the subspace orthogonal to the constants is λ2 = 1 − gap( K). Hence 

2dTV (μK t, π ) = ∑

> y∈Ω

π(y)

∣∣∣(Kth)( y)

∣∣∣ ≤ ∥ Kth∥2,π ≤ (1 − gap( K)) t∥h∥2,π .

Moreover, 

∥h∥22,π = ∑

> x∈Ω

π(x)

( μ(x)

π(x) − 1

)2

= ∑

> x∈Ω

μ(x)2

π(x) − 1,

which proves (6). If μ is M -warm, then 

∑

> x∈Ω

μ(x)2

π(x) ≤ M ∑

> x∈Ω

μ(x) = M. 

Together with 1 − gap( K) ≤ e− gap( K), this proves (7). The spectral gap can be captured by the Poincaré inequality. For a kernel K reversible with respect to π, the Dirichlet form of a function f : Ω → R is 

EK (f, f ) = 12

∑

> x,y ∈Ω

π(x)K(x, y ) ( f (x) − f (y)) 2 ,

and the variance of f with respect to π is 

Var π(f ) = ∑

> x∈Ω

π(x) ( f (x) − Eπ[f ]) 2 = 12

∑

> x,y ∈Ω

π(x)π(y) ( f (x) − f (y)) 2 . (8) The spectral gap is exactly the largest constant γ for which the Poincaré inequality γ Var π(f ) ≤EK (f, f ) holds for every f , that is [LP17, Lemma 13.7 and Remark 13.8], 

gap( K) = inf    

> f: Var π(f)̸=0

EK (f, f )Var π(f ) . (9) 14 Lower bounding the spectral gap therefore amounts to establishing a Poincaré inequality: if 

Var π(f ) ≤ C · E K (f, f ) for every f : Ω → R, then gap( K) ≥ 1 

> C

.A general method for establishing such a spectral gap bound is the canonical path technique of Jerrum and Sinclair [JS89] and Diaconis and Stroock [DS91], in the multicommodity flow formula-tion of Sinclair [Sin92]. View the Markov chain as a directed state graph on the vertex set Ω, in which an ordered pair η = ( z, z ′) of distinct states forms a transition whenever K(z, z ′) > 0, and equip this transition with the capacity 

Q(η) = Q(z, z ′) := π(z)K(z, z ′),

which is symmetric in z and z′ by reversibility. A path from x to y is a sequence of transitions 

γ = ( η1, . . . , η ℓ) with ηi = ( zi−1, z i), z0 = x, zℓ = y, and z0, . . . , z ℓ pairwise distinct; its length 

|γ| = ℓ is the number of transitions it uses. Let Px,y be the set of simple paths from x to y, and let 

P = ⋃  

> x̸=y

Px,y .

Definition 13 (flow and congestion) . A flow for K is a function Γ : P → R≥0 that routes π(x)π(y)

units of demand from x to y for every ordered pair of distinct states, namely 

∀x̸ = y ∈ Ω, ∑

> γ∈P x,y

Γ( γ) = π(x)π(y).

The congestion of Γ is 

ρ(Γ) = max 

> η

1

Q(η)  

> ∑
> γ∈P :η∈γ

Γ( γ) |γ|,

where the maximum ranges over all transitions η and η ∈ γ means that γ traverses η.It is well known that a flow of small congestion certifies a Poincaré inequality. 

Lemma 14 ([Sin92]) . For every flow Γ and every f : Ω → R, it holds that gap( K) ≥ 1 

> ρ(Γ)

.

3.2 Basic properties of the Markov chain 

We now verify Proposition 10. Two facts are used throughout. First, π is strictly positive on every state of Ω: indeed π(v, F ) ∝ cvDp(F ), where cv ≥ 14qv > 0 by assumption and Dp(F ) > 0 because Ω

has been restricted to the support of π in Section 2.2. Second, G is simple, so the two endpoints of an arc are distinct and every proposal of K1 that differs from the current state changes the marked vertex. 

Proof of Proposition 10. Aperiodicity is immediate, since K(X, X ) ≥ 12 > 0 for every X ∈ Ω, so that 1 belongs to the set of return times of every state. For irreducibility, it is straightforward to verify that any (u, F ) ∈ Ω can reach the state (s, E ) using transitions K1 and K2 and the state 

(s, E ) can reach any (u, F ) ∈ Ω by reversing the transitions. We turn to reversibility. The identity kernel is reversible with respect to every distribution, so by the decomposition K = 12 I + 14 K1 + 14 K2 it suffices to prove that K1 and K2 are both reversible with respect to π.Let R be the proposal kernel of K1, so that K1(X, Y ) = R(X, Y ) min {1, π(Y ) 

> π(X)

} for every Y̸ = X.We claim that R is symmetric. Fix two distinct states X = ( v, F ) and Y = ( u, F ′). As noted above, 15 a proposal that differs from the current state moves the marked vertex v to the other endpoint of the chosen arc, so R(X, Y ) = 0 unless u̸ = v, which we assume from now on. By the two cases in the definition of K1, each of the relevant proposals is made with probability 1 

> m

· 12 , and therefore 

R(X, Y ) = N (X, Y )2m ,

where N (X, Y ) ∈ { 0, 1, 2} is the number of arcs e ∈ E such that (a) e = ( v, u ) and F ′ = F ∪ { e}; or (b) e = ( u, v ), e ∈ F , and F ′ ∈ { F, F \ { e}} .Here (a) is the first case in the definition of K1 and (b) is the second one. Note that N (X, Y ) can be as large as 2: this happens exactly when F ′ = F and both (v, u ) and (u, v ) are in F . Now an arc e satisfies (a) for the ordered pair (X, Y ) if and only if it satisfies (b) for (Y, X ). Indeed, if 

e = ( v, u ) and F ′ = F ∪ { e}, then e ∈ F ′ and F ∈ { F ′, F ′ \ { e}} , which is (b) for (Y, X ); conversely, if e = ( v, u ), e ∈ F ′ and F ∈ { F ′, F ′ \ { e}} , then F ∪ { e} = F ′, which is (a) for (X, Y ). Applying this equivalence gives N (X, Y ) = N (Y, X ), and the claim follows. Consequently, for all X̸ = Y ,

π(X)K1(X, Y ) = R(X, Y ) min {π(X), π (Y )} = π(Y )K1(Y, X ), (10) which is the detailed balance condition for K1. Note that infeasible proposals cause no harm: they have π(Y ) = 0 and are rejected with probability one. For K2, fix an arc e ∈ E and a state X = ( v, F ) ∈ Ω, and write F1 = F ∪ { e} and F0 = F \ { e}.The update keeps the marked vertex v fixed and changes at most the membership of e, so the only possible transition between two distinct states is between (v, F 0) and (v, F 1). If s cannot reach 

v via F0, the update moves to (v, F 1) with probability one, and since the current state is in Ω it must already be (v, F 1), so no transition between distinct states occurs. Otherwise both (v, F 0) and 

(v, F 1) are valid states of the chain, unless Dp(F0) = 0 , in which case pe = 1 and the transition to 

(v, F 0) has probability 1 − pe = 0 . By the definition of K2,

K2

> (

(v, F 0), (v, F 1)) = pe

m , K2

> (

(v, F 1), (v, F 0)) = 1 − pe

m ,

and detailed balance for K2 holds because π(v,F 1) 

> π(v,F 0)

= Dp(F1) 

> Dp(F0)

= pe 

> 1−pe

.

3.3 Lower bound on the spectral gap 

The Markov chain K = 12 I + 14 K1 + 14 K2 of Section 2.2 is lazy, and by Proposition 10 it is irreducible and reversible with respect to π = πc,p, so Lemma 12 applies to it. The main technical result of this section is the following lower bound on the spectral gap. 

Theorem 15. For the Markov chain K = 12 I + 14 K1 + 14 K2, where the vertex weights satisfy  

> 14qv

≤ cv ≤ 4 

> qv

for all v ∈ V , we have gap( K) = Ω  

> (1
> nm 2
> )

.

Note that Theorem 11 is a direct consequence of Lemma 12 and Theorem 15. 16 3.3.1 Reduction to the ideal chain 

The weights c in Theorem 15 are only assumed to lie within a constant factor of the ideal weights 

c∗ 

> v

= 1 

> qv

. The first step is to reduce the general case to the ideal case. Let c∗ = ( c∗

> v

)v∈V , let 

π∗ = πc∗,p, and let K∗ = 12 I + 14 K∗ 

> 1

+ 14 K∗ 

> 2

be the Markov chain of Section 2.2 with c replaced by c∗,called the ideal chain . Since ∑  

> F∈Ωv

Dp(F ) = qv, the normalising constant of π∗ is ∑ 

> v∈V

c∗

> v

qv = n,so that the ideal stationary distribution is 

∀(v, F ) ∈ Ω, π∗(v, F ) = Dp(F )

n q v

.

In other words, under π∗ the marked vertex is uniform over V , and conditioned on the marked vertex being v, the arc set is distributed as Dp conditioned on Ωv. The next lemma shows that the two chains have comparable spectral gaps, so that only the ideal chain needs to be analysed. 

Lemma 16. Let c be as in Theorem 15. It holds that gap( K) ≥ 14096 gap( K∗).

Proof. The assumption on the weights says exactly that cvqv ∈

[ 14 , 4

]

for every v ∈ V , so the normalising constant Z = ∑ 

> u∈V

cuqu of π satisfies Z ∈ [ n 

> 4

, 4n]. For every (v, F ) ∈ Ω,

π(v, F )

π∗(v, F ) = cvDp(F )/Z 

Dp(F )/(nq v) = n c vqv

Z ∈

[ n/ 44n , 4nn/ 4

]

=

[ 116 , 16 

]

. (11) We next compare the Dirichlet forms of the two chains. The heat-bath update does not involve the vertex weight parameters, so K∗ 

> 2

= K2, and (11) gives 

π(X)K2(X, Y ) = π(X)

π∗(X) π∗(X)K∗ 

> 2

(X, Y ) ≥ 116 π∗(X)K∗ 

> 2

(X, Y ).

The Metropolis update uses the same proposal kernel R for both vertex weight vectors, so (10), which holds for any choice of the weights, applies to K1 and to K∗ 

> 1

alike. Combining it with (11), 

π(X)K1(X, Y ) = R(X, Y ) min {π(X), π (Y )}≥ 116 R(X, Y ) min {π∗(X), π ∗(Y )} = 116 π∗(X)K∗ 

> 1

(X, Y ).

As π(X)K(X, Y ) = 14 π(X)K1(X, Y ) + 14 π(X)K2(X, Y ) for X̸ = Y , and the Dirichlet form involves only such pairs, summing the two bounds yields 

∀f : Ω → R, EK (f, f ) ≥ 116 EK∗ (f, f ).

For the variances, applying (11) to both factors in (8), 

Var π(f ) = 12

∑ 

> X,Y ∈Ω

π(X)π(Y ) ( f (X) − f (Y )) 2

≤ 256 2

∑ 

> X,Y ∈Ω

π∗(X)π∗(Y ) ( f (X) − f (Y )) 2 = 256 Var π∗ (f ).

Both π and π∗ are supported on all of Ω, so Var π(f ) = 0 if and only if f is constant, in which case 

Var π∗ (f ) = 0 . Using the variational characterisation (9) of the spectral gap, we obtain 

gap( K) = inf    

> f: Var π(f)̸=0

EK (f, f )Var π(f ) ≥ 14096 inf     

> f: Var π∗(f)̸=0

EK∗ (f, f )Var π∗ (f ) = 14096 gap( K∗).

17 3.3.2 Multicommodity flow for the ideal chain 

By Lemma 16, it suffices to prove Theorem 15 for the ideal chain. To ease the notation, the weights are fixed to the ideal ones from now on, and the stars are dropped: throughout the rest of this section, we use the weight cv = 1 

> qv

for all vertices v ∈ V , and π = π∗ and K = K∗ denote the corresponding stationary distribution and Markov chain, so that π(v, F ) = Dp(F ) 

> n q v

for all (v, F ) ∈ Ω.The goal is then to show that gap( K) = Ω( 1 

> nm 2

).We now construct a flow of small congestion, which by Lemma 14 lower bounds the spectral gap. This requires a path between every ordered pair of states, and these paths are obtained by routing everything through the states whose marked vertex is the source. Call these states the 

s-layer , and denote by 

Λ := {(s, F ) ∈ Ω} = {(s, F ) | F ∈ Ωs, Dp(F ) > 0} .

As qs = 1 , the ideal distribution satisfies π(s, F ) = Dp(F ) 

> n

on Λ, so π(Λ) = 1 

> n

and the law of π

conditioned on Λ is Dp. It therefore suffices to construct one path from each state in Ω to each state of the s-layer Λ: a path between an arbitrary pair of states is then obtained by concatenating two of them, one taken backwards. Formally, for every x ∈ Ω and every (s, F ′) ∈ Λ, we will construct one path γx,F ′ in the state graph of K. Given all the paths (γx,F ′ )x∈Ω,(s,F ′)∈Λ, we can construct a set of paths Rx,y from x to 

y for every ordered pair of distinct states x, y ∈ Ω in the following way 

Rx,y =

{

γx,y,F ′ = γx,F ′ ◦ γ−1  

> y,F ′

| (s, F ′) ∈ Λ

}

,

where γ−1  

> y,F ′

is the reversal of γy,F ′ , ◦ denotes an operation that first do path concatenation and then erase the cycles of the resulting walk so that γx,y,F ′ is again a path. We remark that reversibility of the Markov chain makes the state graph symmetric, so the reversal of a path is again a path in the state graph. Define the set of all these paths as R = ⋃  

> x̸=y

Rx,y .Consider the paths (γx,F ′ )x∈Ω,(s,F ′)∈Λ. For any transition η, define the load of η as 

L(η) = ∑

> x∈Ω

∑ 

> (s,F ′)∈Λ

π(x) Dp(F ′) · 1 [η ∈ γx,F ′

] . (12) Intuitively, L(η) is the amount of demand crossing η when every state x sends π(x)Dp(F ′) units of demand to (s, F ′) along γx,F ′ , for every (s, F ′) ∈ Λ. The next lemma shows that if we can construct paths (γx,F ′ )x∈Ω,(s,F ′)∈Λ such that the length of every path is small and the load of every transition is small, then we can define a flow in the sense of Definition 13 that is supported on R and has small congestion, and as a consequence we can lower bound the spectral gap of K. Recall that for any transition η = ( z, z ′), the capacity is Q(η) = π(z)K(z, z ′).

Lemma 17. Suppose that ∣∣γx,F ′

∣∣ ≤ ℓ for every x ∈ Ω and every (s, F ′) ∈ Λ, and that L(η) ≤ B·Q(η)

for every transition η. Then gap( K) ≥ 14ℓB .Proof. Let Γ assign the amount π(x)π(y)Dp(F ′) to the path γx,y,F ′ , for every ordered pair x̸ = y

and every (s, F ′) ∈ Λ, the contributions of different intermediate states leading to the same path being added up, and let Γ vanish on every path outside R. Since ∑  

> (s,F ′)∈Λ

Dp(F ′) = 1 , this is indeed a flow: 

∀x̸ = y ∈ Ω, ∑

> γ∈R x,y

Γ( γ) = ∑ 

> (s,F ′)∈Λ

π(x)π(y)Dp(F ′) = π(x)π(y).

18 It remains to bound the congestion of Γ. Fix a transition η = ( z, z ′) and let ¯η = ( z′, z ) be its reversal, which is also a transition and satisfies Q(¯ η) = Q(η) by reversibility. Erasing cycles only shortens a walk and removes transitions from it, so ∣∣γx,y,F ′ 

> ∣∣

≤ 2ℓ, and γx,y,F ′ traverses η only if 

γx,F ′ traverses η or γy,F ′ traverses ¯η. Therefore   

> ∑
> γ∈R :η∈γ

Γ( γ) |γ| ≤ 2ℓ ∑ 

> x̸=y
> ∑
> (s,F ′)∈Λ

π(x)π(y)Dp(F ′) (1 [η ∈ γx,F ′ 

> ]

+ 1 [¯η ∈ γy,F ′

> ])

≤ 2ℓ (L(η) + L(¯ η)) ≤ 4ℓB · Q(η),

where the second inequality follows by summing over y in the first term and over x in the second, both of which contribute a factor ∑ 

> w∈Ω

π(w) = 1 . Hence ρ(Γ) ≤ 4ℓB , and Lemma 14 completes the proof. Next, we construct the paths (γx,F ′ )x∈Ω,(s,F ′)∈Λ. Fix x = ( u, F ) ∈ Ω and (s, F ′) ∈ Λ. Fix a total order of the arcs in E. Run a modified breadth-first search (BFS) from s in G, stopping as soon as 

u is discovered. This process is essentially a BFS on (V, F ) from s, except that we want to record the order of arcs being inspected. To be more precise, we maintain a first-in-first-out (FIFO) queue of discovered but not yet explored vertices. When a vertex is removed from the queue, inspect all 

of its outgoing arcs e ∈ E from smallest to largest according to the fixed order. If e / ∈ F , skip it. If 

e ∈ F and its head w has not yet been discovered, discover w and add it to the queue. The process continues until u is discovered. Record e in the order in which it is inspected, and append all the uninspected arcs from smallest to largest according to the fixed order. This gives a permutation 

a1, a 2, . . . , a m of all arcs in E. As (u, F ) ∈ Ω, the search must discover u. Let 

s = v0 −→ v1 −→ · · · −→ vr = u

be the discovery path from s to u. For every 1 ≤ i ≤ r, let τi be the position of the discovery arc 

(vi−1, v i) in the permutation, so that aτi = ( vi−1, v i). If r ≥ 1, the BFS construction ensures that 

1 ≤ τ1 < · · · < τ r ≤ m. In all cases, set τ0 = 0 and τr+1 = m + 1 . An example is given in Figure 4. 

sabue1 ∈ F e3 ∈ Fe2 /∈ Fe4 /∈ F e5

open-arc set: F = {e1, e 3}

fixed order: e1 < e 2 < e 3 < e 4 < e 5

solid blue arc: traversed discovery arc dashed red arc: inspected and skipped dotted gray arc: not inspected 

Initial state. The FIFO queue is Q = [ s], and the inspection list is I = () .

Step 1: remove s. Its outgoing arcs are e1 < e 4. Inspect e1 ∈ F , discover a, and add a

to the queue. Then inspect e4 /∈ F and skip it. Hence Q = [ a] and I = ( e1, e 4).

Step 2: remove a. Its outgoing arcs are e2 < e 3. Inspect e2 /∈ F and skip it. Then inspect e3 ∈ F , discover u, and stop the search. Hence Q = [ u] and I = ( e1, e 4, e 2, e 3).

Append the uninspected arcs. Appending e5 gives (a1, . . . , a 5) = ( e1, e 4, e 2, e 3, e 5).Thus (v0, v 1, v 2) = ( s, a, u ) and (τ1, τ 2) = (1 , 4) .Figure 4: An example of the modified BFS and the resulting arc permutation. 19 For every 0 ≤ k ≤ m, define an arc set Hk ⊆ E by taking its first k arcs in the permutation 

a1, . . . , a m according to F and its remaining arcs according to F ′, that is, 

1[aj ∈ Hk] = 

> {

1[aj ∈ F ], j ≤ k, 

1[aj ∈ F ′], j > k, 1 ≤ j ≤ m. 

Thus Hm = F and H0 = F ′. It is easy to see that every Hk has positive probability under Dp.We now construct the path γx,F ′ from x = ( u, F ) to (s, F ′) by scanning the arcs in the reverse order am, a m−1, . . . , a 1, starting from (vr, H m) = ( u, F ). Basically, we flip the arc if it is in F ⊕ F ′,and in the meantime go backwards from u to s along the discovery path. Note that we can move the marked vertex only if the current arc is the discovery arc entering it. Consider the iteration that scans ak and let i ∈ { 0, . . . , r } be the unique index satisfying τi ≤ k < τ i+1 . The construction maintains the following invariant: at the beginning of the iteration scanning ak, the current state is (vi, H k). The invariant holds when the scan starts, namely for k = m, because then i = r and the starting state is (vr, H m) = ( u, F ). Now suppose the invariant holds at the beginning of the iteration that scans ak. There are two cases. 

• If k /∈ { τ1, . . . , τ r}, keep the marked vertex vi fixed and replace Hk by Hk−1. If 1[ak ∈

F ] = 1[ak ∈ F ′], then Hk = Hk−1 and no transition is needed. Otherwise, this change is a transition of K2. Indeed, every arc on the discovery path from s to vi belongs to both Hk

and Hk−1, so vi remains reachable after the update. The resulting state is (vi, H k−1).

• If k = τi for some 1 ≤ i ≤ r, then ak = ( vi−1, v i) and ak ∈ Hk ∩ F . Use a K1 transition to move the marked vertex from vi to vi−1 and at the same time replace Hk by Hk−1. If 

ak ∈ F ′, this is the proposal that retains ak; otherwise, it is the proposal that deletes ak. In either case the proposed state is (vi−1, H k−1). It is feasible because every arc on the shorter discovery path from s to vi−1 still belongs to Hk−1, and the Metropolis acceptance probability is positive because both states belong to the support of π.In both cases the invariant is maintained at the start of the next scan: in the first case τi < k , so that τi ≤ k − 1 < τ i+1 and the iteration ends in the state (vi, H k−1), and in the second case k = τi,so that τi−1 ≤ k − 1 < τ i and the iteration ends in the state (vi−1, H k−1). After the whole reverse scan, the arc set has changed from F to F ′, and the marked vertex has moved backwards along the discovery path from vr = u to v0 = s. The path γx,F ′ is obtained by keeping only the non-trivial transitions, and the end state is (v0, H 0) = ( s, F ′). Each scan contributes at most one transition, so 

> ∣∣

γx,F ′ 

> ∣∣

≤ m. (13) Moreover, while the marked vertex is fixed, every nontrivial K2 transition changes a distinct arc. Every nontrivial K1 transition moves the marked vertex one step backwards along the simple discovery path, so once the construction leaves a vertex, it never returns to it. Hence no state is visited twice, and γx,F ′ is a simple path in the transition graph of K.

3.4 Analysis of the load and congestion 

The key property of the flow defined in the last subsection is its low load and congestion. 20 Lemma 18. The family of paths (γx,F ′ )x∈Ω, (s,F ′)∈Λ constructed above satisfies 

L(η) = ∑

> x∈Ω

∑ 

> (s,F ′)∈Λ

π(x) Dp(F ′) · 1 [η ∈ γx,F ′

] ≤ 8nmQ (η)

for every transition η = ( z, z ′) of K.Proof. Fix an oriented transition η = ( z, z ′) and a value u ∈ V for the marked vertex. Consider all pairs (F, F ′) such that (u, F ) ∈ Ω, (s, F ′) ∈ Λ, and γ(u,F ),F ′ traverses η. Formally, define the set 

Aη,u := {(F, F ′) | (u, F ) ∈ Ω, (s, F ′) ∈ Λ, and γ(u,F ),F ′ traverses η}.

If Aη,u = ∅, its contribution is zero. We may therefore assume that Aη,u ̸ = ∅. Write z = ( w, B )

and z′ = ( w′, B ′). The transition η determines a distinguished arc a⋆. For a K2 transition, a⋆ is the unique arc on which B and B′ differ. For a K1 transition, the construction moves the marked vertex backwards from w to w′, and a⋆ = ( w′, w ) is its discovery arc; this arc is unique because G

is simple. Fix a pair (F, F ′) ∈ A η,u . The permutation (a1, . . . , a m) is determined by (u, F ) and may therefore vary with the pair. Let k = k(F, F ′) be the unique position of a⋆ in this permutation, so that ak = a⋆. The process of constructing the path γ(u,F ),F ′ from x = ( u, F ) to (s, F ′) defines a sequence of arc sets F ′ = H0, H 1, . . . , H m = F , and it traverses η from (w, H k) = ( w, B )

to (w′, H k−1) = ( w′, B ′) while scanning a⋆. At this pair-dependent scan position k, define the encoding ˜Hk ⊆ E by 

1[aj ∈ ˜Hk] = 

{1[aj ∈ F ′], j ≤ k, 

1[aj ∈ F ], j > k, 1 ≤ j ≤ m. 

The hybrid Hk and the encoding ˜Hk are determined by (F, F ′) together with the corresponding permutation, and their dependence on the chosen pair is suppressed in the notation. For every arc 

e ∈ E, the two indicators 1[e ∈ Hk] and 1[e ∈ ˜Hk] are 1[e ∈ F ] and 1[e ∈ F ′], possibly in the opposite order. In other words, ˜Hk ⊕ Hk = F ⊕ F ′ and ˜Hk ∩ Hk = F ∩ F ′. Since Dp is a product measure, we have 

Dp(F )Dp(F ′) = Dp(Hk)Dp( ˜Hk). (14) We now consider the two types of transitions separately. First, suppose that η = ( z, z ′) is a nontrivial transition of K2. Then w′ = w, and the arc sets B and B′ differ exactly at a⋆; in particular, B \ { a⋆} = B′ \ { a⋆}. Such a transition can only occur when pa⋆ < 1. Since the heat-bath update chooses a⋆ with probability 1/m and K chooses K2 with probability 1/4, its capacity is 

Q(η) = π(z)K(z, z ′) = Dp(B)

nq w

K(z, z ′) = Dp,−a⋆ (B \ { a⋆})pa⋆ (1 − pa⋆ )4mnq w

, (15) where Dp,−a⋆ denotes the product measure on arc subsets of E \ { a⋆}.Recall that (u, F ) needs to send π(u, F )Dp(F ′) units of demand to (s, F ′) along γ(u,F ),F ′ . For every (F, F ′) ∈ A η,u , exactly one of Hk and ˜Hk contains a⋆, that is, 

{1[a⋆ ∈ Hk], 1[a⋆ ∈ ˜Hk]} = {0, 1}.

21 Moreover, B = Hk and B′ = Hk−1. Therefore (14) and (15) give 

π(u, F )Dp(F ′) = Dp(F )Dp(F ′)

nq u

= Dp(Hk)Dp( ˜Hk)

nq u

= pa⋆ (1 − pa⋆ )

nq u

Dp,−a⋆

(B \ { a⋆})Dp,−a⋆

( ˜Hk \ { a⋆})

= 4 mQ (η) · qw

qu

Dp,−a⋆

( ˜Hk \ { a⋆}).

To upper bound the overall contribution from Aη,u , we use the following claim. The proof is deferred until after both types of transitions have been analysed. 

Claim 19. For the fixed η and u, where k = k(F, F ′) in each summand, 

∑ 

> (F,F ′)∈A η,u

qw

qu

Dp,−a⋆

( ˜Hk \ { a⋆}) ≤ 1. (16) Summing first over all demands with fixed u, applying Claim 19 to the resulting inner sum, and then summing over all choices of u, we obtain 

L(η) = ∑

> u∈V

∑ 

> (F,F ′)∈A η,u

π(u, F )Dp(F ′)= 4 mQ (η) ∑

> u∈V

∑ 

> (F,F ′)∈A η,u

qw

qu

Dp,−a⋆

( ˜Hk \ { a⋆}) ≤ 4nmQ (η).

It remains to consider transitions η of K1. The transition η = ( z, z ′) moves the marked vertex backwards from w to w′ while scanning the discovery arc a⋆ = ( w′, w ). Since a⋆ is a discovery arc, 

a⋆ ∈ B. Moreover, B and B′ differ at most at a⋆. Hence B \ { a⋆} = B′ \ { a⋆}. Define 

θ = θa⋆ (B′) := 

{pa⋆ , a⋆ ∈ B′,

1 − pa⋆ , a⋆ /∈ B′.

Selecting the arc a⋆ and the corresponding one of the two proposals in K1 has probability 1/(2 m).Other proposals may lead to the same proposed state z′, but they can only increase the capacity. 2

Hence the detailed balance condition (10) and the coefficient 1/4 of K1 in K imply 

Q(η) ≥ 18m min {π(z), π (z′)} = 18mn min 

{ Dp(B)

qw

, Dp(B′)

qw′

}

(by B \ { a⋆} = B′ \ { a⋆}) = Dp,−a⋆ (B \ { a⋆})8mn min 

{ pa⋆

qw

, θqw′

}

. (17) We will use the following inequality: 

qw ≥ pa⋆ qw′ . (18) Indeed, the event that s can reach w′ is independent of the outgoing arc a⋆ = ( w′, w ): whenever s

can reach w′, there is a simple path witnessing this event, and such a path does not use a⋆. Thus,        

> 2This can occur when a⋆∈B′and the reverse arc (w, w ′)also belongs to B.

22 with probability pa⋆ qw′ , the vertex s can reach w′ and the arc a⋆ is open. When this happens, s

can reach w. Since θ ≤ 1, and (18) gives θ/q w′ ≥ pa⋆ θ/q w, both terms in the minimum in (17) are at least pa⋆ θ/q w. Consequently, 

Q(η) ≥ Dp,−a⋆ (B \ { a⋆})pa⋆ θ

8mnq w

. (19) For every (F, F ′) ∈ A η,u , we have Hk = B and Hk−1 = B′. Hence a⋆ ∈ Hk and 1[a⋆ ∈ ˜Hk] = 

1[a⋆ ∈ F ′] = 1[a⋆ ∈ B′]. Applying (14) and (19), 

π(u, F )Dp(F ′) = Dp(F )Dp(F ′)

nq u

= Dp(Hk)Dp( ˜Hk)

nq u

= Dp,−a⋆ (B \ { a⋆})pa⋆ θDp,−a⋆

> ( ˜

Hk \ { a⋆})

nq u

≤ 8mQ (η) · qw

qu

Dp,−a⋆

> ( ˜

Hk \ { a⋆}).

As in the K2 case, summing first over all demands with fixed u, applying Claim 19 to the resulting inner sum, and then summing over all choices of u, we obtain 

L(η) = ∑ 

> u∈V
> ∑
> (F,F ′)∈A η,u

π(u, F )Dp(F ′) ≤ 8nmQ (η).

This completes the proof of Lemma 18, subject to Claim 19. We then prove Claim 19. We will use the following classical positive-correlation inequality for product measures. Let I be a finite set and, for each i ∈ I, let μi be a probability measure on 

{0, 1}. Let μ = ⊗ 

> i∈I

μi be the product measure on {0, 1}I . On the space {0, 1}I , define a partial order x ⪯ y if xi ≤ yi for every i ∈ I. We say that an event A ⊆ { 0, 1}I is increasing if x ∈ A and 

x ⪯ y implies y ∈ A.

Lemma 20 (Harris’s inequality [Har60]) . If A and B are increasing events, μ(A ∩ B ) ≥ μ(A)μ(B).Proof of Claim 19. For each pair (F, F ′) ∈ A η,u , let k = k(F, F ′) be as above and call the arc set 

> ˜

Hk \ { a⋆} ⊆ E \ { a⋆} its encoding . We first show that the encoding map is injective even though k

is not fixed, namely that (F, F ′) can be uniquely recovered from the encoding ˜Hk \ { a⋆} even if the value of k is not known. The transition η determines the arc sets Hk = B and Hk−1 = B′ before and after the transition, and hence also determines whether a⋆ belongs to ˜Hk, because 

1[a⋆ ∈ ˜Hk] = 1[a⋆ ∈ F ′] = 1[a⋆ ∈ Hk−1].

Thus the encoding, together with η, determines both Hk and the entire arc set ˜Hk. It remains to recover the permutation and the position k. Recall that constructing the permutation requires running the modified BFS from s until u is discovered. We replay this BFS without knowing k,using Hk to determine membership in F until and including the inspection of a⋆, and using ˜Hk

strictly after a⋆ has been inspected. More precisely, suppose that the first ℓ − 1 inspected arcs have been recovered. These arcs and their recovered inspection outcomes determine the full BFS control state—the queue, the discovered vertices, the vertex currently being explored, and its outgoing-arc cursor—and hence determine the next inspected arc aℓ. If a⋆ has not yet been inspected, use 1[aℓ ∈ Hk] as the 23 indicator of membership in F , and switch to ˜Hk only after processing aℓ when aℓ = a⋆. If a⋆ has already been inspected, use 1[aℓ ∈ ˜Hk]. In either case this indicator determines the outcome of the inspection, and hence the next BFS control state. Inductively, this recovers the inspected arcs in their original order until u is discovered. If a⋆ has been inspected by that time, its position in the recovered sequence is k. Otherwise, the BFS has stopped before inspecting a⋆; append the remaining uninspected arcs in the fixed order, as in the original construction, and locate a⋆ in this appended suffix. This again recovers both the entire permutation a1, . . . , a m and the position k. Finally, for positions at most k, the indicators of F and F ′ are respectively those of Hk and ˜Hk, while after position k their roles are reversed. Hence both F and F ′ are recovered, proving that the encoding map is injective. We have also recovered the discovery path from s to u. Recall that η = ( z, z ′) is fixed and that 

w is the marked vertex of z; in particular, w is determined by η. Write w = vi on the recovered discovery path. The invariant in the path construction gives τi ≤ k < τ i+1 (and k = τi in the K1

case). Therefore, every arc on the suffix 

w = vi −→ vi+1 −→ · · · −→ vr = u

of the discovery path has position greater than k. Hence every such arc belongs to ˜Hk and none of them is the arc a⋆. We obtain the following key observation. 

Observation 21. The vertex w can reach u via arcs in ˜Hk \ { a⋆}.

The injectivity proved above shows that distinct pairs (F, F ′) ∈ A η,u give distinct encodings. Moreover, by Observation 21, each such encoding contains a path from w to u. Since this path does not use a⋆, we obtain  

> ∑
> (F,F ′)∈A η,u

Dp,−a⋆

> ( ˜

Hk \ { a⋆}) ≤ Pr Dp [w can reach u without using a⋆]

≤ Pr Dp [w can reach u] . (20) To apply Lemma 20, identify each arc set S ⊆ E with its indicator vector in {0, 1}E . Under this identification, Dp = ⊗ 

> e∈E

Bernoulli( pe) is a product measure, and the coordinatewise partial order is precisely set inclusion of arc sets. The events that s can reach w and that w can reach u are increasing: if an arc set contains a path witnessing either event, then every larger arc set contains the same path. Applying Lemma 20 to these two events, and observing that their intersection implies that s can reach u, gives 

qu ≥ Pr Dp [s can reach w and w can reach u]

≥ Pr Dp [s can reach w] Pr Dp [w can reach u] = qw Pr Dp [w can reach u] . (21) Combining (20) and (21) proves Claim 19 and completes the proof. 

3.4.1 Proof of the lower bound on the spectral gap 

Finally, we put all the pieces together to prove the lower bound on the spectral gap in Theorem 15. 

Proof of Theorem 15. First consider the ideal weights c∗ 

> v

= 1 /q v; recall that in the preceding con-struction this ideal chain was denoted by K after dropping the stars. By (13), the paths constructed 24 above have length at most ℓ = m, and by Lemma 18 their load satisfies L(η) ≤ 8nmQ (η) for every transition η. Thus we may take B = 8 nm in Lemma 17, which gives 

gap( K∗) ≥ 14ℓB ≥ 14m · 8nm = 132 nm 2 .

For the original weights c, Lemma 16 now yields 

gap( K) ≥ 14096 gap( K∗) = Ω  

> (

1

nm 2

> )

.

3.5 Lower bounds and discussion on the mixing time 

Now we discuss some possible improvement and limitations of the Markov chain. First, to avoid the Ω( n3) lower bound on paths (or multiple paths between s and t), we can change K1 to propose an adjacent arc from the marked vertex instead of a uniformly at random arc, and then use the Metropolis filter to get the correct stationary distribution. This change would improve the mixing time to O(n2) on paths, but it would not improve the overall upper bound in the mixing time analysis above. It is because the contribution in the congestion from K1 and K2 in the current analysis are of the same order, and improving the contribution from K1 alone would not change the final bound. For simplicity of presentation, we choose to use the random arc proposal version of K1.On the other hand, even for the adjacent proposal version of K1, there is an Ω( n3) lower bound on the inverse spectral gap. Here is an example. Fix p to be the open probability for all edges. Let r be an odd number, and s = v0, v 1, . . . , v r be a directed path from s to vr, and introduce new vertices u1, . . . , u r. Add arcs (vi, u j ) for 0 ≤ i ≤ r and 1 ≤ j ≤ r unless i = j̸ = r. Moreover, add a set of arcs from a matching (u1, u 2), (u3, u 4), . . . , (ur−2, u r−1). This way, all vertices have the same number of adjacent arcs, which is not necessary for the lower bound but makes the calculation easier. Moreover, n = 2 r + 1 , and m = Θ( n2). To show the lower bound on the inverse spectral gap, we use (9) and let the testing function f be defined by f (vi, F ) = i for any F and 0 ≤ i ≤ r, and 

f (ui, F ) = 0 for any F and 1 ≤ i ≤ r. Since I and K2 do not change the marked vertex, they do not contribute anything to the Dirichlet form, and only the contribution from K1 matters. We omit the calculation details, and the conclusion here is that Var π(f ) = Θ( n2) and EK (f, f ) = Op(n−1),which implies the Ω( n3) lower bound on the inverse spectral gap. Essentially, this is still the path example, except that a lot of arcs are added to reduce the useful proposal probability of arcs in K1.In any case, our mixing time bound is still a polynomial factor away from the lower bound above, especially in dense graphs. To further improve the mixing time analysis, one may need the local coupling technique from [CFJ +25]. However that appears to be non-trivial and we leave that for future work. 

# 4 Remaining proofs for FPRAS 

In this section, we give the missing proofs for the FPRAS, namely Item (2) of Lemma 5 and the proof of Lemma 8. Both proofs are standard and straightforward and we give them here for completeness. 25 Proof of Item (2) of Lemma 5. Abbreviate qv = qv(p) and π = πc,p, and let 

Z = ∑

> u∈V

cuqu and rv = ∑ 

> F∈Ωv

π(v, F ) = cvqv

Z .

Thus, rv is the probability that the marked vertex of a sample from π equals v. The hypothesis on the weights says that cvqv ∈  

> [14

, 4

> ]

for every v ∈ V , so Z ∈ [ n 

> 4

, 4n] and hence 

rv ≥ 116 n ∀v ∈ V. (22) Define the number of samples as 

N :=  

> ⌈

768 nε2 log  

> (

8nδ

> )⌉

= O 

> (

nε2 log  

> (

nδ

> ))

.

Let X1, . . . , X N be independent ideal samples from π, and let Nv be the number of these samples whose marked vertex equals v. Then E[Nv] = N r v. By the multiplicative Chernoff bound and (22), by setting α = ε 

> 4

, we have 

Pr [ |Nv − N r v| > αN r v] ≤ 2 exp 

> (

− α2N r v

3

> )

≤ 2 exp 

> (

− ε2N

768 n

> )

≤ δ

4n .

Therefore, with probability at least 1 − δ 

> 4

, we have (1 − α)N r v ≤ Nv ≤ (1 + α)N r v for every v ∈ V .Since qs = 1 , the definition of rv gives, for the ideal-sample ratio estimator qv = cs 

> cv
> Nv
> Ns

,

qv

qv

= cs

cvqv

· Nv

Ns

= rs

rv

· Nv

Ns

= Nv/(N r v)

Ns/(N r s) ∈ 

> [

1 − α

1 + α , 1 + α

1 − α

> ]

⊆ [1 − ε, 1 + ε], ∀v ∈ V, 

where the last inclusion follows from α = ε 

> 4

and ε < 1.We now replace the ideal samples by actual ones. Run N independent copies of the Markov chain in Item (1) of Lemma 5. For every copy j, independently draw F0,j ∼ D p, start the chain from (s, F 0,j ), use independent transition randomness, and run it with total variation error bounded by η = δ 

> 4N

. Denote the outputs of these uncapped runs by ˜X1, . . . , ˜XN , and let ˜Nv be the number of these outputs whose marked vertex is v. By the coupling characterisation of the total variation distance and a union bound, the ideal and actual samples can be coupled identically with probability at least 1 − N η = 1 − δ 

> 4

.It remains to impose a worst-case running-time bound. Let B be the bound guaranteed by Item (1) of Lemma 5 with η = δ 

> 4N

and ζ = δ 

> 2N

, so that B = O

> (

n2m2 log  

> (4nN δ
> )

+ m log  

> (2Nδ
> ))

.Each uncapped run takes at most B time except with probability at most δ 

> 2N

. Hence, by a union bound, the total running time of all N uncapped runs is at most N B with probability at least 1 − δ 

> 2

.The algorithm simulates the runs sequentially while maintaining an operation counter, including during every breadth-first search, and stops before executing any operation that would cause the count to exceed N B , in which case it returns the all-one vector. If all runs finish before the cutoff but ˜Nv = 0 for some v ∈ V , it also returns the all-one vector. Otherwise, it returns 

ˆqv = cs

cv

> ˜

Nv

> ˜

Ns

(v ∈ V ).

26 On the intersection of the concentration and coupling events above, ˜Nv = Nv > 0 for every v, and the returned estimates equal the ideal-sample estimates qv. Thus the zero-count fallback introduces no additional failure event. The operation cutoff bounds the running time in the worst case and changes the output only on an event of probability at most δ 

> 2

. A union bound over the concentration, coupling, and cutoff events proves the claimed approximation guarantee with probability at least 

1 − δ 

> 4

− δ 

> 4

− δ 

> 2

= 1 − δ.Substituting the choice of N into N B gives 

O 

> (

n3m2

ε2 log  

> (

nεδ 

> )

log  

> (

nδ

> ))

.

The additional time needed to form the counts and compute the estimates is absorbed in this bound, which completes the proof. 

Proof of Lemma 8. Set the length of the schedule as 

L = min 

> {

j ≥ 1 : βj ≤ pmin 

> }

= O

> (

n log  

> (

2

pmin 

> ))

.

Indeed, since log(1 /β ) = − log(1 − 1/(4 n)) ≥ 1/(4 n),

L ≤ 1 + log(1 /p min )log(1 /β ) ≤ 1 + 4 n log  

> (

1

pmin 

> )

= O

> (

n log  

> (

2

pmin 

> ))

.

For every e ∈ E and 0 ≤ i ≤ L, define p(i) 

> e

= max {pe, β i}. We verify that this construction satisfies Definition 7. By the definition of L,

p(0)  

> e

= max {pe, 1} = 1 , p(L) 

> e

= max 

> {

pe, β L}

= pe.

Thus it satisfies the endpoints p(0) = 1 and p(L) = p.For every e ∈ E and 1 ≤ i ≤ L,

βp (i−1)  

> e

= β max 

> {

pe, β i−1}

= max 

> {

βp e, β i}

≤ max 

> {

pe, β i}

= p(i) 

> e

≤ p(i−1)  

> e

.

Hence the sequence satisfies Definition 7. Finally, the thresholds can be computed recursively, and constructing each probability vector requires one scan over the m arcs. The total running time is O (m(L + 1)) = O(mL ).

# 5 Hardness for unreliability 

At last, we show complementary hardness for the s − t unreliability, Theorem 3. Let us define some notation first. Let G = ( V, E ) be an undirected graph. We consider the case where all edges open with the same probability independently. This only makes our hardness result stronger. Let Gp be the random subgraph where each edge e ∈ E is open with probability p independently. Then the 

s − t unreliability is defined as follows, where s, t ∈ V :27 UnRel G(s, t ; p) := Pr Gp [s̸ → t] = ∑      

> F⊆E:
> scannot reach tin (V,F )

p|F |(1 − p)|E\F |.

Now we are ready to prove Theorem 3. We note that the main construction here is due to Provan and Ball [PB83]. 

Proof of Theorem 3. Given a bipartite graph G = ( L ∪ R, E ), where L and R are the two vertex classes. We may assume that G contains no isolated vertices. We first construct a new graph 

H = ( V, E H ) as follows. The vertex set is V = L ∪ R ∪ { s, t }. For each edge {u, v } ∈ E, where 

u ∈ L and v ∈ R, add one copy of each of the edges {s, u }, {u, v }, and {v, t } to EH . Note that this will introduce parallel edges, and |EH | = 3 |E|. An illustration is given in Figure 5. 

u1

u2

v1

v2

L RGsu1

u2

v1

v2

tL RH

Figure 5: The transformation from G to H. Each edge {u, v } ∈ E, with u ∈ L and v ∈ R,contributes one copy of each of {s, u }, {u, v }, and {v, t }.We call a subset S of vertices an s-set if s ∈ S and t̸ ∈ S. Let δH (S) denote the cut set induced by S in H, and let I(S) = ( L \ S) ∪ (S ∩ R). We claim that 

|δH (S)| = |E| + 2 eH (I(S)) , (23) where eH (I(S)) denotes the number of edges in H between vertices of I(S). Note that by definition, 

I(S) ⊆ L ∪ R and hence eH (I(S)) = eG(I(S)) . To see the claim, consider each edge {u, v } ∈ E. It contributes 3 to the cut if both u and v are in I(S), and contributes 1 otherwise. Thus, by (23), 

S defines a minimum s − t cut in H if and only if I(S) is an independent set in G, and this is a bijection. Now, suppose the number of independent sets in G is Z, and the open probability is q > 0. Let 

n := |L ∪ R| and m := |E|. We have that 

Z(1 − q)mq2m ≤ UnRel H (s, t ; q) ≤ Z(1 − q)m + 2 n(1 − q)m+2 . (24) For each F ⊆ EH such that s cannot reach t in (V, F ), it naturally induces an s-set SF , which is the set of vertices reachable from s. For any s-set S that can be induced this way, the total probability of all F such that SF = S is at least (1 − q)|δH (S)|q3m−| δH (S)| and at most (1 − q)|δH (S)|. Then the lower bound follows from the aforementioned bijection. For the upper bound, we use (23), and the two terms correspond to minimum s − t cuts and other s − t cuts, respectively. Thus, the estimates (24) imply that if 1 − q = exp( −Cn/ε ) for a sufficiently large constant C, UnRel H (s,t ;q)(1 −q)m is a 1 ± ε

approximation of Z.28 The remaining question is how to implement exponentially small close probability. This is easy because we can replace each edge in H by ℓ parallel paths of length 2. If each new edge is open with probability p, then effectively, this implements a close probability of (1 − p2)ℓ, and also removes the use of parallel edges. For our needs, it suffices to set ℓ := O(n/ε ). The resulting graph has size polynomial in n and 1/ε , which finishes the reduction. 

# Acknowledgments and use of AI 

Weiming Feng thanks Tianyi Zhang for extensive and insightful discussions on the two-terminal reliability problem during his time at ETH Zürich in 2024. He also acknowledges support from the Hong Kong Research Grants Council under ECS Grant No. 27202725. The key ideas underlying the algorithm were discovered by GPT-5.6 Sol Ultra. In fact, HG ran the model for >10 hours each to search for an FPRAS for undirected s − t reliability and a hardness proof for it, and failed at both tasks. WF also had a similar experience independently for undirected s − t reliability. The decisive success came from YF. He ran the model on a prompt adapted from the one OpenAI used for the cycle double cover problem, asking it to remove the acyclicity restriction from the result of Feng and Guo [FG24]. It appears that directly asking for an FPRAS for directed s − t reliability is the right choice, even though it is the harder question. The run lasted 20–21 hours and produced an algorithm based on a Markov chain that samples from a distribution more complicated than the one in Definition 4. That construction is not used verbatim here, but two of its central ideas are retained: the annealing scheme and the flow used to analyse the Markov chain. For consistency with standard mathematical exposition, the words “we” and “our” are used throughout the paper, including when presenting ideas that originate in the output of the model. The authors verified and simplified the algorithm and the Markov chain proposed by the model, improved the running time, and wrote all of the proofs. They take full responsibility for the correctness of every statement and argument in the paper. GPT-5.6 Sol and Opus 5 were also used to edit the text. 

# References 

[ACJR21] Marcelo Arenas, Luis Alberto Croquevielle, Rajesh Jayaram, and Cristian Riveros. #NFA admits an FPRAS: Efficient enumeration, counting, and uniform generation for logspace classes. J. ACM , 68(6):48:1–48:40, 2021. [AvBGM25] Antoine Amarilli, Timothy van Bremen, Octave Gaspard, and Kuldeep S. Meel. Ap-proximating queries on probabilistic graphs. Log. Methods Comput. Sci. , 21(4):30:1– 30:31, 2025. [Bal80] Michael O. Ball. Complexity of network reliability computations. Networks , 10(2):153– 165, 1980. [Bal86] Michael O. Ball. Computational complexity of network reliability analysis: An overview. IEEE Trans. Reliab. , 35(3):230–239, 1986. [BP83] Michael O. Ball and J. Scott Provan. Calculating bounds on reachability and connect-edness in stochastic networks. Networks , 13(2):253–278, 1983. 29 [CFJ +25] Xiaoyu Chen, Weiming Feng, Zhe Ju, Tianshun Miao, Yitong Yin, and Xinyuan Zhang. Faster mixing of the Jerrum-Sinclair chain. In FOCS , pages 1007–1028. IEEE, 2025. [CGZZ24] Xiaoyu Chen, Heng Guo, Xinyuan Zhang, and Zongrui Zou. Near-linear time samplers for matroid independent sets with applications. In RANDOM , pages 32:1–32:12, 2024. [Col87] Charles J. Colbourn. The Combinatorics of Network Reliability , volume 4 of The International Series of Monographs on Computer Science . Oxford University Press, New York, 1987. [DGGJ04] Martin E. Dyer, Leslie Ann Goldberg, Catherine S. Greenhill, and Mark Jerrum. The relative complexity of approximate counting problems. Algorithmica , 38(3):471–500, 2004. [DS91] Persi Diaconis and Daniel Stroock. Geometric bounds for eigenvalues of Markov chains. 

Ann. Appl. Probab. , 1(1):36–61, 1991. [FG24] Weiming Feng and Heng Guo. An FPRAS for two terminal reliability in directed acyclic graphs. In ICALP , pages 62:1–62:19, 2024. [GGL16] Leslie Ann Goldberg, Rob Gysel, and John Lapinskas. Approximately counting locally-optimal structures. J. Comput. System Sci. , 82(6):1144–1160, 2016. [GJ19] Heng Guo and Mark Jerrum. A polynomial-time approximation algorithm for all-terminal network reliability. SIAM J. Comput. , 48(3):964–978, 2019. [GM20] Amir Kafshdar Goharshady and Fatemeh Mohammadi. An efficient algorithm for computing network reliability in small treewidth. Reliab. Eng. Syst. Saf. , 193:106665, 2020. [Har60] Theodore E. Harris. A lower bound for the critical probability in a certain percolation process. Proc. Cambridge Philos. Soc. , 56(1):13–20, 1960. [Jer81] Mark Jerrum. On the Complexity of Evaluating Multivariate Polynomials . Ph.D. dis-sertation, Department of Computer Science, University of Edinburgh, 1981. Technical Report CST-11-81. [JS89] Mark Jerrum and Alistair Sinclair. Approximating the permanent. SIAM J. Comput. ,18(6):1149–1178, 1989. [JSV04] Mark Jerrum, Alistair Sinclair, and Eric Vigoda. A polynomial-time approximation algorithm for the permanent of a matrix with nonnegative entries. J. ACM , 51(4):671– 697, 2004. [Kan94] Ravi Kannan. Markov chains and polynomial time algorithms. In FOCS , pages 656– 671, 1994. [Kar01] David R. Karger. A randomized fully polynomial time approximation scheme for the all-terminal network reliability problem. SIAM Rev. , 43(3):499–522, 2001. 30 [KL85] Richard M. Karp and Michael Luby. Monte-Carlo algorithms for the planar multiter-minal network reliability problem. J. Complexity , 1(1):45–64, 1985. [LP17] David A. Levin and Yuval Peres. Markov Chains and Mixing Times . American Math-ematical Society, Providence, Rhode Island, second edition, 2017. With contributions by Elizabeth L. Wilmer. [PB83] J. Scott Provan and Michael O. Ball. The complexity of counting cuts and of computing the probability that a graph is connected. SIAM J. Comput. , 12(4):777–788, 1983. [Pro86] J. Scott Provan. The complexity of reliability computations in planar and acyclic graphs. SIAM J. Comput. , 15(3):694–702, 1986. [Sin92] Alistair Sinclair. Improved bounds for mixing rates of Markov chains and multicom-modity flow. Combin. Probab. Comput. , 1(4):351–370, 1992. [SW85] A. Satyanarayana and R. Kevin Wood. A linear-time algorithm for computing K-terminal reliability in series-parallel networks. SIAM J. Comput. , 14(4):818–832, 1985. [Val79] Leslie G. Valiant. The complexity of enumeration and reliability problems. SIAM J. Comput. , 8(3):410–421, 1979. [ZL11] Rico Zenklusen and Marco Laumanns. High-confidence estimation of small s-t relia-bilities in directed acyclic networks. Networks , 57(4):376–388, 2011. 31