---
title: "Latent Heuristic Search: Continuous Optimization for Automated Algorithm Design"
title_zh: 潜在启发式搜索：自动化算法设计的连续优化
authors: "Cheikh Ahmed, Mahdi Mostajabdaveh, Zirui Zhou"
date: 2026-05-16
pdf: "https://arxiv.org/pdf/2605.17137v1"
tags: ["query:ad"]
score: 9.0
evidence: 提出连续启发式发现框架，结合LLM和梯度搜索实现自动化算法设计
tldr: 现有算法设计方法将大语言模型集成到进化框架中，但在离散程序语法空间中搜索效率低。本文提出连续启发式发现框架，通过编码器将程序映射到连续嵌入，训练可微代理模型进行梯度搜索，并用归一化流约束优化轨迹。在TSP、CVRP等组合优化问题上的实验表明，该方法性能与最优离散进化基线相当，为自动化算法设计提供了新途径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.17137v1/fig-001.webp\", \"caption\": \"Table 4. Knapsack Performance. Comparison by correlation type. Objective values are mean± standard deviation over five seeds; time values are seed averages. (↑) indicates higher is better.\", \"page\": 10, \"index\": 1, \"width\": 783, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2605.17137v1/fig-002.webp\", \"caption\": \"Table 6. Distribution of Heuristics in the Augmented Dataset\", \"page\": 19, \"index\": 2, \"width\": 397, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2605.17137v1/fig-003.webp\", \"caption\": \"Fig. 1. Latent Space Visualization. A 2D t-SNE projection of program embeddings for the TSP. The color gradient represents the heuristic score, showing that high-performing programs (lighter colors) cluster together rather than being randomly dispersed.\", \"page\": 12, \"index\": 3, \"width\": 717, \"height\": 617}, {\"url\": \"assets/figures/arxiv/2605.17137v1/fig-004.webp\", \"caption\": \"Table 1. TSP Performance. Comparison on TSP-50, TSP-100, and TSP-200. Objective values are mean ± standard deviation over seeds; time values are seed averages. (↓) indicates lower is better.\", \"page\": 9, \"index\": 4, \"width\": 727, \"height\": 196}]"
motivation: 解决现有基于LLM的启发式发现方法在离散程序空间中搜索效率低、优化困难的问题。
method: 构建编码器将离散程序映射为连续嵌入，训练可微代理模型预测性能，用归一化流引导梯度上升，优化后映射为软提示条件化冻结LLM生成新程序。
result: 在TSP、CVRP、KSP和在线装箱问题上，性能与现有离散进化基线方法竞争。
conclusion: 连续潜在空间优化是自动化算法设计的有效补充，提供了替代离散搜索的新方法论。
---

## 摘要
将大语言模型（LLMs）整合到进化框架中，为自动化启发式发现建立了一种新范式。尽管这些方法前景广阔，但它们通常在程序语法的离散空间中进行搜索，依赖随机采样来导航高度非凸的优化景观。本文提出了一种连续启发式发现框架，将优化转移到学习到的潜在流形上。我们采用编码器将离散程序映射为连续嵌入，并训练一个可微分的替代模型来预测性能，从而实现基于梯度的搜索。为了正则化优化轨迹，一个可逆的归一化流将这些嵌入映射到结构化的高斯先验上，在此我们执行梯度上升。得到的优化潜在向量通过一个学习到的映射器投影为软提示，这些提示条件化一个冻结的大语言模型以合成新的可执行启发式方法。我们在旅行商问题（TSP）、带容量约束的车辆路径问题（CVRP）、背包问题（KSP）和在线装箱问题（OBP）上评估了所提出的方法。实验结果表明，连续潜在空间优化在性能上达到了与最先进的离散进化基线相当的水平，同时为自动化算法设计提供了一种互补的方法论选择。实现代码可在\url{https://github.com/cheikh025/LHS}获取。

## Abstract
The integration of Large Language Models (LLMs) into evolutionary frameworks has established a new paradigm for automated heuristic discovery. Despite their promise, these methods typically search in the discrete space of program syntax, relying on stochastic sampling to navigate a highly non-convex optimization landscape. This work proposes a continuous heuristic discovery framework that shifts optimization to a learned latent manifold. We employ an encoder to map discrete programs into continuous embeddings and train a differentiable surrogate model to predict performance, enabling gradient-based search. To regularize the optimization trajectory, an invertible normalizing flow maps these embeddings to a structured Gaussian prior, where we perform gradient ascent. The resulting optimized latent vectors are projected through a learned mapper into soft prompts, which condition a frozen LLM to synthesize novel executable heuristics. We evaluate the proposed method on the Traveling Salesman Problem (TSP), the Capacitated Vehicle Routing Problem (CVRP), the Knapsack Problem (KSP), and Online Bin Packing (OBP). Empirical results demonstrate that continuous latent-space optimization achieves performance competitive with state-of-the-art discrete evolutionary baselines while offering a complementary methodological alternative for automated algorithm design. The implementation code is available at \url{https://github.com/cheikh025/LHS}.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：将大语言模型（LLMs）集成到进化框架中已成为自动化启发式发现的新范式。现有方法（如 FunSearch、EoH、ReEvo）在离散的程序语法空间中搜索，依赖随机采样（变异、重组）来导航高度非凸的优化景观。这种离散空间搜索效率低，需要大量昂贵评估才能发现一致改进。
- **核心问题**：如何将启发式搜索从离散程序语法空间转移到连续的潜在空间，以利用梯度信息进行更系统、更高效的优化，同时保持生成程序的可执行性？
- **整体含义**：本文提出连续启发式发现框架（Latent Heuristic Search, LHS），通过学习程序嵌入、可微代理模型和归一化流，在结构化潜在空间中进行梯度上升优化，为自动化算法设计提供了一种全新的方法论补充。

### 2. 论文提出的方法论

- **核心思想**：将离散程序编码为连续嵌入向量，训练一个可微分的性能预测代理，在潜在空间中使用梯度上升搜索更优嵌入；通过归一化流将嵌入映射到平滑的高斯先验空间以稳定搜索，最终将优化后的潜在向量映射为软提示，条件化冻结的 LLM 生成新程序代码，并执行基准评估。
- **关键技术细节**：
  - **编码器**：采用预训练嵌入模型（Qwen3-Embedding-0.6B），输出 128 维向量 \( z \)。
  - **可替代表率 \( f_\theta \)**：在归一化流后的先验空间 \( U \) 上训练，使用 RankNet 成对排序损失，预测程序性能。
  - **归一化流 \( F_\phi \)**：使用 RealNVP（4个耦合层），将编码器嵌入 \( z \) 映射到标准高斯先验 \( u \)，确保优化轨迹保持在程序-like 流形上。训练使用最大似然估计。
  - **潜在到提示映射器 \( \phi_\omega \)**：将优化后的 \( z \) 投影为 \( K=16 \) 个软提示向量，用于条件化冻结的 LLM（Qwen3-4B-Instruct-2507），训练时使用重建损失（teacher forcing）。
  - **搜索过程**：在 \( U \) 空间中执行梯度上升（\( T=5 \) 步，学习率 \( \eta=0.001 \)），然后逆映射回 \( z \)，通过映射器获得软提示，LLM 生成候选代码，执行得到分数。算法1 总结了完整流程。

### 3. 实验设计

- **数据集/场景**：四种组合优化问题
  - 旅行商问题（TSP）：节点数 50/100/200
  - 带容量约束的车辆路径问题（CVRP）：客户数 50/100
  - 在线装箱问题（OBP）：物品序列长度 1k / 5k，容量 100
  - 背包问题（KSP）：三种相关性类型（不相关、弱相关、强相关），50 物品，容量 100
- **基准方法**：FunSearch、Evolution of Heuristics (EoH)、ReEvo、MCTS-AHD
- **公平性控制**：所有方法使用相同的 LLM 生成器（Qwen3-4B-Instruct-2507）、相同的 LLM 预算（100 次推理调用）、相同的参考程序初始化、相同的种群大小（10）、相同的评估沙箱。每个方法在每种设置下运行 5 个随机种子。
- **消融实验**：在 TSP 上比较 LHS 与两个变体：
  - No-Flow：直接在原始嵌入空间 \( Z \) 中优化（无归一化流）
  - No-Grad：不使用梯度，改用高性能程序对的中点插值
- **可视化**：t-SNE 投影 TSP 程序嵌入，观察性能聚类。

### 4. 资源与算力

- **论文未明确说明**使用的 GPU 型号、数量或训练时长。仅在附录中提到使用 Flash Attention 2 和梯度检查点来优化内存。因此无法给出具体的算力开销。这一缺失可能影响结果的可复现性评估。

### 5. 实验数量与充分性

- **主要实验**：在 4 个任务共 7 个场景（TSP-50/100/200，CVRP-50/100，OBP-1k/5k，KSP 三种相关性）上进行对比，每个场景 5 个随机种子，结果全面。
- **消融实验**：仅在 TSP 一个任务上进行了消融（3 种方法），且只报告了成功率和目标值，缺乏在其他任务上的验证。
- **总体评价**：实验覆盖了组合优化中几个经典的、不同特性的问题，对比基线具有代表性，随机种子和均值标准差保证了统计可靠性。但消融覆盖面较窄，缺乏对超参数（如优化步数、学习率）的敏感性分析；未与其他连续优化方法（如 CMA-ES）直接对比。实验基本客观公平，但可以更深入。

### 6. 论文的主要结论与发现

- 连续潜在空间优化（LHS）在 TSP 和 CVRP 上取得了最佳平均目标值，性能与最优离散进化基线相当甚至更好。
- 归一化流是必要的：搜索在原始嵌入空间 \( Z \) 会导致成功率大幅下降（61% vs 74%），而使用流可以保持解码稳定性。
- 梯度搜索优于简单插值：插值成功率高但目标值差；梯度搜索能更有效地向高质量区域移动，但存在离流形风险。
- 潜在空间可视化验证了高性能程序在嵌入空间中聚集，支持了连续搜索的合理性。
- LHS 在不同任务上表现出竞争力，尤其在路由领域优势明显。

### 7. 优点

- **创新方法论**：将启发式搜索从离散程序空间转移到连续潜在空间，引入了梯度信息，与传统的离散进化思维形成互补。
- **系统设计**：归一化流正则化搜索空间、成对排序损失训练代理、低秩映射器参数高效，整体设计环环相扣。
- **公平比较**：严格控制 LLM 模型、推理预算、初始化等变量，隔离了搜索策略本身的差异。
- **跨任务复用**：训练好的归一化流和映射器可以跨不同优化问题复用（只需重新训练轻量代理），提高了实际应用效率。

### 8. 不足与局限

- **初始语料依赖**：训练流和映射器需要较大规模初始程序语料（约 4500 个），生成和评估成本较高。
- **代理漂移风险**：随着搜索偏离初始分布，代理可能给出“假上升”信号，文中提到但未提供稳健性分析或主动学习对策。
- **消融实验不足**：仅在一个任务上测试了变体，缺乏对优化步骤数、学习率、流层数等关键超参数的敏感性分析。
- **任务覆盖有限**：仅测试了四个组合优化问题，对更复杂或不同性质的问题（如图着色、调度）的泛化性未知。
- **缺乏与其他连续优化方法的对比**：例如与 CMA-ES 在潜在空间中的搜索（如 NPO）的直接比较未被提及。
- **可复现细节缺失**：未报告 GPU 型号、训练时间等计算资源信息，可能影响复现和资源预估。

（完）
