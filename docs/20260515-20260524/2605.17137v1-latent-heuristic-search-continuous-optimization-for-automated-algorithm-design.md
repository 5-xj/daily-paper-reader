---
title: "Latent Heuristic Search: Continuous Optimization for Automated Algorithm Design"
title_zh: 潜在启发式搜索：面向自动化算法设计的连续优化
authors: "Cheikh Ahmed, Mahdi Mostajabdaveh, Zirui Zhou"
date: 2026-05-16
pdf: "https://arxiv.org/pdf/2605.17137v1"
tags: ["query:ad"]
score: 9.0
evidence: 使用大语言模型进行连续优化的自动启发式发现
tldr: 现有自动算法设计方法多在离散程序语法空间搜索，优化景观高度非凸。本文提出连续启发式发现框架，将离散程序编码为潜在嵌入，通过可微代理模型进行梯度搜索，并用归一化流约束优化轨迹。在TSP、CVRP、KSP和OBP问题上，该方法性能与最先进的离散进化基线相当，为自动化算法设计提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.17137v1/fig-001.webp\", \"caption\": \"Table 4. Knapsack Performance. Comparison by correlation type. Objective values are mean± standard deviation over five seeds; time values are seed averages. (↑) indicates higher is better.\", \"page\": 10, \"index\": 1, \"width\": 783, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2605.17137v1/fig-002.webp\", \"caption\": \"Table 6. Distribution of Heuristics in the Augmented Dataset\", \"page\": 19, \"index\": 2, \"width\": 397, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2605.17137v1/fig-003.webp\", \"caption\": \"Fig. 1. Latent Space Visualization. A 2D t-SNE projection of program embeddings for the TSP. The color gradient represents the heuristic score, showing that high-performing programs (lighter colors) cluster together rather than being randomly dispersed.\", \"page\": 12, \"index\": 3, \"width\": 717, \"height\": 617}, {\"url\": \"assets/figures/arxiv/2605.17137v1/fig-004.webp\", \"caption\": \"Table 1. TSP Performance. Comparison on TSP-50, TSP-100, and TSP-200. Objective values are mean ± standard deviation over seeds; time values are seed averages. (↓) indicates lower is better.\", \"page\": 9, \"index\": 4, \"width\": 727, \"height\": 196}]"
motivation: 离散程序空间中的随机采样难以高效导航非凸优化景观，需要更有效的连续优化方法。
method: 使用编码器将程序映射为连续嵌入，训练可微代理模型预测性能，通过归一化流引导梯度上升优化潜在向量，再经映射器生成软提示以合成新启发式。
result: 在旅行商、车辆路径、背包和在线装箱问题上，连续潜在空间搜索达到与离散进化基线竞争的性能。
conclusion: 潜在连续优化为自动化算法设计提供了一种有效的替代方案，弥补了离散搜索的局限性。
---

## 摘要
将大型语言模型（LLM）集成到进化框架中，为自动化启发式发现建立了新范式。尽管这些方法前景广阔，但它们通常是在程序语法的离散空间中进行搜索，依赖随机采样来探索高度非凸的优化景观。本文提出了一种连续启发式发现框架，将优化转移到学习的潜在流形上。我们采用编码器将离散程序映射为连续嵌入，并训练可微替代模型来预测性能，从而实现基于梯度的搜索。为了正则化优化轨迹，可逆归一化流将这些嵌入映射到结构化的高斯先验分布，在此进行梯度上升。优化后的潜在向量通过学习映射器投影到软提示中，这些软提示条件化冻结的LLM，以合成新的可执行启发式算法。我们在旅行商问题（TSP）、容量车辆路径问题（CVRP）、背包问题（KSP）和在线装箱问题（OBP）上评估了所提出的方法。实验结果表明，连续潜在空间优化达到了与最先进的离散进化基线相竞争的性能，同时为自动化算法设计提供了一种互补的方法论选择。实现代码可在\url{https://github.com/cheikh025/LHS}获取。

## Abstract
The integration of Large Language Models (LLMs) into evolutionary frameworks has established a new paradigm for automated heuristic discovery. Despite their promise, these methods typically search in the discrete space of program syntax, relying on stochastic sampling to navigate a highly non-convex optimization landscape. This work proposes a continuous heuristic discovery framework that shifts optimization to a learned latent manifold. We employ an encoder to map discrete programs into continuous embeddings and train a differentiable surrogate model to predict performance, enabling gradient-based search. To regularize the optimization trajectory, an invertible normalizing flow maps these embeddings to a structured Gaussian prior, where we perform gradient ascent. The resulting optimized latent vectors are projected through a learned mapper into soft prompts, which condition a frozen LLM to synthesize novel executable heuristics. We evaluate the proposed method on the Traveling Salesman Problem (TSP), the Capacitated Vehicle Routing Problem (CVRP), the Knapsack Problem (KSP), and Online Bin Packing (OBP). Empirical results demonstrate that continuous latent-space optimization achieves performance competitive with state-of-the-art discrete evolutionary baselines while offering a complementary methodological alternative for automated algorithm design. The implementation code is available at \url{https://github.com/cheikh025/LHS}.

---

## 论文详细总结（自动生成）

# 论文总结：Latent Heuristic Search: Continuous Optimization for Automated Algorithm Design

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：现有基于大型语言模型（LLM）的自动启发式算法设计方法（如 FunSearch、EoH、ReEvo）通常在**离散的程序语法空间**中进行搜索，依赖随机采样（变异、重组等）来探索高度非凸的优化景观，导致优化效率低、评估成本高。
- **核心问题**：能否将程序搜索从离散空间转移到**连续潜在空间**，利用梯度信息进行更高效的优化，同时保持执行评估的严格性？
- **整体含义**：本文提出一种**连续启发式发现框架（LHS）**，通过编码器将离散程序映射为连续嵌入，训练可微代理模型预测性能，并利用归一化流引导梯度上升搜索，最终通过软提示解码生成新启发式。在四个组合优化问题上验证了其与最先进离散进化方法竞争的性能，为自动化算法设计提供了新的方法论路径。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：将启发式程序的搜索从离散的代码 token 空间转移到**学习的连续潜在流形**，利用梯度上升优化，并通过归一化流（Normalizing Flow）约束搜索轨迹，使之保持在可解码的程序流形上。
- **关键技术细节**：
  1. **编码器**：使用 Qwen3-Embedding-0.6B 模型，结合 Matryoshka 表示学习，将每个程序 p 嵌入为 128 维向量 z = E(p)。
  2. **可微代理模型**：训练一个 MLP 代理 fθ，在潜在空间预测程序性能。为避免直接回归绝对分数的脆弱性，采用 **RankNet 排序损失**，从 N 个程序构造 O(N²) 对偏好对训练，提高鲁棒性。
  3. **归一化流**：使用 RealNVP 流 Fφ，将原始潜在空间 Z 映射到结构化高斯先验空间 U（标准正态分布）。训练时最大化对数似然（MLE）。搜索时在 U 空间进行梯度上升，然后通过逆映射 Fφ⁻¹ 得到潜在代码 z*。
  4. **解码路径**：映射器 ϕω 将潜在向量 z* 投影为 K=16 个连续软提示嵌入 h，作为前缀条件化冻结的 LLM（Qwen3-4B-Instruct），生成可执行的启发式程序 ˆp。
  5. **搜索算法**（Algorithm 1）：从种子程序开始，编码→映射到 U→梯度上升 T 步→逆映射→解码→执行评估→更新数据库。迭代 R 轮，每轮采样 B 个种子。
- **关键公式**：
  - 目标：最大化程序性能 s(p)，定义为实例级目标成本的聚合（统一为最大化形式）。
  - 梯度上升：u(t+1) = u(t) + η ∇u fθ(u(t))
  - 映射器训练：最小化程序 token 的负对数似然（教师强制）。
  - 排序损失：Lrank = -E_{(ui,uj)~Dr}[log σ( fθ(ui) - fθ(uj) )]

## 3. 实验设计：数据集/场景、基准、对比方法
- **任务场景**：
  - Traveling Salesman Problem (TSP) — 规模 50/100/200
  - Capacitated Vehicle Routing Problem (CVRP) — 规模 50/100
  - Online Bin Packing (OBP) — 项目序列长度 1k/5k
  - Knapsack Problem (KSP) — 三种相关系数类型（Uncorrelated, Weakly Correlated, Strongly Correlated）
- **基准（Benchmark）**：使用 LLM4AD 平台，所有方法使用相同的代码生成骨干模型（Qwen3-4B-Instruct-2507），相同的 LLM 调用预算（100 次推理），相同的初始参考程序，种群大小 10。最终评估在固定验证集上报告均值和标准差（5 个随机种子）。
- **对比方法**：
  - FunSearch（进化采样+执行评估）
  - Evolution of Heuristics (EoH)（共同进化思想+代码）
  - ReEvo（反思反馈引导进化搜索）
  - MCTS-AHD（蒙特卡洛树搜索探索）
  - 消融变体：No-Flow（直接 Z 空间优化）、No-Grad（线性插值）

## 4. 资源与算力
- **文中明确提及的内容**：
  - 使用 Qwen3-4B-Instruct 作为解码器，Qwen3-Embedding-0.6B 作为编码器。
  - 使用 Flash Attention 2 和梯度检查点优化内存。
  - 数据生成使用了 GPT-5.2 和 GPT-OSS-20B。
- **未明确说明的方面**：
  - GPU 型号、数量、训练时长、总计算量等均未提供。
  - 未报告代理模型、归一化流、映射器的训练时间或硬件配置。
- **结论**：论文在算力资源上描述不足，无法量化训练成本。

## 5. 实验数量与充分性
- **实验数量**：
  - 主实验覆盖 4 个组合优化问题，每个问题包含多个规模或类型（TSP:3个规模, CVRP:2个规模, OBP:2个序列长度, KSP:3种相关性），共约 10 个实验设置。
  - 每个实验使用 5 个随机种子，结果报告均值±标准差，统计可靠。
  - 消融实验：在 TSP 上进行了 Ablation Study（LHS vs No-Flow vs No-Grad），报告成功率、目标值和运行时间。
  - 可视化：t-SNE 投影潜在空间。
- **充分性与公正性**：
  - 对比方法实现源于 LLM4AD 平台，共享同一骨干模型、预算、初始化，控制了变量。
  - 所有方法使用相同的执行评估沙箱，确保了评估标准的一致性。
  - 不足：消融仅针对 TSP，未在其他任务上验证；未与更基础的连续优化基线（如 CMA-ES 在潜在空间）对比；未进行超参数敏感性分析。
  - 总体来说，实验设计较严谨，覆盖多任务，消融验证了关键组件必要性，但扩展性有限。

## 6. 论文的主要结论与发现
- **主要结论**：连续潜在空间优化（LHS）在 TSP、CVRP 上取得了最佳平均目标值，在 KSP 和 OBP 上性能与离散进化方法持平或略有优势，证明了连续优化作为离散搜索的可行替代。
- **关键发现**：
  - 归一化流搜索（U 空间）显著优于直接 Z 空间搜索，成功率从 61% 提升至 74%，目标值更优。
  - 梯度搜索（LHS）优于线性插值（No-Grad），后者虽然解码成功率高（86%），但优化效果差。
  - 潜在空间的高性能区域形成聚类，验证了连续表示捕捉语义相似性的假设。

## 7. 优点：方法或实验设计上的亮点
- **方法创新**：首次将归一化流与可微代理模型结合，用于 LLM 驱动的启发式程序搜索，实现了真正的梯度引导优化，而非离散随机变异。
- **解码策略**：使用软提示（prefix-tuning）条件化冻结 LLM，参数高效（仅 1.8M 参数），避免了全模型微调，保证了生成质量。
- **训练目标**：采用排序损失（RankNet）而非回归，对噪声更鲁棒，且从有限评估中构造大量训练信号（O(N²) 对）。
- **实验设计公正**：统一骨干模型、评估沙箱、预算和初始化，公平比较。
- **可视化分析**：t-SNE 投影直观展示了性能聚类，验证了优化空间的可利用性。

## 8. 不足与局限
- **实验覆盖有限**：
  - 仅测试了 4 个组合优化问题，未涉及更广泛的算法设计任务（如调度、图问题）。
  - 消融仅针对 TSP，缺少在其他任务上验证各组件贡献。
- **可扩展性与鲁棒性**：
  - 依赖初始程序数据集（~4500 个启发式），生成和评估成本较高；当搜索远离训练分布时，代理模型可能存在分布偏移，导致“假上升”。
  - 解码成功率仅为 74%（LHS），低于插值的 86%，说明优化可能将潜在向量推离有效流形。
  - 超参数（学习率 η、步数 T）敏感，文中未进行系统调优分析。
- **资源与可复现性**：
  - 未提供 GPU 算力细节，难以评估部署成本；代码虽开源，但训练流程可能依赖特定平台（如 GPT-5.2）。
- **对比基线缺失**：未与同样使用连续优化的方法（如 NPO、GENESYS）对比，也未与直接 CMA-ES 在潜在空间优化的方案比较。
- **理论分析不足**：缺乏对流形保持、收敛性等理论保证。

（完）
