---
title: "Agentic Algorithm Engineering: Improving Shared-Memory Exact Minimum Cuts"
title_zh: 智能体算法工程：改进共享内存精确最小割
authors: "David A. Bader, Adil Chhabra, Ernestine Großmann, Monika Henzinger, Alexander Noe, Christian Schulz"
date: 2026-09-07
pdf: "https://arxiv.org/pdf/2609.07204v1"
tags: ["query:ad"]
score: 6.0
evidence: 智能体算法工程，用自主智能体发现并改进算法
tldr: 最小割问题要求将无向边权图节点分成两块并最小化割边权值和。现有 VieCut 精确算法已结合非精确界、依赖界的归约、改进数据结构与并行收缩，但仍需大量手工调优。本文提出 Agentic Algorithm Engineering（AAE），让自主大语言模型代理在既有代码库上运行算法工程循环：形成运行时损失假设、实现改动、在固定实例集上基准测试，并保留或丢弃改动。即便算法已高度手工调优，代理仍在真实世界 k-core 和 DIMACS core 实例上取得显著优化：顺序与 32 线程分别加速 1.28/1.63 倍和 6.26/127 倍。这表明 AAE 能自动化挖掘共享内存精确最小割的高性能优化空间。
source: arxiv
selection_source: fresh_fetch
motivation: 现有精确最小割算法虽经大量手工调优，性能工程仍依赖专家反复假设、实现与基准测试，成本高且难以持续挖掘优化空间。
method: 提出 Agentic Algorithm Engineering：让自主大语言模型代理在既有代码库上自动执行形成假设、实现改动、固定实例基准测试、保留或丢弃的算法工程循环。
result: 在真实世界 k-core 上顺序与 32 线程分别加速 1.28 和 1.63 倍；在 DIMACS core 实例上分别加速 6.26 和 127 倍。
conclusion: 表明 AAE 可在高度手工调优后继续发现显著优化，为共享内存精确最小割及更广算法工程提供自动化新范式。
---

## 摘要
无向边加权图的最小割问题要求将其节点集划分为两个块，同时最小化割边的加权和。过去几年中，我们为该问题设计了一系列快速算法。我们最快的精确算法使用一个非精确算法来获得该问题的更好的界，依赖该界的归约、改进的数据结构和并行收缩例程。它已在开源软件包 VieCut 中提供，并且在真实世界实例上，其性能超过此前最快的求解器：顺序运行至多快 2.5 倍，并行运行至多快 12.9 倍。我们使用智能体算法工程（AAE）改进该算法，这是我们在此引入的一种方法。在该方法中，自主大语言模型智能体在现有代码库上运行算法工程循环：它们对运行时间损失在何处形成假设，实现这些假设，在固定实例集上对结果进行基准测试，并保留或丢弃该改动。尽管我们已经对该算法进行了大量手动调优，该智能体仍找到了显著的优化，特别是在 DIMACS core 实例上：在真实世界 k-core 上，加速因子分别为 1.28（顺序）和 1.63（32 线程）；在 DIMACS core 实例上，分别为 6.26 和 127。

## Abstract
The minimum cut problem for an undirected edge-weighted graph asks us to divide its set of nodes into two blocks while minimizing the weighted sum of the cut edges. Over the last years, we engineered a range of fast algorithms for this problem. Our fastest exact algorithm uses an inexact algorithm to obtain a better bound for the problem, reductions that depend on this bound, improved data structures and parallel contraction routines. It is available in the open-source package VieCut and, on real-world instances, outperformed the previously fastest solvers by a factor of up to 2.5 sequentially and up to 12.9 when run in parallel. We improve this algorithm using agentic algorithm engineering (AAE), a methodology that we introduce here, in which autonomous large language model agents run the algorithm engineering cycle on an existing code base: they form hypotheses about where running time is lost, implement them, benchmark the result on a fixed instance set and keep or discard the change. Even though we had already tuned our algorithm by hand extensively, the agent finds significant optimizations, in particular on the DIMACS core instances: factors of 1.28 (sequential) and 1.63 (32 threads) on real-world k-cores, and 6.26 and 127 on the DIMACS core instances.