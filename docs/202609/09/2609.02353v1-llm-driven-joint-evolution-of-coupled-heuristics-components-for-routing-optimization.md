---
title: LLM-Driven Joint Evolution of Coupled Heuristics Components for Routing Optimization
title_zh: LLM驱动的路由优化耦合启发式组件联合进化
authors: "Juntao Wei, Yangming Zhou, Zhibin Jiang, Shan Jiang"
date: 2026-09-02
pdf: "https://arxiv.org/pdf/2609.02353v1"
tags: ["query:ad"]
score: 9.0
evidence: 大模型联合进化路由优化的耦合启发式组件，即大模型驱动的自动算法发现
tldr: 组合优化启发式设计依赖专家，现有LLM进化方法多孤立演化组件，忽视彼此依赖。本文提出LLM-HCJG，在共享蓝图下联合生成并协同进化相互耦合的启发式组件，应用于引导局部搜索，将解初始化与惩罚构造联合设计并嵌入在线搜索，且从TSP迁移至CVRP。在合成与41个公开基准中，该方法稳定取得低最优性差距，在多数TSPLIB和全部CVRPLIB实例上达到最优或并列最优。消融表明增益源自跨组件兼容与对齐，展现有限样本下高效跨实例迁移能力。
source: arxiv
selection_source: fresh_fetch
motivation: 现有LLM进化方法孤立演化组件，忽视组件间耦合对搜索状态的影响，导致启发式性能受限。
method: 提出LLM-HCJG框架，基于共享蓝图联合生成并协同进化GLS的初始化与惩罚构造，嵌入在线搜索并迁移至CVRP。
result: 在41个公开基准上实现低最优性差距，TSPLIB中28/29和CVRPLIB中12/12达到最优或并列最优。
conclusion: 联合进化提升组件兼容性，支持有限样本下跨实例迁移，为耦合启发式自动设计提供有效范式。
---

## 摘要
组合优化的启发式设计仍然高度依赖专家知识，而现有的大语言模型（LLM）增强进化方法通常进化孤立的算法组件，即使一个组件决定了另一个组件操作的搜索状态。本文提出LLM驱动的启发式组件联合生成（LLM-HCJG），一种基于种群的框架，在共享设计蓝图下联合生成并共同进化相互依赖的启发式组件。应用于引导局部搜索（GLS）时，LLM-HCJG将解决方案初始化与惩罚构建耦合，并将生成的配对嵌入增强的在线搜索机制。所得形式进一步从旅行商问题（TSP）迁移到带容量约束的车辆路径问题（CVRP）。理论分析确立了两个组件之间不可分离的状态转换效应以及生成一致性方面的优势。在合成实例和41个公共TSPLIB/CVRPLIB基准上，LLM-HCJG持续获得低最优性差距，包括在29个TSPLIB实例中的28个和全部12个CVRPLIB实例上取得最佳或并列最佳结果。消融和结构分析进一步表明，这些收益与跨组件兼容性和对齐相关，而非孤立组件的重组。这些结果支持在有限样本、适度成本训练下，所评估路由设置中的有效跨实例迁移。

## Abstract
Heuristic design for combinatorial optimization remains heavily reliant on expert knowledge, while existing large language model (LLM)-enhanced evolutionary methods typically evolve isolated algorithmic components, even when one determines the search state on which another operates. This paper proposes LLM-driven Heuristic Components Joint Generation (LLM-HCJG), a population-based framework that jointly generates and co-evolves interdependent heuristic components under a shared design blueprint. Applied to guided local search (GLS), LLM-HCJG couples solution initialization with penalty construction and embeds the generated pair into an enhanced online search mechanism. The resulting form is further transferred from the traveling salesman problem (TSP) to the capacitated vehicle routing problem (CVRP). Theoretical analysis establishes the non-separable state-transition effects between the two components and the advantage in generation consistency. Across synthetic instances and 41 public TSPLIB/CVRPLIB benchmarks, LLM-HCJG attains consistently low optimality gaps, including best or tied-best results on 28 of 29 TSPLIB instances and all 12 CVRPLIB instances. Ablation and structural analyses further indicate that these gains are associated with cross-component compatibility and alignment rather than isolated-component recombination. These results support effective cross-instance transfer within the evaluated routing settings under limited-sample, modest-cost training.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究背景**：组合优化（如物流与运输中的路由问题）的启发式设计高度依赖专家经验和手工规则，难以适应动态、多样化的实际场景。精确算法在大规模问题上不可行，而现有学习方法往往任务特化、迁移性不足。
- **核心问题**：大语言模型（LLM）增强的进化计算方法通常只对**孤立的算法组件**进行演化，即使这些组件在搜索过程中存在强耦合关系——例如，一个组件决定了另一个组件所作用的搜索状态。这种孤立演化忽略了组件间的结构依赖，限制了生成启发式的整体效能。
- **整体含义**：论文主张将“LLM 作为算法设计者”的范式从单一组件生成推进到**耦合组件的联合生成与共同进化**，使自动设计出的启发式能够在具体的元启发式算法（如引导局部搜索，GLS）中以一致的设计原则协同工作。

## 2. 论文提出的方法论

- **核心思想**：提出 **LLM 驱动的启发式组件联合生成（LLM-HCJG）** 框架。它在共享“设计蓝图”的约束下，联合生成并共同进化两个相互依赖的 GLS 组件：
  - **解决方案初始化函数（solution initialization）**：决定搜索的起始状态；
  - **惩罚构造函数（penalty construction）**：决定距离矩阵更新方式，从而引导后续搜索逃离局部最优。
- **个体表示**：每个个体表示为一个完整算法，采用三层结构：
  1. 组件函数层：每个组件用标准模板描述任务、输入输出约束等；
  2. 个体组合层：个体形式化为 `a_j = (Z_j, F_j^s, F_j^u)`，其中 `Z_j` 是**共享设计蓝图**（一句话描述两个组件的互补角色和共同机制），`F_j^s` 和 `F_j^u` 分别由 Thought 和 Code 实现；
  3. 进化提示层：定义初始化、交叉、变异等提示，并包含蓝图引导的协调信息。
- **共享蓝图机制**：LLM 先生成蓝图 `Z_j`，再条件化生成两个组件。通过结构化验证保证蓝图一致性，降低组件间设计原则错配的概率。论文从理论上证明了：联合生成可以消除蓝图错配概率，而独立生成时错配概率为正。
- **联合进化流程**：使用种群大小为 `N`，每一代通过赌轮盘/排序选择父代，依概率进行交叉（`σ1=1.0`）和变异（`σ2=0.5`），采用精英保留式种群管理。
- **GLS 泛化到 CVRP**：将 TSP 的单环 GLS 机制扩展到容量约束多路线 CVRP，在局部搜索中使用 intra-route 2-opt、inter-route relocate、inter-route swap 等可行性保持邻域操作。
- **增强在线搜索机制**（框架级固定机制，不由 LLM 生成）：
  - 影响边选择：取 `max(D′−D, 0)` 中 top-5 边作扰动目标；
  - 端点定向扰动：围绕被选边端点执行 2-opt、relocate 或 swap；
  - 扰动后局部改善；
  - 周期性回到当前最优解：每 50 次外循环重置一次。

## 3. 实验设计

- **合成场景**：
  - TSP：每个规模 {20, 50, 100} 各 1000 个随机欧氏实例；
  - CVRP：每个规模 {20, 50, 100} 各 64 个实例，depot 在 (0.5, 0.5)，客户需求为 [1,9] 整数，容量=50，约 10–11 辆车。
- **公共基准**：
  - TSPLIB：29 个实例（规模 51–200），沿用 EoH 研究的实例选择；
  - CVRPLIB：12 个实例，来自 A/B/E/P/M/X 系列（规模 32–190）。
- **对比方法**：
  - 手工启发式：LS、GLS、知识引导 GLS（KGLS-r/c）、精英偏置 GLS（EBGLS）；
  - 神经网络方法：AM、POMO、LEHD、GNNGLS、NeuralGLS；
  - 先前 LLM-EC 方法：EoH、ReEvo。
- **评测细节**：每组测试最多 1000 次外循环或给定时间预算（TSP 100 秒、CVRP 20 秒）；重复 10 次取均值；对比指标以 LKH3 或已知最优解为参考的最优性差距。
- **消融设计**：
  - 将初始化与惩罚组件交叉组合：`EoH–EoH`、`EoH–HCJG`、`HCJG–EoH`、`HCJG–HCJG`；
  - 另做带 95% bootstrap 置信区间和 Wilcoxon 符号秩检验的配对差异分析。

## 4. 资源与算力

- 论文运行在 **Intel Core i9-14900HX (2.20 GHz)** 的 CPU 环境下，未明确报告 GPU 型号、数量或大规模并行算力。
- 使用 LLM 为 **GPT-4o-mini**。
- 训练成本：
  - TSP 训练平均约 **34 分钟**；
  - CVRP 训练平均约 **183 分钟**；
  - 每个任务包含约 410 次候选生成尝试（TSP 得到 361 个可用个体，CVRP 得到 390 个，有效率分别为 88.0% 和 95.12%）。
- 论文对比指出，其训练预算远低于 EoH 在 TSP-GLS 上约两天的搜索成本；但没有报告 API 调用费用或其他计算资源（如显存）的具体数值。

## 5. 实验数量与充分性

- **实验量级较充分**：
  - 合成 TSP 每个规模 1000 个实例，合成 CVRP 每个规模 64 个实例；
  - TSPLIB 29 个实例，CVRPLIB 12 个实例；
  - 包含 TSP/CVRP 的训练收敛过程、最终种群轨迹和最优启发式代码分析。
- **消融与统计检验**：
  - 四组跨组件配对消融在合成 TSP 与 TSPLIB 上均有结果；
  - 对 TSPLIB/CVRPLIB 和消融结果做了配对 bootstrap 置信区间与 Wilcoxon 检验，p 值均小于 0.05，说明优势并非偶然。
- **公平性讨论**：
  - 与 EoH 和 ReEvo 比较时，二者只演化惩罚更新函数而固定初始化函数，LLM-HCJG 则联合演化两个组件——这种设置有利于体现联合进化的价值，但也意味着并非完全等价的 LLM 搜索空间比较；
  - 神经网络基线结果取自原论文，未在同一条件下复现；
  - CVRP 对比仅纳入 GLS、EoH、ReEvo 等少数方法，缺少 KGLS/EBGLS 和更广泛的神经方法；
  - 总体而言，实验数量与统计检验策略足以支撑其主要结论，但在方法间搜索预算、模型版本和计算一致性上仍有一定局限。

## 6. 论文的主要结论与发现

- LLM-HCJG 在合成实例和公开基准上均取得稳定领先：
  - TSPLIB：平均 gap 为 **0.017%**，29 个实例中 28 个取得最好/并列最好结果；
  - CVRPLIB：平均 gap 降至 **1.686%**，12 个实例全部最好/并列最好；
  - TSP100 合成实例 gap 为 0.070%，CVRP100 为 3.526%，优于 EoH（0.147% / 10.852%）和 ReEvo（0.293% / 11.276%）。
- FA-GLS 对照表明，框架级增强本身有贡献，但 LLM-HCJG 的进一步优势来自**联合共演化的组件兼容性**，而非单一组件替换。
- 消融中 `HCJG–HCJG` 显著优于两种“混合”配对，并且 `HCJG–HCJG` 优于 `EoH–HCJG` 和 `HCJG–EoH`，支持两个组件在搜索状态转移上不可分离的理论结论。
- 结论：LLM 不仅能生成孤立启发式规则，还能通过共享蓝图塑造具体算法框架的搜索方向；且有限训练样本下可跨实例、跨问题泛化。

## 7. 优点

- **方法层面**：
  - 提出“组件—个体—进化提示”的三层设计，能够显式建模组件间结构依赖；
  - 引入共享设计蓝图，使两个组件的生成条件一致，避免独立生成下的设计原则冲突；
  - 同时提供理论证明（非可分离性、蓝图一致性的错配概率）和经验验证，较强地支撑“联合进化”的必要性；
  - 将 TSP 启发式迁移到 CVRP 时保留了 GLS 的可解释性，并加入明确的可行性保持邻域操作。
- **实验层面**：
  - 覆盖合成数据、公共 TSPLIB/CVRPLIB、跨规模与跨问题迁移；
  - 包含 FA-GLS 对照组和四种组件交叉消融，清晰区分框架机制和 LLM 组件收益；
  - 使用 bootstrap 置信区间和配对检验，结论更具统计依据；
  - 训练成本低，实用性强。

## 8. 不足与局限

- **规模与泛化边界**：公开基准主要限于 51–200 节点规模，缺少更大规模（如 500/1000+）的路由实例，对实际大规模物流场景的推广性仍待验证。
- **问题覆盖有限**：框架只验证了 TSP 和 CVRP 两类路由问题，尚不清楚能否直接扩展到带时间窗、多车场、动态需求等更复杂的问题。
- **对比公平性**：
  - EoH/ReEvo 在比较中只演化惩罚组件，LLM-HCJG 演化两个组件，两者的搜索空间与查询预算不同；
  - 神经网络方法结果取自原论文，没有统一硬件和训练预算复现；
  - CVRP 实验缺少更多元的方法类别作对比。
- **框架机制的固定性**：四个框架级搜索增强机制是手工设定的，未自动演化或调参；其适用范围本身取决于这些预设机制是否合理。
- **对 LLM 生成质量的依赖**：结构校验约束了函数格式和数值稳定性，但语义层面的正确性仍依赖 LLM；若换用性能更弱的 LLM，联合生成优势是否能保持并未讨论。
- **算力信息披露不完整**：未报告 GPU 型号、实际 API 调用量、Token 成本或每轮进化查询数，难以完整复算“训练成本”。

（完）
