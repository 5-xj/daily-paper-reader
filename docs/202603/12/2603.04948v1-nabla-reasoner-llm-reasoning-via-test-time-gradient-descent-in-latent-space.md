---
title: "$\\nabla$-Reasoner: LLM Reasoning via Test-Time Gradient Descent in Latent Space"
title_zh: "$\\nabla$-Reasoner: 通过潜在空间中的测试时梯度下降实现大语言模型推理"
authors: "Peihao Wang, Ruisi Cai, Zhen Wang, Hongyuan Mei, Qiang Liu, Pan Li, Zhangyang Wang"
date: 2026-03-05
pdf: "https://arxiv.org/pdf/2603.04948v1"
tags: ["query:ahd"]
score: 6.0
evidence: 通过潜空间微分优化迭代改进策略
tldr: 针对大语言模型推理时计算扩展中离散搜索效率低下的问题，本文提出 $\nabla$-Reasoner 框架。该方法引入可微文本优化（DTO），在解码循环中利用模型似然和奖励模型的梯度信号，在潜在空间内通过梯度下降实时优化 token 概率。实验表明，该方法在数学推理任务上显著提升了准确率并降低了计算开销，实现了从零阶搜索到一阶优化的范式转变。
selection_source: fresh_fetch
motivation: 现有的推理时扩展方法主要依赖低效的离散搜索或试错，缺乏对生成策略的直接优化手段。
method: 提出一种迭代生成框架，通过在解码过程中对 token Logits 进行可微优化，结合梯度下降和拒绝采样来精炼文本表示。
result: "在挑战性数学推理基准上实现超 20% 的准确率提升，同时减少了 10-40% 的模型调用次数。"
conclusion: 该工作证明了在推理阶段采用一阶梯度优化比传统搜索更高效，为提升 LLM 推理能力开辟了新方向。
---

## 摘要
扩展大语言模型（LLMs）的推理时计算已解锁了前所未有的推理能力。然而，现有的推理时扩展方法通常依赖于低效且次优的离散搜索算法或试错提示来改进在线策略。在本文中，我们提出了 $\nabla$-Reasoner，这是一个迭代生成框架，它将针对 token logit 的可微优化集成到解码循环中，以实时优化策略。我们的核心组件——可微文本优化（DTO），利用来自 LLM 似然度和奖励模型的梯度信号来精炼文本表示。$\nabla$-Reasoner 进一步结合了拒绝采样和加速设计，以增强解码的鲁棒性并提高速度。在理论上，我们证明了在样本空间中执行推理时梯度下降以最大化奖励，与通过 KL 正则化强化学习对齐 LLM 策略具有对偶性。在实证方面，$\nabla$-Reasoner 在具有挑战性的数学推理基准测试中实现了超过 20% 的准确率提升，同时与强基准相比，模型调用次数减少了约 10-40%。总之，我们的工作引入了从测试时零阶搜索到一阶优化的范式转变，为增强 LLM 推理能力提供了一条具有成本效益的路径。

## 速览
**TLDR**：针对大语言模型推理时计算扩展中离散搜索效率低下的问题，本文提出 $\nabla$-Reasoner 框架。该方法引入可微文本优化（DTO），在解码循环中利用模型似然和奖励模型的梯度信号，在潜在空间内通过梯度下降实时优化 token 概率。实验表明，该方法在数学推理任务上显著提升了准确率并降低了计算开销，实现了从零阶搜索到一阶优化的范式转变。 \
**Motivation**：现有的推理时扩展方法主要依赖低效的离散搜索或试错，缺乏对生成策略的直接优化手段。 \
**Method**：提出一种迭代生成框架，通过在解码过程中对 token Logits 进行可微优化，结合梯度下降和拒绝采样来精炼文本表示。 \
**Result**：在挑战性数学推理基准上实现超 20% 的准确率提升，同时减少了 10-40% 的模型调用次数。 \
**Conclusion**：该工作证明了在推理阶段采用一阶梯度优化比传统搜索更高效，为提升 LLM 推理能力开辟了新方向。

---

## Abstract
Scaling inference-time compute for Large Language Models (LLMs) has unlocked unprecedented reasoning capabilities. However, existing inference-time scaling methods typically rely on inefficient and suboptimal discrete search algorithms or trial-and-error prompting to improve the online policy. In this paper, we propose $\nabla$-Reasoner, an iterative generation framework that integrates differentiable optimization over token logits into the decoding loop to refine the policy on the fly. Our core component, Differentiable Textual Optimization (DTO), leverages gradient signals from both the LLM's likelihood and a reward model to refine textual representations. $\nabla$-Reasoner further incorporates rejection sampling and acceleration design to robustify and speed up decoding. Theoretically, we show that performing inference-time gradient descent in the sample space to maximize reward is dual to aligning an LLM policy via KL-regularized reinforcement learning. Empirically, $\nabla$-Reasoner achieves over 20% accuracy improvement on a challenging mathematical reasoning benchmark, while reducing number of model calls by approximately 10-40% compared to strong baselines. Overall, our work introduces a paradigm shift from zeroth-order search to first-order optimization at test time, offering a cost-effective path to amplify LLM reasoning.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **$\nabla$-Reasoner** 的新型大语言模型（LLM）推理框架，旨在通过在推理阶段引入梯度优化来提升模型的逻辑推理能力。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的推理时计算扩展（Inference-time Scaling）方法（如 Best-of-N 采样、树搜索 ToT、插件式规划 RAP 等）大多属于**零阶搜索（Zeroth-order Search）**。这些方法依赖于大量的随机采样和试错，在面对长链推理和稀疏奖励信号时，搜索效率低下且容易陷入局部最优。
*   **研究动机**：作者认为推理过程可以建模为在奖励景观（Reward Landscape）上的优化问题。既然 LLM 和奖励模型（RM）通常是可微的，那么利用**一阶梯度信息（First-order Gradients）**来引导搜索方向，理论上会比盲目采样更高效。

### 2. 方法论：$\nabla$-Reasoner
核心思想是将离散的 Token 生成过程转化为在连续 Logit 空间中的可微优化问题。

*   **可微文本优化（DTO）**：
    *   **目标函数**：设计了一个结合奖励最大化和语言模型似然度的损失函数：$L(y) = -\lambda r(y|x) - \log \pi_{LLM}(y|x)$。其中奖励项引导模型向高分答案靠拢，似然项（正则项）确保生成的文本保持流畅且符合原始分布。
    *   **参数化与优化**：使用直通估计器（Straight-Through Estimator, STE）处理离散 Token，在 Token 的 Logit 空间执行梯度下降。
*   **迭代解码流程**：
    1.  **初始化**：基于当前前缀生成一个完整的推理链（Rollout）。
    2.  **策略改进**：利用 DTO 对该推理链的 Logits 进行 $T$ 步梯度更新。
    3.  **重采样与拒绝采样**：从优化后的第一个 Token 分布中采样。如果新 Token 与原 Token 不同，则进行新的 Rollout，并根据奖励模型决定是否接受该修改。
    4.  **循环**：将确定的 Token 加入前缀，重复上述过程。
*   **加速技术**：
    *   **梯度缓存**：仅在 Token 类别发生变化时重新计算梯度。
    *   **轨迹复用**：复用之前的推理链和 KV 缓存。
    *   **启发式 Token 选择**：仅对低置信度或高梯度的关键 Token 执行优化。
*   **理论支撑**：论文证明了在样本空间进行推理时梯度下降，在数学上等价于执行 KL 正则化的强化学习（如 PPO）的非参数化版本。

### 3. 实验设计
*   **数据集**：专注于数学推理任务，包括 **MATH-500**、**AMC**、**AIME24** 和 **AIME25**。
*   **模型基座**：Qwen-2.5-Math-7B（Base/Instruct）和 Llama-3.1-8B-Instruct。
*   **奖励模型**：Skywork-V2 系列（4B 和 8B 版本）。
*   **对比方法（Baselines）**：
    *   **推理时方法**：贪婪解码、Self-Consistency (SC)、Best-of-N (BoN)、ToT、RAP、TPO。
    *   **训练时方法**：SFT（监督微调）、GRPO（强化学习）。

### 4. 资源与算力
*   **硬件环境**：实验在 **8 张 NVIDIA H200 (140GB) GPU** 集群上运行。
*   **效率度量**：论文引入了“模型调用次数（Model Calls）”作为理论效率指标。在理想的并行注意力机制下，一次梯度反传与一次前向推理的耗时相当。
*   **具体耗时**：在附录中提到，处理 AMC 数据集的 83 个问题，$\nabla$-Reasoner 总耗时约 152.1 秒（含优化开销），而 BoN (N=8) 耗时 136.1 秒。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流模型基座和 4 个难度递增的数学数据集，实验设置较为全面。
*   **消融与分析**：
    *   **测试时缩放定律**：分析了随计算量增加性能的变化曲线。
    *   **奖励模型敏感度**：测试了不同规模奖励模型对结果的影响。
    *   **拒绝率分析**：验证了 DTO 确实提高了采样 Token 的质量（拒绝率显著低于随机采样）。
*   **客观性**：通过多次独立运行取平均值，并与经过大规模强化学习训练的方法（如 GRPO）进行直面对比，体现了实验的严谨性。

### 6. 主要结论与发现
*   **性能飞跃**：在 Qwen-2.5-7B-Instruct 上，$\nabla$-Reasoner 在 MATH-500 达到 80.4% 的准确率，显著超过所有推理时基准。
*   **成本效益**：相比强基准 BoN，该方法在达到相同或更高准确率的同时，减少了 **10-40%** 的模型调用次数。
*   **超越训练**：仅通过推理时优化，该方法能达到甚至超过某些经过全量强化学习训练（如 GRPO）的模型性能。

### 7. 优点
*   **范式创新**：成功将“一阶优化”引入 LLM 推理解码循环，打破了传统采样搜索的局限。
*   **理论完备**：建立了推理时梯度下降与参数化强化学习（PPO）之间的理论联系。
*   **系统优化**：通过梯度缓存和 Token 选择等技术，解决了梯度优化在自回归解码中计算开销过大的痛点。

### 8. 不足与局限
*   **模型依赖**：性能高度依赖于基座模型和奖励模型的质量。如果奖励模型存在“奖励作弊（Reward Hacking）”风险，优化过程可能会失效。
*   **词表限制**：要求基座模型和奖励模型必须共享相同的词表（Vocabulary），以便进行端到端的 Logit 优化。
*   **工程复杂度**：相比简单的采样方法，该框架在生产环境中的集成（如与 vLLM 等推理引擎结合）需要更复杂的系统级协同设计。
*   **任务局限**：目前主要在数学推理任务上验证，在创意写作或开放式对话等缺乏明确可微奖励的任务上效果尚待验证。

（完）
