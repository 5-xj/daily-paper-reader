---
title: "stratum: A System Infrastructure for Massive Agent-Centric ML Workloads"
title_zh: stratum：面向大规模以智能体为中心的机器学习工作负载的系统基础设施
authors: "Arnab Phani, Elias Strauss, Sebastian Schelter"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03589v1"
tags: ["query:ahd"]
score: 7.0
evidence: 智能体流水线搜索与自动机器学习流水线优化的基础设施
tldr: 随着大语言模型的发展，智能体驱动的机器学习流水线搜索成为新趋势，但现有的 Python 生态系统因其顺序执行和解释性限制，难以应对此类大规模探索性任务。本文提出 Stratum 系统基础设施，通过解耦执行与规划、批量编译优化执行图以及引入 Rust 高性能运行时，无缝集成现有 Python 库并显著提升执行效率。Stratum 解决了智能体工作负载与传统 ML 系统之间的不匹配，为大规模自动化机器学习提供了高效支撑。
selection_source: fresh_fetch
motivation: 现有的 Python ML 生态系统主要面向人类交互式工作流，无法高效处理 LLM 智能体在自动化流水线搜索中产生的大规模、高并发且迭代频繁的任务。
method: Stratum 采用解耦执行与规划的设计，将批量流水线编译为优化的执行图，并利用 Rust 编写的高性能运行时在异构后端上高效执行。
result: 初步实验结果显示，Stratum 在处理大规模智能体流水线搜索任务时，相比现有方案可实现高达 16.6 倍的性能提升。
conclusion: Stratum 成功填补了高性能 ML 系统与 Python 生态系统之间的鸿沟，为未来大规模智能体驱动的机器学习工作负载奠定了坚实的系统基础。
---

## 摘要
大语言模型（LLM）的最新进展改变了机器学习（ML）流水线的开发和评估方式。LLM 催生了一种新型工作负载——智能体流水线搜索（agentic pipeline search），其中自主或半自主智能体负责生成、验证并优化完整的 ML 流水线。这些智能体主要在流行的 Python ML 库上运行，并表现出高度的探索性行为。这导致了成千上万次用于数据分析、流水线生成以及流水线阶段迭代优化的执行任务。然而，现有的基于 Python 的 ML 生态系统是围绕 Pandas 和 scikit-learn 等库构建的，这些库是为以人为中心、交互式且顺序的工作流设计的，并受到 Python 解释执行模型、库级隔离以及对执行大量流水线的运行时支持有限等因素的制约。与此同时，系统社区提出的许多高性能 ML 系统要么针对狭窄的工作负载类别，要么需要专门的编程模型，这限制了它们与 Python ML 生态系统的集成，使其在很大程度上不适用于基于 LLM 的智能体。这种日益严重的不匹配暴露了在支持大规模智能体流水线搜索方面面临的根本性系统挑战。因此，我们提出了 stratum，这是一个统一的系统基础设施，它在智能体流水线搜索过程中将流水线执行与规划和推理相解耦。Stratum 与现有的 Python 库无缝集成，将批量流水线编译为优化的执行图，并在包括新型基于 Rust 的运行时在内的异构后端上高效执行。我们展示了 stratum 的架构愿景及早期原型，讨论了关键的设计决策，并概述了开放性挑战和研究方向。最后，初步实验表明，stratum 可以显著加速大规模智能体流水线搜索，提升幅度高达 16.6 倍。

## 速览
**TLDR**：随着大语言模型的发展，智能体驱动的机器学习流水线搜索成为新趋势，但现有的 Python 生态系统因其顺序执行和解释性限制，难以应对此类大规模、高探索性的工作负载。本文提出 stratum 系统基础设施，通过解耦执行与规划、批量编译优化执行图以及引入 Rust 编写的高性能运行时，无缝集成现有 Python 库并提升执行效率。stratum 解决了智能体与传统 ML 系统间的失配问题，为大规模自动化机器学习提供了高效支撑。 \
**Motivation**：现有的 Python ML 生态系统是为人类交互式工作流设计的，无法高效处理 LLM 智能体在自动化流水线搜索中产生的大规模、迭代式和高并发的执行需求。 \
**Method**：提出 stratum 架构，通过将流水线执行与智能体规划解耦，将批量流水线编译为优化的执行图，并利用 Rust 编写的新型运行时在异构后端高效执行。 \
**Result**：初步实验结果显示，stratum 在处理大规模智能体驱动的机器学习流水线搜索任务时，能够实现高达 16.6 倍的性能提升。 \
**Conclusion**：stratum 成功填补了高性能 ML 系统与 Python 生态系统之间的空白，为支持未来大规模智能体中心化的机器学习工作负载奠定了基础。

---

## Abstract
Recent advances in large language models (LLMs) transform how machine learning (ML) pipelines are developed and evaluated. LLMs enable a new type of workload, agentic pipeline search, in which autonomous or semi-autonomous agents generate, validate, and optimize complete ML pipelines. These agents predominantly operate over popular Python ML libraries and exhibit highly exploratory behavior. This results in thousands of executions for data profiling, pipeline generation, and iterative refinement of pipeline stages. However, the existing Python-based ML ecosystem is built around libraries such as Pandas and scikit-learn, which are designed for human-centric, interactive, sequential workflows and remain constrained by Python's interpretive execution model, library-level isolation, and limited runtime support for executing large numbers of pipelines. Meanwhile, many high-performance ML systems proposed by the systems community either target narrow workload classes or require specialized programming models, which limits their integration with the Python ML ecosystem and makes them largely ill-suited for LLM-based agents. This growing mismatch exposes a fundamental systems challenge in supporting agentic pipeline search at scale. We therefore propose stratum, a unified system infrastructure that decouples pipeline execution from planning and reasoning during agentic pipeline search. Stratum integrates seamlessly with existing Python libraries, compiles batches of pipelines into optimized execution graphs, and efficiently executes them across heterogeneous backends, including a novel Rust-based runtime. We present stratum's architectural vision along with an early prototype, discuss key design decisions, and outline open challenges and research directions. Finally, preliminary experiments show that stratum can significantly speed up large-scale agentic pipeline search up to 16.6x.

---

## 论文详细总结（自动生成）

这是一份关于论文《stratum: A System Infrastructure for Massive Agent-Centric ML Workloads》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着大语言模型（LLM）驱动的机器学习工程（MLE）智能体兴起，产生了一种全新的工作负载——**智能体流水线搜索（Agentic Pipeline Search）**。这种负载具有极高的探索性，智能体会生成成千上万个类似的 Python 流水线进行迭代优化。
*   **研究背景**：现有的 Python ML 生态（如 Pandas, scikit-learn）是为人类交互式、顺序执行设计的，存在 Python 解释器性能瓶颈、库级隔离、缺乏高并发支持等问题。而现有的高性能 ML 系统往往需要专门的编程语言（DSL），难以被基于 Python 语料训练的 LLM 智能体直接利用。
*   **整体含义**：论文提出了 **stratum**，旨在构建一个既能无缝兼容现有 Python 生态，又能通过底层系统优化（如 Rust 运行时、批处理编译）来支撑大规模智能体自动化搜索的基础设施。

### 2. 论文提出的方法论
stratum 的核心思想是将流水线的“规划/推理”与“执行”解耦，其关键技术包括：
*   **声明式抽象**：基于 `skrub` 库将任意 Python ML 代码包装成延迟执行的算子有向无环图（DAG），使系统能够感知全局计算逻辑。
*   **逻辑优化器**：
    *   **流水线融合**：将智能体生成的批量流水线融合为一个统一的 DAG。
    *   **重写规则**：应用谓词下推、投影下推、公共子表达式消除（CSE）等经典数据库优化技术，减少冗余计算。
    *   **算子下放（Lowering）**：将高层算子（如交叉验证）展开为细粒度算子，扩大优化空间。
*   **Rust 后端运行时**：针对频繁使用的算子开发 Rust 内核，通过 `PyO3` 绑定，绕过 Python 的全局解释器锁（GIL），实现零拷贝数据访问和高性能执行。
*   **运行时优化**：
    *   **算子选择**：根据数据特征和成本估算，在 Python、Rust 或分布式后端间选择最优实现。
    *   **多级并行**：结合算子间（Inter-op）和算子内（Intra-op）并行。
    *   **中间结果重用**：利用哈希缓存机制，在不同迭代间重用昂贵的预处理结果。

### 3. 实验设计
*   **数据集/场景**：使用了 Kaggle 上的 **UK Housing（英国房价）数据集**，模拟企业级数据科学任务（包含缺失值处理、特征编码、多种模型训练）。
*   **Benchmark**：使用 **AIDE 智能体** 生成的真实工作负载，包含两个迭代阶段（探索阶段和超参微调阶段）。
*   **对比方法**：
    1.  **Base**：AIDE 智能体的原始顺序执行方式。
    2.  **Base_par**：基于多进程（Multiprocessing）的朴素并行执行方式。
    3.  **stratum**：开启所有优化的完整系统。

### 4. 资源与算力
*   **硬件环境**：单节点服务器，配备 **AMD EPYC 7443P CPU**（24 核心/48 线程），**256 GB RAM**。
*   **软件环境**：Ubuntu 20.04, Python 3.11, scikit-learn 1.8。
*   **GPU 使用**：文中实验主要针对表格数据和 CPU 密集型算子，**未明确提及 GPU 训练时长或型号**（但在愿景中提到了未来对 GPU 和 PyTorch 的集成计划）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了端到端性能对比实验和消融实验（Ablation Study）。
*   **充分性评价**：作为一篇 **Vision（愿景型）论文**，其实验部分属于初步验证（Preliminary）。虽然实验组数不多（主要针对一个典型智能体负载和不同数据规模），但涵盖了逻辑优化、算子选择和并行化等核心模块的贡献分析。实验设计客观，对比了工业界常用的多进程并行方案，证明了其设计的有效性。

### 6. 主要结论与发现
*   **显著加速**：在端到端测试中，stratum 相比顺序执行（Base）实现了高达 **16.6倍** 的加速，相比朴素并行（Base_par）实现了 **7.8倍** 的加速。
*   **优化贡献**：消融实验显示，逻辑优化（如 CSE）贡献了约 2.2倍加速，而引入原生 Rust 算子选择进一步带来了 4.5倍的提升。
*   **资源效率**：stratum 避免了多进程并行带来的巨大序列化开销和内存冗余（Base_par 的内存消耗是 stratum 的 8倍）。

### 7. 优点
*   **生态兼容性**：不要求智能体改变编写 Python 代码的习惯，通过 `skrub` 自动捕获逻辑，极具实用价值。
*   **系统级融合**：将数据库的查询优化思想（批处理、CSE）成功引入到高度动态的 ML 流水线搜索场景中。
*   **高性能后端**：利用 Rust 解决 Python 性能瓶颈，同时保持了接口的灵活性。

### 8. 不足与局限
*   **原型阶段**：目前 Rust 算子库覆盖有限，许多算子仍需回退到 Python 执行。
*   **成本估算难题**：由于 Python 库（如 Pandas）内部内存管理不透明，系统目前难以做出极其精确的成本预测。
*   **UDF 黑盒**：对于用户自定义函数（UDF）或某些特殊库，系统仍将其视为黑盒，限制了优化深度。
*   **深度学习支持**：目前主要侧重于表格数据和经典 ML，对深度神经网络（DNN）和 GPU 算子的集成尚处于早期探索阶段。

（完）
