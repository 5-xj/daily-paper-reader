---
title: Polynomial Speedup in Diffusion Models with the Multilevel Euler-Maruyama Method
authors: Arthur Jacot
date: 20260325
pdf: "https://arxiv.org/pdf/2603.24594v1"
tags: ["query:transformer", "query:attention"]
tldr: 本研究针对扩散模型采样效率低的问题，提出了多级 Euler-Maruyama (ML-EM) 方法。该方法利用一系列精度和计算成本递增的漂移项近似器（如不同规模的 UNet），通过大量低成本评估和少量高成本评估来求解 SDE。在漂移项满足特定计算复杂度条件下，ML-EM 能将采样成本降低至等同于单次最精确网络评估的水平，实现了多项式级的加速，并在 CelebA 数据集上验证了其有效性。
motivation: 传统的扩散模型采样方法计算成本高昂，通常需要多次调用高成本的大型神经网络。
method: 提出 ML-EM 方法，通过训练不同规模的 UNet 作为多级近似器，结合多级蒙特卡洛思想优化 SDE 求解过程。
result: 在 CelebA 64x64 图像生成任务中实现了高达四倍的加速，且采样成本仅相当于单次最大规模网络的评估。
conclusion: ML-EM 为扩散模型提供了多项式级的加速，且在网络规模越大时，其加速效果预计将更加显著。
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24594v1/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 450, \"height\": 398}, {\"url\": \"assets/figures/arxiv/2603.24594v1/fig-002.webp\", \"caption\": \"\", \"page\": 4, \"index\": 2, \"width\": 450, \"height\": 398}]"
---

## 速览
**TLDR**：本研究针对扩散模型采样效率低的问题，提出了多级 Euler-Maruyama (ML-EM) 方法。该方法利用一系列精度和计算成本递增的近似器（如不同规模的 UNet）来求解 SDE 或 ODE，仅需少量高精度模型评估和多次低成本模型评估。在漂移项计算复杂度较高的 HTMC 机制下，ML-EM 能将采样成本降低至与单次高精度模型评估相当，实现了多项式级别的加速，显著提升了图像生成效率。 \
**Motivation**：扩散模型在采样过程中需要多次评估高成本的漂移项，导致生成速度受限。 \
**Method**：引入多级 Euler-Maruyama 方法，通过结合不同规模的 UNet 模型，以极低的高精度模型调用次数实现 SDE 求解。 \
**Result**：在 CelebA 数据集上实现了高达 4 倍的加速，且总计算成本仅相当于对最大规模网络进行一次评估。 \
**Conclusion**：ML-EM 实现了采样效率的多项式级提升，特别适用于需要大规模神经网络的实际应用场景。

---

## Abstract
We introduce the Multilevel Euler-Maruyama (ML-EM) method compute solutions of SDEs and ODEs using a range of approximators $f^1,\dots,f^k$ to the drift $f$ with increasing accuracy and computational cost, only requiring a few evaluations of the most accurate $f^k$ and many evaluations of the less costly $f^1,\dots,f^{k-1}$. If the drift lies in the so-called Harder than Monte Carlo (HTMC) regime, i.e. it requires $ε^{-γ}$ compute to be $ε$-approximated for some $γ>2$, then ML-EM $ε$-approximates the solution of the SDE with $ε^{-γ}$ compute, improving over the traditional EM rate of $ε^{-γ-1}$. In other terms it allows us to solve the SDE at the same cost as a single evaluation of the drift. In the context of diffusion models, the different levels $f^{1},\dots,f^{k}$ are obtained by training UNets of increasing sizes, and ML-EM allows us to perform sampling with the equivalent of a single evaluation of the largest UNet. Our numerical experiments confirm our theory: we obtain up to fourfold speedups for image generation on the CelebA dataset downscaled to 64x64, where we measure a $γ\approx2.5$. Given that this is a polynomial speedup, we expect even stronger speedups in practical applications which involve orders of magnitude larger networks.