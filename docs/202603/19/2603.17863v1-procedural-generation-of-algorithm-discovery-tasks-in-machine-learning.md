---
title: Procedural Generation of Algorithm Discovery Tasks in Machine Learning
title_zh: 机器学习中算法发现任务的过程生成
authors: "Alexander D. Goldie, Zilin Wang, Adrian Hayler, Deepak Nathani, Edan Toledo, Ken Thampiratwong, Aleksandra Kalisz, Michael Beukman, Alistair Letcher, Shashank Reddy, Clarisse Wibault, Theo Wolf, Charles O'Neill, Uljad Berdica, Nicholas Roberts, Saeed Rahmani, Hannah Erlebach, Roberta Raileanu, Shimon Whiteson, Jakob N. Foerster"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17863v1"
tags: ["query:ahd"]
score: 10.0
evidence: 算法发现任务的程序化生成
tldr: 本研究针对现有机器学习算法发现任务集存在的评估不严谨、数据污染及任务同质化等问题，提出了 DiscoGen。这是一个程序化任务生成器，能自动生成数百万个涵盖优化器设计、损失函数开发等跨领域的算法发现任务。通过 DiscoGen 及其基准测试集 DiscoBench，研究者可以更有效地训练和评估算法发现智能体（ADA）。该工具不仅解决了现有基准的局限性，还为自动化机器学习的未来研究提供了强大的基础设施支持。
selection_source: fresh_fetch
motivation: 现有的算法发现任务集受限于评估方法不当、数据污染以及问题过于饱和或相似，阻碍了自动化算法开发系统的进步。
method: 提出了 DiscoGen，一个能够程序化生成数百万个跨领域、不同难度和复杂度的算法发现任务的生成器，并包含一个固定的基准测试子集 DiscoBench。
result: DiscoGen 能够生成涵盖强化学习优化器和图像分类损失函数等多样化任务，并成功应用于算法发现智能体的提示优化实验。
conclusion: DiscoGen 为算法发现领域提供了大规模、高质量的任务资源，为未来自动化机器学习研究开辟了具有影响力的全新方向。
---

## 摘要
自动化机器学习算法的开发具有开启新突破的潜力。然而，到目前为止，我们改进和评估算法发现系统的能力一直受到现有任务集的限制。这些任务集存在许多问题，例如：评估方法不佳、数据污染，以及包含已饱和或非常相似的问题。在这里，我们介绍了 DiscoGen，这是一个用于机器学习算法发现任务的过程生成器，例如为强化学习开发优化器或为图像分类开发损失函数。受强化学习中过程生成成功的启发，DiscoGen 涵盖了来自一系列机器学习领域的数百万个具有不同难度和复杂性的任务。这些任务由少量配置参数指定，可用于优化算法发现智能体 (ADAs)。我们提出了 DiscoBench，这是一个由 DiscoGen 任务的固定、小型子集组成的基准测试，用于对 ADAs 进行原则性评估。最后，我们提出了由 DiscoGen 赋能的若干具有雄心且有影响力的研究方向，并进行了实验展示其在 ADA 提示优化中的应用。DiscoGen 已在 https://github.com/AlexGoldie/discogen 开源。

## Abstract
Automating the development of machine learning algorithms has the potential to unlock new breakthroughs. However, our ability to improve and evaluate algorithm discovery systems has thus far been limited by existing task suites. They suffer from many issues, such as: poor evaluation methodologies; data contamination; and containing saturated or very similar problems. Here, we introduce DiscoGen, a procedural generator of algorithm discovery tasks for machine learning, such as developing optimisers for reinforcement learning or loss functions for image classification. Motivated by the success of procedural generation in reinforcement learning, DiscoGen spans millions of tasks of varying difficulty and complexity from a range of machine learning fields. These tasks are specified by a small number of configuration parameters and can be used to optimise algorithm discovery agents (ADAs). We present DiscoBench, a benchmark consisting of a fixed, small subset of DiscoGen tasks for principled evaluation of ADAs. Finally, we propose a number of ambitious, impactful research directions enabled by DiscoGen, in addition to experiments demonstrating its use for prompt optimisation of an ADA. DiscoGen is released open-source at https://github.com/AlexGoldie/discogen.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **DiscoGen** 的系统，旨在通过过程生成（Procedural Generation）技术解决机器学习算法发现领域的瓶颈。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心问题**：自动化算法发现（Algorithm Discovery）目前受限于任务集规模小、手动构建成本高、评估方法不严谨（缺乏明确的元训练/元测试划分）以及严重的数据污染（LLM 在预训练阶段可能见过任务答案）。
*   **整体含义**：论文借鉴了强化学习中过程生成环境（如 Procgen）的成功经验，开发了 DiscoGen。它能自动生成数百万个具有挑战性且多样化的算法发现任务，为训练和评估算法发现智能体（ADA）提供了大规模、标准化的基础设施。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：通过组合不同的**领域（Domain）**、**可编辑模块（Modules）**、**数据集**和**后端**，程序化地生成任务。
*   **关键技术细节**：
    *   **任务构成**：每个任务包含元训练集（ADA 可见并实验）和元测试集（对 ADA 隐藏，用于最终评估）。
    *   **模块化设计**：将算法拆分为可编辑模块（如损失函数、优化器、网络架构、训练循环）。ADA 仅需在给定的接口（Interface）内编写代码。
    *   **组合爆炸**：任务数量随模块（$m$）和数据集（$d$）的数量呈组合级增长。公式定义为：$N_{tasks} \approx 2 \cdot 3 \cdot b \cdot (2^m - 1) \cdot (3^d - 2^{d+1} + 1)$。目前支持超过 4 亿个潜在任务。
    *   **DiscoBench**：从生成器中提取的一个固定子集，包含“单模块编辑”和“全模块编辑”两类任务，用于基准测试。
    *   **ADA 优化循环**：利用生成的任务对 ADA 进行迭代优化（如提示词优化或权重微调）。

### 3. 实验设计
*   **数据集与场景**：涵盖 10 个领域，包括强化学习（On-Policy/Off-Policy RL）、计算机视觉分类、语言建模、连续学习、贝叶斯优化、模型取消学习（Unlearning）等。
*   **Benchmark**：使用 **DiscoBench**。
*   **对比方法**：
    *   **模型对比**：评估了 DeepSeek-v3.2、Devstral2 和 GPT-OSS 120B 三种开源大模型作为 ADA 核心的表现。
    *   **基准线**：与人类设计的标准算法（All Fixed Baseline）进行对比。
    *   **案例研究**：进行了提示词优化（Prompt Optimization）实验，对比了在不同数量任务（1, 5, 10, 30 个）上优化后的提示词在 DiscoBench 上的泛化能力。

### 4. 资源与算力
*   **算力设备**：主要使用 **Nvidia H200 GPU**。
*   **训练时长**：总计消耗约 **23,000 GPU 小时**（包含开发、测试和正式实验）。
*   **成本**：API 调用费用约为 **1,200 美元**。
*   **可访问性**：论文指出 DiscoGen 任务可在消费级显卡（如 2080Ti/3080）上运行，具有较好的普及性。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 10 个领域上进行了广泛测试。
    *   针对强化学习领域，测试了所有 15 种可能的模块组合。
    *   提示词优化实验经历了 30 次迭代。
*   **充分性与公平性**：实验采用了 3 个随机种子，并使用 Elo 等级分和 95% 置信区间进行评估。通过元训练/元测试的严格分离，确保了评估的客观性，避免了过拟合特定数据集的风险。

### 6. 主要结论与发现
*   **任务难度**：增加可编辑模块的数量会显著降低 ADA 的成功率，但能提高算法性能的上限。
*   **模型表现**：目前的开源模型在复杂任务（如同时编辑所有模块）上成功率较低，仍有巨大提升空间。DeepSeek-v3.2 在单模块任务中表现较强。
*   **泛化规律**：提示词优化实验证明，**在更多样化的任务上进行优化，能显著提升 ADA 在未见任务上的泛化性能**。
*   **算法创新**：ADA 能够发现一些非直观但有效的算法（如结合了 CNN 和 MLP 的双路径强化学习架构）。

### 7. 优点（亮点）
*   **规模化**：解决了算法发现任务稀缺的问题，支持数亿级任务生成。
*   **评估严谨**：通过隐藏元测试集和过程生成，极大降低了数据污染和评估作弊的风险。
*   **高度模块化**：允许研究者精确控制 ADA 的搜索空间（从简单的超参微调到复杂的架构设计）。
*   **开源贡献**：提供了完整的开源框架，方便社区扩展新领域。

### 8. 不足与局限
*   **ADA 成功率瓶颈**：在复杂任务中，ADA 经常因为语法错误或逻辑过拟合导致运行失败，鲁棒性有待提高。
*   **计算成本**：虽然单个任务运行较快，但进行大规模 ADA 优化（元-元学习）仍需极高的算力支撑。
*   **评估指标单一**：目前主要关注性能（Performance），对算法的能效（Energy）和速度（Speed）评估尚处于起步阶段。
*   **潜在污染风险**：一旦 DiscoBench 任务公开，未来模型预训练时仍可能将其纳入语料库（作者建议未来通过私有 API 缓解）。

（完）
