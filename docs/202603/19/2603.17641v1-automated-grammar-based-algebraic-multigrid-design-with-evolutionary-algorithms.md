---
title: Automated Grammar-based Algebraic Multigrid Design With Evolutionary Algorithms
title_zh: 基于演化算法的自动化语法驱动代数多重网格设计
authors: "Dinesh Parthasarathy, Wayne Mitchell, Arjun Gambhir, Harald Köstler, Ulrich Rüde"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17641v1"
tags: ["query:ahd"]
score: 9.0
evidence: 使用演化算法自动设计多网格算法
tldr: 本研究针对代数多重网格（AMG）算法组件选择复杂的问题，提出了一种基于进化算法的自动化设计方法。通过上下文无关文法引导的遗传编程（GP），在庞大的搜索空间中自动构建具有灵活循环模式（如非递归循环和特定层平滑序列）的高效AMG算法。实验表明，该方法在hypre库中显著提升了AMG作为求解器和预处理器的性能。
selection_source: fresh_fetch
motivation: 传统多重网格算法的效率高度依赖于人工选择组件，且非标准循环模式的搜索空间巨大，难以手动优化。
method: 采用上下文无关文法引导的遗传编程（GP）算法，从成熟的算法构建块中自动演化出具有灵活循环结构的代数多重网格方法。
result: 在线性代数库hypre上的数值实验证明，自动生成的非标准循环模式能有效提升多重网格作为求解器和预处理器的效率。
conclusion: 结合进化算法与语法引导的搜索，为自动化设计高性能、非递归的代数多重网格算法提供了一种有效且互补的新策略。
---

## 摘要
虽然多重网格方法在求解许多重要的偏微分方程时具有渐近最优性，但其效率在很大程度上取决于对各个算法组件的精心选择。与近期利用深度学习技术优化特定多重网格组件的方法不同，我们采用了一种互补策略，利用演化算法从经过验证的算法构建块中构建高效的多重网格循环。在此，我们将展示其在生成具有所谓“灵活循环”（即特定层级的平滑序列和非递归循环模式）的高效代数多重网格方法中的应用。包含此类非标准循环的搜索空间难以通过人工导航，它是利用受上下文无关语法引导的遗传规划（GP）生成的。使用线性代数库 hypre 进行的数值实验表明，这些非标准 GP 循环在作为求解器和预条件子时，均具有提升多重网格性能的潜力。

## Abstract
Although multigrid is asymptotically optimal for solving many important partial differential equations, its efficiency relies heavily on the careful selection of the individual algorithmic components. In contrast to recent approaches that can optimize certain multigrid components using deep learning techniques, we adopt a complementary strategy, employing evolutionary algorithms to construct efficient multigrid cycles from proven algorithmic building blocks. Here, we will present its application to generate efficient algebraic multigrid methods with so-called \emph{flexible cycling}, that is, level-specific smoothing sequences and non-recursive cycling patterns. The search space with such non-standard cycles is intractable to navigate manually, and is generated using genetic programming (GP) guided by context-free grammars. Numerical experiments with the linear algebra library, \emph{hypre}, demonstrate the potential of these non-standard GP cycles to improve multigrid performance both as a solver and a preconditioner.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种利用演化算法自动化设计代数多重网格（AMG）算法的新方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：多重网格方法（Multigrid）虽然在理论上具有最优的计算复杂度，但其效率高度依赖于平滑器、粗网格校正和循环模式（如V-cycle, W-cycle）等组件的组合。
*   **研究动机**：
    *   **搜索空间巨大**：非递归、层级特定的“灵活循环”（Flexible Cycling）组合空间极广（可达 $10^{23}$ 数量级），人工无法穷举优化。
    *   **现有方法局限**：现有的深度学习方法多侧重于优化单个组件（如平滑器参数），而忽略了整体循环结构的创新。
    *   **易用性需求**：非专家用户难以针对特定物理问题设计出最优的线性求解器。

### 2. 方法论
*   **核心思想**：采用**语法引导的遗传编程（G3P）**，在给定的算法构建块基础上，自动合成符合逻辑约束的高效 AMG 程序。
*   **关键技术细节**：
    *   **上下文无关文法（CFG）**：定义了一套生产规则，确保演化出的算法在语法上正确且符合多重网格的算法逻辑（如限制、平滑、粗网格校正的顺序）。
    *   **灵活循环（Flexible Cycles）**：允许非递归的访问模式和层级特定的平滑序列，打破了传统 V 循环的对称限制。
    *   **多目标优化（NSGA-II）**：以“单次迭代成本（$T/N$）”和“收敛率（$\rho$）”为双重适应度函数，寻找帕累托最优解。
    *   **非侵入式集成**：通过 Python 接口与成熟的 C++ 线性代数库 `hypre` (BoomerAMG) 集成，直接在高性能求解器上评估性能。

### 3. 实验设计
*   **实验场景**：
    1.  **3D 各向异性泊松方程**：测试求解器在不同物理参数和大规模网格下的表现。
    2.  **zrad3D 多物理场问题**：涉及辐射扩散和电子传导的随时间演化系统，测试 AMG 作为预条件子（Preconditioner）的性能。
*   **Benchmark（基准）**：
    *   **Default**：`hypre` 库中的默认 BoomerAMG 配置。
    *   **Tuned**：经过人工精细调优的传统 V 循环参数组合。
*   **对比方法**：将 GP 自动生成的非标准循环与上述基准在求解时间、迭代次数和弱缩放性（Weak Scaling）方面进行对比。

### 4. 资源与算力
*   **算力平台**：使用了 LLNL 的 Ruby 和 RZHound 集群。
*   **计算成本**：
    *   **泊松问题**：约 29 分钟，消耗 15.5 节点时（Node-hours）。
    *   **zrad3D 问题**：约 55 分钟，消耗 29.3 节点时。
*   **并行规模**：演化过程在 32 个节点上运行，利用 64 个 MPI 进程进行种群并行评估。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 5 次独立的 GP 演化运行以验证重复性。
    *   **弱缩放实验**：泊松问题网格规模从 $10^6$ 扩展至 $10^9$（10亿）自由度；zrad3D 扩展至 6400 万自由度。
    *   **泛化性测试**：在 40 个不同的时间步矩阵上评估了同一个演化出的预条件子。
*   **充分性评价**：实验设计非常充分，不仅涵盖了静态问题，还验证了算法在时间步演化、网格规模剧增以及不同硬件平台上的鲁棒性和通用性。

### 6. 主要结论与发现
*   **性能超越**：GP 生成的灵活循环在所有测试用例中均优于默认配置，且在多数情况下优于人工调优的 V 循环，加速比通常在 **1.5x 到 2x** 之间。
*   **结构创新**：GP 发现了一些非直观的策略，例如在最昂贵的细网格层减少平滑次数，而在稍粗的层级增加平滑，从而在保证收敛的同时降低总计算成本。
*   **强泛化能力**：在小规模、单个时间步上训练得到的算法，可以直接应用于大规模和整个时间序列的模拟，无需重新演化。

### 7. 优点
*   **自动化程度高**：无需人工干预即可发现超越专家经验的算法结构。
*   **兼容性强**：生成的算法直接运行在生产级库 `hypre` 上，具有极高的实用价值。
*   **无需标注数据**：与监督学习不同，该方法通过直接评估获取反馈，不需要预先准备大量的“最优解”数据集。

### 8. 不足与局限
*   **离线成本**：虽然在线运行很快，但初期的演化过程（训练阶段）需要消耗数十个节点时，对于极小规模问题可能不划算。
*   **搜索空间限制**：目前的语法仍基于专家预设的构建块，如果构建块本身不包含某种创新操作，GP 也无法凭空创造。
*   **物理覆盖面**：目前主要针对扩散主导（Diffusion-dominated）的问题，对于对流主导或波动方程等更复杂问题的适用性尚待验证。

（完）
