---
title: Projection-Free Evolution Strategies for Continuous Prompt Search
title_zh: 面向连续提示搜索的无投影演化策略
authors: "Yu Cai, Canxi Huang, Xiaoyu He"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13786v1"
tags: ["query:llm-ec"]
score: 8.0
evidence: 用于大模型连续提示词搜索的演化策略
tldr: 针对连续提示搜索中随机投影无法有效捕捉低维结构的问题，本文提出了一种基于进化策略的无投影搜索方法。该方法直接在全提示空间进行优化，并结合了针对内在维度的自适应机制，在不增加计算开销的情况下提升了搜索能力。此外，引入置信度正则化以增强少样本场景下的泛化性能。在GLUE基准测试上的实验证明，该方法显著优于现有基准。
selection_source: fresh_fetch
motivation: 现有的随机投影方法无法有效捕捉提示空间中的本质低维结构，限制了连续提示搜索的性能。
method: 提出一种在全空间优化的无投影进化策略，并结合内在维度自适应机制和置信度正则化。
result: 在GLUE基准的七个自然语言理解任务上，该方法显著超越了现有的基线模型。
conclusion: 无投影的进化策略能更有效地利用提示空间的结构特征，是提升连续提示搜索性能的有效途径。
---

## 摘要
连续提示搜索为自然语言处理任务中传统的参数微调提供了一种计算高效的替代方案。然而，其在实际应用中的有效性往往受到黑盒性质以及目标景观固有高维性的显著阻碍。现有方法通常通过将搜索限制在随机投影的低维子空间中来缓解这些挑战。然而，投影机制的有效性及其潜在动机仍然模糊不清。在本文中，我们首先通过实证研究证明，尽管提示空间具有低维结构，但随机投影无法充分捕捉这一核心结构。受此发现启发，我们提出了一种基于演化策略的无投影提示搜索方法。通过在全提示空间中直接优化，并结合针对本征维度校准的自适应机制，我们的方法在不增加额外计算开销的情况下实现了具有竞争力的搜索能力。此外，为了弥合少样本场景下的泛化差距，我们引入了一种基于置信度的正则化机制，系统地增强了模型对目标词化器（verbalizers）的置信度。在 GLUE 基准测试的七项自然语言理解任务上的实验结果表明，我们提出的方法显著优于现有的基准模型。

## Abstract
Continuous prompt search offers a computationally efficient alternative to conventional parameter tuning in natural language processing tasks. Nevertheless, its practical effectiveness can be significantly hindered by the black-box nature and the inherent high-dimensionality of the objective landscapes. Existing methods typically mitigate these challenges by restricting the search to a randomly projected low-dimensional subspace. However, the effectiveness and underlying motivation of the projection mechanism remain ambiguous. In this paper, we first empirically demonstrate that despite the prompt space possessing a low-dimensional structure, random projections fail to adequately capture this essential structure. Motivated by this finding, we propose a projection-free prompt search method based on evolutionary strategies. By directly optimizing in the full prompt space with an adaptation mechanism calibrated to the intrinsic dimension, our method achieves competitive search capabilities without additional computational overhead. Furthermore, to bridge the generalization gap in few-shot scenarios, we introduce a confidence-based regularization mechanism that systematically enhances the model's confidence in the target verbalizers. Experimental results on seven natural language understanding tasks from the GLUE benchmark demonstrate that our proposed approach significantly outperforms existing baselines.

---

## 论文详细总结（自动生成）

这篇论文针对预训练语言模型（PLM）的连续提示搜索（Continuous Prompt Search）提出了改进方案。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：连续提示搜索通常面临极高的维度（如 RoBERTa-Large 搜索 50 个 token 对应 51,200 维），导致黑盒优化算法收敛极慢。
*   **研究动机**：
    *   现有主流方法（如 BBT）假设提示空间是低维的，并使用**随机投影（Random Projection）**将搜索限制在固定子空间内。
    *   作者通过实证发现：虽然提示空间确实存在低维结构（本征维度约在 250 左右），但**随机投影并不能有效捕捉这一结构**，反而限制了搜索质量。
    *   在少样本（few-shot）场景下，全空间搜索容易出现过拟合，导致泛化能力差。

### 2. 方法论
论文提出了**无投影演化策略（Projection-Free Evolution Strategies）**，核心包含两个关键技术：
*   **本征维度感知自适应机制（ID-Aware Adaptation）**：
    *   不再将高维空间投影到低维，而是在全空间直接优化。
    *   修改演化策略（如 (1+1)-ES 和 SaES）中的步长自适应公式。关键在于将控制步长更新平滑度的阻尼因子 $\tau$ 从依赖环境维度 $d$ 校准为依赖**本征维度 $\tilde{d}$**（即 $\tau = \sqrt{2\tilde{d}}$）。这使得算法在高维空间中仍能保持高效的步长调整。
*   **置信度正则化（Confidence-Based Regularization, CR）**：
    *   **核心思想**：防止模型仅在候选标签集（Verbalizers）内做相对排序，而忽略了全局词表的置信度。
    *   **公式逻辑**：在标准交叉熵损失（CE）基础上增加一个正则项，惩罚那些在全词表 $V$ 中全局置信度较低的提示词。通过最大化目标标签集在全词表中的概率占比，提升模型的泛化性能。

### 3. 实验设计
*   **数据集**：选取了 GLUE 基准测试中的 7 个自然语言理解任务：MRPC, MNLI, QQP, SST-2, RTE, CoLA, QNLI。
*   **评测指标**：F1 分数（MRPC, QQP）、MCC（CoLA）、准确率（其他）。
*   **对比方法**：
    *   **零样本/少样本基准**：人工提示（ManualPrompt）、语境学习（ICL）。
    *   **优化基准**：BBT（基于投影的 CMA-ES）、ZOSGD（零阶随机梯度下降）、RLPrompt（强化学习）、BDPL（离散提示优化）。
*   **实验设置**：统一采用 16-shot（每类 16 个样本）的少样本设置。

### 4. 资源与算力
*   **硬件设备**：实验使用了 **NVIDIA RTX 4090 GPU**。
*   **算力开销**：文中给出了“Time/FE (s)”（每次函数评估的平均耗时）。结果显示，无投影方法（约 0.15s/FE）与基于投影的 BBT（约 0.14s/FE）计算开销基本持平，证明了该方法在不增加额外计算负担的情况下提升了性能。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 7 个任务上进行了主实验，每个实验运行 5 个随机种子并取平均值。
    *   进行了**本征维度（ID）的敏感性分析**（测试了 $\tilde{d}$ 从 50 到 1000 的变化）。
    *   进行了**正则化系数 $\beta$ 的敏感性分析**（测试了 $10^{-2}$ 到 $10^2$ 的范围）。
    *   **跨架构验证**：除了 RoBERTa-Large，还在 67 亿参数的因果语言模型 **OPT-6.7B** 上验证了通用性。
*   **充分性评价**：实验设计较为全面，涵盖了消融实验、超参数敏感性分析以及不同模型架构的迁移验证，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **本征维度存在上限**：实验证实提示空间的本征维度确实很低（约 250），且不随提示词长度增加而显著增长。
*   **无投影更优**：直接在全空间优化并配合 ID 感知自适应，比随机投影子空间搜索更有效。
*   **正则化是关键**：引入 CR 正则化显著弥合了全空间搜索的泛化差距，使模型在测试集上的表现大幅提升。
*   **SaES 稳定性更强**：在处理超大规模模型（如 OPT-6.7B）时，具有种群多样性的 SaES 比单体演化策略 (1+1)-ES 更稳定。

### 7. 优点与亮点
*   **理论驱动**：通过实证测量 ID 揭示了现有投影方法的局限性，为算法改进提供了坚实的动机。
*   **简单高效**：提出的 ID 感知自适应仅需修改一个阻尼因子参数，无需复杂的投影矩阵运算，易于实现且计算开销低。
*   **即插即用**：CR 正则化项可以轻松集成到其他零阶优化器（如 ZOSGD）中，具有良好的通用性。

### 8. 不足与局限
*   **超参数依赖**：虽然 $\beta$ 的敏感性曲线较平滑，但在不同任务上仍需进行粗粒度的搜索以确定最佳正则化强度。
*   **任务表现差异**：在 CoLA 等极具挑战性的任务上，虽然有所提升，但绝对性能（MCC 约 8.32）仍有较大提升空间。
*   **应用限制**：目前主要集中在分类任务（通过 Verbalizers 映射），对于生成式任务（如长文本摘要）的适用性尚未深入探讨。

（完）
