---
title: Numerical Superoptimization for Library Learning
title_zh: 用于库学习的数值超级优化
authors: "Jonas Regehr, Mitch Briles, Zachary Tatlock, Pavel Panchekha"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24812v1"
tags: ["query:ahd"]
score: 9.0
evidence: 通过超级优化自动发现新的数学原语
tldr: 本研究针对数值软件中数学原语的选择问题，提出了数值库学习框架 GrowLibm。该工具重新利用 Herbie 超优化器的搜索与评估机制，从浮点内核工作负载中自动挖掘、排名并筛选出最具潜力的候选原语。实验表明，GrowLibm 识别出的原语能使内核速度提升达 2.2 倍，并大幅提高计算精度。此外，通过 LLVM 匹配器在实际应用中实现了显著的端到端性能优化，为数值库的自动化扩展提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.24812v1/fig-001.webp\", \"caption\": \"Fig. 5. Distributions of the size, frequency, and urgency heuristics for a PROJ run. Size and frequency are well-distributed, providing good discriminative signal. Urgency is concentrated near zero with a long tail of high-urgency candidates, confirming the bimodal structure that justifies two-stage filtering.\", \"page\": 10, \"index\": 1, \"width\": 806, \"height\": 195}, {\"url\": \"assets/figures/arxiv/2603.24812v1/fig-002.webp\", \"caption\": \"Fig. 6. Library primitives suggested by GrowLibm on three target applications: PROJ, CoolProp, and Basilisk (in order, separated by horizontal lines). For each library primitive, “Naive Implementation” shows the cost (inverse throughput, relative to a baseline) and error (Herbie’s average bits of error metric) of implementing the formula directly, while “Expert Implementation” shows the cost and error of the implementation suggested by our expert. GrowLibm’s suggestions are recognizable as simple, generic primitives whose naive implementations are typically inaccurate, while the expert implementations are, by and large, extremely accurate with acceptable costs.\", \"page\": 14, \"index\": 2, \"width\": 660, \"height\": 358}, {\"url\": \"assets/figures/arxiv/2603.24812v1/fig-003.webp\", \"caption\": \"Fig. 1. The GrowLibm pipeline. Given numerical kernels converted to FPCore [11, 18], a standard floatingpoint interchange format, the generation phase runs Herbie to explore equivalent programs, extracts subexpressions from Herbie’s intermediates, and canonicalizes and deduplicates them. The selection phase iteratively ranks candidates by frequency, urgency, and size; resolves implications between top candidates so that redundant variants are not both selected; and adds the winners to Herbie’s platform for re-evaluation. A final Herbie pass confirms which candidates are actually used in optimized kernels, producing the proposed primitives.\", \"page\": 3, \"index\": 3, \"width\": 821, \"height\": 369}, {\"url\": \"assets/figures/arxiv/2603.24812v1/fig-004.webp\", \"caption\": \"Fig. 10. Speedup for five PROJ projections, compiled using our prototype LLVM pass and tested across 10,000,000 points. Higher is better.\", \"page\": 18, \"index\": 4, \"width\": 633, \"height\": 318}, {\"url\": \"assets/figures/arxiv/2603.24812v1/fig-005.webp\", \"caption\": \"Fig. 2. The forward ellipsoidal projection function for the Swiss Oblique Mercator projection from the PROJ library [42]. Note the complex compositions of mathematical functions, including 2 atan(exp(𝑥)) − 𝜋/2, log(tan(𝜋/4 + 0.5𝑥)), and log((1 + 𝑥)/(1 − 𝑥)). Accurately evaluating these compositions is challenging in floating point and generally beyond the capabilities of existing numerical superoptimizers.\", \"page\": 4, \"index\": 5, \"width\": 712, \"height\": 191}, {\"url\": \"assets/figures/arxiv/2603.24812v1/fig-006.webp\", \"caption\": \"Fig. 3. A numerical expert’s implementation of log((1 + 𝑥)/(1 − 𝑥)), using a polynomial that is accurate for inputs |𝑥 | < .1716. This implementation borrows from the famous fdlibm math library. The input range is sufficient for the use case in Figure 2 because there the relevant input 𝑥 is the eccentricity of the Earth times the sine of the oblique angle, well within that input range.\", \"page\": 4, \"index\": 6, \"width\": 628, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2603.24812v1/fig-007.webp\", \"caption\": \"Fig. 11. Timeline of GrowLibm’s stages on the PROJ, Basilisk, and CoolProp applications.\", \"page\": 19, \"index\": 7, \"width\": 775, \"height\": 565}, {\"url\": \"assets/figures/arxiv/2603.24812v1/fig-008.webp\", \"caption\": \"Fig. 8. Lowest error achieved by Herbie for kernels in all three target applications that use GrowLibmsuggested library primitives. Error is reported as a percentage of the 64-bit result (bits of error divided by 64); lower is better. PROJ kernels are split into two charts (high-error and low-error) for readability. GrowLibmsuggested library primitives enable dramatic reductions in error for most kernels.\", \"page\": 17, \"index\": 8, \"width\": 805, \"height\": 900}, {\"url\": \"assets/figures/arxiv/2603.24812v1/fig-009.webp\", \"caption\": \"Fig. 7. Speedup curves for PROJ, CoolProp, and Basilisk; higher is better. Each curve compares Herbie cost estimates with and without the GrowLibm-suggested library primitives for a given level of accuracy. Specifically, each curve aggregates all of the kernels in the target application where a GrowLibm-suggested library primitive is used andwhichmeet a given average accuracy level.GrowLibm-suggested library primitives significantly speed up implementations across the accuracy spectrum, with the largest speedups at high accuracies where expert implementations of GrowLibm-suggested primitives are most important.\", \"page\": 16, \"index\": 9, \"width\": 805, \"height\": 195}]"
motivation: 现有的数值超优化器虽能优化浮点内核，但如何确定哪些新的数学原语值得被优先实现仍是一个待解决的关键问题。
method: 提出 GrowLibm 框架，利用超优化器的中间搜索结果挖掘候选原语，并基于反事实效用进行排名和冗余消除。
result: "在科学应用测试中，该方法使内核速度提升高达 2.2 倍，显著提高了最大可达精度，并实现了 5% 的端到端性能增长。"
conclusion: 数值库学习能有效识别并实现高性能、高精度的可重用原语，显著增强了现有数值超优化工具的优化能力。
---

## 摘要
数值软件依赖于 sin、exp 和 log 等数学原语的快速、准确实现。现代超级优化器可以针对给定的一组此类原语优化浮点内核，但一个更根本的问题仍未解决：首先哪些新原语值得实现？我们将此问题表述为数值库学习：给定一组浮点内核工作负载，识别出那些通过专家级实现能最大程度提高速度和准确性的数学原语。我们的核心见解是，数值超级优化器已经具备了非常适合解决这一问题的机制。它们的搜索过程恰好可以枚举候选原语，其等价性程序可以泛化并去重候选原语，而其成本模型可以估计反事实效用（counterfactual utility）：即如果某个给定原语可用，工作负载将获得多少提升。我们提出了 GrowLibm，它将 Herbie 超级优化器重新用作数值库学习器。GrowLibm 从超级优化器的中间搜索结果中挖掘候选原语，根据反事实效用对其进行排名，并剪枝冗余候选。在三个科学应用（PROJ、CoolProp 和 Basilisk）中，GrowLibm 识别出了紧凑且可重用的原语，这些原语可以使用标准数值技术有效地实现。当使用这些专家实现扩展 Herbie 时，在固定准确度下，内核速度提升高达 2.2 倍，且最大可达准确度也有所提高，其中一个案例从 56.0% 提升至 93.5%。我们还原型化了一个 LLVM 匹配器，用于识别优化后 IR 中的学习原语，在五个 PROJ 投影中恢复了 26 个替换点，并将端到端应用性能提高了多达 5%。

## Abstract
Numerical software depends on fast, accurate implementations of mathematical primitives like sin, exp, and log. Modern superoptimizers can optimize floating-point kernels against a given set of such primitives, but a more fundamental question remains open: which new primitives are worth implementing in the first place?   We formulate this as numerical library learning: given a workload of floating-point kernels, identify the mathematical primitives whose expert implementations would most improve speed and accuracy. Our key insight is that numerical superoptimizers already have the machinery well-suited to this problem. Their search procedures happen to enumerate candidate primitives, their equivalence procedures can generalize and deduplicate candidates, and their cost models can estimate counterfactual utility: how much the workload would improve if a given primitive were available.   We present GrowLibm, which repurposes the Herbie superoptimizer as a numerical library learner. GrowLibm mines candidate primitives from the superoptimizer's intermediate search results, ranks them by counterfactual utility, and prunes redundant candidates. Across three scientific applications (PROJ, CoolProp, and Basilisk), GrowLibm identifies compact, reusable primitives that can be implemented effectively using standard numerical techniques. When Herbie is extended with these expert implementations, kernel speed improves by up to 2.2x at fixed accuracy, and maximum achievable accuracy also improves, in one case from 56.0% to 93.5%. We also prototype an LLVM matcher that recognizes learned primitives in optimized IR, recovering 26 replacement sites across five PROJ projections and improving end-to-end application performance by up to 5%.

---

## 论文详细总结（自动生成）

### 论文总结：用于库学习的数值超级优化 (Numerical Superoptimization for Library Learning)

#### 1. 核心问题与整体含义
数值软件（如地理信息系统、物理模拟、航天动力学）的性能和精度高度依赖于底层的数学原语（如 `sin`, `exp`, `log`）。虽然现有的数值超级优化器（如 Herbie）可以优化给定的数学表达式，但它们受限于现有的原语库。
**核心问题**：在特定的应用领域中，哪些**新的**数学原语最值得被专家实现，以最大程度地提升整个工作负载的速度和精度？
**研究动机**：目前缺乏自动化的方法来识别这些“高价值”的潜在原语。本论文提出了“数值库学习”的概念，旨在从实际代码中自动挖掘并建议应优先开发的新数学函数。

#### 2. 方法论
论文提出了 **GrowLibm** 框架，其核心思想是“反向利用”超级优化器的搜索机制：
*   **候选生成 (Generation)**：运行 Herbie 优化器，但不只看最终结果，而是挖掘其搜索过程中产生的所有中间等价表达式。通过提取子表达式并进行算子切割，生成大量候选原语。
*   **语义去重 (Deduplication)**：利用 e-graph（等价图）技术和变量名标准化，识别并合并语义相同但写法不同的候选原语（例如 `log(1+x)` 和 `log(y+1)`）。
*   **反事实效用排名 (Ranking by Counterfactual Utility)**：
    *   **尺寸 (Size)**：倾向于更短、更简单的表达式（易于实现）。
    *   **频率 (Frequency)**：在搜索空间中出现的次数。
    *   **紧迫性 (Urgency)**：当前优化器在没有该原语时，对该表达式的优化效果有多差。
*   **冗余剪枝 (Pruning via Implication)**：如果原语 A 的存在能让原语 B 的收益消失（即 A 蕴含 B），则剔除 B，避免浪费专家精力。
*   **专家实现**：GrowLibm 输出数学定义，由数值专家使用多项式逼近、范围缩减等标准技术进行高性能实现。

#### 3. 实验设计
*   **应用场景/数据集**：
    1.  **PROJ**：开源地理空间坐标转换库（92个内核）。
    2.  **CoolProp**：流体热物理性质计算库（24个内核）。
    3.  **Basilisk**：航天动力学模拟框架（28个内核）。
*   **Benchmark**：以“Vanilla Herbie”（仅使用标准库原语的优化结果）作为基准。
*   **对比维度**：
    *   **内核级**：对比使用新原语前后的速度-精度帕累托前沿（Pareto Frontier）。
    *   **应用级**：开发了一个原型 LLVM 匹配器，在编译后的 PROJ 实际代码中自动替换学习到的原语，测量端到端性能。

#### 4. 资源与算力
*   **硬件环境**：Intel i7-8700K CPU, 32 GiB RAM, 运行 Ubuntu 系统。
*   **运行耗时**：GrowLibm 的全流程（生成、去重、排名、筛选）在三个案例上分别耗时约 **1.5 到 3 小时**。
*   **算力说明**：该方法主要消耗 CPU 资源进行符号搜索和数值采样，未提及使用 GPU。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个完全不同领域的科学计算应用，共计 144 个数值内核。
*   **充分性**：实验不仅在 Herbie 的成本模型下进行了评估，还通过专家手动实现 C 代码并集成到 LLVM 编译器中进行了实测。
*   **客观性**：通过对比“专家实现”与“朴素实现”的成本和误差，客观展示了新原语的实际价值。实验覆盖了从理论挖掘到部署验证的全链路。

#### 6. 主要结论与发现
*   **性能提升**：在相同精度下，新原语使内核速度提升高达 **2.2倍**。
*   **精度突破**：能够达到以前无法实现的精度水平，例如某个 PROJ 内核的精度从 56.0% 提升至 93.5%。
*   **可重用性**：识别出的原语（如 `invgud`, `log1pmd`）在应用中被多次复用（最高达 87 次）。
*   **部署可行性**：LLVM 匹配器成功在 PROJ 中识别出 26 个替换点，实现了高达 5% 的端到端提速。

#### 7. 优点
*   **创新视角**：首次将超级优化器作为“库学习器”，解决了“该写什么函数”的决策难题。
*   **实用性强**：生成的原语符合数值专家的直觉，且易于使用标准数值工具（如 Sollya）实现。
*   **闭环验证**：从自动发现到编译器集成，提供了完整的技术路径。

#### 8. 不足与局限
*   **实现成本**：虽然发现了原语，但仍需要数值专家进行高质量的 C/C++ 实现，未能实现完全自动化。
*   **难度预估偏差**：部分被选中的原语（如 `sinquot`）在实际实现时发现极其困难且运行缓慢，目前的排名启发式算法未能完美预测实现的复杂度。
*   **LLVM 原型限制**：目前的 LLVM 匹配器缺乏成本模型，在某些情况下为了精度会牺牲少量性能（导致 3-6% 的变慢），需要更精细的权衡机制。

（完）
