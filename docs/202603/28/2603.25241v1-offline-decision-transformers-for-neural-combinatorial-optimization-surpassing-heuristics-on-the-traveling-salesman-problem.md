---
title: "Offline Decision Transformers for Neural Combinatorial Optimization: Surpassing Heuristics on the Traveling Salesman Problem"
title_zh: 用于神经组合优化的离线决策 Transformer：在旅行商问题上超越启发式算法
authors: "Hironori Ohigashi, Shinichiro Hamada"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25241v1"
tags: ["query:llm-ec"]
score: 9.0
evidence: 针对旅行商问题的神经组合优化
tldr: 针对旅行商问题（TSP）等组合优化难题，本文提出一种基于离线强化学习的决策Transformer框架。该方法通过整合指针网络处理可变动作空间，并利用分位数回归进行乐观的收益条件建模，直接从启发式算法数据集中学习。实验证明，该模型不仅能模仿现有算法，还能通过合成策略超越其训练所用的四种经典启发式算法，展示了离线RL在提升组合优化性能方面的巨大潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25241v1/fig-001.webp\", \"caption\": \"Table 2: Optimality gap (%) for different RTG targets (Fixed vs. Predicted) and values of α on N = 20, 50.\", \"page\": 6, \"index\": 1, \"width\": 847, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2603.25241v1/fig-002.webp\", \"caption\": \"Table 11: Actual average solution costs corresponding to the optimality gaps reported in Table 2.\", \"page\": 10, \"index\": 2, \"width\": 752, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2603.25241v1/fig-003.webp\", \"caption\": \"Table 10: Actual average solution costs corresponding to the optimality gaps reported in Table 1. The first row \\\"Optimal\\\" shows the average cost of the optimal solutions.\", \"page\": 10, \"index\": 3, \"width\": 582, \"height\": 429}, {\"url\": \"assets/figures/arxiv/2603.25241v1/fig-004.webp\", \"caption\": \"Figure 1: Overview of the proposed method’s architecture. The transformer encoder calculates a node embedding vector from the coordinates of the graph nodes. As observation ot and action at, the embedding vectors fe t and fe t+1 of the nodes transitioned at time t and t+ 1, respectively, are input to the causal transformer decoder along with the RTG R̂t. The value R̃t is the RTG prediction by the causal transformer decoder.\", \"page\": 3, \"index\": 4, \"width\": 746, \"height\": 261}, {\"url\": \"assets/figures/arxiv/2603.25241v1/fig-005.webp\", \"caption\": \"Table 7: Architectural details of the Causal transformer decoder.\", \"page\": 9, \"index\": 5, \"width\": 572, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2603.25241v1/fig-006.webp\", \"caption\": \"Table 1: Optimality gap (%) and its standard deviation for each method on the test dataset. The \\\"Data\\\" column indicates the heuristic method used for training. The \\\"Method\\\" column represents the original heuristic method (Original), behavior cloning (BC), and our proposed method (DT).\", \"page\": 5, \"index\": 6, \"width\": 594, \"height\": 396}]"
motivation: 传统的在线强化学习在组合优化中难以部署且未充分利用现有的启发式算法知识。
method: 结合指针网络处理节点选择的动态动作空间，并采用分位数回归对目标收益进行乐观估计。
result: 实验结果显示，该方法在TSP问题上生成的路径质量始终优于其训练数据中包含的四种经典启发式算法。
conclusion: 离线强化学习能够有效整合并超越现有领域知识，为解决复杂的组合优化问题提供了新路径。
---

## 摘要
像旅行商问题（TSP）这样的组合优化问题在工业界至关重要，但属于 NP 困难问题。神经组合优化已展现出潜力，但其对在线强化学习（RL）的依赖阻碍了部署，且未能充分利用数十年的算法知识。我们通过应用离线强化学习框架——决策 Transformer（Decision Transformer）来解决这些局限性，直接从启发式解的数据集中学习更优策略；其目标不仅是模仿，而是综合并超越这些启发式算法。具体而言，我们（i）集成了一个指针网络（Pointer Network）以处理与实例相关的、可变的节点选择动作空间，以及（ii）采用分位数回归（expectile regression）对待获得回报（Return-to-Go）进行乐观调节，这对于最优值差异巨大的实例至关重要。实验表明，我们的方法始终能产生比其训练所用的四种经典启发式算法更高质量的路径，证明了离线强化学习在释放并超越现有领域知识中所蕴含性能方面的潜力。

## Abstract
Combinatorial optimization problems like the Traveling Salesman Problem are critical in industry yet NP-hard. Neural Combinatorial Optimization has shown promise, but its reliance on online reinforcement learning (RL) hampers deployment and underutilizes decades of algorithmic knowledge. We address these limitations by applying the offline RL framework, Decision Transformer, to learn superior strategies directly from datasets of heuristic solutions; it aims to not only to imitate but to synthesize and outperform them. Concretely, we (i) integrate a Pointer Network to handle the instance-dependent, variable action space of node selection, and (ii) employ expectile regression for optimistic conditioning of Return-to-Go, which is crucial for instances with widely varying optimal values. Experiments show that our method consistently produces higher-quality tours than the four classical heuristics it is trained on, demonstrating the potential of offline RL to unlock and exceed the performance embedded in existing domain knowledge.

---

## 论文详细总结（自动生成）

这是一份关于论文《Offline Decision Transformers for Neural Combinatorial Optimization: Surpassing Heuristics on the Traveling Salesman Problem》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何利用现有的启发式算法（Heuristics）知识，通过离线学习的方式解决复杂的组合优化问题（如 TSP），并实现对原有算法性能的超越。
*   **研究动机**：
    *   **在线强化学习（RL）的局限**：现有的神经组合优化（NCO）多依赖在线 RL，这在实际工业部署中面临数据获取成本高、环境模拟复杂等挑战。
    *   **领域知识利用不足**：过去几十年积累的大量启发式算法和专家经验在在线 RL 框架下未得到充分利用。
    *   **离线 RL 的潜力**：决策 Transformer（Decision Transformer, DT）在其他领域证明了其能从次优数据中学习并合成更优策略的能力，但在处理动作空间多变的组合优化问题上尚属空白。

### 2. 论文提出的方法论
该方法将 TSP 建模为序列建模问题，核心架构结合了 **Encoder-Decoder Transformer**：
*   **核心思想**：通过“目标条件化”（Goal-conditioning）学习，将期望的收益（Return-to-Go, RTG）作为输入，引导模型生成高质量解。
*   **关键技术细节**：
    1.  **集成指针网络（Pointer Network）**：标准 DT 无法处理 TSP 中随实例变化的动作空间（节点索引）。模型引入指针网络，通过注意力机制在隐藏状态和节点嵌入之间建立联系，实现对特定实例节点的选择。
    2.  **分位数回归（Expectile Regression）预测 RTG**：由于不同 TSP 实例的最优路径长度差异巨大，设定统一的 RTG 目标不现实。模型使用分位数回归动态预测每个实例可能达到的最高收益。
    3.  **损失函数**：采用多任务学习，结合了节点选择的交叉熵损失（$L_{CE}$）和 RTG 预测的非对称平方损失（$L^\alpha_2$）。
*   **算法流程**：编码器提取节点特征 -> 解码器接收当前路径、已访问节点嵌入及预测的 RTG -> 指针网络输出下一个访问节点的概率分布。

### 3. 实验设计
*   **数据集**：2D 欧几里得 TSP 实例，规模包括 $N=20, 50, 100$。训练集包含 100 万个实例，验证和测试集各 1 万个。
*   **数据来源（训练标签）**：使用了四种经典启发式算法生成的解：
    1.  最近邻算法 (NN)
    2.  最近插入法 (NI)
    3.  最远插入法 (FI)
    4.  模拟退火算法 (SA)
*   **Benchmark 与对比方法**：
    *   **基准**：原始启发式算法的性能。
    *   **对比方法**：行为克隆（Behavior Cloning, BC），即不带 RTG 条件的监督学习。
    *   **评估指标**：相对于精确最优解（Exact Optimal）的差距（Optimality Gap %）。

### 4. 资源与算力
*   **硬件环境**：单台服务器，配备 1 张 **NVIDIA GeForce RTX 4090 (24GB VRAM)** 和 Intel Xeon Silver 4314 CPU。
*   **训练时长**：
    *   $N=20$：约 19 小时。
    *   $N=50$：约 1 天 3 小时。
    *   $N=100$：约 1 天 23 小时。
*   **推理速度**：单个实例预测耗时在 0.63 到 1.10 秒之间（随规模增加）。

### 5. 实验数量与充分性
*   **实验规模**：针对 3 种问题规模和 4 种数据来源分别进行了实验，总计 12 组核心对比实验。
*   **消融实验**：
    *   对比了固定 RTG（如设为 0 或均值）与预测 RTG 的效果。
    *   研究了分位数参数 $\alpha$ 对性能的影响。
    *   探讨了在推理时手动添加 RTG 偏移量（Offset）对结果的改进。
*   **充分性评价**：实验设计较为全面，涵盖了从简单贪心到复杂随机搜索的多种数据源，且测试集规模（1万）足以保证统计意义上的客观性。

### 6. 论文的主要结论与发现
*   **超越启发式**：DT 模型在所有测试场景下均优于其训练所用的原始启发式算法。特别是在 SA 数据集上，性能提升接近一倍。
*   **RTG 的关键作用**：带 RTG 条件的 DT 显著优于行为克隆（BC），证明了模型不是简单的模仿，而是学会了关联动作与结果。
*   **乐观预测的重要性**：使用分位数回归（$\alpha > 0.5$）进行乐观的 RTG 预测，比标准回归更能引导模型发现“最佳轨迹”。
*   **缝合效应**：模型能够从次优的轨迹片段中“缝合”出比任何单一训练样本都更好的完整路径。

### 7. 优点
*   **创新性结合**：首次将决策 Transformer 与指针网络结合，解决了离线 RL 在变长动作空间组合优化问题中的应用难题。
*   **实用性强**：无需复杂的在线环境模拟，直接利用现有算法数据即可提升性能，降低了 NCO 的落地门槛。
*   **性能卓越**：证明了离线学习不仅能达到专家水平，还能通过策略合成实现“青出于蓝而胜于蓝”。

### 8. 不足与局限
*   **对数据多样性的依赖**：在缺乏多样性的数据（如 NN 算法）上，模型提升空间有限，说明离线学习的效果受限于训练数据的探索程度。
*   **RTG 预测仍显保守**：实验显示手动增加 RTG 偏移量能进一步提升性能，说明模型自带的 RTG 预测机制在预测“极限性能”时仍偏保守。
*   **规模限制**：实验最高仅测试到 $N=100$，对于更大规模（如 $N>1000$）的 TSP 问题，Transformer 的二次方复杂度可能会带来计算瓶颈。

（完）
