---
title: "AutoPDR: Circuit-Aware Solver Configuration Prediction for Hardware Model Checking"
title_zh: AutoPDR：面向硬件模型检测的电路感知求解器配置预测
authors: "Guangyu Hu, Chen Chen, Xiaofeng Zhou, Jiaxi Zhang, Wei Zhang, Hongce Zhang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25048v1"
tags: ["query:ahd"]
score: 9.0
evidence: 使用图学习进行自动求解器配置预测
tldr: "AutoPDR是一个电路感知的求解器配置框架，旨在优化硬件验证中的PDR算法。针对PDR对参数高度敏感且手动调优困难的问题，该研究结合图学习与静态电路特征，利用专家知识过滤了78%的搜索空间，实现了对特定电路最优配置的智能预测。实验证明，该系统在基准测试中显著优于默认配置，为自动化形式验证提供了高效实用的解决方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25048v1/fig-001.webp\", \"caption\": \"TABLE I: PDR Parameter Constraint Rules for Search Space Reduction\", \"page\": 4, \"index\": 1, \"width\": 1032, \"height\": 324}, {\"url\": \"assets/figures/arxiv/2603.25048v1/fig-002.webp\", \"caption\": \"Fig. 7: Performance comparison between AutoPDR’s Top-k predicted configurations and the default configuration. When k > 1, the shortest runtime among the Top-k configurations is used. Points above the diagonal line indicate cases where our predicted configurations outperform the baseline pdr.\", \"page\": 6, \"index\": 2, \"width\": 1062, \"height\": 345}]"
motivation: PDR算法性能受参数配置影响巨大，而传统的手动调优或自动化框架难以满足硬件验证对效率和专业知识的高要求。
method: 提出一种结合图表示学习与静态电路特征的预测框架，通过约束过滤减少搜索空间，并提取电路拓扑的结构与功能模式。
result: 在综合基准测试中，该方法显著提升了验证性能，并能根据电路特征自动识别并选择最合适的求解策略。
conclusion: AutoPDR证明了结合电路感知特征与图学习在自动化形式验证参数调优中的有效性和实用价值。
---

## 摘要
属性定向可达性（PDR）是硬件和软件系统形式化验证的一种强大算法，但其性能对参数配置高度敏感。手动调参耗时且需要领域专业知识，而传统的自动调参框架并不适用于像 PDR 这样对时间敏感的验证任务。本文提出了一种电路感知的求解器配置框架，该框架利用图学习在基于 PDR 的验证中进行智能启发式选择。我们的方法将图表示与静态电路特征相结合，为特定电路预测最优的 PDR 求解配置。我们通过基于约束的参数过滤融入专家先验知识，以消除无效和低效的配置，并减少了 78% 的搜索空间。我们的特征提取流水线捕获了电路拓扑和组件模式的结构、功能及连接特性。在综合基准测试集上的实验评估表明，与默认配置和常用设置相比，性能有显著提升。该系统成功识别了特定电路的参数模式，并根据电路特性自动选择最合适的求解策略，使其成为自动化形式化验证工作流中的实用工具。

## Abstract
Property Directed Reachability (PDR) is a powerful algorithm for formal verification of hardware and software systems, but its performance is highly sensitive to parameter configurations. Manual parameter tuning is time-consuming and requires domain expertise, while traditional automated parameter tuning frameworks are not well-suited for time-sensitive verification tasks like PDR. This paper presents a circuit-aware solver configuration framework that employs graph learning for intelligent heuristic selection in PDR-based verification. Our approach combines graph representations with static circuit features to predict optimal PDR solving configurations for specific circuits. We incorporate expert prior knowledge through constraint-based parameter filtering to eliminate invalid and inefficient configurations and reduce 78% search space. Our feature extraction pipeline captures structural, functional, and connectivity characteristics of circuit topology and component patterns. Experimental evaluation on a comprehensive benchmark suite demonstrates significant performance improvements compared to default configurations and commonly-used settings. The system successfully identifies circuit-specific parameter patterns and automatically selects the most suitable solving strategies based on circuit characteristics, making it a practical tool for automated formal verification workflows.

---

## 论文详细总结（自动生成）

### 论文总结：AutoPDR - 面向硬件模型检测的电路感知求解器配置预测

#### 1. 核心问题与研究动机
*   **核心问题**：属性定向可达性（PDR/IC3）算法在硬件形式化验证中表现卓越，但其性能高度依赖于复杂的参数配置（如泛化策略、抽象机制等）。
*   **研究动机**：
    *   **调参困难**：手动调参需要深厚的领域专家知识且极其耗时。
    *   **搜索空间巨大**：参数组合存在组合爆炸问题，且许多组合在逻辑上是无效或低效的。
    *   **资源消耗高**：PDR 引擎通常消耗大量内存和 CPU，传统的暴力搜索或并行尝试所有配置在实际工业规模电路中不可行。
    *   **电路差异性**：不同结构的电路（如控制密集型 vs. 数据路径型）对最优参数的需求截然不同。

#### 2. 方法论
AutoPDR 提出了一个结合专家知识与深度学习的电路感知框架，包含三个核心组件：
*   **专家知识引导的参数过滤**：
    *   基于 PDR 算法逻辑，制定了一系列蕴含规则（如：若跳过泛化 `-g`，则相关的 `-r`, `-n` 等参数均失效）。
    *   将搜索空间从 512 种组合压缩至 114 种有效配置，减少了 **78%** 的搜索空间。
*   **多维度电路特征提取**：
    *   **静态特征**：提取 11 维向量，涵盖拓扑规模（PI/PO/Latch 数量）、高层功能组件（MUX/XOR/加法器计数）、逻辑深度（最大/平均深度）以及状态空间连接性（寄存器扇出的方差和标准差）。
    *   **图表示学习**：将电路转化为 AIG（与非图），并引入 **影响锥（COI）缩减** 技术以降低图规模。
*   **学习架构与损失函数**：
    *   **模型**：采用 **GraphSAGE** 作为图编码器提取拓扑特征，与静态特征拼接后通过 MLP 预测运行时间。
    *   **秩感知混合损失函数（Rank-Aware Hybrid Loss）**：结合了回归损失（MSE）和成对排序损失（Pairwise Hinge Loss），旨在优化配置之间的相对排序，而非仅仅是绝对时间的预测。

#### 3. 实验设计
*   **数据集**：从 2007 年至 2024 年的硬件模型检测竞赛（HWMCC）基准测试集中筛选。
*   **筛选标准**：剔除简单用例，仅保留解决时间超过 60 秒的非平凡用例（约占总数的 5%）。
*   **Benchmark 与对比方法**：
    *   **Baseline 1**：ABC 求解器的默认 PDR 配置（`pdr`）。
    *   **Baseline 2**：常用的优化配置（`pdr-nct`）。
    *   **评估指标**：解决实例的数量、Kendall’s $\tau$ 和 Spearman’s $\rho$ 排序相关系数。
*   **测试场景**：包含未见过的属性、未见过的电路结构以及具有挑战性的未解决问题。

#### 4. 资源与算力
*   **硬件环境**：
    *   **CPU**: Intel Xeon Platinum 8375C。
    *   **GPU**: **NVIDIA RTX 3090**。
    *   **内存**: 128GB RAM。
*   **软件环境**：Ubuntu 20.04，基于 PyTorch 和 PyTorch Geometric 实现，特征提取部分通过 Rust 接口加速。
*   **运行限制**：每个验证任务设置了 1 小时（3600秒）的墙钟时间限制。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到 COI 预处理在大多数实例上可在 100ms 内完成。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   使用了 192 个极具挑战性的电路作为独立测试集。
    *   对比了 6 种不同的 GNN 架构（GraphSAGE, GCN, GIN 等）以确定最佳编码器。
    *   进行了特征重要性的消融实验（Permutation Importance Test）。
*   **充分性评价**：实验设计较为充分。通过 Top-1、Top-3、Top-5 的预测结果展示了性能的阶梯式提升；涵盖了近 17 年的竞赛数据集，确保了电路类型的多样性；排序相关系数的引入证明了模型在配置推荐上的有效性。

#### 6. 主要结论与发现
*   **性能飞跃**：AutoPDR 的 Top-5 配置相比默认配置多解决了 **90.0%** 的实例，相比常用优化配置（`pdr-nct`）提升了 **33.3%**。
*   **架构优选**：在电路图表征任务中，**GraphSAGE** 的表现优于 GCN 和 GIN，证明了其在捕获电路局部模式和全局属性方面的优势。
*   **关键特征**：实验发现 **寄存器扇出的标准差（Flop Std）** 和 **平均逻辑能级（Avg Lvl）** 是影响 PDR 性能最关键的电路特征。

#### 7. 优点
*   **电路感知性强**：不仅考虑了图拓扑，还结合了硬件特有的功能组件（如加法器、MUX）信息。
*   **搜索空间优化**：利用专家经验进行硬约束过滤，极大地提高了学习效率和实用性。
*   **排序优化导向**：损失函数的设计直击“寻找最优配置”这一核心目标，而非盲目追求回归精度。
*   **实用性高**：引入 COI 缩减技术，使得模型能够处理工业级规模的电路图。

#### 8. 不足与局限
*   **泛化范围限制**：目前主要针对 ABC 工具中的 PDR 实现，尚未验证在其他模型检测工具（如 Cadence Jasper 或 Avery）上的通用性。
*   **静态预测局限**：模型基于静态特征进行预测，无法捕捉验证过程中的动态反馈（如 SAT 求解器的冲突趋势）。
*   **冷启动开销**：虽然推理很快，但对于新电路仍需进行特征提取和图构建，对于极小规模电路可能存在额外开销。
*   **参数覆盖**：虽然涵盖了 9 个核心参数，但 PDR 还有更多细粒度的启发式开关未被纳入考虑。

（完）
