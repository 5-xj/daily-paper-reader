---
title: "SciDesignBench: Benchmarking and Improving Language Models for Scientific Inverse Design"
title_zh: SciDesignBench：科学逆向设计的语言模型基准测试与改进
authors: "David van Dijk, Ivan Vrkic"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12724v1"
tags: ["query:llm-ec"]
score: 8.0
evidence: 使用语言模型搜索组合设计空间
tldr: 本研究针对科学逆向设计难题，提出了包含14个领域、520个任务的基准测试SciDesignBench。通过评估多种大模型在单次设计、反馈优化等场景下的表现，发现现有模型成功率较低。为此，研究者提出了RLSF训练方法，显著提升了模型在模拟器反馈下的设计能力，为科学推理和自动化设计提供了新路径。
selection_source: fresh_fetch
motivation: 科学逆向设计在寻找满足特定目标的输入时面临巨大的组合空间搜索挑战，且缺乏系统性的评估基准。
method: 提出了SciDesignBench基准测试，涵盖多种设计场景，并引入了基于模拟器反馈的强化学习训练方法（RLSF）。
result: "现有最强模型在零样本任务中成功率仅为29%，而RLSF微调后的8B模型在多个领域将单轮成功率提升了8-17个百分点。"
conclusion: 模拟器反馈下的逆向设计不仅是衡量科学推理能力的有效基准，也是将昂贵的测试时计算转化为模型权重的实用手段。
---

## 摘要
科学和工程领域中许多最重要的问题都是逆向问题：即在给定预期结果的情况下，寻找能够实现该结果的设计。评估候选方案是否符合规范通常是常规操作；例如可以计算结合能、模拟反应器产率或预测药物代谢动力学特征。但在组合设计空间中搜索满足这些目标的输入在本质上更具挑战性。我们推出了 SciDesignBench，这是一个包含 14 个科学领域、520 个基于模拟器的任务基准，涵盖了单次设计、短时域反馈、长时域优化以及种子设计优化五种设置。在包含 10 个领域的共享核心子集上，尽管解析率显著更高，但表现最好的零样本模型成功率仅为 29.0%。模拟器反馈有所帮助，但排行榜随步长（horizon）而变化：Sonnet 4.5 在单轮从头设计（de novo design）中表现最强，而 Opus 4.6 在经过 20 轮基于模拟器的优化后表现最强。提供初始种子设计再次改变了排行榜，这表明受限修改需要一种与不受限从头生成本质上不同的能力。随后，我们提出了 RLSF，一种基于模拟器反馈的训练方案。经过 RLSF 微调的 8B 模型在三个领域中将单轮成功率提高了 8-17 个百分点。总之，这些结果表明，基于模拟器的逆向设计既可以作为科学推理的基准，也可以作为将昂贵的测试时计算摊销到模型权重中的实用基础。

## Abstract
Many of the most important problems in science and engineering are inverse problems: given a desired outcome, find a design that achieves it. Evaluating whether a candidate meets the spec is often routine; a binding energy can be computed, a reactor yield simulated, a pharmacokinetic profile predicted. But searching a combinatorial design space for inputs that satisfy those targets is fundamentally harder. We introduce SciDesignBench, a benchmark of 520 simulator-grounded tasks across 14 scientific domains and five settings spanning single-shot design, short-horizon feedback, long-horizon refinement, and seed-design optimization. On the 10-domain shared-core subset, the best zero-shot model reaches only 29.0% success despite substantially higher parse rates. Simulator feedback helps, but the leaderboard changes with horizon: Sonnet 4.5 is strongest in one-turn de novo design, whereas Opus 4.6 is strongest after 20 turns of simulator-grounded refinement. Providing a starting seed design reshuffles the leaderboard again, demonstrating that constrained modification requires a fundamentally different capability from unconstrained de novo generation. We then introduce RLSF, a simulator-feedback training recipe. An RLSF-tuned 8B model raises single-turn success rates by 8-17 percentage points across three domains. Together, these results position simulator-grounded inverse design as both a benchmark for scientific reasoning and a practical substrate for amortizing expensive test-time compute into model weights.

---

## 论文详细总结（自动生成）

### 论文总结：SciDesignBench —— 科学逆向设计的语言模型基准测试与改进

#### 1. 核心问题与整体含义
*   **研究背景**：科学与工程中的许多核心挑战属于“逆向问题”（Inverse Problems），即已知目标结果，寻找能实现该结果的设计输入（如设计特定药代动力学特征的药物、特定转化率的反应器等）。
*   **核心问题**：现有的科学大模型（LLM）评估主要集中在知识检索、研究推理或数学解题，缺乏对“结构化设计生成”及“基于科学模拟器（Oracle）验证”能力的测试。
*   **研究动机**：验证前沿模型是否能真正解决科学逆向设计难题，并探索是否能利用模拟器反馈来训练更强的模型。

#### 2. 方法论
*   **SciDesignBench 基准测试**：
    *   包含 14 个科学领域（如药物设计、生物学、物理、工程等），共 520 个任务。
    *   任务分为“从头设计”（De Novo）和“种子优化”（Optimization）两类。
    *   引入四级难度（L1-L4），从单目标到多目标、从宽容差到严容差。
*   **RLSF（基于模拟器反馈的强化学习）训练方案**：
    *   **第一阶段：SFT（监督微调）**：使用 QLoRA 在模拟器生成的（目标, 设计）对上微调，建立格式规范和领域先验。
    *   **第二阶段：GRPO（组相对策略优化）**：将模拟器作为环境提供奖励信号。
    *   **奖励函数**：基于模拟结果与目标之间的距离，辅以可行性（JSON 格式）和简洁性奖励。
    *   **思维链（CoT）**：允许模型在输出 JSON 设计前进行自由推理，通过强化学习优化推理路径。

#### 3. 实验设计
*   **数据集/场景**：涵盖 14 个领域，包括 PK/PD 剂量设计、分子对接（Docking）、代谢通量分析（FBA）、量子电路合成、PID 控制器调优等。
*   **对比方法**：评估了 7 个前沿模型：GPT-5.2、Claude Opus 4.6、Claude Sonnet 4.5/4.6、Gemini 3.1 Pro、Gemini 2.0 Flash 和 GPT-4o。
*   **评估模式**：
    1.  1 轮从头设计（1-turn de novo）
    2.  5 轮反馈（5-turn feedback）
    3.  20 轮长时域细化（20-turn refinement）
    4.  1 轮优化（1-turn optimization）
    5.  20 轮长时域优化（20-turn optimization）

#### 4. 资源与算力
*   **训练算力**：
    *   **ADMET 优化**：使用 4 张 H100 GPU，训练约 2.5 小时（800 步）。
    *   **PK/PD 剂量设计**：使用 8 张 A100 GPU，训练约 12 小时（800 步）。
    *   **分子对接**：使用了物理引擎 AutoDock Vina，单次评估需 5-10 秒，属于计算密集型。
*   **推理评估**：由于涉及 7 个模型、5 种设置、520 个任务及最高 20 轮迭代，计算成本极高，因此每项任务仅尝试 K=3 次。

#### 5. 实验数量与充分性
*   **实验规模**：总计 520 个任务，覆盖 14 个领域，实验设计较为全面。
*   **消融实验**：对学习率调度、组大小（Group Size）、SFT 类型（专家数据 vs 多领域数据）以及防过拟合措施进行了消融研究。
*   **客观性与公平性**：
    *   使用了“共享核心”（Shared-core）子集进行模型间公平对比。
    *   训练集与测试集在种子（Seed）和目标值上严格分离。
    *   **局限性**：由于成本限制，K=3 的采样次数较少，可能存在一定的随机性偏差。

#### 6. 主要结论与发现
*   **零样本设计极具挑战**：最强模型在 10 领域核心测试中的成功率仅为 29.0%，远低于其 JSON 解析率，说明失败主因是科学逻辑而非格式。
*   **长时域能力是独立维度**：Sonnet 4.5 在单轮设计中最强，但 Opus 4.6 在 20 轮反馈细化后反超，说明“单次生成”与“利用反馈迭代”是不同的能力。
*   **优化不等于从头设计**：提供初始种子设计会改变模型排名，且有时反而会因陷入局部最优而降低性能。
*   **RLSF 效果显著**：通过模拟器反馈训练，8B 规模的小模型在特定领域（如 ADMET、PK/PD、对接）的成功率提升了 8-17 个百分点，甚至在某些指标上超越了前沿大模型。

#### 7. 优点
*   **闭环验证**：不同于以往的文本基准，该基准要求生成可执行的设计，并由科学模拟器给出客观评分。
*   **多维度评估**：区分了从头设计与优化、短时域与长时域反馈，揭示了模型能力的多样性。
*   **训练范式创新**：RLSF 证明了可以将昂贵的模拟器计算在离线阶段“摊销”到模型权重中，实现高效的单次推理。

#### 8. 不足与局限
*   **模拟器保真度**：使用了简化的科学模型（如线性基因网络、理想反应器），可能无法完全代表复杂的现实物理世界。
*   **领域覆盖**：虽然涵盖 14 个领域，但仍缺少气候建模、蛋白质折叠、电子电路等重要科学领域。
*   **奖励黑客（Reward Hacking）风险**：在基于经验数据的预测器（如 ADMET）上进行强化学习时，模型可能会利用预测器的漏洞而非学习真实的物理规律。
*   **模型规模限制**：RLSF 仅在 8B 模型上做了概念验证，尚未在大规模模型上验证其扩展性。

（完）
