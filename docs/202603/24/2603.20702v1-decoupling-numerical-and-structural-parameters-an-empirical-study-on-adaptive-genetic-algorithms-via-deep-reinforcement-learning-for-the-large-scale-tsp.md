---
title: "Decoupling Numerical and Structural Parameters: An Empirical Study on Adaptive Genetic Algorithms via Deep Reinforcement Learning for the Large-Scale TSP"
title_zh: 解耦数值参数与结构参数：基于深度强化学习的自适应遗传算法在大规模旅行商问题中的实证研究
authors: "Hongyu Wang, Yuhan Jing, Yibing Shi, Enjin Zhou, Haotian Zhang, Jialong Shi"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20702v1"
tags: ["query:llm-ec"]
score: 8.0
evidence: 通过深度强化学习实现的大规模TSP自适应遗传算法
tldr: "本研究探讨了演化算法中参数配置对可扩展性的影响，将控制变量分为数值参数（如交叉率）和结构参数（如种群规模）。通过构建基于Recurrent PPO的双层深度强化学习框架，动态调节遗传算法参数以解决大规模旅行商问题（TSP）。实验表明，该方法在大型实例上将最优性差距降低了约45%。研究揭示了结构灵活性在防止算法停滞和跳出局部最优方面起决定性作用，为自动化算法设计提供了新方向。"
selection_source: fresh_fetch
motivation: 旨在区分并量化数值参数与结构参数在演化算法应对大规模组合优化问题时的不同贡献。
method: 利用双层深度强化学习框架（Recurrent PPO）作为探测工具，动态解耦并调节遗传算法的控制变量。
result: "学习到的策略在rl5915等大规模TSP实例上显著优于静态基准，最优性差距缩减达45%。"
conclusion: 结构参数的动态重构比细微的数值概率调整更能有效防止算法陷入局部最优，应成为未来算法设计的重点。
---

## 摘要
合理的参数配置是进化算法（EAs）成功的先决条件。尽管已经提出了各种自适应策略，但所有控制维度是否对算法的可扩展性具有同等贡献仍是一个悬而未决的问题。为了研究这一点，我们将控制变量分为数值参数（如交叉率和变异率）和结构参数（如种群规模和算子切换），并假设它们发挥着不同的作用。本文提出了一项实证研究，利用双层深度强化学习（DRL）框架来解耦并分析这两个维度对旅行商问题（TSP）的影响。我们采用循环 PPO（Recurrent PPO）智能体来动态调节这些参数，将 DRL 模型视为揭示进化动力学的探针。实验结果证实了该方法的有效性：学习到的策略优于静态基准，在最大的测试实例（rl5915）上将最优性差距缩小了约 45%。在此验证框架的基础上，我们的消融分析揭示了一个基本见解：虽然数值微调提供了局部细化，但结构可塑性是防止停滞并促进跳出局部最优的关键因素。这些发现表明，未来的自动化算法设计应优先考虑动态结构重构，而非细粒度的概率调整。为便于复现，源代码可在 https://github.com/StarDream1314/DRLGA-TSP 获取。

## Abstract
Proper parameter configuration is a prerequisite for the success of Evolutionary Algorithms (EAs). While various adaptive strategies have been proposed, it remains an open question whether all control dimensions contribute equally to algorithmic scalability. To investigate this, we categorize control variables into numerical parameters (e.g., crossover and mutation rates) and structural parameters (e.g., population size and operator switching), hypothesizing that they play distinct roles. This paper presents an empirical study utilizing a dual-level Deep Reinforcement Learning (DRL) framework to decouple and analyze the impact of these two dimensions on the Traveling Salesman Problem (TSP). We employ a Recurrent PPO agent to dynamically regulate these parameters, treating the DRL model as a probe to reveal evolutionary dynamics. Experimental results confirm the effectiveness of this approach: the learned policies outperform static baselines, reducing the optimality gap by approximately 45% on the largest tested instance (rl5915). Building on this validated framework, our ablation analysis reveals a fundamental insight: while numerical tuning offers local refinement, structural plasticity is the decisive factor in preventing stagnation and facilitating escape from local optima. These findings suggest that future automated algorithm design should prioritize dynamic structural reconfiguration over fine-grained probability adjustment. To facilitate reproducibility, the source code is available at https://github.com/StarDream1314/DRLGA-TSP

---

## 论文详细总结（自动生成）

这篇论文对基于深度强化学习（DRL）的自适应遗传算法（GA）在解决大规模旅行商问题（TSP）中的参数控制进行了深入的实证研究。以下是该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在演化算法（EAs）中，不同的控制维度（参数）对算法可扩展性的贡献是否相等？
*   **研究动机**：传统的自适应参数控制主要集中在“数值参数”（如交叉率、变异率）的微调，而忽略了“结构参数”（如种群规模、算子切换）的动态调整。作者假设结构可塑性在处理大规模复杂问题时比数值微调更为关键。
*   **背景**：随着问题规模扩大，固定结构的算法容易陷入局部最优，而深度强化学习为动态配置算法提供了强大的建模能力。

### 2. 方法论
*   **核心思想**：提出一种双层控制框架 **DRLGA**，将 DRL 智能体作为高层决策者（Controller），将遗传算法作为底层执行者（Worker），实现数值与结构参数的解耦控制。
*   **关键技术细节**：
    *   **POMDP 建模**：将参数控制建模为部分可观测马尔可夫决策过程。
    *   **Recurrent PPO**：采用集成 LSTM 的循环近端策略优化算法，以捕捉演化过程中的非马尔可夫特征（如停滞计数、历史改进趋势）。
    *   **状态空间**：包含 4 个静态特征（规模、分布等）和 11 个动态特征（当前 Gap、改进情况、种群方差等）。
    *   **动作空间**：同时输出数值参数（CR、MR）和结构参数（种群规模、三种算子模式、锦标赛规模）。
    *   **算子模式**：设计了三种模式（Mode 0: 初始探索；Mode 1: 强化探索；Mode 2: 细粒度收敛），结合了 Inversion、Double-Bridge 和 2-Opt 等操作。
    *   **课程学习**：采用从简单（50城市）到复杂（1200城市）的在线课程学习策略进行训练。

### 3. 实验设计
*   **数据集**：使用 TSPLIB 中的大规模实例，包括 `fl1577`、`u2152`、`pcb3038`、`fnl4461` 和最大的 `rl5915`。
*   **Benchmark（基准）**：高度优化的**静态遗传算法（Static GA）**，使用固定的经验最优参数。
*   **对比实验**：
    1.  **性能对比**：DRLGA 与 Static GA 在大规模实例上的 Gap 和推理时间对比。
    2.  **消融实验**：分别固定交叉率（Fixed CR）、变异率（Fixed MR）、算子模式（Fixed Op）和种群规模（Fixed Pop），以验证各维度的重要性。

### 4. 资源与算力
*   **硬件环境**：使用高性能节点，配备 64 核 AMD EPYC 7452 CPU (@2.35GHz)、DDR4 内存和 InfiniBand 高速互连。
*   **训练细节**：在 64 个并行环境中训练 500 万个时间步（timesteps），使用 Adam 优化器。
*   **算力说明**：文中强调了计算密集型的训练过程，但未明确提及是否使用了 GPU（PPO 训练通常在 CPU/GPU 混合环境下进行，但文中重点描述了 CPU 核心数）。

### 5. 实验数量与充分性
*   **实验规模**：针对性能对比测试了 5 个超大规模实例，针对消融实验测试了 5 个不同规模（100 到 1002 城市）的实例。
*   **重复性**：每组实验独立重复运行 **30 次**，报告平均指标，以消除随机性影响。
*   **充分性**：实验涵盖了从 100 到近 6000 城市的跨度，验证了算法的 **Zero-Shot（零样本）泛化能力**（即在 1200 规模训练，直接在 5915 规模测试），实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能提升**：DRLGA 显著优于静态基准，在最大实例 `rl5915` 上将最优性差距（Optimality Gap）降低了约 **45%**。
*   **核心发现**：**结构可塑性是决定性因素**。消融实验表明，固定算子模式会导致性能剧烈下降，而固定数值参数（如变异率）的影响相对较小。
*   **智能行为**：DRL 智能体学会了“逆退火”行为，即在演化后期增加变异率以强制注入熵，从而打破大规模问题中的停滞状态。

### 7. 亮点
*   **解耦视角**：首次明确区分并量化了数值参数与结构参数在自适应演化中的不同作用。
*   **强泛化性**：展示了极强的规模泛化能力（Train Small, Solve Large），证明学习到的是通用的演化控制逻辑而非特定问题的解。
*   **工程实现**：利用 Numba JIT 加速底层 GA，保证了实验在大规模 TSP 上的可行性。

### 8. 不足与局限
*   **计算开销**：DRLGA 的推理时间约为静态 GA 的 **2 倍**，这在对实时性要求极高的场景下可能是一个限制。
*   **对比范围**：主要与静态 GA 对比，未广泛对比其他最先进的神经组合优化（NCO）模型或专门的 TSP 求解器（如 LKH3）。
*   **问题单一性**：目前仅在 TSP 上进行了验证，虽然提到了未来会扩展到 VRP 等约束问题，但目前尚不确定该结论在其他类型优化问题中的普适性。

（完）
