---
title: "FormalEvolve: Neuro-Symbolic Evolutionary Search for Diverse and Prover-Effective Autoformalization"
title_zh: FormalEvolve：面向多样化且对证明器有效的自动形式化的神经符号进化搜索
authors: "Haijian Lu, Wei Wang, Jing Liu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19828v2"
tags: ["query:llm-ec"]
score: 9.0
evidence: 结合LLM驱动变异与交叉的神经符号演化框架
tldr: 针对自动形式化中语义一致性不代表证明有效性的问题，本文提出FormalEvolve框架。该框架将自动形式化视为受限的测试时搜索任务，结合大模型驱动的变异、交叉与符号化AST重写，通过进化算法生成多样且易于证明的形式化代码。在CombiBench和ProofNet上，该方法显著提升了语义命中率和下游证明性能，并增强了生成结果的多样性。
selection_source: fresh_fetch
motivation: 现有的自动形式化方法即使语义正确，也往往因结构问题导致自动证明器难以处理，且生成结果缺乏多样性。
method: 提出一种受编译约束的神经符号进化框架，结合LLM驱动的变异交叉与符号化AST重写操作来搜索最优形式化表达。
result: "在CombiBench和ProofNet上分别达到58.0%和84.9%的语义命中率，并有效降低了成功案例的分布集中度。"
conclusion: 实验证明，通过进化搜索提升形式化代码的多样性和结构合理性，能显著增强自动证明器的效率和成功率。
---

## 摘要
自动形式化旨在将自然语言数学翻译为可编译、机器可检查的陈述。然而，语义一致性并不等同于证明器的有效性：即使是语义一致的形式化，在证明搜索成本和成功率方面也可能存在显著差异。在这项工作中，我们将自动形式化建模为在预算限制下对语义一致性库的测试时搜索，并提出了 FormalEvolve，一个由编译门控的神经符号进化框架。FormalEvolve 通过大语言模型（LLM）驱动的变异和交叉（结合有界补丁修复）生成多样化的候选方案，同时符号抽象语法树（AST）重写操作进一步注入了结构多样性。在 CombiBench 和 ProofNet 数据集上，在 T = 100 的严格生成器调用预算下，FormalEvolve 的语义命中率（SH@100）分别达到 58.0% 和 84.9%，并降低了语义成功在不同问题间的集中度（即更低的基尼系数）。在固定的证明器预算下，FormalEvolve 还提升了 CombiBench 上的下游证明性能。代码将公开发布。

## Abstract
Autoformalization aims to translate natural-language mathematics into compilable, machine-checkable statements. However, semantic consistency does not imply prover effectiveness: even semantically consistent formalizations can differ substantially in proof-search cost and success rate. In this work, we formulate autoformalization as a budgeted, test-time search for semantically consistent repertoires, and propose FormalEvolve, a compilation-gated neuro-symbolic evolutionary framework. FormalEvolve generates diverse candidates via LLM-driven mutation and crossover with bounded patch repair, while symbolic Abstract Syntax Tree (AST) rewrite operations further inject structural diversity. On CombiBench and ProofNet, under a strict generator-call budget of T = 100, FormalEvolve reaches semantic hit rates (SH@100) of 58.0% and 84.9%, and reduces cross-problem concentration of semantic successes(lower Gini). Under a fixed prover budget, FormalEvolve also improves downstream proving performance on CombiBench. Code will be released publicly.

---

## 论文详细总结（自动生成）

以下是对论文《FormalEvolve: Neuro-Symbolic Evolutionary Search for Diverse and Prover-Effective Autoformalization》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：自动形式化（将自然语言数学翻译为机器可检查的代码，如 Lean 4）中存在“语义一致性不等于证明有效性”的问题。即使翻译在语义上正确，不同的表述方式也会显著影响自动证明器（Prover）的搜索成本和成功率。
*   **研究动机**：现有的方法往往只追求单一的最优输出，且容易在简单问题上过度堆叠采样，而在困难问题上缺乏突破。作者认为需要构建一个**多样化的候选库（Repertoire）**，通过测试时搜索（Test-time Search）来对冲证明器的脆弱性。

### 2. 方法论
FormalEvolve 是一个受编译门控的神经符号进化框架，其核心流程如下：
*   **核心思想**：将自动形式化建模为在有限生成预算（T）下的库搜索任务，结合 LLM 的生成能力与符号化的 AST（抽象语法树）变换。
*   **关键技术细节**：
    *   **编译门控存档（Compilation-gated Archive）**：维护一个仅包含可编译代码的候选池，并使用“岛屿模型（Island Model）”保持种群多样性。
    *   **进化算子**：
        *   **LLM 驱动的变异与交叉**：包括全量重写（Full Patching）、局部差异编辑（Diff Patching）和结合多个亲本特征的交叉编辑（Cross Patching）。
        *   **有界补丁修复（Bounded Repair）**：当生成代码编译失败或语义不符时，利用编译器反馈进行有限次数的自动修复，修复过程计入生成预算。
    *   **符号化 EvolAST 算子**：在 LLM 采样陷入僵局时，利用预定义的符号化规则（如交换律、结合律、对偶关系等）对 AST 进行重写。该算子**不消耗 LLM 调用预算**。
    *   **使用惩罚的选择机制**：在选择亲本进行进化时，对已频繁使用的候选者施加惩罚，强制搜索算法探索未知的形式化空间。

### 3. 实验设计
*   **数据集**：
    *   **ProofNet**：涵盖本科水平数学题的 Lean 4 形式化基准。
    *   **CombiBench**：专注于组合数学，具有更强的领域偏移和风格差异。
*   **对比方法（Baselines）**：
    *   **直接采样（Sample）**：无修复、无进化的基准。
    *   **编译修复（Compile Repair）**：仅包含语法修复。
    *   **语义修复（Compile+Semantic Repair）**：包含语法和基于判别器反馈的语义修复。
    *   **混合控制组（Hybrid Control）**：使用与 FormalEvolve 相同的强力模型（Qwen3）进行修复，但不使用进化搜索。
*   **评估指标**：语义命中率（SH@T）、编译命中率（CH@T）、基尼系数（Gini，衡量成功案例的分布均匀度）以及下游证明性能（pass@k, theorem-complete@k）。

### 4. 资源与算力
*   **模型使用**：
    *   **种子模型**：Kimina-Autoformalizer-7B（用于初始化）。
    *   **补丁/修复模型**：Qwen3-30B-A3B。
    *   **证明器**：Goedel-Prover-V2-32B。
    *   **语义判别器**：CriticLean-Qwen3-14B。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到使用了 Kimina Lean Server 进行大规模并行验证，且实验是在严格的“生成器调用次数（T=100）”预算下进行的。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流数据集上进行了完整测试（ProofNet 186 题，CombiBench 100 题），每道题分配 100 次 LLM 调用预算。
*   **消融实验**：针对 EvolAST 算子、补丁修复机制、岛屿数量等关键组件进行了消融分析。
*   **充分性与公平性**：实验设计较为客观。通过“严格预算会计（Strict Accounting）”确保所有对比方法在相同的 LLM 调用次数下进行，避免了因算力堆叠导致的性能虚高。同时引入基尼系数分析，证明了方法在解决困难问题上的普适性。

### 6. 主要结论与发现
*   **性能提升**：在 T=100 预算下，FormalEvolve 在 CombiBench 上的语义命中率从 46.0% 提升至 58.0%，在 ProofNet 上从 78.0% 提升至 84.9%。
*   **分布更均匀**：相比基准方法，FormalEvolve 的基尼系数更低，说明它不仅在简单题目上表现好，还能有效覆盖更多中等难度的题目。
*   **证明有效性**：生成的候选库显著提升了下游证明器的成功率，尤其是在 CombiBench 上，定理证明完成数从 8/100 提升至 13/100。
*   **多样性的价值**：实验证明，即使是语义一致的陈述，多样化的结构表述也能显著对冲证明器的失败风险。

### 7. 优点与亮点
*   **神经符号结合**：巧妙地将 LLM 的灵活性与符号化 AST 重写的严谨性结合，且 EvolAST 算子不占预算，极具性价比。
*   **预算意识（Budget-aware）**：研究设定在严格的调用预算内，更符合实际应用中对推理成本的控制需求。
*   **关注分布质量**：不仅关注最高分，还通过基尼系数关注解决问题的广度，这在自动形式化研究中较为新颖。

### 8. 不足与局限
*   **判别器依赖**：语义一致性的判断依赖于 LLM Judge（CriticLean），虽然经过验证，但仍存在误判风险，可能导致搜索方向偏差。
*   **证明器耦合**：虽然提升了证明有效性，但这种有效性可能与特定的证明器（如 Goedel-Prover）强耦合，在其他证明工具上的泛化性有待验证。
*   **基准测试重叠风险**：论文提到强力模型可能在训练中见过部分基准测试数据，虽然通过 CombiBench 缓解了此问题，但潜在的数据污染风险依然存在。

（完）
