---
title: Improving Coherence and Persistence in Agentic AI for System Optimization
title_zh: 提升用于系统优化的智能体 AI 的连贯性与持久性
authors: "Pantea Karimi, Kimia Noorbakhsh, Mohammad Alizadeh, Hari Balakrishnan"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21321v1"
tags: ["query:llm-ec"]
score: 10.0
evidence: 用于设计高性能系统启发式算法的智能体研究员架构
tldr: 针对LLM在系统优化中面临的局部最优陷阱和长程上下文退化问题，本文提出Engram架构。该架构通过解耦长周期探索与单一上下文窗口，利用持久化存档和研究摘要实现知识积累。实验证明，Engram在多云组播、LLM路由及数据库KV缓存优化等领域表现优异，显著提升了智能体在复杂系统设计中的连贯性与持久性。
selection_source: fresh_fetch
motivation: 现有的LLM智能体在处理复杂系统优化时，容易陷入局部最优且难以在长周期任务中保持上下文连贯性。
method: 提出Engram架构，通过持久化存档代码快照并提炼“研究摘要”，让后续智能体能在全新的上下文窗口中继承并深化先前的研究成果。
result: 在多云组播、LLM推理请求路由和数据库KV缓存重用等多个领域，Engram均展现出超越现有方法的性能。
conclusion: 通过解耦探索过程与上下文限制并引入持久化知识管理，可以有效提升AI智能体在复杂系统启发式算法设计中的创造力和效率。
---

## 摘要
设计高性能系统启发式算法是一个具有创造性的迭代过程，需要专家提出假设并执行多步概念转变。虽然大语言模型（LLMs）在自动化这一循环方面展现出潜力，但由于两种关键的失效模式，它们在处理复杂系统问题时仍面临困难：进化邻域偏差（evolutionary neighborhood bias）和连贯性上限（coherence ceiling）。进化方法通常依赖标量基准分数，容易陷入局部最优，在需要协调的多步变更时往往会失败。相反，现有的智能体框架在长时程下会遭受上下文退化，或者无法在独立运行之间积累知识。我们提出了 Engram，这是一种智能体研究员架构，通过将长时程探索与单一上下文窗口的限制解耦，解决了上述局限性。Engram 将探索组织为一系列智能体，这些智能体迭代地设计、测试并分析机制。在每次运行结束时，智能体将代码快照、日志和结果存储在持久化存档（Archive）中，并将高层建模见解提炼为简洁、持久的研究摘要（Research Digest）。随后的智能体则从全新的上下文窗口开始，通过阅读研究摘要来基于先前的发现进行构建。我们发现 Engram 在多个领域表现出优异的性能，包括多云组播、LLM 推理请求路由，以及优化带有自然语言查询的数据库中的 KV 缓存重用。

## Abstract
Designing high-performance system heuristics is a creative, iterative process requiring experts to form hypotheses and execute multi-step conceptual shifts. While Large Language Models (LLMs) show promise in automating this loop, they struggle with complex system problems due to two critical failure modes: evolutionary neighborhood bias and the coherence ceiling. Evolutionary methods often remain trapped in local optima by relying on scalar benchmark scores, failing when coordinated multi-step changes are required. Conversely, existing agentic frameworks suffer from context degradation over long horizons or fail to accumulate knowledge across independent runs.   We present Engram, an agentic researcher architecture that addresses these limitations by decoupling long-horizon exploration from the constraints of a single context window. Engram organizes exploration into a sequence of agents that iteratively design, test, and analyze mechanisms. At the conclusion of each run, an agent stores code snapshots, logs, and results in a persistent Archive and distills high-level modeling insights into a compact, persistent Research Digest. Subsequent agents then begin with a fresh context window, reading the Research Digest to build on prior discoveries.   We find that Engram exhibits superior performance across diverse domains including multi-cloud multicast, LLM inference request routing, and optimizing KV cache reuse in databases with natural language queries.

---

## 论文详细总结（自动生成）

### 论文总结：提升用于系统优化的智能体 AI 的连贯性与持久性 (Engram)

#### 1. 核心问题与整体含义（研究动机和背景）
设计高性能的系统启发式算法（如网络路由、缓存策略）是一个需要高度创造力和多步逻辑推演的迭代过程。尽管大语言模型（LLM）已被用于自动化这一过程，但现有方法存在两大瓶颈：
*   **进化邻域偏差 (Evolutionary Neighborhood Bias)：** 基于代码进化的系统（如 OpenEvolve）依赖标量分数进行筛选，容易陷入局部最优，难以实现需要多步协调的重大算法架构转变。
*   **连贯性上限 (Coherence Ceiling)：** 现有的智能体框架（如 Glia）在长周期任务中会遇到“上下文腐烂”或注意力不均的问题；而独立的多次运行又无法积累知识，导致重复犯错。
**Engram** 的提出旨在通过解耦长周期探索与单一上下文窗口的限制，实现研究过程的连贯性与知识的持久化。

#### 2. 方法论：核心思想与关键技术
Engram 采用了一种“智能体接力”的研究架构，核心组件包括：
*   **解耦探索与上下文：** 将研究任务分解为一系列连续的智能体。每个智能体在全新的、干净的上下文窗口中启动，避免了长对话导致的性能下降。
*   **持久化研究摘要 (Research Digest)：** 这是一个简洁的外部文档，记录了高层建模见解、失败诊断和后续建议。新智能体通过阅读摘要继承前人的知识。
*   **持久化存档 (Archive)：** 存储所有历史代码快照、执行日志和实验结果，供智能体按需检索。
*   **科学研究循环：** 每个智能体遵循“提出假设 $\rightarrow$ 实现代码 $\rightarrow$ 实验测试 $\rightarrow$ 结果分析”的闭环。
*   **挣扎协议 (Struggle Protocol)：** 在系统提示词中加入特定指令，鼓励智能体在遇到性能下降或报错时不要立即放弃，而是通过增加日志、分析瓶颈等方式坚持探索复杂的优化方向（如从简单启发式转向 MILP 求解器）。

#### 3. 实验设计
论文在三个主要领域及 ADRS 基准测试上进行了评估：
*   **多云组播 (Multi-cloud Multicast)：** 在 71 个节点的拓扑中优化数据传输成本和时间。
*   **LLM 推理请求路由：** 使用 Vidur 模拟器，在分布式 GPU 集群上优化请求的平均响应时间。
*   **数据库 KV 缓存重用 (LLM-SQL)：** 优化自然语言查询下的前缀缓存命中率。
*   **对比方法：** 包括人类专家设计的 SOTA 算法、EoH (Evolution of Heuristics)、FunSearch、OpenEvolve 和 Glia。
*   **基准模型：** 主要使用 OpenAI o3 和 gpt-5.2（注：论文标注时间为 2026 年）。

#### 4. 资源与算力
*   **模拟环境：** LLM 路由实验使用了 4 张 NVIDIA A10 GPU 模拟 Llama-3-8B 的推理负载。
*   **搜索算力：** 论文未明确给出总的 GPU 小时数，但提到每个方法运行 10 次，每次运行包含 100 次模拟评估预算。
*   **模型调用：** 实验涉及大量的 LLM API 调用，以支持数百个智能体步骤的接力。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 9 个不同的系统优化问题（8 个来自 ADRS 基准，1 个 LLM 路由）。
*   **统计严谨性：** 每个实验重复 10 次，并报告了 90% 的自助法（bootstrapped）置信区间。
*   **消融实验：** 针对单一智能体、仅摘要、仅代码传递、无存档等变体进行了详细对比，验证了各组件的有效性。
*   **充分性评价：** 实验设计较为全面，不仅对比了最终得分，还分析了性能随模拟次数变化的收敛曲线，客观展示了 Engram 在长周期任务中的优势。

#### 6. 主要结论与发现
*   **超越人类专家：** Engram 在 9 个任务中的 8 个超过了人类设计的 SOTA 算法。
*   **突破局部最优：** 在多云组播任务中，Engram 是唯一能从简单的贪心算法转向复杂的混合整数线性规划（MILP）或动态规划（DP）方案的架构，而进化算法往往停留在局部改进。
*   **容忍暂时退化：** 实验观察到 Engram 能够忍受中间步骤的性能下降，通过诊断错误最终实现更大的性能跃迁。
*   **知识积累：** 研究摘要显著减少了后续智能体重复失败尝试的概率，提高了探索效率。

#### 7. 优点
*   **架构创新：** 巧妙地解决了 LLM 上下文长度与推理质量之间的矛盾。
*   **定性突破：** 证明了 AI 智能体不仅能做“代码微调”，还能进行“概念转变”和复杂的数学建模。
*   **鲁棒性：** 对提示词（Prompt）的敏感度低于进化算法，在不同提示深度下均表现稳定。

#### 8. 不足与局限
*   **成本问题：** 这种多智能体接力模式涉及大量的 LLM 调用和多次模拟运行，时间与金钱成本较高。
*   **应用限制：** 目前主要在模拟器（Simulator）环境中验证，在真实、高动态且具有随机性的物理系统中的表现尚待观察。
*   **依赖底层模型：** 实验显示性能高度依赖于底层模型的推理能力（如 o3 或更高版本），在较弱的模型上可能无法实现复杂的建模转变。

（完）
