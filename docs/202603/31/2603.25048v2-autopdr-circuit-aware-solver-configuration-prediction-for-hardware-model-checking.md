---
title: "AutoPDR: Circuit-Aware Solver Configuration Prediction for Hardware Model Checking"
title_zh: AutoPDR：面向硬件模型检测的电路感知求解器配置预测
authors: "Guangyu Hu, Chen Chen, Xiaofeng Zhou, Jiaxi Zhang, Wei Zhang, Hongce Zhang"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25048v2"
tags: ["query:ahd"]
score: 9.0
evidence: 自动求解器配置与智能启发式选择
tldr: "本研究提出AutoPDR，一个针对硬件模型检测中PDR算法的电路感知求解器配置框架。针对PDR性能对参数高度敏感且手动调优困难的问题，该框架结合图学习与静态电路特征，通过提取电路拓扑的结构与功能特性来预测最优配置。此外，引入专家先验知识过滤了78%的搜索空间。实验证明，AutoPDR能根据电路特性自动选择最佳策略，显著优于默认配置，提升了形式验证的自动化水平。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25048v2/fig-001.webp\", \"caption\": \"Fig. 7: Performance comparison between AutoPDR’s Top-k predicted configurations and the default configuration. When k > 1, the shortest runtime among the Top-k configurations is used. Points above the diagonal line indicate cases where our predicted configurations outperform the baseline pdr.\", \"page\": 6, \"index\": 1, \"width\": 1062, \"height\": 345}, {\"url\": \"assets/figures/arxiv/2603.25048v2/fig-002.webp\", \"caption\": \"TABLE I: PDR Parameter Constraint Rules for Search Space Reduction\", \"page\": 4, \"index\": 2, \"width\": 1032, \"height\": 324}]"
motivation: PDR算法性能受参数影响巨大，而传统自动调优方法在处理时间敏感的硬件验证任务时效率低下。
method: 结合图表示学习与静态电路特征提取，并利用专家知识约束过滤无效参数空间，实现智能化的求解器配置预测。
result: "该系统成功缩减了78%的参数搜索空间，并在基准测试中表现出显著优于默认配置的验证性能。"
conclusion: AutoPDR证明了结合电路结构特征与机器学习可以有效优化硬件验证流程，是实现高效自动化形式验证的实用工具。
---

## 摘要
属性定向可达性（PDR）是硬件和软件系统形式化验证的一种强大算法，但其性能对参数配置高度敏感。手动调参耗时且需要领域专业知识，而传统的自动调参框架并不适用于像 PDR 这样对时间敏感的验证任务。本文提出了一种电路感知的求解器配置框架，该框架利用图学习在基于 PDR 的验证中进行智能启发式选择。我们的方法将图表示与静态电路特征相结合，为特定电路预测最优的 PDR 求解配置。我们通过基于约束的参数过滤融入专家先验知识，以消除无效和低效的配置，并减少了 78% 的搜索空间。我们的特征提取流水线捕获了电路拓扑和组件模式的结构、功能及连接特性。在综合基准测试集上的实验评估表明，与默认配置和常用设置相比，性能有显著提升。该系统成功识别了特定电路的参数模式，并根据电路特性自动选择最合适的求解策略，使其成为自动化形式化验证工作流中的实用工具。

## Abstract
Property Directed Reachability (PDR) is a powerful algorithm for formal verification of hardware and software systems, but its performance is highly sensitive to parameter configurations. Manual parameter tuning is time-consuming and requires domain expertise, while traditional automated parameter tuning frameworks are not well-suited for time-sensitive verification tasks like PDR. This paper presents a circuit-aware solver configuration framework that employs graph learning for intelligent heuristic selection in PDR-based verification. Our approach combines graph representations with static circuit features to predict optimal PDR solving configurations for specific circuits. We incorporate expert prior knowledge through constraint-based parameter filtering to eliminate invalid and inefficient configurations and reduce 78% search space. Our feature extraction pipeline captures structural, functional, and connectivity characteristics of circuit topology and component patterns. Experimental evaluation on a comprehensive benchmark suite demonstrates significant performance improvements compared to default configurations and commonly-used settings. The system successfully identifies circuit-specific parameter patterns and automatically selects the most suitable solving strategies based on circuit characteristics, making it a practical tool for automated formal verification workflows.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **AutoPDR** 的框架，旨在通过机器学习自动优化硬件模型检测中 PDR（Property Directed Reachability，又称 IC3）算法的参数配置。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：PDR 算法是硬件验证中最有效的算法之一，但其性能高度依赖于启发式参数配置（如泛化策略、抽象机制等）。
*   **研究动机**：
    *   **手动调优难**：需要深厚的领域知识且极其耗时。
    *   **搜索空间大**：参数组合呈爆炸式增长（如 ABC 工具中有 512 种组合），并行穷举会耗尽内存和 CPU 资源。
    *   **电路相关性**：不同结构的电路（如控制密集型 vs. 数据路径型）所需的最优参数截然不同。
    *   **现有方法不足**：传统的自动调优（如随机搜索）忽略了电路的结构信息。

### 2. 方法论
AutoPDR 结合了专家知识与深度学习，核心流程包括：
*   **参数空间过滤**：利用专家先验知识制定蕴含规则（如：若跳过泛化，则相关的子策略均失效）。这成功将搜索空间从 512 种缩减至 114 种（减少了 78%），确保了配置的语义合理性。
*   **电路特征提取（双重表示）**：
    *   **静态特征**：提取 11 维向量，涵盖拓扑规模（PI/PO/Latch 数量）、功能组成（MUX/XOR/加法器计数）、逻辑深度和状态空间连接性（寄存器扇出的方差等）。
    *   **图表示**：将电路转换为 AIG（与非图），并利用 **影响锥（COI）** 算法进行剪枝以减小图规模，便于 GNN 处理。
*   **模型架构**：
    *   使用 **GraphSAGE** 作为编码器提取电路拓扑嵌入。
    *   将拓扑嵌入、静态特征和 PDR 参数向量拼接，输入多层感知机（MLP）预测运行时间。
*   **秩感知混合损失函数（Rank-Aware Hybrid Loss）**：结合了回归损失（MSE）和成对排序损失（Pairwise Hinge Loss）。其核心思想是：准确预测绝对时间是次要的，正确排列不同配置的优劣顺序才是关键。

### 3. 实验设计
*   **数据集**：采用 2007 年至 2024 年硬件模型检测竞赛（HWMCC）的基准测试集。
*   **筛选标准**：剔除了简单用例，仅保留运行时间超过 60 秒的非平凡（Non-trivial）案例。最终评估集包含 192 个电路，涵盖了未见过的属性和电路家族。
*   **对比基准（Benchmark）**：
    *   `pdr`：ABC 工具的默认配置。
    *   `pdr-nct`：工业界常用的优化配置。
    *   AutoPDR 的 Top-1、Top-3、Top-5 预测结果。

### 4. 资源与算力
*   **硬件环境**：Intel Xeon Platinum 8375C CPU，单张 **NVIDIA RTX 3090 GPU**，128GB 内存。
*   **软件实现**：基于 PyTorch 和 PyTorch Geometric，特征提取部分通过 Rust 接口调用 ABC 工具以加速。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到推理过程和 COI 预处理（通常在 100ms 内）非常高效。

### 5. 实验数量与充分性
*   **实验规模**：在 192 个具有挑战性的测试用例上进行了验证。
*   **消融与对比实验**：
    *   对比了 6 种不同的 GNN 架构（GraphSAGE, GCN, GIN 等），验证了 GraphSAGE 的优越性。
    *   进行了特征重要性分析（Permutation Test），证明了寄存器扇出标准差和平均逻辑电平对预测影响最大。
    *   **充分性评价**：实验覆盖了近 20 年的竞赛题目，且专门针对“难解”题目进行测试，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升显著**：AutoPDR 的 Top-5 配置解决的问题数量比默认配置增加了 **90.0%**，比常用优化配置（pdr-nct）增加了 **33.3%**。
*   **泛化能力强**：模型能够有效处理训练集中未见过的电路结构和验证属性。
*   **排序优化有效**：通过 Top-k 策略，系统能以极小的尝试代价（如仅尝试 5 种配置）覆盖最优解。

### 7. 优点
*   **电路感知**：首次深入结合了电路的图结构特征与高层功能统计特征。
*   **搜索空间优化**：通过专家规则大幅缩减无效搜索空间，降低了学习难度和计算开销。
*   **实用性高**：提供了 Top-k 预测机制，非常适合在并行验证环境中使用。

### 8. 不足与局限
*   **参数范围限制**：目前主要针对 ABC 工具中的 PDR 参数，尚未扩展到其他求解器（如 Cadence Jasper 或其他开源工具）的特有参数。
*   **冷启动开销**：虽然推理快，但对于全新的、规模极大的电路，构建图表示和特征提取仍存在一定的初始开销。
*   **动态调整缺失**：目前是静态配置预测，尚未实现验证过程中的动态参数调整（如强化学习方法）。

（完）
