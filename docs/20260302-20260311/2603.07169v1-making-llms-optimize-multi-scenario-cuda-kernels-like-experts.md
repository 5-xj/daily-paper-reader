---
title: Making LLMs Optimize Multi-Scenario CUDA Kernels Like Experts
title_zh: 使大语言模型能够像专家一样优化多场景 CUDA 算子
authors: "Yuxuan Han, Meng-Hao Guo, Zhengning Liu, Wenguang Chen, Shi-Min Hu"
date: 2026-03-07
pdf: "https://arxiv.org/pdf/2603.07169v1"
tags: ["query:ahd"]
score: 9.0
evidence: 利用大模型自动优化 CUDA 内核
tldr: 针对手动优化GPU算子耗时费力且现有LLM优化方法局限于机器学习领域的问题，本文提出了MSKernelBench基准测试集，涵盖科学计算、稀疏矩阵等多种场景。同时开发了CUDAMaster系统，这是一个多智能体、硬件感知的自动优化框架，利用性能分析信息构建完整工具链。实验表明，CUDAMaster在多项任务中显著优于现有方法，部分性能甚至超越了cuBLAS等专家级闭源库。
selection_source: fresh_fetch
motivation: 现有的LLM驱动的GPU内核优化方法主要局限于机器学习算子，缺乏针对科学计算和稀疏矩阵等更广泛场景的通用优化方案和系统评估。
method: 提出了涵盖多场景的基准测试集MSKernelBench，并开发了基于多智能体协作、具备硬件感知能力且能自动利用性能分析信息的优化系统CUDAMaster。
result: "CUDAMaster在多数算子上实现了显著加速，性能比Astra提升约35%，在部分案例中达到或超过了cuBLAS等专家级闭源库的水平。"
conclusion: 本研究证明了通过多智能体系统和硬件反馈，LLM能够像专家一样在多种复杂场景下实现高效的CUDA内核自动优化。
---

## 摘要
手动优化 GPU 算子是一项具有挑战性且耗时的任务。随着大语言模型（LLMs）的快速发展，自动化的 GPU 算子优化正逐渐成为现实。然而，目前的 LLM 驱动的自动化优化方法主要局限于机器学习应用（如 PyTorch 算子优化），而忽视了更广泛的领域，如科学计算中的稀疏矩阵运算。扩展到这些更广泛的应用为基准测试和算法带来了新的挑战。因此，开发一种通用的自动化算子优化方法成为我们的研究重点。在本文中，我们通过引入 MSKernelBench 解决了多场景设置下缺乏系统评估的问题。该基准测试涵盖了多个场景，包括基础代数运算、常见的 LLM 算子、稀疏矩阵算子和科学计算例程，且均支持 FP32 和 BF16 精度。基于该基准测试，我们提出了 CUDAMaster，这是一个多智能体、硬件感知的算子优化系统，它利用性能分析信息并自动构建完整的编译与执行工具链。实验结果表明，CUDAMaster 在大多数算子上实现了显著的加速，性能优于 Astra 约 35%。在某些情况下，其性能甚至达到或超过了高度优化的闭源库（如 cuBLAS）。展示每个算子原始代码和优化代码的 Demo 可在 https://hanyx2021.github.io/MSKernelBenchDemo/ 获取。

## Abstract
Optimizing GPU kernels manually is a challenging and time-consuming task. With the rapid development of LLMs, automated GPU kernel optimization is gradually becoming a tangible reality. However, current LLM-driven automated optimization methods narrowly focus on machine learning applications, such as PyTorch operator optimization, while overlooking broader domains like sparse matrix operations in scientific computing. Extending to these broader applications brings new challenges for the benchmark and algorithm. Therefore, developing a general-purpose automated kernel optimization method becomes our primary focus. In this paper, we address the absence of systematic evaluation for multi-scenario settings by introducing MSKernelBench, which spans multiple scenarios, including fundamental algebraic operations, common LLM kernels, sparse matrix operators, and scientific computing routines, each supporting both FP32 and BF16 precision. Building on this benchmark, we introduce CUDAMaster, a multi-agent, hardware-aware system for kernel optimization that leverages profiling information and automatically constructs the full compilation and execution toolchain. Experimental results demonstrate that CUDAMaster achieves significant speedups across most operators, outperforming Astra by about 35%. In several cases, its performance matches or surpasses that of highly optimized, closed-source libraries such as cuBLAS. A demo showcasing the original and optimized code for each operator is available at https://hanyx2021.github.io/MSKernelBenchDemo/.

---

## 论文详细总结（自动生成）

以下是对论文《Making LLMs Optimize Multi-Scenario CUDA Kernels Like Experts》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：手动优化 GPU 内核（CUDA Kernel）极度依赖专家经验且耗时费力。虽然已有研究尝试利用 LLM 自动优化，但存在两大局限：
    1.  **场景单一**：现有基准（如 KernelBench）过度集中于深度学习算子，忽视了科学计算、稀疏矩阵运算等更广泛的 HPC（高性能计算）领域。
    2.  **反馈不足**：现有方法往往缺乏对底层硬件瓶颈（如计算受限、内存延迟受限等）的深入感知，导致优化策略盲目。
*   **整体含义**：本文旨在通过构建一个多场景基准测试集（MSKernelBench）和一个硬件感知的多智能体系统（CUDAMaster），使 LLM 能够像人类专家一样，基于性能分析数据对各种复杂计算任务进行深度优化。

### 2. 方法论
CUDAMaster 系统由**硬件分析过滤器**和**多智能体协作框架**两部分组成：
*   **硬件分析过滤器（Hardware Analysis Filter）**：
    *   利用 NVIDIA Nsight Compute 收集细粒度的硬件计数器。
    *   **瓶颈分类**：采用 **Otsu 算法**（大津法）自动确定阈值，将算子分为：计算受限（Compute Bound）、内存延迟受限（Memory Latency Bound）和内存带宽受限（Memory Bandwidth Bound）。
    *   **特征提取**：针对不同瓶颈类型，过滤并提取最相关的指标（如 L2 命中率、SM 利用率等），减少 LLM 处理冗余信息的负担。
*   **多智能体框架（CUDAMaster）**：
    *   **Planner Agent**：分析过滤后的硬件 Profile 数据，制定高层优化策略（如循环展开、共享内存分块等）。
    *   **Coder Agent**：根据策略编写 CUDA 代码，并严格遵守预定义的 C 语言包装接口。
    *   **Compiler Agent**：自动生成编译命令和执行脚本，处理复杂的编译器优化选项。
    *   **Debug Agent**：当代码编译失败或运行结果错误时，结合错误日志和内存检查工具进行修复。
*   **算法流程**：系统进行 $R$ 轮迭代优化，每轮包含“规划-编码-编译-执行-反馈”闭环，若结果错误则触发最多 $D$ 轮调试。

### 3. 实验设计
*   **Benchmark (MSKernelBench)**：
    *   包含 **50 个核心任务**，涵盖 9 大类：稠密线性代数、稀疏矩阵、归一化、激活函数、损失函数、LLM 算子序列、张量处理、模板计算（Stencil）、数学算法。
    *   **多精度支持**：每个任务均提供 FP32 和 BF16 版本。
    *   **多尺度评估**：在不同数据规模下测试，并使用**复杂度加权平均加速比**作为最终指标，公式为 $P = \frac{\sum T(N_i) S_i}{\sum T(N_i)}$（其中 $T$ 为理论复杂度，$S$ 为实测加速比）。
*   **对比方法**：
    *   **工业级闭源库**：cuBLAS, cuDNN, cuSPARSE。
    *   **现有 SOTA 框架**：Astra（针对 SGLang 算子优化）。
*   **测试模型**：OpenAI o4-mini 和 DeepSeek-V3.2。

### 4. 资源与算力
*   **硬件环境**：所有内核编译和性能分析均在 **NVIDIA RTX 4090 GPU** 上完成。
*   **算力消耗**：文中未明确给出总训练/推理时长，但详细记录了 API 成本。例如，使用 o4-mini 优化一个任务的平均成本约为 **$0.27**，消耗约 **12.3 万 Token**；DeepSeek-V3.2 的成本则显著更低（约 $0.03）。

### 5. 实验数量与充分性
*   **实验规模**：共进行了 100 个优化任务（50 算子 × 2 精度）。
*   **消融实验**：
    *   测试了不同迭代轮数（$R$）和调试轮数（$D$）的影响。
    *   对比了“无 Profile”、“全量 Profile”与“过滤 Profile”的效果。
*   **充分性评价**：实验设计较为全面，不仅对比了加速比，还分析了硬件瓶颈的转移情况（如从延迟受限转为带宽受限），证明了优化的客观性和科学性。

### 6. 主要结论与发现
*   **性能卓越**：CUDAMaster 在多数算子上实现了显著加速，整体性能比 Astra 提升约 **35%**。
*   **超越专家库**：在点积（Dot Product）、二维卷积（Conv2D）和稀疏矩阵向量乘法（SpMV）等任务中，生成的代码性能**达到甚至超过了 cuBLAS、cuDNN 和 cuSPARSE**。
*   **模型差异**：OpenAI **o4-mini** 在生成正确且高效的代码方面显著优于 DeepSeek-V3.2，尤其是在高加速比阈值下表现更稳健。
*   **过滤策略有效**：硬件指标过滤策略在保持优化效果的同时，减少了约 30%-40% 的 Token 消耗和成本。

### 7. 优点
*   **领域扩展**：填补了非深度学习场景下 CUDA 自动优化的空白，对科学计算具有重要意义。
*   **硬件感知闭环**：通过 Otsu 算法科学地分类硬件瓶颈，使 LLM 的优化建议更具针对性，而非简单的试错。
*   **端到端自动化**：不仅优化代码，还自动处理编译工具链和多尺度验证，实用性强。

### 8. 不足与局限
*   **模型依赖**：系统高度依赖高性能闭源模型（如 o4-mini），在开源模型上的表现仍有提升空间。
*   **单算子局限**：目前主要针对单个算子进行优化，尚未涉及多算子融合（Fusion）在复杂计算图层面的全局优化。
*   **硬件覆盖**：实验主要在 RTX 4090 上进行，对于数据中心级 GPU（如 H100/B200）的特定架构特性（如 Tensor Core 的深度利用）探讨较少。

（完）
