---
title: "Symbolic Discovery of Iterative Algorithms: A Continuous Latent Space Bayesian Optimization Framework"
title_zh: 迭代算法的符号发现：一种连续潜在空间贝叶斯优化框架
authors: "Tongjia Liu, Ilias Mitrai"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01552v1"
tags: ["query:ad"]
score: 9.0
evidence: 使用变分自编码器和贝叶斯优化的符号算法发现
tldr: 本文提出一种自动发现迭代优化算法的符号化方法。将算法发现视为离散优化问题，通过变分自编码器学习更新函数的连续表示，将离散搜索转化为连续搜索，再利用贝叶斯优化探索新算法。在两项案例中成功发现符号形式的更新函数，且计算时间低于传统数学规划方法。该方法无需对函数形式做任何假设，为算法自动设计提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01552v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1782, \"height\": 649, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01552v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 908, \"height\": 545, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01552v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1891, \"height\": 556, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01552v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 913, \"height\": 544, \"label\": \"Figure\"}]"
motivation: 现有算法发现依赖人工设计或数学规划，效率低且受限于假设，亟需自动发现新迭代优化算法的通用框架。
method: 采用变分自编码器将离散更新函数空间映射为连续潜空间，然后利用贝叶斯优化在该连续空间中搜索最优更新函数。
result: 在两个案例中成功发现符号形式的新更新函数，且计算时间显著低于数学规划方法。
conclusion: 提出的连续潜空间贝叶斯优化框架能高效自动发现迭代算法，无需先验假设，为算法设计提供新途径。
---

## 摘要
在本文中，我们考虑了迭代优化算法的自动发现。我们将算法发现任务表述为一个离散优化问题，并使用潜在空间贝叶斯优化搜索新的更新函数。所提出的框架首先使用变分自编码器学习更新函数的离散空间的连续表示，将算法发现任务从离散搜索问题转化为连续搜索问题。随后，该连续表示用于使用贝叶斯优化搜索新算法。应用于两个案例研究表明，所提出的方法可以在不对更新函数形式做任何假设的情况下，以符号形式发现新的更新函数。此外，发现新更新函数所需的计算时间低于现有的基于数学规划的方法。

## Abstract
In this paper, we consider the automated discovery of iterative optimization algorithms. We formulate the algorithm discovery task as a discrete optimization problem and search for new update functions using latent space Bayesian Optimization. The proposed framework first learns a continuous representation of the discrete space of update functions using variational autoencoders, transforming the algorithm discovery task from a discrete to a continuous search problem. The continuous representation is subsequently used to search for new algorithms using Bayesian optimization. Application to two case studies shows that the proposed approach can discover new update functions in symbolic form without any assumptions on the functional form of the update function. Moreover, the computational time required to discover the new update functions is lower than existing mathematical programming-based approaches.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：迭代优化算法中更新函数通常依赖人工设计，耗时且需要领域知识。现有自动化方法（如神经网络参数化、基函数表示、遗传编程、混合整数规划）存在可解释性与可扩展性之间的权衡：黑箱模型难以分析、固定函数形式限制探索空间、随机搜索效率低、数学规划计算成本高。
- **论文动机**：提出一种新的自动发现框架，能够在无先验函数形式假设的条件下，以符号形式（表达式树）发现可解释的迭代更新函数，同时保持样本效率和计算可行性。
- **整体含义**：将算法发现形式化为离散优化问题，通过变分自编码器（VAE）学习连续潜在空间，再利用贝叶斯优化（BO）高效搜索，为自动算法设计提供了一种兼具可解释性与计算效率的新范式。

## 2. 方法论
### 核心思想
- 将更新函数表示为深度为 \( d \) 的完美二叉树，节点分配二元/一元操作符和操作数（变量、梯度、常数）。
- 利用 VAE 将离散的表达式树空间（整数编码）映射为连续潜在空间 \( \mathbb{R}^{N_h} \)。
- 在潜在空间中使用贝叶斯优化搜索最优潜在点，通过解码得到候选更新函数，并评估其计算性能（求解常数优化问题）。

### 关键技术细节
1. **表达式树表示**：
   - 二进制变量 \( y_{on} \) 指示节点 \( n \) 是否分配操作符 \( o \)。
   - 树结构满足 arity 约束（例如：二元操作符只能出现在内部节点，且必须有两个子节点）。
2. **VAE 训练**：
   - 输入：整数编码 \( \hat{\mathbf{y}} \)（每个节点一个整数表示操作符）。
   - 编码器输出均值和方差 \( (\mu, \sigma^2) \)，通过重参数化采样潜在变量 \( \mathbf{z} = \mu + \sigma \odot \epsilon \)。
   - 解码器将 \( \mathbf{z} \) 映射回整数编码，损失函数为重构损失（交叉熵） + KL 散度（乘权重 \( \beta \)）。
3. **算法评估**：
   - 对于固定树结构，求解非线性规划问题，优化常数 \( \mathbf{c} \) 使平均性能（如最后一次迭代梯度范数）最小。
   - 若问题不可行或解码树无效，赋予大惩罚值。
4. **BO 搜索**：
   - 初始化：随机采样若干有效树并评估。
   - 使用高斯过程（Matérn 核）作为代理模型，采24集函数为 Log Expected Improvement（LEI）。
   - 在潜在空间优化采集函数得到新候选点，解码后评估，更新代理模型，循环直至迭代次数达到上限。

## 3. 实验设计
### 场景与数据集
- **Case Study 1**：无约束非凸函数最小化。
  - 两个函数：\( F_1(x) = (x_1-2)^2 + 2x_2^2 - x_1x_2 \)，\( F_2(x) = (x_1-2)^4 + 2(x_1 - 2x_2)^2 \)。
  - 每个函数使用两个随机起始点（固定随机种子）。
- **Case Study 2**：线性回归问题 \( \min 0.5\|Ax - b\|^2 \)。
  - 两个随机生成的 \( (A_i, b_i) \)，条件数分别为 \( \kappa_1 = 16.87 \)，\( \kappa_2 = 7.88 \)。
  - 同样每个函数两个起始点。
- **共同参数**：最大迭代次数 \( N_{it} = 5 \)，性能指标为最后一次迭代的梯度范数。

### Benchmark 与对比方法
- **基准算法**：最速梯度下降（GD），其步长通过优化最小化达到预定收敛阈值的迭代次数获得。
- **对比方法**：数学规划方法（MINLP 模型，从文献[6]继承），使用全局求解器 BARON（3600秒）和SCIP求解。与本文 BO 方法比较计算时间和最终目标值。

## 4. 资源与算力
- 论文**未明确提及使用的 GPU 型号、数量或 GPU 训练**。VAE 训练（500 次迭代，学习率 1e-2）在 CPU 上完成，耗时约 1.66 秒。BO 搜索和常数优化同样使用 CPU（Pyomo + SCIP）。
- 总计算时间：Case 1 为 121 秒（其中评估耗时 112 秒，BO 选择耗时 9 秒）；Case 2 为 176 秒（评估 157 秒，选择 18 秒）。所有实验均可在常规 CPU 上运行。

## 5. 实验数量与充分性
- **实验规模**：仅两个案例，每个案例只有 2 个函数 × 2 个起始点 = 4 个实例。实验数量较少，属于概念验证性质，缺乏大规模多任务验证。
- **消融分析**：对 VAE 潜在空间维度（2–14）进行了敏感性分析，以确定最优维度（设为7）。
- **对比公平性**：与数学规划方法比较时，设置相同的问题实例和性能指标。但未与遗传编程、大语言模型（LLM）等方法对比，也未进行跨问题分布的泛化测试。
- **充分性评价**：实验能证明框架在给定小规模设置下的有效性，但不足以支持广泛适用性。没有进行统计显著性检验或超参数鲁棒性分析。结论基本客观，但需谨慎推广。

## 6. 主要结论与发现
- **Case Study 1**：发现新更新规则 \( x_{i+1} = x_i - 0.035(2.18 + x_i) \nabla F(x_i) \)，具有状态相关的步长。在5次迭代内梯度范数达到 GD 需12次迭代的水平。同时框架重新发现了最速下降法。
- **Case Study 2**：发现规则 \( x_{i+1} = x_i - 0.11 \nabla F(x_i) - 1.61\times10^{-3} x_i \)，在条件数高的第一个问题上优于 GD（目标值 1.657 vs 1.679）。
- **计算效率**：BO 方法在 121 秒内找到比 BARON 在 3600 秒内更好的解；SCIP 找到的解（GD）目标值 2.8，低于 BO 的 2.08。
- **总体**：提出的连续潜在空间 BO 框架能高效自动发现符号化迭代算法，无需先验假设，且可解释性强。

## 7. 优点
- **无需假设函数形式**：表达式树表达覆盖任意功能形式，VAE 学习连续空间使搜索系统化。
- **可解释性**：最终发现的更新函数以符号形式呈现，便于分析收敛性。
- **样本效率**：贝叶斯优化适合昂贵评估（每次评估需全局优化常数），总调用次数少（Case 1 共 31 次评估）。
- **计算时间远低于数学规划**：避免穷举式混合整数搜索，尤其适用于性能函数无闭式表达的场景。
- **方法通用性**：框架表述与具体任务无关，可推广到求解方程组、微分方程等。

## 8. 不足与局限
- **实验覆盖不足**：仅两个案例，函数类型简单，起始点少，无法验证在复杂大规模问题或不同分布上的泛化能力。
- **常数优化开销**：每次评估需全局求解非线性规划（SCIP），仍可能成为瓶颈（文中建议可使用局部优化加速，但未验证）。
- **解码有效性风险**：VAE 解码不能保证得到符合 arity 规则的树（文中称未遇到问题，但潜在风险存在），未采用文献中的约束解码技巧。
- **无理论保证**：BO 的收敛性依赖代理模型和采集函数，未提供算法发现质量的严格保证。
- **对比缺失**：未与遗传编程、LLM 等典型符号发现方法对比，也未进行跨重复实验的统计评估。
- **应用限制**：当前仅考虑无约束优化，需要进一步验证扩展到约束优化、偏微分方程等场景。

（完）
