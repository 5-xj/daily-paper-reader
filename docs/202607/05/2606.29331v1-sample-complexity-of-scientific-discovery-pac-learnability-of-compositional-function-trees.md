---
title: "Sample Complexity of Scientific Discovery: PAC Learnability of Compositional Function Trees"
title_zh: 科学发现的样本复杂度：组合函数树的PAC可学习性
authors: "Şuayp Talha Kocabay, Talha Rüzgar Akkuş, Kerem Yalçın"
date: 2026-06-28
pdf: "https://arxiv.org/pdf/2606.29331v1"
tags: ["query:sr"]
score: 8.0
evidence: 符号回归树的PAC可学习性
tldr: 论文聚焦于科学发现中的符号回归问题，该问题因表达式空间随深度指数增长而被认为统计上不可行。作者从PAC学习框架出发，分析由有限光滑算子（如加法、乘法、正弦、指数）和仿射映射构成的组合函数树的样本复杂度。他们证明Rademacher复杂度不随符号结构数量指数爆炸，而受深度d和算子Lipschitz常数控制；通过有限并集界和Maurer型向量压缩得到风险上界O(L^d/√n)。实验显示泛化差距与理论复杂度项正相关，为符号回归的可学习性提供了理论基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.29331v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 757, \"height\": 558}, {\"url\": \"assets/figures/arxiv/2606.29331v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 758, \"height\": 553}, {\"url\": \"assets/figures/arxiv/2606.29331v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 756, \"height\": 551}, {\"url\": \"assets/figures/arxiv/2606.29331v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 745, \"height\": 400}, {\"url\": \"assets/figures/arxiv/2606.29331v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 756, \"height\": 550}, {\"url\": \"assets/figures/arxiv/2606.29331v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 756, \"height\": 441}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.29331v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 911, \"height\": 459}, {\"url\": \"assets/tables/arxiv/2606.29331v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1783, \"height\": 1076}, {\"url\": \"assets/tables/arxiv/2606.29331v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1053, \"height\": 428}]"
motivation: 现有符号回归常因假设空间随深度组合膨胀被视为统计困难，本文旨在从PAC学习角度证明组合函数树的样本复杂度受深度和Lipschitz常数控制，而非指数爆炸。
method: 利用PAC学习框架分析由有限算子（+、×、sin、exp）和仿射映射构建的组合函数树的Rademacher复杂度，运用有限并集界和Maurer型向量压缩推导风险界。
result: "证明Rademacher复杂度上界为(Kb√2L)^{d-1}乘深度1复杂度，风险界O(L^d/√n)，实验验证泛化差距与复杂度项(L̂^d)/√n正相关。"
conclusion: 该工作为符号回归的统计可行性提供了理论依据，表明深度和算子Lipschitz常数是控制样本复杂度的关键因素。
---

## 摘要
通过符号回归进行科学发现通常被视为统计和计算上难以处理的，因为表达式的假设空间随深度组合增长。本文通过PAC学习的视角重新审视统计方面，重点关注从有限的光滑算子（如$\{+,\times,\sin,\exp\}$和仿射映射）词汇表构建的组合函数树。我们证明了相关的泛化量——Rademacher复杂度，从而超额风险，不一定随不同符号结构的数量呈指数增长，而是受控于(i)深度$d$和(ii)沿组合计算图的基本算子的Lipschitz常数。具体地，在算子的温和Lipschitz条件以及有界仿射叶节点下，对词汇表大小$K=|\mathcal{H}_{\mathrm{base}}|$的有限并界结合Maurer型向量收缩得到$\mathfrak{R}_n(\mathcal{H}_{\mathrm{comp}}^{d}) \leq (Kb\sqrt{2}L)^{d-1}\mathfrak{R}_n(\mathcal{H}_{\mathrm{comp}}^{1})$，其中$b$是arity界；相应的高概率风险界在$K,b=O(1)$且$\mathfrak{R}_n(\mathcal{H}_{\mathrm{comp}}^{1})=O(n^{-1/2})$时按$\mathcal{O}(L^{d}/\sqrt{n})$缩放。我们用一个模块化代码库补充理论，该代码库在控制深度的合成“类物理”目标上训练可微算子树（而非MLP），并显示经验泛化差距与预测的复杂度项$(\widehat{L}^{d})/\sqrt{n}$正相关。

## Abstract
Scientific discovery via symbolic regression is often viewed as statistically and computationally intractable because the hypothesis space of expressions grows combinatorially with depth. This paper revisits the statistical side through the lens of PAC learning, focusing on compositional function trees built from a finite vocabulary of smooth operators (e.g., $\{+,\times,\sin,\exp\}$ and affine maps). We prove that the relevant generalization quantity, Rademacher complexity, hence the excess risk, does not necessarily blow up exponentially with the number of distinct symbolic structures, but is controlled by (i) the depth $d$ and (ii) the Lipschitz constants of the base operators along the composed computation graph. Concretely, under mild Lipschitz conditions on operators and bounded affine leaves, a finite-union bound over a vocabulary of size $K=|\mathcal{H}_{\mathrm{base}}|$ together with Maurer-type vector contraction yields $\mathfrak{R}_n(\mathcal{H}_{\mathrm{comp}}^{d}) \leq (Kb\sqrt{2}L)^{d-1}\mathfrak{R}_n(\mathcal{H}_{\mathrm{comp}}^{1})$ with arity bound $b$; corresponding high-probability risk bounds scale as $\mathcal{O}(L^{d}/\sqrt{n})$ when $K,b=O(1)$ and $\mathfrak{R}_n(\mathcal{H}_{\mathrm{comp}}^{1})=O(n^{-1/2})$. We complement the theory with a modular codebase that trains differentiable operator trees (not MLPs) on synthetic "physics-like" targets of controlled depth and shows that the empirical generalization gap correlates positively with the predicted complexity term $(\widehat{L}^{d})/\sqrt{n}$.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：符号回归（Symbolic Regression, SR）因假设空间（表达式树）随深度组合膨胀而被认为统计和计算上不可行。本文旨在从 PAC 学习理论角度澄清：统计泛化能力并非必然受限于符号结构的指数数量，而是由深度和算子的 Lipschitz 稳定性控制。
- **背景**：科学机器学习需要从少量数据中发现可解释的函数关系，但深度神经网络不产生简洁符号律。符号回归虽然能产出可解释表达式，但其搜索难度常被归咎于“不可学习”。作者将计算困难与统计可学习性分离，为符号回归的统计可行性提供理论根基。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：组合函数树（深度为 \(d\)，词汇表大小 \(K=|\mathcal{H}_{\mathrm{base}}|\)，最大枝数 \(b\)）的 Rademacher 复杂度受算子 Lipschitz 常数 \(L\) 和深度 \(d\) 的乘积控制，而非符号结构数量。
- **关键技术细节**：
  - 定义组合假设空间 \(\mathcal{H}^d_{\mathrm{comp}}\)，递归构建：深度 \(1\) 为基类（含仿射映射），深度 \(d\) 为算子 \(h\in\mathcal{H}_{\mathrm{base}}\) 作用于两个（或多个）深度 \(d-1\) 子树的输出。
  - 假设：算子局部 Lipschitz（\(\forall h, \mathrm{Lip}(h)\leq L\)），仿射叶节点有界（\(\|\theta\|_2\leq W_{\max}\)，\(|\theta_0|\leq B_{\text{leaf}}\)）。
  - 利用 **Maurer 向量收缩不等式**（Lemma 4.4）和 **有限词汇表并集界**（Lemma 4.6）推导出核心式：
    \[
    \mathfrak{R}_n(\mathcal{H}^d_{\mathrm{comp}}) \leq (K b \sqrt{2} L)^{d-1}\,\mathfrak{R}_n(\mathcal{H}^1_{\mathrm{comp}}).
    \]
    其中 \(\mathfrak{R}_n(\mathcal{H}^1_{\mathrm{comp}})=O(n^{-1/2})\) 来自经典 Rademacher 界（仿射类）。
  - 进而得到 PAC 风格风险界（Theorem 4.9）：
    \[
    \text{以概率 }1-\delta,\quad R(f)\leq \widehat{R}_S(f) + \mathcal{O}\left(\frac{B L^d}{\sqrt{n}}\right) + B^2\sqrt{\frac{\log(1/\delta)}{2n}}.
    \]
  - 样本复杂度（Corollary 4.11）：要达到 \(\epsilon\) 超额风险，需 \(n\gtrsim B^2 L^{2d}/\epsilon^2 + (B^4/\epsilon^2)\log(1/\delta)\)，深度影响通过 \(L^{2d}\) 体现而非指数级枚举。
- **流程**：理论推导 → 常数显式界（结合词汇表大小、枝数） → 翻译为样本复杂度 → 实验验证。

## 3. 实验设计：数据集 / 场景、基准、对比方法
- **数据集**：合成“类物理”目标，输入 \(x\sim\mathrm{Unif}([-1,1])\)，输出 \(y=f^\star(x)+\epsilon\)，噪声水平分深度设置（\(d=1,2\) 时 \(\sigma=0.01\)；\(d=3,4\) 时 \(\sigma=0.02\)）。
- **场景**：可实现性（realizability）设置——真实函数 \(f^\star\) 完全位于同一假设族（相同深度、算子词汇表），避免模型错误指定。深度 \(d\in\{1,2,3,4\}\)，每个深度有具体公式（如 \(d=1: wx+b\)；\(d=2: \sin(wx+b_0)+b_1\)；\(d=3: \exp(-(ax+b_0))\cdot\sin(wx+b_1)\)；\(d=4\) 再加线性项）。
- **基准**：无外部对比方法（不比较其他 SR 算法或神经网络）。实验目的是验证理论预测的**定性缩放关系**：泛化差距（test MSE - train MSE）与复杂度项 \((\widehat{L}^d)/\sqrt{n}\) 正相关。
- **对比方法**：无直接对比，只有不同深度、不同样本量下的自身比较。

## 4. 资源与算力
- **文中未明确说明算力详情**：仅提及使用 Python 和 PyTorch 实现，训练使用 Adam 优化器（lr=1e-2, batch size=256, weight decay=0, 最大 5000 epochs，early stopping）。未报告 GPU 型号、数量、总训练时长或总计算量。属于较小规模的实验，理论上单 GPU 可完成。

## 5. 实验数量与充分性
- **实验数量**：深度 \(d\in\{1,2,3,4\}\) × 样本量 \(n\in\{50,100,500,1000,5000\}\) × 20 个随机种子 = 400 个训练模型。每个模型独立训练。
- **充分性**：实验设计聚焦于验证**定性缩放**而非定量精确匹配。主要结果：
  - 图 1/2 展示了泛化差距随 \((b\widehat{L}^d)/\sqrt{n}\) 大致呈线性上包络。
  - 对数-对数 Pearson 相关系数 0.48，幂律拟合 \(R^2\approx 0.23\)，斜率 \(\alpha\approx 0.20\)。
  - 深度越深，\(\widehat{L}\) 越大（尤其含 \(\exp\) 时），差距越大；增加 \(n\) 按 \(1/\sqrt{n}\) 缩小。
- **公平性**：使用可实现性设置，优化充分（early stopping），没有与其他 SR 方法比较，避免了超参数调优偏差，但缺乏真实世界数据或模型误指定场景的验证。实验足够支持理论结论的定性趋势，但定量较弱。

## 6. 论文的主要结论与发现
- **统计可学习性**：组合函数树的泛化由 Lipschitz 稳定性和深度控制，而非符号结构数量，因此在小样本下可能达到低超额风险。
- **显式界**：Rademacher 复杂度上界为 \((Kb\sqrt{2}L)^{d-1} \cdot O(n^{-1/2})\)，风险界 \(\mathcal{O}(L^d/\sqrt{n})\)。
- **实验验证**：泛化差距与 \((\widehat{L}^d)/\sqrt{n}\) 正相关，支持理论定性缩放，表明对于稳定算子和中等深度，科学发现统计可行。
- **局限性**：计算搜索仍是主要挑战；理论界可能宽松。

## 7. 优点：方法或实验设计上的亮点
- **理论清晰**：严格分离计算困难与统计可学习性，首次为符号回归的 PAC 可学习性提供形式化分析。
- **技术融合**：巧妙结合 Maurer 向量收缩、有限并界、递归深度论证，得到简洁乘性界。
- **实验设计简洁**：可实现性设置排除模型错误指定，直接检验统计缩放；使用 Lipshitz 代理 \(\widehat{L}\)（梯度范数）而非最坏情形常数，更贴近实际训练。
- **可复现**：开源代码仓库，依赖版本明确。

## 8. 不足与局限
- **实验覆盖窄**：仅一维输入、均匀分布、可实现的合成数据；缺乏高维输入、真实数据集、模型误指定、分布偏移等场景。
- **Lipschitz 估计不完善**：\(\widehat{L}\) 基于梯度范数，可能低估最坏情形 Lipschitz 常数（尤其 \(\exp\) 函数），且未与全局界比较。
- **理论界可能宽松**：均匀界常数大，实际泛化差距可能更小；实验相关系数仅 0.48，说明还有大量方差未被解释（优化误差、噪声等）。
- **缺乏与现有 SR 方法的对比**：未与遗传编程、深度符号回归等方法比较性能或样本效率。
- **计算资源未报告**：无法评估实验的可复现成本。

（完）
