---
title: "How to Guide LLM Generation: Dual-Surrogate Guided Search for Automated Heuristic Design"
title_zh: 如何指导LLM生成：用于自动启发式设计的双代理引导搜索
authors: "Yuhan Wang, Chaoda Peng, Xingyu Wu, Sheng-Hao Wu, Zhi-Hui Zhan"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13911v1"
tags: ["query:ad"]
score: 9.0
evidence: 提出使用大语言模型的双代理引导搜索用于自动化启发式设计
tldr: 现有LLM自动启发式设计方法在有限预算下依赖固定规则选择父启发式和生成操作，效率受限。提出双代理引导的搜索方法，通过过渡代理预测子代表示分布，实例条件效用代理估计子代预期性能，并采用不确定性感知采集规则选择动作。在多种启发式设计任务中取得竞争性结果，消融实验表明其超越了简单存档排序或固定操作偏好。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13911v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1817, \"height\": 754, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13911v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1556, \"height\": 314, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13911v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 662, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13911v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 703, \"height\": 315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13911v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 657, \"height\": 422, \"label\": \"Table\"}]"
motivation: LLM自动启发式设计中，预生成动作选择依赖规则，未直接建模动作效果，导致搜索效率低。
method: 提出Dual-Surrogate Guided方法，用过渡代理和实例条件效用代理分别预测子代分布和预期性能，结合不确定性感知规则选择操作-父代对。
result: 在多样化启发式设计套件中，与强基线竞争，消融分析显示行为超越简单存档排序或固定操作偏好。
conclusion: 双代理引导可显著提升LLM自动启发式设计的搜索效率，为预生成动作选择提供有效建模。
---

## 摘要
大型语言模型（LLM）通过从任务描述和评估器反馈生成可执行的启发式代码，使得自动化启发式设计（AHD）变得越来越实用。然而，在有限的查询和评估预算下，搜索效率关键取决于预生成决策。在每次LLM查询和黑盒评估之前，系统必须选择哪些存档的启发式作为父代重用，以及哪个生成算子应该转换它们。现有方法通常使用预定义规则选择此类动作，使得每个具体算子-父代动作的预期结果仅被间接建模。因此，我们提出了\emph{\fullmethod{}}（\method{}），一种用于基于LLM的AHD中算子-父代选择的代理引导动作选择模块。\method{}通过用两个互补的代理对预生成动作进行评分来引导LLM代码生成过程。具体地，提出了一个过渡代理来预测由算子-父代动作诱导的子代表示的潜在分布，同时提出了一个实例条件效用代理来估计采样子代潜在因子的预期性能。此外，我们提出了一种不确定性感知的获取规则，结合预测效用、效用不确定性和过渡不确定性来选择下一个LLM生成动作。在一个多样化的启发式设计套件中，\method{}与强LLM-AHD基线竞争，消融和动作选择分析表明其行为超越了简单的存档排序或固定算子偏好。

## Abstract
Large language models (LLMs) have made automated heuristic design (AHD) increasingly practical by generating executable heuristic code from task descriptions and evaluator feedback. Yet under a limited query and evaluation budget, search efficiency depends critically on a pre-generation decision. Before each LLM query and black-box evaluation, the system must choose which archived heuristics to reuse as parents and which generation operator should transform them. Existing methods typically choose such actions with predefined rules, leaving the expected outcome of each concrete operator-parent action only indirectly modeled. Therefore, we propose \emph{\fullmethod{}} (\method{}), a surrogate-guided action-selection module for operator-parent selection in LLM-based AHD. \method{} guides the LLM code-generation process by scoring pre-generation actions with two complementary surrogates. Specifically, a transition surrogate is proposed to predict the latent distribution of the child representation induced by an operator-parent action, while an instance-conditioned utility surrogate is proposed to estimate the expected performance of sampled child latents. Moreover, we propose an uncertainty-aware acquisition rule that combines predicted utility, utility uncertainty, and transition uncertainty to select the next LLM generation action. Across a diverse heuristic-design suite, \method{} is competitive with strong LLM-AHD baselines, and ablation and action-selection analyses suggest that its behavior goes beyond simple archive ranking or fixed operator preferences.

---

## 论文详细总结（自动生成）

# 论文总结：How to Guide LLM Generation: Dual-Surrogate Guided Search for Automated Heuristic Design

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在基于大型语言模型（LLM）的自动启发式设计（AHD）中，LLM 在有限查询和评估预算下生成高效启发式代码。现有方法（如进化搜索、树搜索）通常使用预定义规则（如存档排序、随机选择、固定算子调度）来选择“预生成动作”（即选择哪个父代启发式和哪个生成算子）。这些规则没有直接建模每个具体算子-父代动作的预期效果，导致搜索效率受限。
- **核心问题**：如何将过去的评估数据转化为对下一个 LLM 生成动作的有效指导，从而在有限预算下提高搜索效率。由于在 LLM 查询前子代代码不可用，无法直接应用标准代理模型（如贝叶斯优化）对候选解打分。
- **整体含义**：提出一种可学习的动作选择模块，通过两个互补的代理模型（过渡代理和实例条件效用代理）来评分预生成动作，从而引导 LLM 代码生成过程，提升样本效率。

## 2. 论文提出的方法论

- **核心思想**：Dual-Surrogate Guided Search (DGS) 在每次 LLM 查询前，使用过渡代理和实例条件效用代理联合评估算子-父代动作的预期收益与不确定性，并基于不确定性感知的采集规则选择最优动作。
- **关键技术细节**：
  - **共享潜在表示**：将启发式代码通过 ModernBERT 编码为嵌入，再通过可学习的轻量 MLP 映射到任务自适应潜在空间（公式 3）。
  - **实例条件效用代理**：对每个训练实例学习一个嵌入，通过交互特征（连接、逐元素乘积、绝对差、余弦相似度）将潜在表示与实例嵌入结合，使用 H 个头的集成网络预测每个实例上的效用，并输出均值和不确定性。训练采用 MSE 损失和排名损失（保持实例内偏好顺序），并加入漂移惩罚防止表示剧烈变化。
  - **过渡代理**：给定一个动作（算子 + 父集），输入父代潜在分布的均值和方差以及算子嵌入，通过 MLP 预测子代潜在的高斯分布（均值和方差）。训练使用负对数似然损失。
  - **不确定性感知采集规则**（公式 19）：动作的得分 = 预测子代效用的期望 + βu × 效用不确定性 + βu × λτ × 过渡不确定性。其中过渡不确定性用预测方差的大小衡量。利用蒙特卡洛采样估计期望。
- **算法流程**（Algorithm 1）：
  1. 用 n0 次独立初始化生成初始存档。
  2. 收集 nw 个随机动作的过渡数据。
  3. 循环：更新存档，构建训练集；训练/更新表示、效用集成和过渡代理（交替完整更新和快速更新）；从 top-K 父池枚举可行动作；计算每个动作的采集得分；选择得分最高的动作，执行 LLM 生成和评估，更新存档。

## 3. 实验设计

- **任务与数据集**：涵盖五种启发式设计任务：
  - 旅行商问题（TSP，不同规模）
  - 背包问题（KP）
  - 在线装箱问题（OBP）
  - 可接受集问题（ASP）
  - 容量车辆路径问题结合蚁群优化（CVRP-ACO）
- **基准方法**：FunSearch、EoH、ReEvo、MCTS-AHD。所有方法使用相同 LLM（GPT-4o-mini），搜索预算 200 次生成与评估。
- **对比方式**：每个运行三次，报告平均排名和测试性能。此外进行了控制变量研究、消融研究、离线表示实验、算子使用分析和扩展算子集实验。
- **评价指标**：平均排名（表1）、最佳训练得分（消融表3）、搜索轨迹曲线（图2）等。

## 4. 资源与算力

- 论文未明确说明使用的 GPU 型号、数量或训练时长。仅提到使用 GPT-4o-mini 作为生成 LLM，以及 DGS 本身训练轻量代理（MLP、集成头等）。未提及代理训练所需的计算资源具体细节。

## 5. 实验数量与充分性

- **数量**：主实验在 5 种不同任务上进行，每个任务包含不同设置（如 TSP 多规模）；控制研究在 TSP 上对比 6 种变体；消融研究在 TSP 和 OBP 上对比 9 种变体；额外分析包括离线选择、算子序列分析、扩展算子集等。
- **充分性与公平性**：
  - 对比方法均为近年代表性 LLM-AHD 方法，使用相同 LLM 和预算，较为公平。
  - 控制研究通过固定其他组件、仅改变动作选择方式来验证 DGS 的贡献，设计严谨。
  - 消融研究逐个移除关键组件，分析充分。
  - 局限：实验仅使用一种 LLM（GPT-4o-mini），未测试其他 LLM（如 LLaMA 系列）；任务覆盖有限（5种），但涵盖直接构造启发式和组件式启发式；每次运行仅 3 次重复，统计稳定性略弱。

## 6. 论文的主要结论与发现

- DGS 在 TSP、KP、OBP 上取得最佳平均排名，在 ASP 上与 ReEvo 持平，在 CVRP-ACO 上排名第二（表1）。
- 搜索轨迹显示 DGS 能更快提升最佳存档分数，最终达到更强性能（图2）。
- 控制实验表明：联合学习算子-父代动作比仅学习算子或仅学习父代更好，且优于规则基方法（表2）。
- 消融实验证明：所有组件（排名损失、不确定性系数、漂移惩罚、过渡不确定性、效用集成、交互特征、实例条件、过渡预测）都对最终性能有贡献（表3）。
- 附加分析：DGS 的算子偏好会随搜索阶段自适应变化，而非固定偏好；在扩展算子集下仍然有效。

## 7. 优点

- **方法创新**：将预生成动作选择建模为可学习问题，双代理结构分别处理表示预测和效用评估，是首次在 LLM-AHD 中引入此框架。
- **不确定性处理**：综合效用不确定性和过渡不确定性，平衡探索与利用。
- **实验设计细致**：包含控制实验、消融实验、离线分析、算子分析等，验证了每个设计选择的有效性。
- **通用性**：DGS 可兼容不同算子集，展示了扩展性。
- **表示学习**：通过任务自适应潜在表示和实例条件交互，提升了预测性能。

## 8. 不足与局限

- **计算开销**：DGS 需要训练代理模型，增加了在线计算成本，虽然论文提及交替更新策略控制成本，但与规则基方法相比仍有额外开销。
- **任务覆盖有限**：仅测试五种优化问题，未包含更多实际大规模或黑箱优化问题。
- **LLM 依赖**：仅使用 GPT-4o-mini，未验证在其他 LLM 下的泛化性。
- **超参数敏感性**：多个超参数（K, βu, λτ, MMC 采样数等）需要调整，未提供充分灵敏度分析。
- **统计稳定性**：每次运行仅 3 次重复，可能不足以完全消除随机性。
- **可复现性**：未公开代码和具体训练细节（如学习率、网络结构），降低复现便捷性。

（完）
