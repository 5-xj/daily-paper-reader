---
title: Task Decomposition-Guided Reranking for Adaptive Agent Skill Retrieval
title_zh: 任务分解引导的重排序用于自适应智能体技能检索
authors: "Yanping Chen, Weijie Shi, Wen Yang, Jiajie Xu"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06283v1"
tags: ["query:rerank-train"]
score: 8.0
evidence: 提出技能检索的重排序框架
tldr: 技能库规模增长导致智能体在复杂任务中准确选择技能变得困难，面临语义模糊匹配和任务难度动态影响的问题。提出 SkillReranker，一种推理时重排序框架，通过对任务和技能进行语义分解，构建有向无环执行图，并基于交叉编码器对候选技能进行综合评分，实现自适应技能选择。在 ALFWorld 和 ScienceWorld 上结合三种骨干 LLM 的实验表明，该方法有效提升任务性能、减少交互步骤并降低 token 消耗。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06283v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 813, \"height\": 791}, {\"url\": \"assets/figures/arxiv/2607.06283v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1822, \"height\": 1008}, {\"url\": \"assets/figures/arxiv/2607.06283v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1768, \"height\": 635}, {\"url\": \"assets/figures/arxiv/2607.06283v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 812, \"height\": 1782}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06283v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 864, \"height\": 479}, {\"url\": \"assets/tables/arxiv/2607.06283v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 871, \"height\": 459}, {\"url\": \"assets/tables/arxiv/2607.06283v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1758, \"height\": 788}, {\"url\": \"assets/tables/arxiv/2607.06283v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 741, \"height\": 547}, {\"url\": \"assets/tables/arxiv/2607.06283v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1591, \"height\": 717}]"
motivation: 现有技能选择方法忽略任务难度和技能适用性的动态影响，且面临语义模糊匹配问题。
method: 对任务与技能进行语义分解，构建以状态节点和技能边构成的有向无环执行图，利用交叉编码器分区间评分并选择最优技能集。
result: 在 ALFWorld 和 ScienceWorld 上，结合三种 LLM，SkillReranker 提升任务性能、减少交互步骤并降低 token 消耗。
conclusion: SkillReranker 通过结构化任务-技能对应和重排序，实现了高效自适应的技能检索，可推广至复杂智能体系统。
---

## 摘要
技能使用能够显著提升现代智能体系统完成复杂任务的能力。然而，技能库规模的不断扩大使得准确选择技能变得越来越具有挑战性。在现实场景中，特定任务需求与多个通用但语义相似的候选技能之间常常出现模糊的语义匹配。此外，现有方法在选择最优目标技能集时往往忽视任务难度和技能适用性的动态影响。为了解决这些问题，我们提出了SkillReranker，一种用于自适应技能选择的推理时重排序框架。具体来说，我们首先在任务和技能两侧进行语义分解，生成信息丰富的子任务和执行状态描述，以及表征每个技能功能的转换状态描述。然后利用这些描述构建有向无环执行图，其中中间任务状态被建模为节点，候选技能被建模为边，从而建立结构化的任务-技能对应关系。在此基础上，SkillReranker判断每个状态节点是否满足分割条件以识别子任务区间。对于每个任务区间，我们采用交叉编码器对候选技能进行全面评分，并选择最合适的技能形成最终的目标技能集。在ALFWorld和ScienceWorld上使用三个骨干LLM进行的实验表明，与现有技能选择基线相比，SkillReranker有效提升了任务性能，减少了环境交互步骤，并降低了令牌消耗。

## Abstract
Skill usage can significantly enhance the ability of modern agent systems to complete complex tasks. However, the growing scale of skill libraries makes accurate skill selection increasingly challenging. In real-world scenarios, ambiguous semantic matching often arises between a specific task requirement and multiple generic yet semantically similar candidate skills. Moreover, existing methods tend to overlook the dynamic influence of task difficulty and skill applicability when selecting the optimal target skill set. To address these issues, we propose SkillReranker, an inference-time reranking framework for adaptive skill selection. Specifically, we first perform semantic decomposition on both the task and skill sides, yielding informative subtask and execution-state descriptions as well as transition-state descriptions that characterize each skill's functionality. These descriptions are then used to construct a directed acyclic execution graph, where intermediate task states are modeled as nodes and candidate skills as edges, thereby establishing a structured task-skill correspondence. On this basis, SkillReranker determines whether each state node satisfies the split condition to identify subtask intervals. For each task interval, we employ a cross-encoder to perform comprehensive scoring over candidate skills and select the most suitable ones to form the final target skill set. Experiments on ALFWorld and ScienceWorld with three backbone LLMs show that SkillReranker effectively improves task performance, reduces environment interaction steps, and lowers token consumption compared with existing skill selection baselines.

---

## 论文详细总结（自动生成）

# 论文《Task Decomposition-Guided Reranking for Adaptive Agent Skill Retrieval》详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：随着智能体技能库规模快速增长（文中提及67,884个技能），准确选择匹配当前任务的技能变得困难。主要挑战包括：
  - **语义模糊匹配**：具体任务需求与多个通用但语义相似的候选技能之间难以区分实际功能差异（如两个“加热”类技能可能具有不同的前置条件和执行流程）。
  - **动态影响被忽视**：现有方法在固定Top-k检索时，未考虑任务难度和技能适用性的动态变化，导致要么技能选择不足（无法完成复杂任务），要么选择过多（引入冗余信息，增加决策负担和Token消耗）。

- **整体含义**：该研究旨在提升LLM代理在复杂任务中自适应、准确地从大规模技能库中选择最合适技能集的能力，从而改善任务成功率、执行效率和资源消耗。

## 2. 提出的方法论

### 核心思想
SkillReranker是一个推理时重排序框架，通过对任务和技能进行结构化解析，构建有向无环执行图，动态识别任务阶段，并在每个阶段内利用交叉编码器进行精细评分，从而自适应地确定最终技能集（不再固定Top-k数量）。

### 关键技术细节
1. **结构化解析（Structured Parsing）**：
   - **任务解析（在线）**：使用专家LLM将自然语言任务指令分解为有序的子任务序列（如“获得叉子→清洁叉子→放在台面上”）和对应的中间状态序列（初始状态→状态1→...→最终状态）。
   - **技能解析（离线）**：对技能库中每个技能文档，抽取其**前置状态**（运行前需满足的条件）和**完成状态**（运行后的预期结果）。若无前置条件则标记为None。

2. **执行图构建（Execution Graph Construction）**：
   - 对每个任务，先用嵌入模型（Qwen3-Embedding-0.6B）检索Top-K候选技能（K=30）。
   - 以任务子状态为节点，将每个候选技能映射为有向边：通过交叉编码器计算技能前置状态与各子状态的相似度，确定源节点；计算完成状态与后续子状态的相似度，确定目标节点。若找不到有效目标则丢弃该技能。
   - 同时计算技能文档与任务描述的整体相关性ρ_k。

3. **自适应技能选择（Adaptive Skill Selection）**：
   - **识别阶段分割点**：为每条边计算权重w_k=σ(ρ_k)，然后为每个内部节点i计算三个量：In(i)（结束于i之前的技能强度）、Out(i)（起始于i之后的技能强度）、Cross(i)（跨过i的技能强度）。若In(i) > Cross(i)且Out(i) > Cross(i)，则该节点被标记为分割点，从而将任务划分为多个连续阶段（数量自适应）。
   - **阶段内技能重排序**：对每个阶段，拼接其内部子任务描述，用交叉编码器计算候选技能在全局相关性（ρ_k）和阶段级子任务相关性上的log空间组合分数 φ_k = log σ(ρ_k) + log σ(r(τ, c_k))，选择该阶段分数最高的技能。
   - **去重与输出**：按阶段顺序排列选出的技能并去重，得到最终技能集（数量不超过阶段数，可自适应变化）。

## 3. 实验设计

- **数据集/场景**：
  - **ALFWorld**：家庭操控任务，分为Seen（140个任务）和Unseen（134个任务）两个测试子集。
  - **ScienceWorld**：科学实验文本交互环境，包含30种任务类型，其原始验证集和测试集分别作为Seen（194个实例）和Unseen（211个实例）。

- **基准方法**：
  - **LLM-as-selector**：直接提示LLM（使用Mimo-v2.5-Pro）对候选技能进行重排序。
  - **SkillRouter**：微调双编码器检索器和交叉编码器重排序器选择技能。
  - **Graph of Skills**：构建依赖感知的技能图，通过图推理检索结构化的技能包。
  - 所有基准使用相同的冻结代理和技能池。

- **评估指标**：
  - 平均奖励/分数（0–100，越高越好）
  - 平均环境交互步数（0–30，越低越好）
  - 平均Token消耗（越低越好）

## 4. 资源与算力

- 论文**未明确说明训练过程中使用的GPU型号、数量或训练时长**。仅在实现部分提到：
  - 使用Qwen3-Reranker-0.6B作为交叉编码器，Qwen3-Embedding-0.6B作为嵌入模型（均为较小规模模型）。
  - 实验中的LLM代理基于开源LangChain框架，使用DeepSeek-v4-Flash、GPT-5.4-Mini（API访问，温度0）和Qwen3.6-27B（本地vLLM部署，温度0）。
  - 结构化解析阶段使用DeepSeek-v4-Flash进行语义分解。
  - 技能解析是离线预处理步骤，缓存结果。
  - 推理时引入交叉编码器重排序，相比简单嵌入检索增加了额外开销，但论文未量化此开销。

## 5. 实验数量与充分性

- **主要结果**（表1）：在2个数据集 × 2个分片 × 3种骨干LLM = 12组设置下，报告了平均奖励和平均步数。SkillReranker在11/12的奖励设置和11/12的步数设置中排名第一。
- **Token消耗**（图3及附录表3）：展示了3种骨干模型下各方法的Token消耗，进一步验证效率优势。
- **消融实验**（表2）：使用DeepSeek-v4-Flash和Qwen3.6-27B两个骨干，在4个分片上进行3种变体（w/o Parsing、w/o Graph Edge、w/o Split）的对比，共2×4×3=24个结果。表明各组件均有贡献，尤其是结构化解析和边建模。
- **案例研究**（图4）：展示了两个ALFWorld示例，直观说明自适应选择的效果。

**充分性与客观性评价**：
- 实验覆盖了两种代表性交互基准（家务和科学实验），采用多种评估维度（成功率、步数、Token）。
- 对比了当前主流的技能选择基线，且所有方法共享相同的代理和技能池，确保了公平性。
- 消融实验验证了框架各模块的必要性。
- 主要结果在多个骨干LLM上验证了泛化性，结果统计显著。
- 不足：论文未报告标准差或统计显著性检验；ScienceWorld上Qwen3.6-27B的Seen奖励和Unseen步数略低于最佳基线，但作者已指出仍具竞争力。

## 6. 主要结论与发现

- SkillReranker在两个交互基准和三种骨干LLM上**一致地提升任务成功率**（平均奖励提升幅度2–20+个百分点），同时**减少环境交互步数**（平均减少0.5–2.7步）和**降低Token消耗**（减少5%–25%）。
- 自适应技能选择机制按任务复杂度动态调整技能集数量（在实验中平均每任务选择1.27–1.30个技能），避免了固定Top-k的冗余或不足问题。
- 结构化任务-技能对齐（解析+执行图）比纯文本匹配更能区分语义相似但功能不同的技能，从而提升选择准确性。
- 消融实验表明，**结构化解析贡献最大**（去除后性能下降最显著），其次是图边建模和自适应分割。

## 7. 优点

1. **结构化对齐**：将任务和技能分解为统一的“子状态”空间，通过执行图建模过渡关系，提升了对语义模糊技能的区分能力。
2. **自适应选择**：不再固定Retrieve数量，而是基于任务结构动态决定技能数量和组合，避免过度加载或不足。
3. **跨模型泛化**：在DeepSeek、GPT、Qwen三种不同能力的LLM上均取得一致改进，说明方法不依赖于特定模型。
4. **效率与效果兼得**：不仅提升任务成功率，还显著减少交互步数和Token消耗，具有实际部署价值。
5. **详尽实验**：涵盖多个基准、多个模型、消融分析、Token分析和案例研究，验证充分。

## 8. 不足与局限

1. **依赖LLM解析质量**：任务分解和技能状态解析依赖专家LLM的能力，若解析产生不准确的中间状态或不完整的技能描述，会影响后续图构建和技能选择效果。
2. **计算开销**：相比简单嵌入检索，交叉编码器重排序增加了推理时计算负担（尽管Token消耗降低）；但论文未量化此额外开销在真实部署中的影响。
3. **实验范围有限**：仅聚焦于文本交互环境（ALFWorld/ScienceWorld），未拓展到多模态、开放式、真实世界智能体场景，泛化性待验证。
4. **未报告评估细节**：如标准差、统计显著性检验、不同任务类型的性能差异等，可能掩盖方法在某些困难任务上的不足。
5. **离线处理依赖**：技能解析为离线预处理，需定期更新技能库时重新进行解析，维护成本未讨论。
6. **技能库规模**：虽然使用67,884个技能，但候选集仅检索Top-30，可能限制了在更大库中的有效性；且技能来源单一（skillsmp.com），可能存在偏差。

（完）
