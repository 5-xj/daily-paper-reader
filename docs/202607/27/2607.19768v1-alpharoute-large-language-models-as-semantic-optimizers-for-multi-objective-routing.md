---
title: "AlphaRoute: Large Language Models as Semantic Optimizers for Multi-Objective Routing"
title_zh: AlphaRoute：大语言模型作为多目标路由的语义优化器
authors: "Kabir Murjani, Mishri Bhavsar, Manish I. Patel, Jonti Talukdar"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.19768v1"
tags: ["query:ad"]
score: 8.0
evidence: LLM作为路由的语义优化器，结合启发式搜索和LLM
tldr: "VLSI全局布线是NP难组合优化问题，传统启发式方法在复杂拥塞拓扑上失效。AlphaRoute提出多目标自适应搜索框架，将拆线重布转化为动态优化，通过SHAP分解溢出、3D Dijkstra子图提取和自适应PathFinder策略，并利用大语言模型作为语义策略优化器动态调整惩罚参数。在ISPD 2025基准上，MEMPOOL溢出减少98.6%，ARIANE设计溢出降低29.8倍，惩罚得分从1.780降至0.0538。结果表明，优越的算法搜索几何可克服解释型Python实现的延迟。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19768v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 519, \"height\": 1494}, {\"url\": \"assets/figures/arxiv/2607.19768v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 839, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2607.19768v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 923, \"height\": 667}, {\"url\": \"assets/figures/arxiv/2607.19768v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 809, \"height\": 578}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19768v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 756, \"height\": 193}]"
motivation: 传统启发式布线依赖静态惩罚调度，在复杂拥塞拓扑上性能差，需自适应优化方法。
method: 提出AlphaRoute：多目标自适应搜索框架，结合SHAP分解、3D Dijkstra子图提取、自适应PathFinder策略，并用LLM动态调整惩罚参数。
result: "在MEMPOOL上溢出减少98.6%；ARIANE设计溢出降低29.8倍，惩罚得分0.0538（SOTA 1.780）。"
conclusion: AlphaRoute证明优越搜索几何可克服解释型Python实现的延迟，实现显著性能提升。
---

## 摘要
非常大规模集成电路（VLSI）全局布线是一个NP难组合优化问题，需要在容量受限的3D网格中分配信号网络，同时最小化拥塞、线长和过孔转换。由于传统启发式方法依赖于静态惩罚调度，无法处理复杂拥塞拓扑，我们提出AlphaRoute：一种多目标自适应搜索框架，将拆线和重新布线（R&R）重构为动态优化系统。我们引入基于SHAP的溢出分解来隔离每个网络的拥塞，通过3D Dijkstra迷宫路由和自适应PathFinder策略驱动目标子图提取。关键地，AlphaRoute使用大语言模型（LLMs）作为语义策略优化器。受确定性知识图约束，LLMs解释拥塞度量以动态调整惩罚参数。在ISPD 2025基准测试上，AlphaRoute在MEMPOOL上减少了98.6%的溢出。在受限的ARIANE设计中，我们实现了146,109的溢出（比现有技术降低29.8倍），得到惩罚得分S_orig=0.0538，而现有技术（SOTA）为1.780。这些结果表明，优越的算法搜索几何可以克服解释型Python实现的延迟。

## Abstract
Very Large Scale Integration (VLSI) global routing is an NP-hard combinatorial optimization problem requiring signal net assignment across capacity-constrained 3D grids while minimizing congestion, wirelength, and via transitions. Because traditional heuristics rely on static penalty schedules that fail on complex congestion topologies, we present AlphaRoute: a multi-objective adaptive search framework reformulating rip-up and reroute (R&R) into a dynamic optimization system. We introduce SHAP-based overflow decomposition to isolate per-net congestion, driving targeted subgraph extraction via 3D Dijkstra maze routing and an adaptive PathFinder policy. Crucially, AlphaRoute employs Large Language Models (LLMs) as semantic policy optimizers. Bounded by a deterministic knowledge graph, the LLMs interpret congestion metrics to dynamically adjust penalty parameters. Evaluated on ISPD 2025 benchmarks, AlphaRoute reduces overflow by 98.6% on MEMPOOL. On the constrained ARIANE design, we achieve an overflow of 146,109 (a 29.8x reduction in overflow over the state of the art), yielding a penalized score of S_orig = 0.0538 versus the State-of-the-art (SOTA) 1.780. These results demonstrate that superior algorithmic search geometry can overcome the latency of interpreted Python implementations.

---

## 论文详细总结（自动生成）

# AlphaRoute：大语言模型作为多目标路由的语义优化器 — 论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：VLSI全局布线是一个NP-hard组合优化问题，需在容量受限的3D网格上分配信号网络，同时最小化拥塞、线长和过孔转换。传统启发式方法（如固定的惩罚调度表）无法应对复杂拥塞拓扑，导致下游布线失败、时序退化、面积增大。
- **动机**：现有方法（如CUGR、InstantGR、PathFinder）存在三大局限：惩罚参数静态不变、拆线候选选择未基于逐网溢出贡献、缺乏可解释的决策推理层。
- **整体含义**：本文提出AlphaRoute，将拆线重布（R&R）转化为动态优化系统，利用SHAP分解、3D Dijkstra子图提取、自适应PathFinder策略，并首次引入大语言模型（LLM）作为语义策略优化器，在ISPD 2025基准上显著降低拥塞，为VLSI路由提供了新的范式。

## 2. 论文提出的方法论

### 核心思想
- 将全局路由分解为四个关键模块：**SHAP溢出分析器**、**自适应PathFinder策略**、**3D Dijkstra迷宫路由**、**LLM驱动的策略优化**，并通过**知识图**约束确保安全性。
- 整体流程：输入解析→图构建→网表排序→初始布线→SHAP分析→拆线候选选择→LLM策略建议→知识图验证→自适应PathFinder重布→迭代直至溢出为零或达到停止条件。

### 关键技术细节
- **图构建（Eq.1）**：将每个G-Cell映射为节点，边权重由基础成本、关键性因子和剩余容量共同决定：  
  \( W(e) = (10.0 - 3.0 \cdot \sigma_n) + (11 - c_{z,u}) + (11 - c_{z,v}) \)，其中\(\sigma_n\)为网的关键性，\(c\)为剩余容量。
- **初始布线**：根据网面积\(A_n\)区分：
  - 小网（\(A_n \le 2500\)）：使用Steiner树（RustworkX实现）。
  - 大网（\(A_n > 2500\)）：使用Prim最小生成树 + 柔性L形路由（评估H-first和V-first两种L形，选拥塞代价低的）。
- **SHAP溢出分析**：
  - 逐网溢出得分（Eq.3）：\( \psi_n = \sum_{(z,y,x) \in G_n} \max(0, -c_{z,y,x}) \)。
  - 层溢出分数（Eq.4）：\( \lambda_z = \frac{\sum_{y,x} \max(0,-c)}{\sum_{z,y,x} \max(0,-c)} \)。
  - 拥塞单元占比（Eq.5）：\( f_c = |\{(z,y,x): c<0\}| / (N_L \cdot y_{\max} \cdot x_{\max}) \)。
  - 利用-溢出相关性（Eq.6）：Pearson相关系数\(r_{uo}\)用于判断是否增加历史惩罚权重。
- **自适应PathFinder策略**：
  - 历史成本更新（Eq.7）：\( h^{(k+1)}(e) = h^{(k)}(e) + \alpha^{(k)} \cdot \max(0, -c_e) \)。
  - 重布成本（Eq.8）：\( \text{cost}(e) = W(e) + h^{(k)}(e) + p_f^{(k)} \cdot \max(0, -c_e) \)。
  - \(\alpha\)自动增长因子1.15，SHAP规则（表I）可调整参数（如当单层溢出占主导时降低过孔成本）。
- **LLM日志分析**：向LLM（Llama 3.2 3B本地或Groq API上的Llama 3.3 70B/GPT-OSS 120B）发送结构化指标（当前溢出、溢出变化、拥塞比例、相关性、层占比、当前\(\alpha\)和\(p_f\)），返回JSON建议（\(\alpha\)、\(p_f\)、过孔成本）及推理文本。建议经知识图硬规则验证后应用。
- **拆线候选选择（Eq.9）**：按\(\psi_n\)降序，选择前20%的网（\(\rho=0.20\)）。
- **重布**：面积在2500 < \(A_n \le 15000\)的网使用3D Dijkstra迷宫路由（在边界框扩大2的局部子图上），其余使用Steiner或MST+FlexL。
- **知识图**：RustworkX实现，记录<模式、策略、结果>三元组，用于约束LLM建议。

## 3. 实验设计

- **数据集**：来自ISPD 2025竞赛盲测数据集，四个基准：
  - ARIANE：10层，646×646 G-Cells，105,924个网。
  - BSG：10层，1384×1384 G-Cells，768,239个网。
  - MEMPOOL：10层，1120×1120 G-Cells，157,744个网。
  - NVDLA：10层，386×386 G-Cells，135,814个网。
- **对比方法**：8个累积消融配置（A1~A8）：
  - A1：Steiner树基线。
  - A2：+柔性L路由。
  - A3：+SHAP驱动的拆线重布。
  - A4：+Dijkstra迷宫重布。
  - A5：+知识图。
  - A6：+图神经网络(GNN)拥塞评分。
  - A7：+LLM引导（Llama 3.3 70B）。
  - A8：+LLM引导（GPT-OSS 120B）。
- **评价指标**：ISPD 2025竞赛评分\(S_{\text{orig}}\)（原始得分）和\(S_{\text{scaled}}\)（缩放得分，含运行时间惩罚），以及L2溢出。

## 4. 资源与算力

- 所有实验在**单个NVIDIA A40 GPU（48 GB VRAM）** 上运行，配备48 GB系统RAM、9个vCPU、40 GB磁盘。
- LLM推理使用**Groq Open Models API**（外接服务），本地也测试了Ollama Llama 3.2 3B。
- **未明确说明训练时长或训练轮次**，仅提到迭代收敛曲线（图5）最多3次R&R迭代。
- 整体运行时间约为竞赛中位数（19秒）的**136倍**，作者承认这是Python实现的代价。

## 5. 实验数量与充分性

- **实验数量**：对4个基准（ARIANE、BSG、MEMPOOL、NVDLA）进行了8个消融配置（A1~A8）的完整测试，共32个主要实验。此外，还进行了LLM模型对比（Llama 70B vs GPT-OSS 120B）不显著差异的验证。
- **充分性分析**：
  - 消融实验设计合理，逐组件叠加，清晰展示了每个模块的贡献。
  - 基准覆盖了大、中、小规模设计，具有代表性。
  - 使用了官方评分标准，结果可复现（代码公开）。
  - 不足：仅在ISPD 2025一个竞赛数据集上测试，未在工业级更大规模设计上验证；运行时间未与官方优化实现公平比较（因使用Python）；缺少与更多传统方法的对比（如CUGR、InstantGR的最新报告数字）。
- 总体而言，实验相对充分，消融设计客观，但在泛化性和运行效率对比上存在局限。

## 6. 论文的主要结论与发现

- **主要结论**：AlphaRoute在ARIANE上取得\(S_{\text{orig}}=0.0538\)，比当前SOTA（1.780）降低33.1倍；溢出降低29.8倍（146,109 vs SOTA水平）。在MEMPOOL上溢出减少98.6%。表明**优越的算法搜索几何能克服Python实现的延迟**。
- **关键发现**：
  - 柔性L路由（A2）、SHAP拆线重布（A3）、Dijkstra迷宫路由（A4）贡献了绝大部分溢出减少，而知识图、GNN和LLM层主要提供可解释性，而非进一步降低溢出。
  - LLM在已接近最优的设计上会轻微过罚，但对决策可解释性有重要价值。
  - 策略循环对LLM模型族不敏感（Llama和GPT-OSS结果相似）。

## 7. 优点

- **方法的创新性**：首次将LLM引入VLSI全局路由作为语义策略优化器，并融合SHAP解释性分析，形成闭环自适应系统。
- **可解释性**：SHAP分解和LLM自然语言推理提供了路由决策的可视化理由，便于设计者诊断和迁移经验。
- **结构化安全性**：知识图硬约束确保了LLM建议不违反已知规则，避免失控。
- **模块化设计**：消融实验清晰各组件贡献，便于后续改进。
- **公开代码**：完整代码和评估脚本在GitHub公开，可复现。

## 8. 不足与局限

- **运行效率低**：纯Python实现，运行时约为竞赛中位数（19秒）的136倍，远未达到实用要求。
- **实验覆盖不足**：仅评估ISPD 2025的4个基准，未在更大规模工业设计上验证；未与多种最新SOTA方法（如InstantGR、PAGR）进行同等条件下的比较。
- **LLM角色有限**：消融显示LLM对最终溢出的改善几乎为零，主要贡献在于可解释性。这可能限制了其在性能优化中的实际价值。
- **过罚风险**：LLM在接近最优解时可能提出过激参数调整，导致溢出略有反弹（图3）。
- **缺少统计显著性测试**：未提供多次运行的标准差或置信区间，结果的稳定性未明确。
- **硬件环境不一致**：使用的云实例（9 vCPU, 48GB RAM）比竞赛参考硬件（200GB RAM, 8核CPU, A100 GPU）更受限，运行时间对比可能不公平。

（完）
