---
title: Thin Trees via $k$-Respecting Cut Identities
authors: Mohit Daga
date: 2025-10-14
pdf: "https://arxiv.org/pdf/2510.12050v1"
tags: ["paper:补充", "query:ATSP"]
score: 6.0
evidence: 本文为薄树验证提供新工具，与ATSP近似算法相关。
source: arxiv
selection_source: long-range
paper_media_checked: true
title_zh: 通过$k$-尊重割恒等式的细树
tldr: 薄生成树问题中，验证树的薄性需检查指数级割，缺乏高效证书。本文提出k-尊重割恒等式，将跨越树至多k条边的割权重表示为成对割的函数，构建树局部预言机，实现O_k(1)时间查询。基于此，首次给出固定k下精确计算薄性证书Θ_k(T)的算法，时间复杂度Õ(n^2+n^k)。在平面图及曲面嵌入图上，利用对偶性获得更优的证书界。
motivation: 薄树验证缺乏高效证书，阻碍薄树猜想研究及ATSP近似算法应用。
method: 提出k-尊重割恒等式，将k-割权重简化为2-割函数，构建树局部预言机。
result: 给出固定k下精确薄性证书计算算法，时间复杂度Õ(n^2+n^k)，并输出见证割。
conclusion: 为薄树验证提供首个高效确定性方法，并在平面及曲面图上获得改进界。
reading_content_version: 1
reading_section: quick
---

## 摘要
细生成树位于图论、近似算法和组合优化的交汇点。它们对于长期存在的\emph{细树猜想}至关重要，该猜想询问是否每个$k$-边连通图都包含一棵$O(1/k)$-细树，并且它们支撑了诸如ATSP的$O(\log n/\log\log n)$-近似等算法突破。然而，即使是验证给定树是否为细树这一基本算法任务也一直难以捉摸：检查细度需要推理指数多个割，并且已知没有有效的证书。我们引入了\emph{$k$-尊重割恒等式}的新机制，该机制将跨越生成树至多$k$条边的每个割的权重表示为成对（$2$-尊重）割的简单函数。这产生了一个树局部预言机，在$O(n^2)$预处理后，能在$O_k(1)$时间内评估此类割。基于此预言机，我们给出了第一个程序，用于在$\tilde O(n^2+n^k)$时间内计算任何生成树的精确$k$-细度证书$Θ_k(T)$（对于固定$k$），输出证书值和见证割。在一般图之外，我们的框架在结构化设置中提供了更精确的保证。在平面图中，与环的对偶性和对偶围长意味着每棵生成树都承认一个可验证的证书$Θ_k(T)\le k/λ$（因此对于常数$k$为$O(1/λ)$）。在嵌入在亏格为$γ$的曲面上的图中，精细计数通过相同的集成覆盖给出认证的（每割）界$O((\log n+γ)/λ)$。

## Abstract
Thin spanning trees lie at the intersection of graph theory, approximation algorithms, and combinatorial optimization. They are central to the long-standing \emph{thin tree conjecture}, which asks whether every $k$-edge-connected graph contains an $O(1/k)$-thin tree, and they underpin algorithmic breakthroughs such as the $O(\log n/\log\log n)$-approximation for ATSP. Yet even the basic algorithmic task of \emph{verifying} that a given tree is thin has remained elusive: checking thinness requires reasoning about exponentially many cuts, and no efficient certificates have been known. We introduce a new machinery of \emph{$k$-respecting cut identities}, which express the weight of every cut that crosses a spanning tree in at most $k$ edges as a simple function of pairwise ($2$-respecting) cuts. This yields a tree-local oracle that, after $O(n^2)$ preprocessing, evaluates such cuts in $O_k(1)$ time. Building on this oracle, we give the first procedure to compute the exact $k$-thinness certificate $Θ_k(T)$ of any spanning tree for fixed $k$ in time $\tilde O(n^2+n^k)$, outputting both the certificate value and a witnessing cut. Beyond general graphs, our framework yields sharper guarantees in structured settings. In planar graphs, duality with cycles and dual girth imply that every spanning tree admits a verifiable certificate $Θ_k(T)\le k/λ$ (hence $O(1/λ)$ for constant $k$). In graphs embedded on a surface of genus $γ$, refined counting gives certified (per-cut) bounds $O((\log n+γ)/λ)$ via the same ensemble coverage.

## 专题评审

摘要主要研究薄树验证，仅提及与ATSP近似算法的关联，非直接研究ATSP求解。

原文证据：

> They are central to the long-standing \emph{thin tree conjecture}, which asks whether every $k$-edge-connected graph contains an $O(1/k)$-thin tree, and they underpin algorithmic breakthroughs such as the $O(\log n/\log\log n)$-approximation for ATSP.

仅依据标题与摘要评审；待复核不代表已确认相关。
