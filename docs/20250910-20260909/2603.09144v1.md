---
title: A PTAS for Weighted Triangle-free 2-Matching
authors: "Miguel Bosch-Calvo, Fabrizio Grandoni, Yusuke Kobayashi, Takashi Noguchi"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09144v1"
tags: ["paper:补充", "query:ATSP"]
score: 6.0
evidence: 与ATSP及2-匹配问题相关，提供近似算法新进展。
source: arxiv
selection_source: long-range
paper_media_checked: true
title_zh: 加权无三角形2-匹配问题的多项式时间近似方案
tldr: "加权无三角形2-匹配问题要求寻找最大权重且不含三角形的2-匹配子图，与旅行商问题及2-连通网络设计相关。目前无多项式算法，已知近似比为2/3。本文提出基于局部搜索的多项式时间近似方案，对任意常数ε>0达到(1-ε)近似，填补了该问题的近似算法空白。"
motivation: WTF2M问题缺乏多项式算法，现有2/3近似比粗糙，需更优近似算法。
method: 设计简单局部搜索算法，通过交换操作逐步改进解，并给出非平凡分析证明近似比。
result: "对任意常数ε>0，获得多项式时间(1-ε)近似算法，即PTAS。"
conclusion: 首次为WTF2M提供PTAS，推动相关网络设计问题研究。
reading_content_version: 1
reading_section: quick
---

## 摘要
在加权无三角形2-匹配问题（WTF2M）中，给定一个无向边加权图。我们的目标是计算一个最大权重的子图，该子图是一个2-匹配（即每个节点的度数不超过2）且无三角形（即不包含任何具有3条边的环）。该问题及相关问题的主要动机之一是与旅行商问题以及某些2-连通网络设计问题的实际和理论联系。WTF2M未知是否为NP难问题，同时一般情形下也没有已知的多项式时间算法来求解（仅对某些特殊情况已知多项式时间算法）。最著名的（民间）近似算法简单地计算一个最大权重2-匹配，然后删除每个三角形中最便宜的边：这给出了2/3的近似比。在本文中，我们提出了WTF2M的一个多项式时间近似方案（PTAS），即对于任意给定的常数ε>0，存在一个多项式时间的(1-ε)-近似算法。我们的结果基于一个简单的局部搜索算法和非平凡的分析。

## Abstract
In the Weighted Triangle-Free 2-Matching problem (WTF2M), we are given an undirected edge-weighted graph. Our goal is to compute a maximum-weight subgraph that is a 2-matching (i.e., no node has degree more than $2$) and triangle-free (i.e., it does not contain any cycle with $3$ edges). One of the main motivations for this and related problems is their practical and theoretical connection with the Traveling Salesperson Problem and with some $2$-connectivity network design problems. WTF2M is not known to be NP-hard and at the same time no polynomial-time algorithm to solve it is known in the general case (polynomial-time algorithms are known only for some special cases). The best-known (folklore) approximation algorithm for this problem simply computes a maximum-weight 2-matching, and then drops the cheapest edge of each triangle: this gives a $2/3$ approximation. In this paper we present a PTAS for WTF2M, i.e., a polynomial-time $(1-\varepsilon)$-approximation algorithm for any given constant $\varepsilon>0$. Our result is based on a simple local-search algorithm and a non-trivial analysis.

## 专题评审

研究三角形无关2匹配问题，仅与TSP有理论联系，非ATSP直接求解。

原文证据：

> One of the main motivations for this and related problems is their practical and theoretical connection with the Traveling Salesperson Problem

仅依据标题与摘要评审；待复核不代表已确认相关。
