Title: Thin Tree Verification is coNP-Complete

URL Source: https://arxiv.org/pdf/2512.25043v1

Published Time: Thu, 01 Jan 2026 02:48:27 GMT

Number of Pages: 8

Markdown Content:
# Thin Tree Verification is coNP-Complete 

## Alice Moayyedi ∗

## December 31, 2025 

Abstract 

An α-thin tree T of a graph G is a spanning tree such that every cut of G has at most an α proportion of its edges in T . The thin tree conjecture proposes that there exists a function f such that for any α > 0, every f (α)-edge-connected graph has an α-thin tree. Aside from its independent interest, an algorithm which could efficiently construct an O(1) /k -thin tree for a given k-edge-connected graph would directly lead to an O(1)-approximation algorithm for the asymmetric travelling salesman problem (ATSP) [GS11]. However, it was not even known whether it is possible to efficiently verify that a given tree is α-thin. We prove that determining the thinness of a tree is coNP -hard. 

# 1 Introduction 

Let G be an undirected and unweighted graph. We denote by V (G) the set of vertices in G, and by E(G)the set of edges. Given a set A ⊆ V (G), we denote by δ(A) or δG(A) the set of edges of G with exactly one endpoint in A.

Definition 1. A subgraph S of a graph G is α-thin if it contains at most an α proportion of the edges of each cut; that is, for every cut C = ( A, B ) of G (with A ⊆ V (G), B = V (G) \ A): 

|δS (A)|/|δG(A)| ≤ α

The following conjecture was proposed by Goddyn [God04]: 

Conjecture 1 (Thin Tree Conjecture) . There exists a function f such that, for any α > 0, every f (α)-edge-connected graph has an α-thin spanning tree. 

In addition to a variety of other implications [KO23], if it were possible to construct an O(1) /k -thin tree given a k-edge-connected graph, this would directly yield an O(1)-approximation algorithm for both the standard asymmetric traveling salesman problem (ATSP) [GS11] and a “bottleneck” version of the ATSP (wherein the objective to minimize is the edge traversed of greatest weight) [AKS21]. The former has already been produced through other methods, but the latter would be an improvement on the best known algorithm [AKS21]. Constant-factor thin trees are known to be constructible in certain classes of graph [GS11], but in general graphs α-thin trees are not known to exist for any α which is not a function of 

n = V (G). In fact, as far as we are aware, there has been no published result determining the difficulty of verifying 

a given spanning tree as α-thin. Variously this problem has been described as not being known to have a polynomially-checkable certificate [KO23] or as being NP -hard (without proof) [Alg23]. We present a proof that the problem of verifying whether a spanning tree is α-thin, stated below, is coNP -complete.  

> ∗David R. Cheriton School of Computer Science, University of Waterloo, Canada. anelosima@proton.me

1

> arXiv:2512.25043v1 [cs.CC] 31 Dec 2025

Problem (Thin Tree Verification Problem ( ThinTreeValid )) .

Instance: A graph G, a spanning tree T of G, and a value α ∈ [0 , 1] .

Output: (YES) if T is α-thin—that is, if for all partitions A of G,

|δT (A)|/|δG(A)| ≤ α

—and (NO) otherwise. 

Theorem 1 (Hardness of Thin Tree Verification) . ThinTreeValid is coNP -complete. 

Remark 1. The proof presented does not offer any guarantees of the approximation hardness of Thin-TreeValid , nor does it imply an obvious way to approximate ThinTreeValid ; it is thus of limited value either in demonstrating directly that a spanning tree within a constant factor of the edge connectedness of a graph cannot be found or in finding such a spanning tree. We present this work in hopes that it may be useful indirectly. 

# 2 Proof Strategy 

The proof of coNP -hardness will consist of a reduction in turn through four problems, initially from the unweighted version of the classical problem MaxCut :

Problem (Unweighted Maximum Cut Problem, MaxCut ).

Instance: An (undirected, unweighted) connected graph G and an integer k.

Output: (YES) if there exists a partition (A, B ) of G such that |δ(A)| ≥ k, and (NO) otherwise. 

MaxCut is well-known to be NP -hard [GJS74]. First, we reduce to the problem of finding the cut with the maximal average weight amongst all cuts: 

Problem (Maximum Average Cut Optimisation Problem, MaxAvgCutOpt ).

Instance: A connected graph G with weights w such that ∀we ∈ w, w e ∈ {− 1, 1}.

Output: A nontrivial partition (A, B ) of G such that, for all nontrivial partitions A′, B ′ of G,

 X

> e∈δ(A)

we

 /|δ(A)| ≥ 

 X

> e∈δ(A′)

we

 /|δ(A′)|

Next, we reduce MaxAvgCutOpt to a natural decision variant (on multigraphs, for ease of proof): 

Problem (Maximum Average Cut Decision Problem, MaxAvgCut ).

Instance: A connected multigraph G with weights w such that ∀we ∈ w, w e ∈ {− 1, 1}, and an integer 

k ≥ − 1.

Output: (YES) if there exists a nontrivial partition (A, B ) of G such that 

 X

> e∈δ(A)

we

 /|δ(A)| ≥ k, 

and (NO) otherwise. 

Finally, we reduce this decision problem to the complement of ThinTreeValid :

Problem (The Complement of the Thin Tree Validation Problem, ThinTreeValid C).

Instance: A connected graph G, a spanning tree T of G, and a value α ∈ [0 , 1] .

Output: (NO) if T is α-thin, and (YES) otherwise. 

Additionally, for any graph G and subgraph T of G, we define the T -thickness of a cut ( A, B ) as the proportion of the edges in δ(A) which are in T ; that is, |δT (A)|/|δG(A)|. Note that a subgraph T of G is 

α-thin if and only if no cut of G has a T -thickness strictly greater than α.23 Proof 

Observe that ThinTreeValid is in coNP ; given a ThinTreeValid instance ( G, T, α ), any cut of G with 

T -thickness greater than α certifies that it is a (NO) instance, and such a cut must exist. It thus remains to prove that it is coNP -hard. 

Theorem 2. MaxCut ≤PT MaxAvgCutOpt .Proof. We will use the following algorithm, where A ⊕ B denotes the symmetric difference of A and B:

Algorithm 1: Solving MaxCut using MaxAvgCutOpt .

Input : An (undirected) connected graph G;an integer k;a subroutine maco( G′, w) which solves MaxAvgCutOpt .

Output: MaxCut (G, k ). 

C ← ∅  

> 1

repeat for e ∈ G do 

we ←

(

−1 if e ∈ C

1 if e / ∈ CC′ ← δ(maco( G, w))

Cprev ← CC ← C ⊕ C′

until X

> e∈C′

we ≤ 0Output (YES) if |Cprev | ≥ k; otherwise, output (NO). Note that at each iteration of the loop at line 1 except the last, C′ contains more edges which are not shared with C than it contains edges shared with C. This is because the average weight of a cut in G with weights w is positive if and only if there are more edges in the cut outside C than in it; each edge outside adds 1 to the numerator, and each edge inside adds −1. Every iteration, then, will increase the number of edges in C until the first iteration with nonpositive average weight, at which point the loop at line 1 will terminate. Thus, for each iteration except the last, the size of C is strictly increasing; since |C| ≤ | E(G)|, there will be at most |V (G)|2 iterations. Since each iteration completes with linear runtime and has one call to the 

MaxAvgCutOpt algorithm, algorithm 1 calls the MaxAvgCutOpt subroutine a polynomially bounded number of times with a polynomially bounded amount of extra runtime. The space of cuts of a graph is closed under the symmetric difference, so each C produced is in fact a cut of G. Further, we have that if there exists a cut C∗ of G such that |C∗| > |C|, then C∗ ⊕ C will have positive value in the MaxAvgCutOpt instance ( G, w). Hence the MaxAvgCutOpt subroutine will not return a cut of nonpositive value, and the algorithm will not terminate; it will only terminate when a cut of maximal size in G is obtained. So, given that Cprev is a cut of maximum size in G at the point of termination, the algorithm will output (YES) if there exists a cut in G with size at least k, and (NO) otherwise. 

Theorem 3. MaxAvgCutOpt ≤PT MaxAvgCut .Proof. We use algorithm 2. First, note that line 1 can be completed in a polynomial number of checks to the MaxAvgCut subroutine; all average weights are of the form p/q with |p| ≤ q ≤ n integral, where n = V (G). Thus there are O(n2)weights to check. Further, the minimum possible average weight of any cut is −1, so all inputs are valid 

MaxAvgCut instances. We proceed to the loops. Since we contract a pair of vertices in each iteration of the loop at line 2, and the loop at line 3 involves at most one iteration per edge of G, if this algorithm terminates it does so in 3Algorithm 2: Solving MaxAvgCutOpt using MaxAvgCut 

Input : An (undirected) connected graph G;a set of (integer) weights w of G such that ∀we ∈ w, w e ∈ {− 1, 1};a subroutine mac( G′, w′, k) which solves MaxAvgCut .

Output: A cut of maximal average size in ( G, w).  

> 1

Set k to the maximum average weight of a cut in ( G, w), found using an exhaustive search with 

mac( G, w, k) over all possible maximum average weights (i.e., for each possible weight, run 

mac( G, w, k), and take the largest for which MaxAvgCut (G, w, k ) is (YES)). 

G′ ← G 

> 2

while |V (G′)| > 2 do  

> 3

for (v, u ) ∈ V (G′) × V (G′) do 

G′′ ← the multigraph formed by contracting the vertices v and u in G′.

if mac( G′′ , w, k) then 

G′ ← G′′ 

break Output the partition of G given by the classes of vertices identified with each of the vertices of G′.polynomially many calls to the MaxAvgCut subroutine and with polynomial extra time. To see that it does terminate, note that if there exists a maximum average cut which partitions the vertices v and u into different sets, contracting those vertices does not reduce the value of the maximum average cut of the graph; thus, in any graph with more than two vertices there exist at least two vertices which can be contracted while maintaining the size of the maximum average cut. Thus, the loop at line 3 always finishes with a pair of vertices contracted; eventually, G′ consists of two vertices. It is also clear that the cut in G associated with G′ at the end has an average weight of k, since the only nontrivial cut in G′ has weight k, and the edges in that cut have the same weights as the equivalent edges of the associated cut in G. Thus, this algorithm produces a cut with maximal average weight. 

Theorem 4. MaxAvgCut ≤Pm ThinTreeValid C.Proof. We will show this via Karp reduction, producing from the triplet ( G, w, k ) a graph G′, a tree T ,and a value α such that the ThinTreeValid C instance ( G′, T, α ) is a (YES) instance if and only if the 

MaxAvgCut instance ( G, w, k ) is a (YES) instance. As a brief sketch, we will construct a “blown up” graph which has a large clique for each vertex in the original graph and a collection of edges between those cliques for each edge in the original graph (see Figure 1, though note that the actual number of vertices and edges is not accurately represented). We will then construct a tree T and a threshold α such that all cuts in the original graph with an average weight of at least k correspond to cuts in the new graph which have more that an α-fraction of edges in T (violating the property that T is α-thin), and vice-versa. Intuitively, we arrange for the number of edges in T between cliques to be proportional to the weight of the edge between the associated vertices in the original graph, and we ensure that the cliques are so large that the cuts which split a clique have an extremely low T -thickness (and so are irrelevant for determining what the thinness of the tree is, as it is determined by only the cut of greatest thickness). To simplify the analysis, we make the cliques larger than they need to be to make this property hold; the number of vertices we produce in each clique is 24 · | E(G)|, but a much smaller number would suffice at the cost of complicating the proof. Take the MaxAvgCut instance ( G, w, k ). We first use algorithm 3 and algorithm 4 to construct an associated G′ and T .4G and w

12

> algorithms 3 and 4

G′ and T

Figure 1: A rough schematic of the transformation of G and w into a “blown up” graph G′ and a tree T .

Algorithm 3: Construction of G′

Input : A connected multigraph G.Let m = |E(G)|.

G′ ← (∅, ∅)

for v ∈ V (G) do 

Add to G′ a clique cv consisting of 24 m new vertices. 

for e = ( v, w ) ∈ E(G) do  

> 1

Pick 3 pairs ( v0, w 0), (v1, w 1), (v2, w 2) of unique vertices in G′, one of each pair from cv and one of each from cw, which do not yet have any incident edges to vertices outside of their respective cliques. (That is, pick pairs consisting of vertices that have not been chosen previously as part of a pair by this process.) For each pair, add the edge ( vi, w i) to G′. Call this collection of three edges de.

Algorithm 4: Construction of T

Input : A connected multigraph G;a graph G′ constructed via algorithm 3 from G;a set of integral weights w on G such that ∀we ∈ w, |we| ≤ 1. Let m = |E(G)|.For each v ∈ V (G), let cv be the associated clique in G′.For each e ∈ E(G), let de be the associated triplet of edges in G′.

S ← (∅, V (G′)) 

for v ∈ V (G) do 

Add a path to T ′ connecting the vertices of cv .

for e = ( v, w ) ∈ E(G) do 

Add to S w e + 2 edges from among the edges in de.

T ← S 

> 1

while T is not a tree do 

Find a cycle C in T .Pick an edge e in C which is internal to some clique of G′.Remove e from T .5Claim 1. Both algorithm 3 and algorithm 4 can be performed in polynomial time and on any input matching their constraints. Proof. It is easy to see that algorithm 3 and all parts of algorithm 4 save for the loop at line 1 can be performed in polynomial time, and it is easy to see that all lines in both algorithms except for line 1 of algorithm 3 can always be performed. In algorithm 3, one can see that line 1 is always possible by noting that any vertex has degree at most 

m, so at most 3 m vertices of each clique must be selected by line 1; since 3 m ≥ 24 m while m ≥ 1, there are always enough vertices in each clique to select them in this manner. To see that the loop at line 1 of algorithm 4 can be done in polynomial time, note that finding a cycle can be done in O(m) time with any simple search algorithm, and that any cycle must necessarily contain a clique-internal edge (there exists no cycle in G′ consisting only of inter-clique edges, as no two inter-clique edges share a vertex). Since the number of edges in T decreases by one for each iteration, this loop terminates in polynomial time. 

Claim 2. The T produced by algorithm 4 is a spanning tree of G′.Proof. It is easy to see that the S produced by algorithm 4 is a spanning subgraph of G′, since G is connected and each clique of G′ is internally connected. Explicitly, one can construct a path P ′ in G′ between any two vertices v′ ∈ cv and u′ ∈ cu by taking a path P in G from v to u and replacing each e in P with any edge in de, along with whichever edges in the clique-internal segments are necessary to complete the path. The produced T , then, must also be spanning, since removing a single edge from a cycle can never disconnect any vertices (each vertex in the cycle is still connected via the cycle, and no other vertex could be independently disconnected). Next, we pick an appropriate α to ensure that the MaxAvgCut instance ( G, w, k ) is a (YES) instance if and only if the ThinTreeValid C instance ( G′, T, α ) is a (YES) instance. Suppose that a cut C′ = ( A′, B ′)of G′ respects all clique-internal edges; that is, A′ = Ski=0 cai for some vertices a0, . . . , a k ∈ G. Identify such a cut with a cut C = ( A, B ) of G, with A = {a0, . . . , a k}.

Lemma 1. There exists an ϵ such that with α = (2 + k)/3 − ϵ, any cut C of G with average weight at least 

k corresponds to a cut C′ of G′ which has a T -thickness strictly greater than α. Likewise, any cut C′ of G′

which respects all clique-internal edges and has a T -thickness strictly greater than α corresponds to a cut C

of G with average weight at least k.Proof. Proof provided in subsection 3.1. Lemma 1 implies that if the MaxAvgCut instance ( G, w, k ) is a (YES) instance, then the ThinTreeValid C

instance ( G′, T, α ) is a (YES) instance. 

Lemma 2. With T constructed as in algorithm 4 and α = 2 /3 − ϵ as in Lemma 1, any cut C′ of G′ which partitions the vertices of a clique into different sets has an T -thickness lower than α.Proof. Proof provided in subsection 3.1. Lemma 2 implies that if the ThinTreeValid C instance ( G′, T, α ) is a (YES) instance, then there is a cut in G′ with T -thickness strictly greater than α which respects all clique-internal edges (there exists some cut which is appropriately T -thick, and no cut which splits a clique can be T -thick, so it must be a cut which does not). By Lemma 1, this corresponds to a cut in G with average weight at least k; thus, the 

MaxAvgCut instance ( G, w, k ) is a (YES) instance. Thus, the MaxAvgCut instance ( G, w, k ) is a (YES) instance if and only if the ThinTreeValid C

instance ( G′, T, α ) is a (YES) instance, and since the mapping ( G, w, k ) 7 → (G′, T, α ) can be performed in polynomial time, MaxAvgCut ≤Pm ThinTreeValid C.6Theorem 1 (Hardness of Thin Tree Verification) . ThinTreeValid is coNP -complete. Proof. We have by Theorem 2, Theorem 3, and Theorem 4 that: 

MaxCut ≤PT MaxAvgCutOpt ≤PT MaxAvgCut ≤Pm ThinTreeValid C

⇒ MaxCut ≤PT ThinTreeValid C

Hence ThinTreeValid C is NP -hard; its complement, ThinTreeValid , is thus coNP -hard. Since Thin-TreeValid is in coNP , it is then coNP -complete. 

## 3.1 Proofs of Remaining Lemmas 

Proof of Lemma 1. We want that cuts in G with average weight ≥ k correspond to cuts between cliques in 

G′ with T -thickness > α . A given cut C = ( A, B ) in G corresponds to a cut C′ = ( A′, B ′) in G′ with 3 times the number of edges between the partitions, and the sum of all we with e ∈ δ(A) is equal to the number of edges of δ(A′) that are in T minus 2 for each edge of δ(A). Thus, 

P 

> e∈δ(A)

we

|δ(A)|

= |δT (A′)| − 2|δ(A)||δ(A)|

= |δT (A′)||δG′ (A′)|/3 − 2So: 

k ≤

 X

> e∈δ(A)

we

 /|δ(A)|⇔ (k + 2) /3 ≤ | δT (A′)|/|δG′ (A′)|

However, we want any cut in G′ with an average proportion in T strictly greater than α to correspond to a cut in G with an average weight at least k (≤ is the complement of >, not ≥). Our desired α is then ( k + 2) /3 − ϵ for some ϵ sufficiently small that there are no cuts of G′ with T -thickness in the interval (( k + 2) /3 − ϵ, (k + 2) /3,]. In order to find an ϵ which works, note first that the T -thickness of any cut in G′

is of the form p/q for integers p, q ≤ | E(G′)|. Thus, if we take the set 

S = {(p, q ) : 0 ≤ p, q ≤ | E(G′)|, p/q < (k + 2) /3}

and α = max (p,q )∈S (p/q ), there cannot exist any cut with T -thickness between α and ( k + 2) /3. We set 

ϵ to whatever value ensures that α equals this quantity. This guarantees that any cut in G′ with T -thickness greater than α and which does not assign different vertices of the same clique to different partitions corresponds to a cut in G with an average weight at least 

k; likewise, cuts of this kind with T -thickness at most α have an average weight less than k.

Proof of Lemma 2. Consider any cut C′ = ( A′, B ′) which splits a clique in G′ into two. We will show that 

C′ has a T -thickness strictly lower than ( k + 2) /3 > 1/3 by considering first the internal edges of a single split clique along with all inter-clique edges, and then all other clique-internal edges. Consider some clique it splits, cv , and call the number of vertices in a side of the partition with the fewest vertices a (on the other side, there are 24 m − a vertices). There are ( a)(24 m − a) clique-internal edges, and 7at most 2 a of them are in T . Assume that every single inter-clique edge is in T and there are the maximum number of them; this is 3 m edges. Thus, the T -thickness of the portion of C′ which includes only the edges in cv and all inter-clique edges is at most: 3m + 2 a

3m + a(24 m − a) ≤ 3m + 2 aa(24 m − a) (1) 

≤ 3ma(24 m − a) + 224 m − a (2) Since a ≤ 12 m, we can bound the right term of Equation 2: 224 m − a ≤ 16m

The left term is maximised when a(24 m − a) is minimised. This happens when a = 1: 3ma(24 m − a) ≤ 3m

24 m = 18So the T -thickness of this portion of C is at most 18 + 16m < 13 . Hence, a cut that splits a single clique has a T -thickness strictly less than ( k + 2) /3, and at most α = ( k + 2) /3 − ϵ as given above. We now consider the contributions of the edges internal to other split cliques, if any exist; for any such clique, following the bounds above, at most a 1 /8 portion of the edges contributed to C by that clique are in T . Thus, it cannot be the case that considering the edges from additional split cliques increases the T -thickness of C above 

α.

# Acknowledgements 

We would like to thank Elena Grigorescu for advice and guidance, Greg Rosenthal for extensive proofreading and copyediting, and Kaleb Ruscitti for feedback. 

# References 

[AKS21] Hyung-Chan An, Robert Kleinberg, and David B. Shmoys. “Approximation Algorithms for the Bottleneck Asymmetric Traveling Salesman Problem”. In: ACM Trans. Algorithms 17.4 (Oct. 2021). issn : 1549-6325. doi : 10.1145/3478537 . url : https://doi.org/10.1145/3478537 .[Alg23] Mahtab Alghasi. “Combinatorially Thin Trees and Spectrally Thin Trees in Structured Graphs”. PhD thesis. University of Waterloo, 2023. [GJS74] M. R. Garey, D. S. Johnson, and L. Stockmeyer. “Some simplified NP-complete problems”. In: 

Proceedings of the Sixth Annual ACM Symposium on Theory of Computing . STOC ’74. Seattle, Washington, USA: Association for Computing Machinery, 1974, pp. 47–63. isbn : 9781450374231. 

doi : 10.1145/800119.803884 . url : https://doi.org/10.1145/800119.803884 .[God04] Luis A. Goddyn. Some Open Problems I Like . 2004. url : https : / / web . archive . org / web / 20240617180800 / https : / / www . sfu . ca / ~goddyn / Problems / problems . html (visited on 12/15/2025). [GS11] Shayan Oveis Gharan and Amin Saberi. The Asymmetric Traveling Salesman Problem on Graphs with Bounded Genus . 2011. arXiv: 0909.2849 [cs.DS] . url : https://arxiv.org/abs/0909. 2849 .[KO23] Nathan Klein and Neil Olver. Thin trees for laminar families . 2023. arXiv: 2304.07674 [cs.DS] .

url : https://arxiv.org/abs/2304.07674 .8