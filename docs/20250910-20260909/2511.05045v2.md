---
title: "The Cloven Traveling Salesman: Cycle Covers and the Integrality Gap of Small ATSP Instances"
authors: "Alessandro Sosso, Ambrogio Maria Bernardelli, Stefano Gualandi"
date: 2025-11-07
pdf: "https://arxiv.org/pdf/2511.05045v2"
tags: ["paper:核心", "query:ATSP"]
score: 8.0
evidence: 论文聚焦ATSP积分间隙计算，与专题中组合优化与多面体研究相关。
source: arxiv
selection_source: long-range
paper_media_checked: true
title_zh: 分裂旅行商问题：环覆盖与小规模ATSP实例的整数性间隙
tldr: "针对非对称旅行商问题的小规模实例，提出一种新的枚举算法，通过枚举可映射到半整数顶点的环覆盖对，并利用高效性质检查，避免生成同构顶点，从而精确计算子环消除多面体的积分间隙。该方法在n≤9时快速复现现有结果，并首次得到n=10,11,12的精确值。"
motivation: 现有算法先生成所有顶点再过滤同构，效率低，且小规模ATSP实例的积分间隙尚未完全确定。
method: 枚举环覆盖对，编码为有序划分，通过性质检查判断是否对应子环消除多面体的顶点，避免同构生成。
result: "在n≤9时快速复现现有结果，并首次精确计算n=10,11,12的半整数顶点积分间隙。"
conclusion: 新算法大幅提升效率，扩展了精确积分间隙的已知范围，为ATSP研究提供新工具。
reading_content_version: 1
reading_section: deep
---

## 摘要
本文提出了一种新颖的枚举算法，用于计算非对称旅行商问题（ATSP）子巡回消除公式的小规模实例的整数性间隙。核心思想是枚举可被过滤并映射到子巡回消除多面体半整数顶点的环覆盖对。两个环覆盖被编码为n个数的字典序有序划分，这种编码方式避免了生成多个同构顶点。然而，由于并非每个环覆盖对都能映射到子巡回消除多面体的顶点，我们设计了一个高效的属性检查程序，以控制给定点是否为非对称子巡回消除多面体的顶点。所提出的方法颠覆了文献中先枚举所有可能顶点再过滤同构顶点的算法。利用我们的方法，我们能在极短的时间内复现n≤9时的最新结果，并首次计算了n=10、11、12时非对称子巡回消除多面体半整数顶点的精确整数性间隙。

## Abstract
This work proposes a novel enumeration algorithm for computing the integrality gap of small instances of the subtour elimination formulation for the Asymmetric Traveling Salesman Problem (ATSP).The core idea is to enumerate pairs of cycle covers that can be filtered and mapped to half-integer vertices of the subtour elimination polytope. The two-cycle covers are encoded as lexicographically ordered partitions of $n$ numbers, with an encoding that prevents the generation of several isomorphic vertices. However, since not every cycle cover pair can be mapped to a vertex of the subtour elimination polytope, we have designed an efficient property-checking procedure to control whether a given point is a vertex of the asymmetric subtour elimination polytope. The proposed approach turns upside down the algorithms presented in the literature that first generate every possible vertex and later filter isomorphic vertices. With our approach, we can replicate state-of-the-art results for n<=9 in a tiny fraction of time, and we compute for the first time the exact integrality gap of half-integer vertices of the asymmetric subtour elimination polytope for n=10, 11, 12.

## 专题评审

摘要直接研究ATSP的整数规划间隙，属于ATSP求解方法。

原文证据：

> This work proposes a novel enumeration algorithm for computing the integrality gap of small instances of the subtour elimination formulation for the Asymmetric Traveling Salesman Problem (ATSP).

仅依据标题与摘要评审；待复核不代表已确认相关。

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

**研究动机与背景：**

- **问题定义**：非对称旅行商问题（ATSP）是经典组合优化问题，其子巡回消除公式的线性松弛（ASEP）的**整数性间隙**（Integrality Gap）是衡量松弛质量的关键指标，也是设计近似算法的理论基础。
- **核心挑战**：对于一般情况，ATSP的整数性间隙无界；对于度量权重（metric weights），最大整数性间隙的精确值仍是开放问题，目前仅有上下界（上界从22改进至17，下界为2）。
- **研究缺口**：对于小规模实例（n<15），精确计算最大整数性间隙需要枚举ASEP多面体的所有顶点，但顶点数量随n呈组合爆炸式增长。此前仅能精确计算n≤7的所有类型顶点，以及n=8,9的半整数顶点；n≥10时无完整枚举。
- **研究目标**：首次精确计算n=10, 11, 12时**半整数顶点**（分量取值在{0, 1/2, 1}的顶点）的整数性间隙，并大幅提升计算效率。

## 2. 论文提出的方法论

**核心思想：**

- **结构定理（Theorem 2）**：每个半整数顶点x都可以表示为两个**边不相交环覆盖**（cycle cover）的平均值，即x = ½y₁ + ½y₂。这为枚举提供了理论基础——只需枚举环覆盖对即可覆盖所有半整数顶点。
- **反向流程**：传统方法先枚举所有顶点再过滤同构；本文先通过环覆盖对生成候选点，再检查其是否为顶点，大幅缩小搜索空间。

**关键技术细节：**

- **环覆盖编码**：将环覆盖编码为字典序有序的整数划分序列（每个环从最小顶点开始，环按大小降序排列），保证编码唯一性，避免因环旋转产生的冗余。
- **标准形式（Standard Form）**：通过顶点重标定置换，将环覆盖对的编码转换为标准形式，消除部分同构实例。
- **电路极值性算法（Circuit Extremality Algorithm）**：
  - 将x的支撑集中的弧按度约束向量划分为**电路对**（circuit pairs）；
  - 逐步加入活跃的子巡回消除约束向量，合并电路对；
  - 当所有电路对变为**短路**（shorted）时，x即为极值点（顶点）；
  - 该算法避免了直接计算矩阵秩，大幅加速极值性检验（Proposition 2）。
- **性质检查**：对候选点检查是否满足子巡回消除约束（1d），再通过电路算法验证是否为极值点。

**算法流程（图1）：**

```
环覆盖对生成 → 编码（标准形式）→ 同构过滤 → 子巡回约束检查 → 极值性检查 → 计算整数性间隙
```

## 3. 实验设计

**数据集与场景：**

- 针对**完整有向图**Kₙ上的ATSP实例，n = 5, 6, 7, 8, 9, 10, 11, 12。
- 仅考虑**纯半整数顶点**（分量仅取0或1/2），依据Elliott-Magwood的结果，计算半整数顶点的间隙只需考虑纯半整数顶点。

**Benchmark与对比方法：**

- 对比基准：Elliott-Magwood（2008）的博士论文结果，该工作对n≤7进行了所有类型顶点的穷举，对n=8,9仅计算半整数顶点，n>9仅提供下界。
- 本文复现了n≤9的结果，并扩展至n=10, 11, 12。

**实验内容：**

- 对每个n，统计生成的候选实例数、非同构顶点数、各子流程运行时间。
- 对每个非同构顶点，求解gap问题（辅助LP）以计算其整数性间隙。
- 记录每个n的最大间隙值Gapₙ及达到最大值的顶点结构。

## 4. 资源与算力

- **硬件**：Dell Precision 7960 Tower Workstation，Intel Xeon W-3495X CPU（56物理核心），128 GB内存。
- **软件**：Python实现；同构检查使用C语言库Nauty（通过pynauty接口）；gap问题求解使用商业求解器Gurobi。
- **运行时间**：n=12的完整计算约**12小时51分钟**（其中生成+认证1h34m，子巡回+极值性检查7h12m，间隙计算4h05m）；n=11约18分48秒；n≤10均在1分钟级别。
- **未使用GPU**，全部为CPU计算。

## 5. 实验数量与充分性

**实验规模：**

| n | 生成实例数 | 非同构顶点数 | 总运行时间 |
|---|-----------|-------------|-----------|
| 5 | 11 | 2 | 0.76s |
| 6 | 112 | 11 | 1.90s |
| 7 | 978 | 52 | 1.80s |
| 8 | 10,809 | 365 | 2.40s |
| 9 | 116,463 | 2,931 | 10.00s |
| 10 | 1,527,602 | 26,906 | 1m 11s |
| 11 | 19,938,367 | 274,134 | 18m 48s |
| 12 | 306,394,067 | 3,059,487 | 12h 51m |

**充分性评估：**

- 实验覆盖了n=5至n=12共8个规模，每个规模均进行**完整枚举**（对半整数顶点类），非抽样。
- 对每个非同构顶点均求解gap问题，计算量随n增长显著。
- 复现了此前n≤9的结果，验证了方法的正确性。
- 实验设计合理，但**缺少与现有方法运行时的直接对比**（Elliott-Magwood仅报告n=7穷举耗时超20小时，未提供其他详细运行时数据）。

## 6. 论文的主要结论与发现

- **精确整数性间隙值**：首次计算得到n=10, 11, 12的半整数顶点最大整数性间隙分别为**7/5、10/7、56/39**。
- **最优实例结构**：对于n≥9，最大间隙由Elliott-Magwood提出的递归构造族中的实例达到（通过1-弧插入和2-jack操作从n=4极值实例递归生成）；n=9, 11, 12各存在唯一最优实例。
- **计算效率**：新方法在极短时间内复现了此前所有已知结果（n≤9），验证了方法的有效性。
- **组合增长规律**：生成实例数和非同构顶点数均随n呈指数增长（约一个数量级/每增加1个节点），同构过滤可将冗余减少约两个数量级。
- **计算瓶颈**：n=12时，子巡回约束检查和极值性验证阶段占主导（7h12m），但生成阶段本身是扩展至更大n的固有瓶颈。

## 7. 优点

- **方法论创新**：首次将半整数顶点与环覆盖对的结构联系系统化，提出“先生成候选、后验证”的反向流程，显著缩小搜索空间。
- **理论贡献**：Theorem 2（半整数顶点的环覆盖分解）具有独立理论价值，且可推广至更一般的α-整数顶点（Remark 1）。
- **算法效率**：电路极值性算法避免了昂贵的矩阵秩计算，相比朴素实现获得约350倍加速（n=11）。
- **结果突破**：首次精确计算n=10, 11, 12的半整数顶点整数性间隙，扩展了精确计算的已知范围。
- **可复现性**：代码开源（GitHub），便于其他研究者验证和扩展。

## 8. 不足与局限

- **覆盖范围有限**：仅计算半整数顶点（α=2），未覆盖其他分母的分数顶点（如α=3, 4等），因此Gapₙ的精确值仍未被完全确定（仅对半整数类是最优的）。
- **计算规模受限**：n=12已需约12小时和3亿级候选实例，n=13及以上的完整枚举在计算上不可行，方法扩展性受限。
- **缺少直接对比**：未与Elliott-Magwood等方法进行同硬件条件下的运行时对比，效率提升的量化证据不够直接。
- **同构过滤不彻底**：标准形式编码不唯一，仍需依赖Nauty进行同构检查，该步骤构成额外计算开销。
- **应用范围**：方法针对ATSP设计，虽提及可推广至对称TSP，但未给出具体实现或验证。

（完）
